{
  "name": "qiji-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Qiji 管理端网关（BFF）：用户端 → 本服务 → 第三方 API。持有真 key，翻译/转发/计费/对象存储/规范化。",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "@fastify/cors": "^10.0.1",
    "@fastify/multipart": "^9.0.1",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
