# CLAUDE.md 三段式格式范例（摘自真实文件，2026-08-16 第 234 轮时点）

> 以下均为 `E:\Kaifa\Qiji\qiji\CLAUDE.md` 中的真实原文摘录（长段以 `……` 截略），用于收口时对照措辞与结构。

## 1. 头部「最后更新」行（第 234 轮，含并行收口注记）

```
最后更新：2026-08-16（第234轮：**autodl（autodl.art·ComfyUI 工作流平台）视频渠道接入 · 3 模型/新模式「autodl」·纯服务端**——三条工作流（多图参考/文生视频/首尾帧）各成一个模型，**workflow_id=模型「上游模型名」**（……）；⚠ 鉴权 `Authorization: <Token>` **原样令牌不带 Bearer**（……）；⚠ 顺带修 errorScrub 真缺陷：**域名隐藏挪到模式名占位符保护之前**（……）。server tsc 干净 + admin script 语法过 + 沙盒冒烟 54/54 + 回归 3/3 + 二启幂等。**须重新部署服务端（部署后填 Token + 三个 workflow_id）**；客户端零改动无需打包。详见 §8。⚠ 本行收口第229-233轮并行会话的「最后更新待统一」：228→233 各轮完整记录均在 §8。）
```

要点：一段式浓缩 = 标题（含改动面标注「纯服务端/纯客户端/两端」）→ 要点与 ⚠ → 实测串 → **部署要求加粗** → 「详见 §8」→（如需）并行收口注记。

## 2. 头部「上一轮」行（降级后的旧条目，前缀换、内容不动）

```
上一轮：2026-08-13（第228轮：**用户级「渠道形式模式」开关退役——两控制台用户页只留功能形式四开关·纯服务端**——……）
```

## 3. §8 变更记录条目（第 234 轮开头部分）

标题行五要素：日期 + 第N轮 +（标题 · 实测结论串 · 客户端/部署标注）：

```
- **2026-08-16 第234轮（autodl（autodl.art·ComfyUI 工作流平台）视频渠道接入 · 3 模型/新模式「autodl」·纯服务端 · server tsc 干净 + admin script 语法过 + 沙盒冒烟 54/54 + errorScrub 回归 3/3 + 二启幂等（data 文件零变化；scratchpad 复制 src + junction node_modules + 独立 data，stub fetch 零真实上游、真实库零触碰）· 客户端零改动无需打包）**——用户提供 autodl 三份工作流文档（……），令「新增渠道 autodl」；……
  ①**上游（Base `https://autodl.art`）**（新 [autodl.ts](server/src/translators/autodl.ts)，协议 `autodl-video`，……）
  ②⚠ **workflow_id=模型「上游模型名」（一个工作流=一个模型）**：……
  ……
  实测：server tsc 干净 + admin script 语法过 + **沙盒冒烟 54/54**（……）+ **二启幂等**（10 个 data 文件 hash 零变化）。
  ⚠ **须重新部署服务端**（首启自动补种……；**部署后到管理端：①……填 Token（或环境 AUTODL_API_KEY）；②三个模型的「上游模型名」逐个改成……真实 workflow_id**……）；**客户端零改动无需打包**（……）。**运营注意**：①3 模型全为按秒**占位价**（……**务必先小额真单对账再定真价**）；……**待真机 QA**：三工作流各一张小额真单（提交→轮询→成片转存 OSS 全链路）、……
```

结构：背景一句 → ①②③ 逐点改动（内嵌 ⚠ 勿回退及理由，文件用 `[名](server/src/...)` 相对链接）→ 实测明细 → ⚠ 部署要求（含部署后动作/验证词）→ 运营注意 → 待真机 QA。

## 4. §9A 勿回退清单条目（第 234 轮的三条，插清单顶部、轮次倒序）

```
- **第234轮**：**autodl 鉴权=`Authorization: <Token>` 原样令牌，勿加 Bearer 前缀**（官方文档明写……；加了 Bearer 上游 401）——提交与轮询两处同规，勿照抄其它翻译器。
- **第234轮**：**autodl 的 workflow_id 守卫勿删**——「上游模型名」=各工作流的 workflow_id（……）；翻译器对 占位符/回退成模型 id（`^adl-`）两种「未配置」形态**前置明确报错不发请求不扣费**。……
- **第234轮**：**errorScrub 域名隐藏必须在「模式名占位符保护」之前，勿回退**——autodl 是首个「模式名（autodl）是上游域名（autodl.art）子串」的渠道：……
```

## 5. 并行会话撞号与「待统一」注记的三种真实写法

第 231 轮（自己是并行会话，不动头部，只在 §8 条目末注明）：

```
（⚠ 轮次号说明：本轮与第229/230轮为**并行会话**，文档头部「最后更新」行仍停在第228轮、待其中一方统一收口——本轮未动该行以免与并行会话互相覆盖。）
```

第 232 轮（同上，仅注明待统一收口。撞号顺延的真实先例在第 230 轮条目开头：「轮次号原本都占了 229 → 本轮顺延为 230、`MODES_SEED_VERSION` 顺延为 19」）：

```
⚠ **文档轮次说明**：头部「最后更新」行仍停在第228轮——第229/230/231 轮为并行会话（记录均在本节），本轮同样未动该行以免互相覆盖，**待统一收口**。
```

第 234 轮（负责统一收口的一方，写进头部「最后更新」行末尾）：

```
⚠ 本行收口第229-233轮并行会话的「最后更新待统一」：228→233 各轮完整记录均在 §8。
```

## 6. 三段的定位锚点（行号会漂移，收口时用 Grep 现查）

| 段落 | 标题原文 | 2026-08-16 时点行号 |
|---|---|---|
| 头部链 | `最后更新：…` / 多条 `上一轮：…` | 第 7 行起 |
| 进度快照 | `## 进度快照（新会话先读这里）` | §9A 之前 |
| 勿回退清单 | `## 9A. 历轮锁定规则速查（⚠ 勿回退清单）` | 约 396 |
| 变更记录 | `## 8. 变更记录（第168轮起全文保留；更早只留结论摘要）` | 约 648 |
