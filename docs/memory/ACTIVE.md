# Qiji · 当前工作记忆

> 更新日期：2026-08-28
> 本文件只保存当前目标、证据、阻塞项和下一步；完成后的精确历史归 Git。

## 当前目标

以只读方式核对“存储与数据结构改造”在代码、开发数据和生产环境中的真实进度，形成可安全继续的执行基线（M0）。多层记忆迁移已经完成并通过冷启动验收。

## M0 完成定义

- 逐项建立 P0–P5 的实现矩阵，每个结论链接到代码、测试或只读运行证据。
- 取得生产只读证据：当前部署版本、SQLite 表/列/索引、active storage profile 与桶、verify/reconcile 状态、cleanup mode/scope/amnesty/观察期，以及收藏、引用上报和缩略图端点状态。
- 明确哪些能力只是“代码存在”、哪些已在开发环境验证、哪些已部署生产，禁止跨层推断。
- 核对生产环境可用的 SQLite 安全备份方式并做无写入演练；不执行迁移、切换、生命周期下发或清理。
- 形成一个经用户确认的后续阶段建议与安全门；本 M0 不包含任何生产 apply。

## 存储改造状态：待重新核验

以下只是 2026-08-28 对仓库表面证据的初步盘点，不等同于生产完成声明：

| 阶段 | 当前可见证据 | 结论 |
|---|---|---|
| P0 | 仓库已有迁移、对账与验证脚本及相关 skill | 代码存在，需逐项只读验证 |
| P1–P3 | 仓库可见 OSS 分层/运维、SQLite store、资产恢复与相关测试 | 部分或大部实现可能已落地，需按验收项核对 |
| P4 | 请求日志已有索引/重报文拆分范式 | 仅能确认部分实现，不能外推整个阶段完成 |
| P5 | 暂无足够证据 | 视为未完成 |
| 生产环境 | 本轮未连接生产核验 | 未知；禁止凭本地代码宣称已上线 |

### 安全阻塞

- SQLite 备份说明已统一为：在线只用 `VACUUM INTO` / `.backup`，文件级复制必须先停写；禁止直接复制活跃库。执行前仍须核对生产部署环境和工具可用性。
- 所有迁移/清理脚本先 `dry-run → verify → 明确授权后 apply`；不得把开发库或真实上游作为冒烟目标。
- 生产 OSS、台账、生命周期和配额状态必须从生产只读探测获得，不能沿用 2026-07-30 的数值当现状。

## 待发版与 QA 队列

### 客户端：第 256 轮成果

- 状态：2026-08-28 本机重新执行 client TypeScript、126 个测试文件 / 1352 条用例、Vite 正式构建及 Tauri release build，全部通过；正式产物只含生产服务器地址，不含开发地址。
- 打包义务：已完成。可分享目录为 `release/Qiji-0.1.0-20260828-064224/`，包含 NSIS `.exe`、MSI 与 `SHA256SUMS.txt`；本轮无服务端改动或部署义务。
- 分发提示：当前两个安装包均未做代码签名，Windows 可能显示未知发布者/SmartScreen 提示；正式公开分发前仍建议配置代码签名证书。
- 真机 QA：
  1. 在干净 Windows 环境安装 NSIS 或 MSI，确认登录页默认直连生产服务，双击 logo 可临时展开地址栏。
  2. 导入不含本地 `assets/`、但 `assetBlobs` 有有效 OSS 链接的 `.Qiji`，确认资产页、分镜和画布逐步恢复，重开仍保持。
  3. 运行中移走一份本地资产，重新进入显示点，确认按原 `assetId` 懒下载且只下载一次。
  4. 手改图例资产说明后再匹配/拖入其它素材，确认正文和自定义说明保留。
  5. 验证转深度、视频截帧和二次启动新窗口，覆盖内置模型、FFmpeg、CSP/WebGPU 与多窗口能力。

### 服务端：第 255 轮 QiQi 渠道

- 状态：历史记录显示 server TypeScript、管理端脚本、142/142 沙盒冒烟、二启幂等和部署路径测试通过；本轮尚未重新执行。
- 义务：是否已经部署未知。若未部署，需先按服务端发版流程重新验证并部署，再配置 QiQi 密钥。
- 上线前：核对两款模型实际在线状态，以官方价格或小额真单把占位价改为真价。
- 真单 QA：两种请求形态各跑最小样例，核对小写素材引用、素材约束、进度和成片 OSS 转存。

## 当前仓库保护

- 基线分支：`main`；迁移开始时 HEAD 为 `a76ae88137f467faf88c403b58d266be2ef78670`。
- 开始迁移时工作区已有大量用户改动与 38 个 `.claude/skills` 跟踪删除；这些都不是本次记忆迁移产生，必须原样保留。
- 迁移前旧规格 SHA-256：`E0372494DAF62F603DA54527707D8E9BB4DA813CACA8EC48064FB237FFEFC301`。

## 记忆迁移验证（2026-08-28）

- Qiji 记忆审计：关键文件存在；活动旧入口引用 `0`；错误 AGENTS 权威引用 `0`。
- 旧规格归档 SHA-256 与迁移前工作树完全一致。
- 迁移前 58 个既有脏路径（排除被明确迁移的旧根规格）逐项比较：状态、存在性、大小和 SHA-256 均无意外变化。
- 关键 Markdown 链接检查：6 个入口/技能文件，断链 `0`。
- Skills 结构校验：通用记忆、Qiji 记忆、DDup、项目内 Qiji 记忆及新收口 skill 全部通过。
- `git diff --check` 退出码 `0`（仅有仓库既有的 LF/CRLF 提示）。
- 无聊天、禁读归档的冷启动 agent 能正确复述目标、红线、证据位置和发版义务；发现的“非唯一下一步”与备份歧义已在本次修正。

## 本地开发环境启动器（2026-08-28）

- 根目录新增 `启动开发环境.bat`，由 `scripts/start-dev.ps1` 统一启动服务端和 Tauri 独立桌面客户端；服务端管理页为 `http://localhost:8787/admin`，5173 只作为桌面客户端内部的 Vite 开发端口。
- 正常按回车或关闭主控制窗口时，两项服务都会停止；隐藏看门进程也覆盖了主控制进程被强制关闭的情况。
- 已通过 PowerShell 7 直接冒烟、BAT 端到端冒烟和强制关窗清理验证；清理后 `app.exe`、项目启动辅助进程及 8787/5173 监听均为 `0`。
- 系统已安装 PowerShell 7.6.5 与 Windows Terminal，并把当前用户的默认终端和默认配置设为 PowerShell 7；Windows PowerShell 5.1 仅为系统兼容保留并在 Terminal 中隐藏。
- 本轮只改本地开发工具与对应 skill，不涉及客户端打包或服务端部署；原 M0 目标和唯一下一步不变。

## 客户端一键打包入口（2026-08-28）

- 根目录新增 `打包可分享应用.bat`，由 `scripts/package-client.ps1` 执行依赖/安全红线检查、TypeScript、全量测试、生产地址产物核验及 Tauri 正式打包。
- 每次成功会创建独立的 `release/Qiji-<version>-<时间>/` 分享目录，复制本轮新生成的 NSIS/MSI 并生成 SHA-256 清单；正常双击完成后自动打开该目录，失败时窗口会保留错误信息。
- 本轮首次完整执行已成功产出 Qiji 0.1.0 两种 Windows x64 安装包；未执行干净机器安装与业务真机 QA，相关义务保留在客户端 QA 队列。

## 客户端资产页裂图修复（2026-08-28）

- 根因已确认：三元映射恢复与 `useDisplayUri` 已接入画布等显示点，但 `AssetWorkbench` 的主图、资产/造型/历史/垫图缩略图仍直接使用项目快照旧 URI；因此映射即使恢复成功，资产页仍会加载旧地址并显示裂图替代文字。
- 已新增统一的 `AssetDisplayImage` 封装，并让资产工作台全部图片入口消费同一显示 URI 解析链；Tauri 等待远程资产恢复期间显示阶段进度（主图为“正在从服务端恢复图片”进度条，缩略图为紧凑进度条），不创建 `<img>`，落盘取得本地 URI 后才显示图片。底层 Rust `download_url` 当前不回传字节进度，因此不伪造百分比。新增回归用例分别验证“恢复后使用新地址”和“恢复等待态有进度且无裂图标签”。
- 本机实测：client TypeScript 干净；127 个测试文件 / 1354 条用例全过（较上一基线新增 2 个回归用例）；Vite 正式构建通过。未执行 Tauri 真机打开受影响项目的视觉 QA。
- 发版义务：现有 `release/Qiji-0.1.0-20260828-064224/` 早于本修复，不包含本修复；真机确认图片恢复后需重新打包，才能对外分发修复版。
- 真机 QA：用截图对应项目进入角色配置，确认右侧主图、左侧资产头像、造型卡和历史缩略图均从裂图恢复；再移走本地副本重开项目，确认按同一 `assetId` 下载一次并自动换源。

## 画布节点信息重构（2026-08-28）

- 旧的坐标、尺寸、调试 JSON 面板已整体替换为结果导向的信息层级：节点类型；节点状态（节点 / 生成中及进度 / 结果 ×N / 失败）；当前结果三元地址（资产 ID、服务地址、本地路径）；使用模型、比例、时长与完整提示词。
- 新生成的媒体会按 `assetId` 保存生成当时的模型参数快照。切换堆叠主结果后，面板读取对应结果的模型、比例、时长和提示词，不受节点后来修改设置影响；旧项目没有快照时兼容回退到节点当前参数。
- 本机验证：client TypeScript 干净；128 个测试文件 / 1356 条用例全过（新增 1 个测试文件 / 2 条节点信息回归用例）；Vite 正式构建通过。构建仅有既有的分块及导入提示，无新增失败。
- 发版义务：未执行 Tauri 真机视觉 QA，也未重新打包；现有 `release/Qiji-0.1.0-20260828-064224/` 不包含本次节点信息重构及此前资产页裂图修复。
- 真机 QA：分别检查空节点、生成中、单结果和多结果节点；切换堆叠主结果后确认三元地址与模型属性同步切换；检查长服务地址、本地路径和长提示词的换行、滚动与可读性。

## 画布 Alt 拖动复制语义修正（2026-08-28）

- Alt 拖动普通节点时，原位继续保留完整旧结果与下游关系；被拖走的抽卡副本保留输入、提示词、模型等配置，但清空媒体结果、结果历史、结果参数快照、文本/分镜/对话结果、备用文件地址及在途任务，运行态统一重置为待命。
- `upload` 资产节点是例外：这类节点自身只承载资产结果，复制时保留资产 ID、结果历史和文件信息，但运行态仍归待命。
- 本机验证：定向剪贴板回归 10/10；client TypeScript 干净；128 个测试文件 / 1358 条用例全过（较上一轮新增 2 条复制语义用例）；Vite 正式构建通过。仅有仓库既有构建提示。
- 发版与 QA：未执行 Tauri 手势真机 QA、未重新打包；需检查单节点/多选 Alt 拖动、图片/视频堆叠、文本结果、资产节点，以及撤销一次能否完整恢复复制前结构。

## 第一批渠道精简：开发环境（2026-08-28）

- 用户按供应商名称确认第一批目标为 BYS、mojie、zexitongxue、Aivide、vosle；现有标签里 zexitongxue 实际对应“备用一”，未误删现名“备用二”的 ZhengAPI。
- 执行前已备份开发环境 `channels.json`、`models.json`、`modes.json` 到 `server/data/backups/channel-prune-20260828-094138/`。本地 `mojie` 原本已不存在；已删除其余 4 个渠道、4 个专属模式及按 `channelId` 命中的 41 个模型，未发现其它模型通过 routes 引用这些渠道。
- `channels` / `modes` store 已补内置种子删除墓碑；模型沿用既有 `deletedSeedIds`。两次独立冷启动检查均为残留模型/渠道/模式 `0`，三份数据文件二启 SHA-256 变化 `0`；server TypeScript `npx tsc --noEmit` 通过。
- 生产环境未改动。日常部署包排除 `server/data`，因此本次开发数据删除不会随部署自动删除生产条目；生产清理须在用户再次明确授权后，对生产的同名模型、模式、渠道执行删除。仅部署墓碑代码不会凭空生成生产墓碑，也不会自动删除生产数据。
- 发版义务：墓碑语义属于服务端代码改动，若希望生产管理端今后的内置渠道/模式删除也能跨重启保持，须随下一次服务端版本部署；本轮尚未打包或部署。

## 匹配资产图例边界改为分号（2026-08-28）

- 匹配资产写入的「素材图例」已从中文逗号改为中文分号分隔，例如 `【素材图例】@Image1 是 角色；@Image2 是 场景；`；资产说明中的普通逗号会完整保留，不再被解析器误判为下一条资产边界。
- 解析器继续兼容旧项目的逗号分隔图例；旧图例在下一次匹配资产、增删/重排素材或插入上游文本胶囊时会自然刷新为分号格式。资产模式、画布与实时剪辑共用同一实现。
- 本机实测：client TypeScript 干净；128 个测试文件 / 1359 条用例全过（新增 1 条逗号保留回归用例）；Vite 正式构建通过。构建仅有既有的分块、导入与 `-0` 比较提示，无新增失败。
- 发版与 QA：未执行 Tauri 真机 QA、未重新打包；现有分享安装包不包含本改动。真机需用含逗号的资产名/自定义说明执行匹配、增删和重排，确认图例用分号分界且正文及说明不丢失。

## 画布右键取消垫图去确认（2026-08-28）

- 画布节点垫图区右键现已立即取消，不再弹出二次确认：自加垫图直接移除，上游垫图直接断开对应连线；右键事件同时停止冒泡，不会在取消后再打开画布菜单。资产模式与实时剪辑原本就是立即移除，无需改动。
- 新增 1 条交互回归用例，锁定“阻止浏览器菜单、阻止画布冒泡、立即执行移除”三项行为。本机实测：client TypeScript 干净；129 个测试文件 / 1360 条用例全过；Vite 正式构建通过。构建仅有既有提示，无新增失败。
- 发版与 QA：未执行 Tauri 真机右键 QA、未重新打包；现有分享安装包不包含本改动。真机需分别右键自加垫图和上游连线垫图，确认无弹窗、单步生效、图例与正文 `@tag` 同步重编号，且不出现画布右键菜单。

## 生产团队每日消耗 Excel（2026-08-29）

- 已按用户纠正的“公司先隔离、公司内再分团队”口径重做生产只读报表。团队整体以团长所属公司归属，未分组人员才按个人 `agentId` 归属；“天时动画”和“云视中创”均完整归入简梦，源站不再包含这两个团队或其成员。
- 最新生产快照为 2026-08-29T12:42:15.085Z，窗口是 2026-07-31 至 2026-08-29 共 30 个 UTC 自然日。当前 193 名用户恰好归入一个公司且无重复：源站 13 个团队 / 77 名组内成员 / 71 名未分组，共 148 人、消耗 15,694,913；简梦 2 个团队 / 6 名组内成员 / 35 名未分组，共 41 人、消耗 596,477；净寻 0 个团队 / 4 名未分组，共 4 人、消耗 7,355。
- 正式交付位于 `outputs/team-usage-20260829/公司报表/`，源站、简梦、净寻各一份完整 Excel；每份均为“总表”索引 0、“未分组个人”索引 1、其后仅含本公司团队，日期从 08-29 倒序至 07-31。三份文件公式错误扫描均为 0，并已目检全部 21 个工作表。
- 项目技能 `.agents/skills/qiji-team-usage-excel/` 已修正为公司隔离口径，并固化“团队按团长公司整组归属、成员个人标记不一致时只告警不拆组、各公司人员集合互斥且完整覆盖当前用户”的防回归规则。
- 本轮不改服务端或客户端运行代码，不涉及部署、打包或生产写入；原 M0 目标与唯一下一步不变。

## 唯一下一步

执行存储 M0 只读对账：逐项核对 P0–P5 代码验收、开发数据与生产状态，并产出带来源的实现/部署矩阵。没有 M0 证据和用户明确授权，不进入生产 apply、读写切换或清理。
