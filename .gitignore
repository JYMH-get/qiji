node_modules
dist
*.log
.DS_Store

# TypeScript
*.tsbuildinfo

# Generated by `tsc -b` from vite.config.ts (composite node project)
vite.config.js
vite.config.d.ts

# IDEs and editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 内置 ffmpeg 二进制（约 97MB，不入库；用 src-tauri/scripts/fetch-ffmpeg.ps1 重新拉取）
src-tauri/resources/ffmpeg/*

# 内置 LibTV CLI 二进制（约 60MB，不入库；用 src-tauri/scripts/fetch-libtv.ps1 重新拉取）
src-tauri/resources/libtv/*

# 内置 即梦（Dreamina）CLI 二进制（约 30MB，不入库；用 src-tauri/scripts/fetch-dreamina.ps1 重新拉取）
src-tauri/resources/dreamina/*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 服务端部署产物（用打包技能重新生成，不入库）
qiji-server-deploy.tgz

# 客户端一键打包生成的可分享安装包（本地产物，不入库）
release/

# 个人技能（含大体积宣传媒体，与项目无关不入库）
.claude/skills/huashu-nuwa/

# 内置深度模型 Depth Anything V2-Small（约 45MB，不入库；用 src-tauri/scripts/fetch-depth-model.ps1 重新拉取）
public/depth-model/

# 换机导出包（本地生成，不入库）
outcode/

# 运维巡检/使用分析导出（本地产物，不进仓库）
ops-reports/

# LightRAG 本地 venv（不入库）
.venv-lightrag/

# Graphify 本地知识图谱与中间缓存（可按源代码重新生成，不入库）
graphify-out/

# 第三方 skills 由 npx skills 管理，不入库（清单见 skills-lock.json / ops-reports/skills-plan.md）
.agents/
