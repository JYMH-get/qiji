# Qiji 漫剧创作平台 · 需求规格文档（Living Spec）

> 本文档是用户端与管理端开发的**唯一事实来源**。每轮沟通后更新。
> 状态标记：✅已锁定　🟡讨论中　⬜待定
> 历轮完整细节见 git 历史中的旧版 CLAUDE.md（第80轮起只保留结论，不保留过程叙述）。

最后更新：2026-08-20（第240轮：**AI 工作台二轮迭代（用户截图三反馈）：双页签收进中栏标题栏 + 自由占位编辑移入工作台 + 工作台四栏样式对齐表格模式·纯客户端（双 worktree 并行子代理交付）**——①**页签入标题栏**（省一行竖向空间）：RtcCenterStage 整行 nav 删除，新紧凑件 [RtcCenterTabSwitch.tsx](src/rtc/panel/RtcCenterTabSwitch.tsx) 经 FrameEditor 中栏 `headerExtra` 与分集切换器并排（⚠ draggable=false+mousedown 不冒泡，防标题栏拖拽换位冲突）；②**自由结果占位收敛**：右栏 [RtcFreeGenProps.tsx](src/rtc/panel/RtcFreeGenProps.tsx) 删 提示词/垫素材（只留 身份/时间码/模型/开始生成/进度/重试=ai 设置），编辑面移入中栏新 [RtcFreeGenWorkbench.tsx](src/rtc/panel/RtcFreeGenWorkbench.tsx)（提示词灯箱式大编辑+垫素材拖拽条，逻辑零重写走 freeGenParts 共享件）；分派在 RtcCenterStage WorkbenchBody（placeholder 无 shotRef→自由工作台，否则 RtcShotAiWorkbench）；页签规则 1 扩展为**任何占位符**新选中→工作台；③**工作台四栏样式**（照用户给的表格模式样板）：提示词区改**两行头**——第一行 页签/▦预设方案/**补镜头**（重排算法收编 [shotReindex.ts](src/lib/shotReindex.ts) 单一来源，Frame161195/inferRun/工作台三处共用），第二行**仅本分镜**参数条（家族/线路/模型/方法/时长/比例/分辨率/真人图+放大——⚠ 写入字段与 genShotVideo 读取链逐项核实一把尺：overrides.videoModelKey/method/aspect/resolution/officialAssetIndexes + shot.durationSec）；原文改**逐行气泡+人名着色**渲染（[scriptBubbles.ts](src/lib/scriptBubbles.ts)：▲行=动作浅灰、`名：台词` 人名 10 色哈希恒同色；右键编辑整段保留）；左列 故事板/原文 分界可拖（30-70% 夹取会话态）。client tsc 干净 + **1264 vitest 全过（1250→1264，+14）** + vite build 过。**须客户端重新打包**；服务端零改动无需部署。详见 §8。）

上一轮：2026-08-19（第239轮：**实时剪辑两连：倍速联动时长修复 + 中栏「AI 工作台 / 预览」双页签·纯客户端（双 worktree 并行子代理交付）**——①**变速唯一入口 `setSegmentSpeed`**（[rtcOps.ts](src/lib/rtcOps.ts)，用户实测「10s 素材 2 倍速仍占 10s 时间轨、播放过半出错」修复）：变速联动 targetDurationUs（10s÷2=5s）并**恒回写 sourceDurationUs=round(targetDur×speed) 维持不变量**；减速撞后片段钳到空隙、图片等比缩放；SpeedInput 与属性剪贴板粘贴全部收口（⚠ speed 勿再经纯 patch），播放器源右缘钳制兼作存量失配 doc 防御（定住尾帧不出错）；②**中栏双页签「AI 工作台 / 预览」**（[RtcCenterStage.tsx](src/rtc/RtcCenterStage.tsx)）：工作台=占位符生成工作台移入中栏——三栏：左上故事板预览+历史（设为当前）、左下**原文锁定右键进编辑**、右侧灯箱式提示词区（同源单栏或 故事板·视频页签 + 垫图 RtcMaterialStrip + 匹配资产 + @/#/▦预设 + 推理/生成动作 + 视频历史）；自动切换纯函数收口（rtcCenterTabCore：选占位符→工作台、选素材卡→预览、占位符成片替换→预览、初始按 doc 有无成片、手动永远生效）；⚠ **工作台=不透明叠层，RtcSequencePlayer 仍常驻绝不卸载**（第236轮红线延续）；右栏 [RtcShotWorkbench.tsx](src/rtc/panel/RtcShotWorkbench.tsx) 收敛为「**AI 生成属性**」（生图 模型/比例/分辨率/画质 + 生视频 模型/方法/时长/分辨率/比例 + 图视同源开关——档位与表格模式一把尺（IMG_SIZE/imageResolutionOptions/videoReqOptions）、setter=setMediaSettings 同一函数）；共享件抽 shotWorkbenchParts.tsx 零重写。client tsc 干净 + **1250 vitest 全过（1231→1250，+19）** + vite build 过。**须客户端重新打包**；服务端零改动无需部署。浏览器/真机端到端未做，**待真机 QA**。详见 §8。）

上一轮：2026-08-19（第238轮：**实时剪辑大版本：预览选中框零测量修复 + 剪映复合片段格式逆向绿灯 + 四子代理并行交付（设置二级界面/快捷键体系/标记/定格/关键帧/倒放/裁剪/字幕/转场/复合片段全链路）·客户端 + Rust**——①预览白框与画面错位修复（SelectionOverlay 改**百分比几何零测量**，containFrac 只吃画幅比+素材自然比；拖动解算手势开始现读 getBoundingClientRect——WebView2 下 ResizeObserver 缓存滞后是病根）；②导出剪映草稿补 **meta 素材登记**（素材栏不再空）；③**复合片段草稿格式真机逆向定稿**（[docs/剪映复合片段草稿结构.md](docs/剪映复合片段草稿结构.md)，探针 v1→v4 二分实锤：主时间轴复合段=虚拟素材+12键片段+**8 组伴生素材引用**+drafts 内联子时间轴；子草稿全明文；pyJianYingDraft 不支持复合勿凭它猜）；④**四批并行开发**（快照基线 7e1f32d + 4 个 git worktree 子代理 + 集成 worktree 顺序合并接线）：批1 设置二级界面（快捷键两层表可改键+剪辑参数+预览收编）+全部小件（B/Ctrl+B 分割系、[ ] 全选、Q/W 裁剪、↑↓ 分割点、+/− 大幅移动、F/R 镜像旋转、Ctrl+Shift+S、Ctrl+G 组合、Ctrl+Shift+C/V 属性、替换片段、90s 初始轴、Alt+滚轮平移、图片 3s 可配）；批2 标记（M 系四键+标尺小旗）/定格 G/**关键帧 T**（模型+线性插值+播放器逐帧+面板菱形+剪映 common_keyframes 导出）；批3 倒放 D（Rust reverse_media 转码副本）/画面裁剪 C（编辑器+clip-path 预览+material 克隆导出）/**字幕轨真可用**（编辑+预览+texts 导出）/转场（18 条资源表+导出）；批4 **复合片段全链路**（kind:"compound"+subDocs 嵌套模型、双击进入/面包屑退出、递归预览、照逆向文档导出+Rust subdraft 白名单）。client tsc 干净 + **1189 vitest 全过（1019→1189，+170）** + vite build 过 + cargo check 过。**须客户端重新打包**；服务端零改动无需部署。浏览器/真机端到端未做，**待真机 QA**。详见 §8。）

上一轮：2026-08-17（第236轮：**实时剪辑二轮迭代：面板自由布局 + 全流程独立闭环 + 项目级隔离·纯客户端**——用户实机反馈五条全落地：①面板自由布局（三栏标题栏拖拽换位吸附、左右栏宽/时间轴高/AI 子栏宽全部可拖边界、`Qiji:rtcLayout` 持久化，⚠ 换位用 CSS order 零子树重建播放不断）；②中栏收成单一预览视口（选中素材=素材预览叠层、不选=时间指针预览，⚠ 叠层显隐勿条件卸载播放器）；③「AI 生成」子栏升级四步全流程工作台（剧本编辑→剧集拆分 LLM+快拆→资产拆分（同模板/预设胶囊/断连保护）→逐集智能推理/智能拆分+占位入轨）——实时剪辑不依赖资产模式独立闭环；④左面板资产出图（点资产卡→右栏提示词/模型/生成/主图历史，对齐 AssetWorkbench 走 startGeneration）+ 素材网格 auto-fill 自适应列数；⑤⚠ **项目级隔离**（用户明令勿串项目）：rtcStore/资产选中态按 projectInstanceId 全量重置、**commit/undo/redo 身份守卫**（stale 回写丢弃收编，杜绝 A 项目时间轴写进 B 项目）、同项目外部 rtcDoc 变更收编不回写、placeholderSwap 身份校验。client tsc 干净 + **722 vitest 全过（682→722，+40）** + vite build 过。**须客户端重新打包**；服务端零改动无需部署。详见 §8。）

上一轮：2026-08-17（第235轮：**新增第三模式【实时剪辑】（features.editorMode，路由 /frame-editor）——剪映式多轨时间轴 + AI 生成占位符工作流 + 剪映草稿导出·两端 + Rust**——与资产/画布并列：左=资产面板（项目/收藏/共享+新增视频/音频分类）、中=生成结果与轻量顺序预览、右=属性面板（占位符=分镜行属性化视图，生成走 startShotGeneration/startInfer 唯一路径）、下=自研纯 DOM 多轨时间轴（拖/裁/分/吸附/多选/undo）；数据模型 `rtcDoc` 微秒时间基按剪映草稿结构建（⚠ **segment=素材纯时间窗口引用，split 绝不复制素材；导出 materials 按 assetId 去重单条**）；分镜推理→逐镜占位入轨（幂等）、成片原位替换；TS 自研导出器写明文 draft_content.json 草稿文件夹（格式实锤 pyJianYingDraft，素材复制进草稿 assets\ 自包含，Rust 新增 4 命令路径双向限定）。server/client tsc 干净 + **682 vitest 全过（602→682，+80）** + vite build 过 + admin/agent script 语法过 + cargo check 过 + dist 产物 grep 实锤。**须重新部署服务端（editorMode 开关+两控制台）+ 客户端重新打包**；浏览器/真机端到端未做，待真机 QA。详见 §8。）

上一轮：2026-08-16（第234轮：**autodl（autodl.art·ComfyUI 工作流平台）视频渠道接入 · 3 模型/新模式「autodl」·纯服务端**——三条工作流（多图参考/文生视频/首尾帧）各成一个模型，**workflow_id=模型「上游模型名」**（种子为占位符「请填写workflow_id」，**部署后必须在管理端逐模型填真实 ID**）；⚠ 鉴权 `Authorization: <Token>` **原样令牌不带 Bearer**（控制台「令牌管理」创建、分组选 ComfyUI）；提交 `POST /api/v1/comfyui/comfyui_workflow/{workflow_id}`、查询 `POST .../result/{task_id}`（防御式状态族——文档未公布失败状态词）；⚠ 顺带修 errorScrub 真缺陷：**域名隐藏挪到模式名占位符保护之前**（autodl 模式名是域名 autodl.art 子串，原顺序裸域名逃逸）。server tsc 干净 + admin script 语法过 + 沙盒冒烟 54/54 + 回归 3/3 + 二启幂等。**须重新部署服务端（部署后填 Token + 三个 workflow_id）**；客户端零改动无需打包。详见 §8。⚠ 本行收口第229-233轮并行会话的「最后更新待统一」：228→233 各轮完整记录均在 §8。）

上一轮：2026-08-13（第228轮：**用户级「渠道形式模式」开关退役——两控制台用户页只留功能形式四开关·纯服务端**——用户定稿（截图圈出用户管理可用模式列的动态模式 chips）：「取消渠道形式的模式，功能形式的模式保留（资产、画布、libtv、即梦），因为渠道形式的可以通过渠道禁用。渠道商和源站一致」。⚠ **管控分层定稿（勿回退）**：渠道形式模式（稳定/高速/线路N/备用N/算力/云雾…即 modes 注册表）的管控=**「模式管理」全局启停（第165轮）+ 渠道商详情「可用模式（整商硬闸）」（第130轮，agents 的 features.modes **保留在役**）**两级；**用户级** features.modes 开关整体退役。改动：①[admin/index.html](server/src/admin/index.html) 源站用户页——行 chips/抽屉开关/批量模式下拉全部去掉动态模式（只剩 资产/画布/LibTV/即梦），toggleMode/userModeOn/dchip 删除，抽屉补提示语（整体停用走模式管理、按商停用走渠道商详情）；②[agent/index.html](server/src/agent/index.html) 门户用户页同尺（dchip/toggleMode/userModeOn/visModes/agModeOn 删除，抽屉「部分模式已由平台停用」提示改只看四功能开关）；③[users.ts](server/src/store/users.ts) **启动剥离存量用户行的 features.modes**（按字段存在性幂等，与 P4 剥离同模式；⚠ 必须剥——UI 删了、存量 modes 会隐形封 403 且再无开关可解；其余 features 四键不动）。服务端 403 门禁与客户端隐藏逻辑零改动（`applyAgentFeatureGate(agentId, user.features)` 合成后用户级贡献自然归零，渠道商级硬闸照常生效）；batch-op setMode API 保留（无调用方，无害）。实测：server tsc 干净 + 两控制台 script 语法过（new Function）+ dev 8787 浏览器端到端（管理端用户页 行chips=4/批量下拉=4/抽屉=4开关+提示语、LibTV chip 开→关→开 往返落库、渠道商详情整商硬闸动态模式按钮全在=未误伤；门户以临时商 t228agent 真实账密登录：用户页 chips/批量/抽屉同尺=4；两端零业务控制台报错；启动剥离实锤 dev 库 1 条 features.modes 归零其余键保留；临时商/临时用户已删）。**须重新部署服务端**（首启自动剥离存量用户 modes；部署验证词 `grep -c 渠道形式的模式不再按用户开关 /opt/qiji/server/src/admin/index.html` = 1）；客户端零改动无需打包。**行为变化（运营须知）**：此前对个别用户单独关过某渠道模式的设置随剥离**失效**（该用户恢复可用）——要限制请改用 模式全局启停 或 渠道商整商硬闸。）

上一轮：2026-08-13（第227轮：**资产模式视频界面素材区右击直删（去确认弹窗）·纯客户端**——用户令「素材区的素材右击直接删除，不需要弹窗确认，本身就是快捷设计，弹窗影响效率」。两处同尺：①表格素材区（[Frame161195.tsx](src/views/Frame161195.tsx) 素材缩略格 onContextMenu 去掉 confirmDialog 直接 removeMaterial）；②提示词放大弹窗的素材缩略条（[ShotMaterialStrip.tsx](src/components/ShotMaterialStrip.tsx) 同改并移除失效的 confirmDialog import——该条的悬停 ✕ 本就免确认，右击改直删属语义对齐）。删分镜/删分集等结构性删除的确认弹窗**不动**（只放开素材级快捷删除）。实测：client tsc 干净 + 602 vitest 全过 + 浏览器端到端（dev 8787+5199 临时账号：表格素材区右击「素材甲」→ 零弹窗、store 即时剩「素材乙」；放大弹窗缩略条右击「素材乙」→ 零弹窗、素材清空、缩略条只剩 ＋ 上传键；控制台仅服务端启动窗口期的连接拒绝属环境时序，业务零报错；临时账号已删、localStorage 已清、preview 已停）。**须客户端重新打包**（与第224/225轮客户端改动同一次发版）；服务端零改动。）

上一轮：2026-08-13（第226轮：**管理端「清除失效兑换码」+ 收藏/共享库素材服务端迁移脚本·纯服务端**——用户两点：①兑换码增加清除失效功能；②收藏与共享素材库的素材由服务端直接迁到新 OSS（有台账记录，不必等用户本地恢复）。①**清除失效兑换码**：[redeemCodes.ts](server/src/store/redeemCodes.ts) 新 `pruneInvalidCodes()`（已使用+已过期一次清掉，幂等；⚠ **已过期未使用的渠道商码按商聚合应退面额**——签发时是真金划转、过期=永远兑不了，直接删=白吞商的钱，与门户「作废退回」同语义；store 只聚合清单不碰 agents 防依赖）+ `POST /admin-api/redeem-codes/prune`（[admin.ts](server/src/routes/admin.ts) 逐商 changeAgentCredits 退回 + logCodeIssue「兑换码过期清除退回 ×N」记账；商已删=无处退仅清码；平台码是 mint 不退）+ 兑换码页「清除失效兑换码」按钮（danger 样式 + confirmDo 明示语义，作用于**全部范围**不限当前筛选）。②**迁移脚本 [migrate-fav-shared.mjs](server/scripts/migrate-fav-shared.mjs)**（P0 脚本惯例：默认 dry-run 零网络零写入、`--apply` 真跑、`--limit=N` 限量；零 src 依赖，容器内 `docker exec qiji-server node /app/server/scripts/migrate-fav-shared.mjs` 执行，**无需停服**——assets 表非常驻内存 WAL 并发安全）：目标=favorites DISTINCT asset_id ∪ shared-libs.json assets[].assetId；旧桶链接行（ossBridgeOldBases 判定）→ 下载旧直链 → 与服务端 reputAsset 桥接分支**完全同语义**落新桶（键=`<归属账号>/<旧路径>`、台账 url/oss_key/storage/sha256/has_thumb=0/last_ref_at 全更新）；**sha256 去重**（查库 + ⚠ 进程内同哈希在飞锁——并发下两条同内容行会在对方落库前双双上传，首到者登记 Promise 后到者等它共享、首传失败 resolve(null) 等待方转自传）；失败项如实列出（旧对象 404=只能等用户本地恢复；网络错误重跑即可——**幂等**：已迁行 url 非旧基址自动跳过）；共享库无 assetId 的 url 缓存记录只计数提示（无台账行可迁）。⚠ 脚本结尾用 `process.exitCode` 勿用 `process.exit()`（Windows 带未决句柄硬退偶发 0xC0000409 崩码，沙盒实测）。实测：server tsc 干净 + admin script 语法过 + 脚本 node --check 过 + **沙盒冒烟 16/16**（清码：5 清 2 留/商码退回聚合 750×2/已使用商码不退/落盘/幂等；迁移：双伪 S3（old=下载源 new=PUT 目标，⚠ 测试教训：伪 S3 在本进程时子进程必须异步 spawn——execFileSync 堵死事件循环=下载全超时）dry-run 零网络/收藏+共享 4 迁 1 失败/中文账号退 id 目录/同内容去重 PUT 恰 2 次/失败不动/非目标不迁/重跑只补失败项/三跑候选归零）+ 管理端浏览器验证（按钮+悬浮说明+确认弹窗渲染，取消未执行，dev 数据零变更）。**须重新部署服务端**（tgz 已重打 2026-08-13 8:00 含 224+225+226 三轮；部署验证词 `grep -c pruneInvalidCodes /opt/qiji/server/src/store/redeemCodes.ts` = 1）；**部署后运维**：先 `docker exec qiji-server node /app/server/scripts/migrate-fav-shared.mjs` 看 dry-run 统计 → 确认后加 `--apply`（趁旧桶未销毁尽快跑）；客户端零改动无需打包。
　**补充（存储页口径改「只看新 OSS」·纯服务端 · server tsc 干净 + admin script 语法过 + 三套沙盒冒烟回归 79/79 + dev 浏览器实锤）**——用户看存储页截图定「存储界面应该变了，原 OSS 我们不管了」：容量总览 695GB 大头与清理预览 4.9 万候选全是旧桶存量，口径失真。⚠ **统一口径（勿回退）：统计与清理只看我们在管的新 OSS，旧桶遗留（url 命中 ossBridgeOldBases 的存活行）单列不混算**——①[assets.ts](server/src/store/assets.ts) 新 `notOldUrlCondSql()`（SQL 片段单一来源）；storageStats 全部存活口径加该条件 + 新字段 `bridgeCount/bridgeBytes`（旧桶遗留计数，随桥接恢复/迁移脚本逐步归零）；②[retention.ts](server/src/store/retention.ts) listSweepCandidates **跳过旧桶行**（旧对象删不到、打墓碑还碍统计——其生命周期归桥接恢复语义管）+ sweepPreview 存活分母/unsized 同口径；③管理端容量总览 KPI 改五格（存活/占用标「新 OSS」+ 新「旧桶遗留（已舍弃）」格）+ 口径说明文案。dev 实锤：27,871 条旧基址行全部归入遗留格、新 OSS 存活 5/占用 0B、清理预览「将被清理」27k→0。tgz 已重打（2026-08-13 8:13）。）

上一轮：2026-08-13（第225轮：**登录页隐藏「服务器地址」栏 + 默认直连新服务器·纯客户端**——用户令「客户端隐藏服务器地址，默认为新服务器地址」。①[connectionStore.ts](src/store/connectionStore.ts) 新导出 `DEFAULT_SERVER_URL`（**dev=`http://localhost:8787` 防误连生产 / 打包版=`http://103.120.91.71:8787`**，⚠ 换服务器/上域名只改这一处）；load() 对空地址补默认值（**只补空值不覆盖已存地址**——渠道商节点用户/自定义域名用户填过的地址勿动）。②[LoginPage.tsx](src/canvas/LoginPage.tsx) 服务器地址 label+input 默认不渲染；**双击 logo 切换显示**（渠道商节点用户/运维改地址的隐蔽入口，勿删）+ 挂载时空地址兜底补默认。实测：client tsc 干净 + 602 vitest 全过 + vite build 过（**产物 grep 实锤打包分支=103.120.91.71:8787**，DEV 分支被正确摇树）+ 浏览器端到端（清 localStorage 后登录页无「服务器地址」栏、双击 logo 展开且输入框预填默认地址、零控制台报错）。**须客户端重新打包**（与第224轮桥接改造同一次发版）；服务端零改动。
　**补充（⚠ 用户定稿收紧：完全使用新服务器，不保留旧服务器记录·勿回退）**——首版「只补空值、已存地址不动」被用户否决：[connectionStore.ts](src/store/connectionStore.ts) load() 改为**启动一律把 serverUrl 钉回 DEFAULT_SERVER_URL**（老安装升级后本地存的旧服务器/旧域名地址直接作废；accessKey/account 保留，登录无感）；双击 logo 改的地址只在本次运行期有效、重启回默认——将来要支持渠道商节点常驻自定义地址时再放开。实测：client tsc 干净 + 602 vitest 全过 + 浏览器实锤（预置旧地址 `http://old-server.example.com:8787` → 重载后钉回默认、账号记录保留；测试 localStorage 已清）。）

上一轮：2026-08-13（第224轮：**整体换新对象存储 + 旧链接桥接恢复 + 新对象键布局（账号/类型/年/月/日）+ 内容哈希去重·两端**——用户拍板：**旧 OSS（jianqiji-qiji.cn-nb1）数据全部舍弃**、换新桶 `jianqiji.cn-sy1.rains3.com`，用户凭本地素材恢复（走既有「检查素材」功能）；**第223轮 P3 清理开闸随之搁置**（用户令「直接舍弃」——代码保留、默认 off 零影响，本轮并给 cleanup 补了去重引用数护栏）。①**新对象键布局**（[storage.ts](server/src/store/storage.ts) 新 layout `acct/kind/yyyy/mm/dd`）：新上传键=`<登录账号>/<类型>/<年>/<月>/<日>/<id>.<ext>`（如 `jianyi/image/2026/08/13/C00058078.png`；用户定稿：月份目录月底几十万张没法查阅，「谁生成的用谁账号」+精确到天）。账号经 `sanitizeAcctSegment` 归一（对象键仅 ASCII 硬约束：只留 `[A-Za-z0-9._-]`，中文/空→用户 id→anon）；账号查询**注入**（[index.ts](server/src/index.ts) `setAssetAccountResolver`→getUser().account，防 assets↔users 循环引用）。②**内容哈希去重**（用户令「相同的文件不重复上传」）：assets 新列 `sha256`+索引（存量 NULL 不参与）；createAsset 服务端算哈希、整桶命中存活对象→**零上传**，新行共享 url/oss_key（⚠ **多行共享一物理对象——任何删对象动作先查 `ossKeyRefCount`**，cleanup 真删循环已内置该护栏：引用数>1 只打墓碑不删对象）；直传签发 `/v1/assets/direct` 接受客户端 `sha256`（命中回 `{id,url,dedup:true}` 免上传；[managedClient.ts](src/services/managedClient.ts) directUpload 已带哈希并处理短路；谎报哈希最坏=拿到别人文件的链接，无他害，不为此逐字节校验）。③**旧 OSS 桥接（⚠ 勿回退）**（[assets.ts](server/src/store/assets.ts)）：`settings.json` 可配 `ossBridgeOldBases`（缺省 `["https://jianqiji-qiji.cn-nb1.rains3.com"]`）；`isAssetAlive` 命中旧基址**直接判死**（数据已舍弃，零外网探测）；`reputAsset(id,data,{acct})` 桥接分支：PUT 到 **active 档（新 OSS）**、键=`<恢复者账号>/<旧键路径>`（**域名换、路径保形**，用户定稿格式），台账 url/oss_key/storage/sha256 全更新+has_thumb 清 0（旧桶缩略图作废）+顺带清墓碑；**「谁先恢复用谁目录」**——台账一资产一行，第二人探活即命中新链接直接复用（A 用 B 的链接，不重复上传）。常规自愈（非旧基址）仍原键原 url。reput 路由带恢复者账号（req.user.account）。④**客户端**（[assetHeal.ts](src/services/assetHeal.ts)/[assetCheck.ts](src/services/assetCheck.ts)/managedClient）：`assetAlive` 改返 `{alive,url}`——存活但服务端 url 已变（别人桥接恢复过）→ 新依赖 `adoptUrl` **回写三元映射并换用新链接**；reput 成功同样回写（url 不变时不回写防白脏项目）；「检查素材」三入口与 ensurePublicUrl 提交前自愈**零新 UI 自动获得桥接能力**。实测：两端 tsc 干净 + **602 vitest 全过**（assetHeal/assetCheck 桥接断言更新+3）+ **沙盒冒烟 19/19**（伪 S3：新键布局与 PUT 位置/url 形状/sha 落库/去重零二次上传/引用数=2/findAssetBySha/中文账号退 id/旧基址零探测判死/桥接键=账号+旧路径/台账四字段更新/恢复后探活走新链/第二人直接复用/常规自愈原键原 url）+ **第223轮冒烟 44/44 回归**（含新护栏）+ **真机新桶双实测**：旧链接原图 3.6MB 下载→新桶上传→公网读回逐字节一致；CopyObject 移位至 `jianyi/image/2026/08/13/C00058078.png` 并删旧键（用户指定目标位置实锤）。**须重新部署服务端 + 客户端重新打包**（⚠ **旧客户端在别人恢复过的资产上会继续提交旧域名死链**——旧 assetCheck 不回写 url、探活通过就沿用旧链接，升级客户端才能吃到 adoptUrl 换链；旧客户端自己 reput 恢复的当次可用、下次会话又回旧链）。**生产切换步骤**：⑴管理端「存储」页 OSS 配置改 endpoint `https://cn-sy1.rains3.com` / bucket `jianqiji` / 新 AK/SK（publicBase 留空自动）；⑵服务器上编辑 `/opt/qiji/server/data/settings.json`：`storageProfiles` 增 `"sy1": {"layout": "acct/kind/yyyy/mm/dd", "active": true}` 并把原 active 档改 `"active": false`（新上传才走账号目录）；⑶`docker compose restart`；⑷发新客户端安装包；⑸旧桶暂缓销毁——用户本地副本也丢的素材趁旧桶在还能救（第223轮清理卡保持「关闭」）。）

上一轮：2026-08-13（第223轮·**已搁置**：**OSS 清理上线（P3）——清理执行模块 + 存量特赦 + 管理端「清理执行」卡·纯服务端**（⚠ 第224轮用户令「这一步直接舍弃，我直接换对象存储了」——功能代码保留、默认 mode=off 零影响，勿在旧桶语境下开启；将来若对新桶重启清理，注意 cleanup 已带去重引用数护栏）——用户拍板「之前要统计的 OSS 存储可以开始清理了，半个月多数据量够了」（P2 观察期自 08-01 两端上线起约 13 天，用户判定数据足够；方案 §9 存量特赦本身再给正式存量 30 天保护，提前开闸风险受控）。①**新 [cleanup.ts](server/src/store/cleanup.ts)**：清理 = `ossDeleteStrict`（新增严格版，删失败=**该行不打墓碑**下轮重试——吞错照打墓碑=对象还占容量却从统计消失）+ 台账打 `purged_at` 墓碑；⚠ **台账行与 url/oss_key 永不删**（reputAsset 自愈要 PUT 回原键）；has_thumb 行顺带删 `thumb/` 键（best-effort）+ 无 oss_key 的降级行直接墓碑（noKey 计数）。三道闸门：`mode` off（默认，部署后不动=零动作）/dry（试运行只记报告）/on（真删）+ `scope` tmp（**首批只清 TP**，方案 §9 分批）/all + **每轮 ≤2000 个**（每小时一轮、created_at 最老先删、并发 8、防重入）；按**行自己的 storage 档**定位删除（勿用 active 档）。状态落**独立 `data/cleanup.json`**、报告 `data/cleanup-report.json` 留 40 轮。②**存量特赦（§9）**：mode 首次离开 off（含拨到 dry）自动执行、幂等永不重跑——全部「从未引用」的**正式**资产 `last_ref_at`=当日（⚠ **TP 刻意不特赦**：临时资产就是首批要清的对象，特赦它=首批空转 7 天）、ref 档 <30 时抬到 30 并记 `refBefore`（满 30 天**运营手动**在管理端调回，卡片有天数提醒）。③⚠ **顺带修 settings.json 共写事故面（勿回退）**：[settings.ts](server/src/store/settings.ts) 持**启动时快照**整体覆写 settings.json → 启动后 retention/cleanup 写入的键会被下一次「保存 OSS/注册配置」用旧快照抹掉（特赦的 ref=30 被抹回 14 = 存量保护提前失效、清理提前开删）——`persist()` 改**只覆盖自有 7 键（oss/teamMemberLimit/favQuotaBytes/teamLibQuotaBytes/storageCode/register/deviceLimit）、其余键以磁盘现值为准**；cleanup 状态独立文件是第二道保险。④[retention.ts](server/src/store/retention.ts) 候选查询抽 `listSweepCandidates`（含 oss_key/storage/has_thumb），**预览与清理执行共用同一条查询**（口径永不分叉）；retention 本身仍零删除能力（P2 断言延续，删除只在 cleanup.ts）。⑤**reputAsset 自愈即复活**（[assets.ts](server/src/store/assets.ts) stmtHeal）：重传成功清 `purged_at` + 刷 `last_ref_at`——不清的话 touchAssetRefs 恒回报 needHeal，客户端每次打开项目都重传永不收敛。⑥**管理端**：`GET/PUT /admin-api/cleanup` + `POST /admin-api/cleanup/run`（mode=off/dry 时手动执行=只算不删的预演）；relay SOURCE_ONLY 正则加 cleanup；「存储」页新**「清理执行」卡**（模式/范围下拉 + 保存（开正式删除走 confirmDo）+ 立即执行一轮 + 特赦状态行 + 最近执行表）；保留策略卡徽标改「预览 · 只算不删」。实测：server tsc 干净 + admin script 语法过 + **沙盒冒烟 44/44**（scratchpad 复制 src+junction node_modules+独立 data+**伪 S3**（虚拟主机 sbx.localhost 实测可解析）：默认关闭零动作/特赦幂等·TP 不特赦·ref 抬 30·refBefore 记档/dry 零删除零墓碑/真删 主对象+缩略图 DELETE 实锤+墓碑行 url·oss_key 保留/删失败不打墓碑下轮补删/noKey 直接墓碑/收藏+共享库豁免/scope tmp→all 分批/needHeal→reputAsset 复活（PUT 回原键+墓碑清除+不再进候选）/预览与执行口径一致/报告与状态落盘）+ **settings 合并落盘专项 3/3**（启动后写入的 retentionDays.ref=30 不再被旧快照抹回）+ **dev 8787 浏览器端到端**（「清理执行」卡渲染（已关闭/off/tmp/特赦提示/四卡顺序）、手动预演对真实 dev 库：候选 TP 7,660、另 19,986 待第二批、每轮上限 2000 生效、零控制台报错；测试报告残留已删、dev 配置零变更）。**须重新部署服务端**（`qiji-server-deploy.tgz` 已重打 2026-08-13 5:59 含 222+223 两轮）；客户端零改动无需打包。**部署后操作节奏（管理端「存储」页「清理执行」卡）**：先拨「试运行」（此刻即自动执行存量特赦）看一两天报告 → 「正式删除 · 仅临时资产 TP」→ 观察几天无用户反馈 → 「全部档位」；满 30 天把「被引用」档调回原值（卡片有提醒）。**运营注意**：①生产存量 size_bytes 未回填的行，报告「释放容量」偏低（跑 `reconcile.mjs --apply` 回填后即准）；②tmp/ 桶级 lifecycle（P3 清单第 1 条）**不做**——legacy 平铺布局下 TP 与正式资产同在 `assets/` 前缀，无法按前缀挂规则，由清理任务本身覆盖。）

上一轮：2026-08-13（第222轮：**请求记录报文保留 30→3 天（恢复三档）+ 启动期 VACUUM 空间回收·纯服务端**——用户报 qiji.db 已 2.41GB、服务器每晚卡顿。**排查定性**：真机实测 磁盘 4%（902G 盘）/内存 6.3G/62G/零 swap——磁盘内存全排除，「晚间卡顿」与库体积基本无关（更偏跨境线路/晚高峰，待卡顿现场抓 vmstat/带宽复核）；库体积本身=P4（第183轮）迁移后 `log_details` 承载 **30 天全量①②③④报文**（~150-200MB/天，稳态将到 5-6GB）——属设计使然非泄漏，用户拍板「**报文仅留 3 天**」。改法（[logs.ts](server/src/store/logs.ts)）：①**恢复三档保留**（第129轮删掉的中间档回归）：≤3 天全量（索引+详情）/ 3~30 天仅索引（`DETAIL_KEEP_MS=3天`，trimAndPrune 新增详情档：删 log_details 行 + `detailPruned` 标记随索引落库=重启/每小时不重复探删；⚠ **running 的不清**——启动对账要读 ④ 段救回视频）/ >30 天整天清除（原样）；列表/统计/导出全走索引且成片链接第129轮起预抽进 `resultLink`，不受详情清除影响；②**vacuumIfBloated 仅启动跑**（⚠ 勿挂小时定时器——VACUUM 独占整库阻塞）：freelist >80MB 且占比 >25% 才触发（日常稳态碰不到，只有保留期收紧/大清理后的首启会跑）——SQLite DELETE 只挂 freelist 复用、文件永不自行缩小，没有这步 2.4GB 文件删完也不瘦身。**两控制台零改动**（`detailPruned` 徽标与「完整详情仅保留 3 天」文案是旧三档遗留、本就在，语义自动归位）。实测：server tsc 干净 + **沙盒冒烟 13/13 ×2（二启幂等）**（scratchpad 复制 src+junction node_modules+独立 data：40 天整条清除 / 5 天 success 详情删+标记落库+resultLink 保留 / 5 天 running 四段完整+getRunningLogs 可用 / 1 天完整 / PRAGMA page_count·freelist_count 形状）。**须重新部署服务端**；⚠ **部署首启一次性阻塞约 1~3 分钟**（清 ~27 天存量详情 + 全量标记落库 + VACUUM 2.4GB，日志会打「[logs] VACUUM 完成」）；客户端零改动无需打包。**运维同轮**：服务器 data/ 里 `qiji.db.bak-p0/p4-*` 整库手工备份已堆 16 份 ≈12.5GB，按用户令**只留最新一份**（手工删，路径经 `docker inspect qiji-server` 查 Mounts 定位——`/opt/qiji` 在宿主机上并不存在，1Panel 显示的是容器内路径）。）

上一轮：2026-08-12（第221轮：**密码找回补全——管理端「重置用户密码」+ 个人中心「修改密码」·两端**——用户报「没有找回/重置密码功能，好几个用户忘记密码找不回」。核实：登录页「忘记密码」自助找回（第212轮）两端代码俱在，但①强依赖 SMTP/短信通道（未配=报「通道暂未开放」）②只认邮箱/手机号——**旧式用户名账号（管理端手工建号绑定的那类）没有任何恢复路径**，且管理端此前只有「解绑账号」（P2b 取消激活码后是死胡同：已登出用户解绑后无法自行登录，重新注册=新用户、老账号积分搁浅）。本轮补两个兜底：①**管理端重置用户密码**：`POST /admin-api/users/:id/reset-password`（[admin.ts](server/src/routes/admin.ts) 复用 setUserPassword；无账号用户 400 明确报错）+ 用户抽屉「登录账号」行新「重置密码」按钮（dlg 弹窗、≥6 位前端校验）；「解绑账号」确认文案改明示「**不是找回密码的手段**」+ admin.ts 过时注释修正（「可用激活码重新注册」措辞废止）。②**个人中心自助修改密码**：`POST /v1/password/change`（[routes.ts](server/src/routes.ts) authed：校验旧密码；未绑账号 400 引导先绑定；⚠ **不动 accessKey——其它已登录设备不掉线**，与「重置 API 密钥」刻意区分）+ contract `Endpoints.passwordChange` + managedClient.changePassword + [PersonalCenter.tsx](src/components/PersonalCenter.tsx)「登录账号」已绑定行新「修改密码」折叠表单（当前密码/新密码/确认修改）。实测：两端 tsc 干净 + **601 vitest 全过** + admin script 语法过 + **dev 8787 冒烟 18/18**（绑定用户名账号→登录→未带凭证 401/旧密码错 400/新密码太短 400/修改成功→旧密码 401·新密码 200 且 accessKey 未变→管理端重置三态→无账号 400→解绑后改密 400 引导绑定→登录态重绑成功）+ **两端浏览器端到端**（管理端抽屉按钮/弹窗/短密码报错/保存 toast→API 实锤 UI 设的新密码 200·旧密码 401；客户端 5199 真实账密登录→个人中心「修改密码」展开→错旧密码「当前密码不正确」→修改成功→表单收起→API 实锤生效；控制台 400/403 均为负例与设备抢占预期；临时用户×3/localStorage 全清）。**须重新部署服务端 + 客户端重新打包**（只部署服务端=管理端即可救急重置密码，旧客户端仅缺「修改密码」入口）。**运营注意**：自助「忘记密码」要真正可用，须在管理端「注册与安全」配好 SMTP——未配时用户端报「邮箱通道暂未开放，请联系管理员重置密码」，管理员以本轮「重置密码」兜底；旧式用户名账号永远走管理端兜底。）

上一轮：2026-08-11（第220轮：**渠道商门户「消耗」口径改「名下用户实扣」·纯服务端**——用户报（截图）：门户请求记录「我的消耗」恒 0/「—」、无法统计用户消耗。根因=P1 统一定价后生成请求 `agentCosts` 停写（积分实时扣用户、商不再中转），而门户消耗口径（[logs.ts](server/src/store/logs.ts) `logCostFor` agent 视角）只认 agentCosts。改法（⚠ 单点勿另写）：**agent 视角=带 agentCosts 的记录（发码面额划转/作废退回、P3 渠道节点池扣、统一定价切换前的旧链式结算）取本商积分池那份；无链记录（P1 后的生成请求）取 `cost`=名下用户实扣**——门户 列表/KPI/统计页/导出/详情 全部单点生效；user/platform 视角零改动（管理端「渠道商详情→请求记录”本就有独立「用户扣费」列，不受影响）；agentCosts 仍绝不下发。门户文案同步：「我的消耗（结算积分）」→「消耗（积分）」、列头/悬浮/页脚/CSV/详情 7 处改口径说明。**同轮防白嫖审计（用户令「确认用户是否实际扣积分」）：无漏洞**——代码面 `chargeBilling` 在 dispatch **前**同步扣款（第183轮定式）+ settle 原子落 credit_ops（pre/post 逐笔）+ 失败自动 refund；数据面 dev 库实证：近 300 条成功生成日志 cost>0 者与 credit_ops 逐笔对应（97315→97305→…精确），cost=0 的成功单全部为 echo 测试模型或 **7-11/7-18 该模型定价前的旧单**（image2/gemini 3.1 pro 当时价 0，7-25 起同模型已正常扣 10）；E2E 实测 100→86 提交即扣。实测：server tsc 干净 + **沙盒冒烟 18/18**（scratchpad 复制 src+junction node_modules+独立 data：三视角×生成/节点/发码/作废/旧链式/失败 11 例 + 真实落笔 logSummary/listLogs/投影零 agentCosts 泄漏 7 例）+ **浏览器端到端**（dev 8787 热载新代码（watch worker 当日重启实锤）：临时商+归属用户+echo 模型 7 分×2 单 → 门户登录 → 请求记录页 KPI「消耗（积分）=14」/两行各显 7/新表头悬浮/页脚新文案、统计页 KPI 14+按模型/按名下用户/按步骤 全对；零控制台报错；临时 商/用户/模型 已删（2 条 __t220 测试日志残留无害））。**须重新部署服务端**（门户页面+口径随部署生效）；客户端零改动无需打包。
　**补充（源站商属视图同病同修：platform 视角「无链=用户实扣」·纯服务端 · server tsc 干净 + 沙盒冒烟 13/13 + 管理端浏览器端到端全过（临时数据全清、零控制台报错））**——用户随即报（截图）：源站请求记录切到渠道商范围，列表行有值（列直显 `cost`）但 KPI「消耗积分」恒 0——同一根因换了个视角：商属范围汇总走 `logCostFor` platform 视角，无链记录返回 undefined。修法与 agent 视角同构：**platform 视角无链记录回退 `cost`**（统一定价下积分实时扣用户、无中转，「源站实收」=用户实扣——语义本就成立）；带链记录（发码划转/节点池扣/旧链式）仍取根级实扣，优先级不变。配套：管理端「渠道商详情→请求记录」的「源站实收（旧口径）」列改「源站实收」+ 悬浮/页脚文案按新语义重写（原「仅旧记录有值」措辞废止）。实测：沙盒 13/13（三视角全景回归：生成/节点/发码/旧链式/直属 + 商属范围 logSummary KPI=115、platformCost 列有值、源站直属视图口径不变不混入）+ 浏览器端到端（dev 8787：临时商 2 单 7 分 → 主请求记录页切商范围 KPI「消耗积分=14」+行值 7、统计页商范围 KPI 14+按模型/按用户全对、渠道商详情「源站实收」列 7/7 有值；零控制台报错；临时 商/用户/模型 已删）。与主轮同一次部署生效。）

上一轮：2026-08-09（第219轮：**商业化改造 P4 清理收尾——存量旧字段剥离 + 规格文本收口·两端（注释级）**——商业化改造（P1 统一定价→P2 注册体系→P2b 邀请码→P2c API 密钥→P3 渠道节点）至此**全部四期完结**。盘点结论：退役体系的代码在各轮**绿地删除时已清净**（全库扫 codeCredits/codePricing/parentAgentId/validityDays/bindOrCheckMachine/机器码采集链等零残留；存活的 agentChain/chainPricingVersion/logCodeIssue 均为在役消费点），P4 实做两件事：①**存量数据剥离**（[users.ts](server/src/store/users.ts) 模块加载段，与 P1 agents 迁移同模式）：启动剥离用户行上的死属性 `machineCode/validityDays/expiresAt`（P2b 绿地删了字段定义与读写路径，存量 JSON 行上的旧值只是死数据）——按字段存在性幂等、首次剥离前备份 `users.json.bak-p4-cleanup`（已存在不覆盖）、备份失败不阻塞启动；②**陈旧注释/规格收口**：users.ts（devices 注释=x-device-id 随机 UUID、account 注释去「四层防线」）、routes.ts 登录注释、客户端 App.tsx/LoginPage.tsx 两处机器码措辞；CLAUDE.md §2 鉴权定稿文本（API 密钥+设备头+ank 三凭证体系）、进度快照与 §11.4 管理 API 清单去「批量激活码」。实测：server tsc 干净 + client tsc 干净 + **601 vitest 全过** + **沙盒三启实锤**（p1sand：首启剥离 5 处死属性→0、备份留档含全部旧值、剥离后账号登录照常；再启 users.json 与备份 **md5 零变化=幂等**）。**须重新部署服务端**（与前几轮同一次部署即可，首启自动剥离）；客户端注释级改动随下次打包。**商业化改造整体部署清单（四期一次上）**：备份 data/ → `npm i`（nodemailer/undici/presigner）→ 部署（首启自动：P1 agents 迁移+P2b 邀请码回填+P4 users 剥离）→ 管理端「注册与安全」配 SMTP → 发客户端安装包；渠道商独立部署走 [docs/渠道节点部署手册.md](docs/渠道节点部署手册.md)。）

上一轮：2026-08-15（第233轮：**congge（congchen.top·聪宸）图片+视频渠道接入 · 图片 3 + 视频 4 模型/新模式「congge」·纯服务端**——同站同一把 Bearer sk- 两条协议：`congge-image`（同步单请求，文生图 `/v1/images/generations`、**图生图必须走 `/v1/images/edits`**，垫图 单张 `image`/多张 `images`、⚠ **硬上限 4 张**，字节兜底走 multipart）+ `congge-video`（异步 submit+poll `/v1/videos`，成片回退 `/v1/videos/{id}/content` 带 Bearer 下载）。⚠ **上游视频模型名带空格且大小写敏感**（`seedance2.0 Mini-480p`）逐字照抄，分辨率编在名字里 → 4 个外显模型经 routes 重定向到 9 个上游真名；⚠ **图片侧不发 `size`**（与 `resolution` 冲突）改由客户端像素串换算 比例+分辨率档，**视频侧不发 `size`/`resolution`**（已在模型名里）。占位价（用户定）：图片统一 10 积分/次；视频按秒 mini 30 / fast 40 / 2.0 50 / 2.5 60（720p 锚点，其余档按上游元/秒比例折算）。server/client tsc 干净 + admin script 语法过 + 沙盒冒烟 114/114 + 二启幂等 + 存量库增量核对（+7 模型存量零改动）。**须重新部署服务端（部署后到管理端「congge（聪宸）」渠道填 sk- 密钥，或环境 `CONGGE_API_KEY`）**；客户端零改动无需打包。详见 §8。）

上一轮：2026-08-09（第218轮：**机器码全面舍弃 + API 密钥正名（身份验证与后续对接的唯一凭证）·两端 + Rust**——用户令「将机器码舍弃，增加 api 密钥代表身份，用来身份验证和后续对接」。**语义定稿**：accessKey（qk-）本就是身份凭证——本轮把它**正名为「API 密钥」**（用户可见/可自助管理/可用于第三方 API 对接），并把机器码/硬件指纹**连根拔除**；「同时在线设备限制」（P2 决议 Q5）保留，设备区分改用**前端随机 UUID**（无身份语义、不采集任何硬件信息）。①**服务端**：[auth.ts](server/src/auth.ts) 新 `deviceIdOf(req)`——设备标识= `x-device-id`（新）?? `x-machine-code`（旧客户端兼容收下，当不透明设备 id）；login/register 的 body 同构 `deviceId ?? machineCode ?? 头`；新端点 **`POST /v1/api-key/regenerate`**（用户自助换新密钥：旧钥立即失效=其它设备/外部对接全部 401 登出、**设备表只留本机重新登记**；ank- 节点凭证不在白名单天然 403）；管理端用户页「访问凭证」全部改称「**API 密钥**」（悬浮说明=登录与 API 对接共用 Bearer 头）。②**客户端**：[connectionStore.ts](src/store/connectionStore.ts) 机器码三层体系（硬件指纹/HW_KEY 桥接/initMachineCode 启动等待）整删，换 `getDeviceId()`（同步、`Qiji:deviceId` 随机 UUID 持久化；⚠ **升级续用回退链勿删**：优先读旧 `Qiji:hwCode`/`Qiji:machineCode` 作设备 id——老用户升级后设备身份不变，不会被自己的旧设备记录抢占登出）；[managedClient.ts](src/services/managedClient.ts) 头改 `x-device-id` + login/register body 带 deviceId + 心跳空 body + 新 `regenerateApiKey()`；[App.tsx](src/App.tsx) 启动不再 await 机器码采集；**删 [machineId.ts](src/lib/machineId.ts) + 13 条单测**；**Rust [lib.rs](src-tauri/src/lib.rs) get_hw_fingerprint 命令 + HwFingerprint/b64_utf16le/run_hidden_capture 整删**（不再有任何 PowerShell CIM 采集）；[PersonalCenter.tsx](src/components/PersonalCenter.tsx) 概览页新「**API 密钥**」卡（脱敏显示 `qk-xxx••••xxxx`+显示/复制/**重置**（confirmDialog 明示其它设备将失效）→ 新钥自动写回 connectionStore **本机无感续用**）；contract 新 `Endpoints.apiKeyRegenerate`。实测：两端 tsc 干净 + **cargo check 过** + **601 vitest 全过**（614−13 条 machineId 删除）+ client build 过 + **沙盒冒烟 12/12**（8967：body.deviceId 登录/x-device-id 心跳/**旧 x-machine-code 头兼容收下**/陌生设备 403/登录抢占 d2 顶 d1/**自助重置**（新钥 qk-≠旧钥、旧钥立即 401、本机新钥续用、设备表只留本机——d3 仍 403）/登录响应无 machineCode/ank 凭证拒用）+ **浏览器端到端**（5199+8967：账号登录（设备 id=随机 UUID、无 hw/machineCode 键残留）→个人中心 API 密钥卡（脱敏/显示/复制/重置全流程：确认弹窗→新钥落 localStorage→「已重置——本机已自动换用新密钥」→新钥 /v1/me 200 会话不断）；零控制台报错；测试 localStorage 已清；测试账号 accessKey 已轮换属预期）。**须重新部署服务端 + 客户端重新打包**（旧客户端发 x-machine-code 头照常可用——服务端兼容收下；新客户端只发 x-device-id）。**对接说明（后续开放 API 用）**：第三方对接=`Authorization: Bearer <API 密钥>` 调 /v1 协议（generate/tasks/assets…），无需设备头（首个请求自动占一个设备位；对接场景建议管理端把该用户设备上限调大或 0=不限）。已知边界：①浏览器 dev 清 localStorage=新设备（占一个名额，P2 既有边界不变）；②「设备」不再有硬件对应——同机不同浏览器 profile=不同设备。⚠ **第137轮硬件指纹相关规则（§9A「材料格式勿动」「时序规则」「客户端须重新打包（Rust 命令+启动时序）」）自本轮整体废止**。）

上一轮：2026-08-09（第217轮：**算力（xienlive.com·OctopusAI）视频渠道接入 · 1 模型 MINIMAX_H3/新模式「算力」·纯服务端**——用户提供 curl 示例+价目表（分辨率×是否含视频参考 双维度上游价），情报补全=官方 api.html 内嵌 spec 实拉（提交/首尾帧/查询三端点全形状）。①**上游（Base `https://xienlive.com`，Bearer sk-）**（[suanli.ts](server/src/translators/suanli.ts)，异步 submit+poll 走统一 VideoDriver）：提交 `POST /api/v1/video/generate`（body `{materials?, prompt, model, aspectRatio, durationSeconds, resolution?}`）/首尾帧 `POST /api/v1/video/generate-flf`（firstFrameUrl/lastFrameUrl，首=带故事板>素材第1图、尾=下张——与苏打水/星辰同尺）/查询 `POST /api/v1/video/query {submitId}` → 状态码 **10 待处理/20 处理中/30 失败/40 提交失败/50 成功**，20 态 statusText「生成中 62.5% (5/8)」提取百分比作进度，50 态取 `videoUrls[0]`（兜底 items[0].video.qualities[0].videoUrl）+ items[0].coverUrl。⚠ **素材引用=`<<<N>>>` 0 基全局下标**（materials 图/视/音混排数组；MINIMAX_H3 按 Seedance 系 0 基规则，用户示例实锤）——图例注入 @Image1/@Video1/@Audio1 后按「图在前、视次之、音在后」排列经 `suanliTags` 转写下标（帯故事板追加 materials **末尾**+说明行，不前插防下标错位）。守卫：**参考音频须伴随图或视频**（模型说明明言，纯音频前置明确报错）；空提示词前置拦截。§9 时长/比例/分辨率经 paramPass 原样透传（durationSeconds 上游必填→缺省补 15；分辨率缺省不发）。成片=vlabvod/byteimg CDN 签名直链裸 fetch 可下→轮询循环转存 OSS。②**注册面**：新模式 suanli「算力」（[modes.ts](server/src/store/modes.ts) v17）+ 渠道 `ch-suanli`「算力（xienlive）」+ 模型 **`sl933-minimax-h3`**（音视频同步生成；4-15s；分辨率 480p/720p/1080p/4k（4k=720p 超分）**是普通请求参数非模型编码→routes 只换价不换上游名**（480p 1/秒·720p 2/秒·1080p 5/秒·4k 10/秒 全占位价）；methods omni+frames；素材上限文档未给=933 惯例值；familyId 自动归 **fam-minimax**（第216轮新家族即时复用））+ 协议 `suanli-video` 六处注册（Protocol 联合/BUILTIN_PROTOCOLS/VIDEO_DRIVERS/dispatch case/upstream fallback `config.suanli` env `SUANLI_BASE_URL/SUANLI_API_KEY`/admin PROTOCOLS 下拉）+ errorScrub（品牌 OctopusAI+「算力」（=模式对外名经占位符保护）+协议段 suanli；域名 xienlive.com/vlabvod.com）+ REHOST 白名单（.xienlive.com/.vlabvod.com/.byteimg.com）+ .env.example。③**过程要点**：吸取第216轮教训——**动种子前先停 dev tsx watch**（防编辑中间态被热重启种进真实库），全部编辑完 tsc 过后再重启=真实库一次种对（sl933 familyId 直接 fam-minimax 零修正）。实测：server tsc 干净 + **沙盒冒烟 23/23**（真实库副本：模式/渠道/模型/家族/routes 补种全对；suanliTags 0 基混排下标纯函数；omni 提交 materials 混排+@tag 转写+20s 透传+纯音频守卫+缺省补默认+带故事板追加；frames 走 -flf 首尾正确+带视频拒；提交 success:false 透出上游 message；poll 五态含 62.5% 进度提取/50 无链接明确失败/5xx 不终态；errorScrub 品牌域名全隐+「算力」保留）+ **二启幂等**（四库文件零变化）+ dev 8787 热迁移实锤（模式「算力」v17/渠道/模型 fam-minimax routes=4/health 200）。**须重新部署服务端（部署后到管理端「算力（xienlive）」渠道填 sk- 密钥，或环境 SUANLI_API_KEY）**；客户端零改动无需打包（模式/模型/方法下拉 catalog ≤30s 热更）。**运营注意**：①占位价按上游价目（元/秒 0.12/0.25/0.62/1.27）×≈8 折算,上线前定真价；②上游「含视频参考另有折扣价」（WITHVIDEO 档更便宜）我方单一档保守按无视频档折算——不建模双维度计费；③4k=720p 生成后超分（上游语义,观感≠原生 4k）。**待真机 QA**：小额真单（omni 带图+音、首尾帧各一）、`<<<N>>>` 引用被上游正确识别、成片 vlabvod 直链转存 OSS、20 态进度百分比在客户端的显示。）

上一轮：2026-08-09（第216轮：**星辰（AIStartLab）按 2026-08-09 实时 config 全量更新——「全部接上」：+8 视频线 11 模型 +1 图片线 1 模型（12→24）+ 存量能力对齐 + 2 新家族·纯服务端**——用户令「更新星辰渠道的模型，先列出…全部接上」。情报=真机 `GET /generation/config`（渠道 ch-aistars 密钥实拉）：上游 12 视频线+4 图片线，已接 4+3 线全部还活着。①**新接线路与命名**（[models.ts](server/src/store/models.ts) `ais`/`aisImg` 工厂经 ③ 补种入库，全部**占位价**上线前管理端定真价）：47 普通线路（上游默认线，比 48 专线便宜）=`xc933-sd2.0-p`/`-fast-p`（四档/两档 routes 含 4K）；49 按条+**全系首尾帧**=`xc933-sd2.0-c2`（720p/1080p）/`-fast-c2`；58 限时 720p 按条（卡人脸 慎用、图9视0音0、时长离散档 5/10/15）=`xc900-sd2.0-fast`；54 **Seedance 2.5**（**图30视10音10、4-30s**）=`xc-sd2.5`（素材量超三位数字惯例，名不编码）；59 **海螺 MiniMax H3**（图9视0**音3**）=`xc903-minimax-h3`；56 **快乐马 HappyHorse**（新家族，**仅图生视频**）=`xc900-hh1.0`/`-hh1.1`；60 **可灵 Kling V3**（仅图生视频、图3）=`xc300-kling3`；21 Gemini Omni Flash 重接=**新 id `xc600-gemini-omni`**（旧 xc600-gemini-flash 在墓碑不复活——惯例）；55 图片 High 档=`xc-gpt-image-2-high`。②**存量能力对齐**：[aistars.ts](server/src/translators/aistars.ts) LINES/IMG_LINES 全表按 config 重写（⚠ 第215轮起 aspects/时长仅作缺省默认与文档——参数原样透传；img/vid/aud/needImage/frames 仍是前置守卫）——**51 grok 能力大改**（两款恢复纯文生 needImage 移除；1.0 时长 6/10 两档→连续 1-15s+7 比例；1.5 图上限 1→**7**、5-15s）、**48/47 线 frames 已从 config 消失**（首尾帧改走 53/49 线）、49 fast 恢复 t2v+frames、50 线 sd2.0 时长放宽 4-15+5 比例。存量库走**⑤j 定向迁移**（`xcCaps216Version`，⚠ 全部守卫式只改「仍为旧种子形状」的字段——管理端改过价/改过档的分毫不动，沙盒「改价 99/自定义比例档」双守卫用例实锤）。③**家族 v2**（[families.ts](server/src/store/families.ts) FAMILIES_SEED_VERSION=2）：+`fam-minimax`「MiniMax」（与客户端 LibTV H3 的 fam-minimax 同 id——第203轮「服务端注册同 id 家族显示名自动跟随」在此兑现）+`fam-happyhorse`「HappyHorse」；classifyFamily 新增 minimax/hailuo 与 happyhorse 规则（⚠ **必须在 seedance 数字兜底正则 `/933|900|903/` 之前**——xc903-minimax-h3/xc900-hh 的数字段会被误吞，沙盒实锤过）。④⚠ **dev 环境教训（排障可复用）**：本机 8787 是 tsx watch——**编辑 models.ts 种子/迁移代码途中每次保存都热重启并把「半成品代码」的补种跑进真实 dev 库**（本轮 h3/hh 在 classifyFamily 规则落地前被种入=familyId 钉错 fam-seedance，已经 admin API 修正 3 条）；**生产不受影响**（部署=最终代码一次跑对）；冒烟因此必须「先剥离 dev 库的中间态痕迹还原部署前形状」再跑迁移。实测：server tsc 干净 + **沙盒冒烟 40/40**（A 22：剥离还原后迁移+补种+家族+守卫双用例+墓碑不复活+总数 24；B 二启幂等文件零变化；C 5：全新空库首启直接正确；D 12：kling/hh 无图明确报错、sd2.5 31 图超限报错+12 图 30s 透传、H3 音放行视拒绝、49 fast 首尾帧放行+48 fast 拒绝、gemini 缺省 10s、grok 新能力放行）+ dev 库热迁移实锤（24 模型/13 家族/⑤j 落库/3 familyId 修正/health 200）。**须重新部署服务端**（首启自动 ⑤j 迁移+补种 12 模型+家族 v2）；**客户端零改动无需打包**（模型/家族/方法下拉全走 catalog ≤30s 热更）。**运营注意**：①12 新模型全为**占位价**（视频按上游积分比例折算、图片按上游积分量级），上线前管理端定真价——快乐马/Kling/H3 网关线**零真单**建议先小额摸底；②「卡人脸」线（53/58）与「不卡人脸」线（50/59）说明已注种子注释，运营侧知悉；③grok1.0 上游已改按秒计价（9-12 积分/秒），我们侧仍沿用旧按次占位 12 分——**建议管理端复核 grok 两款定价**；④48 线用户此前若习惯用 fast 首尾帧，现会明确报错——引导改用 53/49 线（-mj/-c2 款）。**待真机 QA**：新线小额真单（尤其 sd2.5 30s 大素材、H3、Kling、快乐马）、客户端家族下拉出现 MiniMax/HappyHorse 分组、49 线首尾帧真单。）

上一轮：2026-08-09（第215轮：**参数夹钳全渠道根除——13 个视频翻译器 时长/比例/分辨率 改原样透传·纯服务端**——用户实报【Seedance】渠道（`ch-jianmeng`，api.nyxen.sbs）客户端请求 20s、服务端发上游 15s 被夹（[jianmeng.ts](server/src/translators/jianmeng.ts) 旧 `Math.max(4, Math.min(15,…))`，与第186/187轮 overseas 同病：**按秒计费按请求参数扣、实发被砍=多扣钱少交货**），令「检查所有渠道是否又有隐藏限制」。全面排查（translators 全目录扫 Math.min/clamp/nearest/白名单回退）：**13 个视频翻译器存在静默夹钳，本轮全部根除**——第188轮「已有渠道的历史夹钳属存量行为」的豁免自此废止。修法=overseas 第188轮定稿模式，新共享 helper [paramPass.ts](server/src/translators/paramPass.ts)（`numberParam`/`stringParam`：**显式值原样透传**（数字串归一 number、非法值原样发出上游明确报错+失败自动退款）、**缺省才补默认**（各家旧默认值保留分毫未动））。名单：jianmeng（主犯）/ aivide / huaying / dimensio / videos（openai-video，含分辨率硬编码 720p→透传）/ sudashui（hn 线就近取档）/ jmt / musem（含分辨率「枚举外不发」静默丢弃→显式即发）/ aistars（能力表夹钳；缺省默认档语义保留为 `defaultDuration(cap)`——gemini 线缺省仍 10s）/ jianmengp（就近取档+能力表回退；缺省=末档）/ jmz（就近取档+分辨率档外静默不发→显式即发；⚠ grok「1080p 须恰好 1 图」业务守卫**保留**=明确报错非改写）/ jmh 视频（模型 ID 现拼就近取档→原样拼入；⚠ grok 比例非 16:9/9:16 改**前置明确报错**——ID 朝向段只有 portrait/landscape 无法编码，旧行为静默回退 landscape 勿恢复）/ jmf（ID 现拼 `+比例+分辨率+时长` 三参数全透传，分辨率仅剥 "p" 属 ID 语法格式转换非值改写）。**刻意不动**：volc `clampEnum`（按**管理端实时模型参数**收敛的防费用失控硬闸，第122轮用户定稿）、轮询进度显示 `Math.min(95,…)`、图片侧 size→比例语义映射（jmh/aistars/jmz image，跨参数转换+按次计费）、客户端本地 CLI `clampDreamina*`（第203轮用户定稿保留）。实测：server tsc 干净 + **沙盒冒烟 34/34**（scratchpad 复制 src+junction node_modules+独立 data 零触碰真实库、stub fetch 零真实上游调用：13 家 20s/30s 透传、缺省补默认、数字串 "30"→30、档外比例/分辨率原样发出、jmz grok 守卫仍报错、jmh grok 3:2 前置报错、jmf/jmh ID 现拼精确）。⚠ 顺手清杀 8787 **不带 watch 的 tsx 僵尸**（第107/112/145/148/178轮同款惯犯，改代码永不生效元凶）并以 `npm run dev`（tsx watch）重启（/health 200）。**须重新部署服务端**；客户端零改动无需打包。**运营注意**：此前被夹钳「悄悄救回」的越档请求现在会真发上游——若管理端某模型参数档位超出上游实际能力，该档请求将明确报错并自动退款（此前=静默改小却按大参数扣费），发现报错即到管理端把该档收掉即可。）

上一轮：2026-08-09（第214轮：**商业化改造 P3——渠道商独立部署（relay 渠道节点）·纯服务端**——用户拍板「P3 启动」（此前建议缓做的记录保留在方案 §6-P3）。**架构**：同一套服务端两角色（[config.ts](server/src/config.ts) `NODE_ROLE=source|relay` + `SOURCE_URL`/`SOURCE_NODE_KEY`）——relay=渠道商自部署节点：**本地拥有**用户体系（P2 注册全套/积分自主发行/团队/兑换码/请求日志/注册设置），**转发**目录/生成/任务/素材经源站 /v1 协议（ank- 节点密钥），源站按平台价扣该商积分池。①**源站侧**：`Agent.nodeKey`（[agents.ts](server/src/store/agents.ts) ank- 按需生成 `regenerateAgentNodeKey`——重置=旧密钥立即失效；`getAgentByNodeKey`+`touchAgentNode` 节流刷活跃）；[auth.ts](server/src/auth.ts) ank- 分支设 `req.agentNode` + **NODE_ALLOWED_ROUTES 白名单**（catalog/generate/batch/tasks/assets 全家/favorites/node/me——用户个体端点一律 403）；[routes.ts](server/src/routes.ts) 节点计费分支：`planNodeBilling`（池不足=「服务暂不可用，请联系你的服务商」中性文案+reason:pool_insufficient）+`chargeNodeBilling`（settle agents 段、userAmount 0）+**响应回传实扣 cost**（generate 附 cost、batch 附 costs[] 与 taskIds 对位——节点镜像扣款的依据）；⚠ **节点日志落笔定式（勿改）**：`cost:0 + agentCosts:[{id:商,cost:池实扣}] + ownerId:商 + userId:nu:<商>:<节点用户> 留痕`——logCostFor 门户（agent 视角）/管理端（platform 视角）天然取到池实扣，**启动对账孤儿退款零改动原路退池**（userAmount 0 对留痕 id 天然 no-op）；任务 billing 同构（setTaskBilling cost 0+agents）。节点收藏透传：favorites 四端点按 `nu:` 合成拥有者（favQuotaById 泛化，配额=全局默认档）——「收藏=永久保留」对节点用户成立。新 `GET /v1/node/me`（池余额/enabled，节点「源站连接」卡用）。②**relay 侧**（新 [relay.ts](server/src/relay.ts) + routes 各端点 `isRelay()` 分支）：⚠ **计费镜像时序（勿改成先扣本地再转发）**——转发前只按**目录预估价**预检（`estimateCostFromCatalog` 与 resolveModelCost 同构；参考视频秒数加权不在预估内），真扣按**源站回传实扣**（金额恒等的唯一依据）；源站拒单（402/403/400）=本地一分不动原样转；异步任务台账 `relay-ledger.json`（taskId→{用户,金额,日志}）——轮询透传见终态结算（失败=`ledgerSettleTerminal` 镜像退款幂等防重复）+**每 10 分钟清扫**兜底（无人轮询的单向源站补查；404 且超 72h=按中断退款——与源站「终态留 48h」对齐）；极端并发本地余额吃穿=扣到 0 告警 `[relay] 本地余额不足实扣`（坏账归节点，§5.3 信任边界）；目录=20s TTL 缓存（since 增量/瞬断沿用旧缓存）；素材=multipart 重组转发/直传签发透传/raw 302 到源站/引用上报透传（保留策略语义不断档）；扩容卡=400 明示不支持。[index.ts](server/src/index.ts) relay 启动：**跳过 reconcileOnStartup**（⚠ 勿启用——会把仍在源站跑的任务当孤儿退款=用户白拿）改跑 `startRelayLoops`。③**两控制台**：管理端渠道商详情新「渠道节点」卡（生成/复制/重置 ank- + 部署指引）+ `/admin` 注入 `window.__NODE_ROLE__`——relay 模式**隐藏源站专属页**（渠道/模型/模板/预设/渠道商/存储/配额）+ 对应 admin API 前缀 403 + 顶部「**源站连接**」状态条（`GET /admin-api/relay/status`：连通/商名/池余额/在途任务/目录版本）；门户新「渠道节点」页（自助 查看/生成/重置密钥 + 五步部署指引，`GET /agent-api/node-key` + `POST /agent-api/node-key/regenerate`）。④交付：.env.example 注释段 + **[docs/渠道节点部署手册.md](docs/渠道节点部署手册.md)**（自检清单/对账方法/边界）。实测：server tsc 干净 + 两控制台 script 语法过 + **双实例沙盒 E2E 37/37**（源站 8967+节点 8968+迷你 SMTP：节点注册本地建号（源站用户数不变）、echo 生成转发**本地扣 5=源站池扣 5 金额恒等**、两侧请求记录（源站 ownerId=商+nu: 留痕）、overseas 未配密钥异步快失败**两侧同退**、池扣穿 402 中性文案+本地未扣、raw 302、ref/上传/直传/resolve 透传、ank 白名单（/v1/me 403、无效 401）、relay 管理端 models 403+users 200+`__NODE_ROLE__` 注入）+ **浏览器三端**（节点 /admin：7 本地页可见/7 源站页隐藏/「源站连接」条 已连通·池 490·在途 0；源站管理端渠道商详情「渠道节点」卡（密钥+复制+重置+指引）；门户「渠道节点」页（密钥自助+部署指引），零控制台报错）。**须重新部署源站服务端**；客户端零改动无需打包；渠道商拿同一套服务端包+ank 密钥自部署（手册见 docs）。已知边界：①节点本地日志仅①②段；②预估价不含参考视频加权（实扣为准）；③节点不支持扩容卡、收藏配额=默认档；④团队/共享库为节点本地体系；⑤源站须公网可达（素材直链/302 指向源站）。**P4 清理收尾待做**。）

上一轮：2026-08-09（第213轮：**商业化改造 P2b——取消激活码签发机制 + 个人/渠道商邀请码（注册时可填）·两端·全新绿地**——用户追加拍板：「取消签发激活码机制，都用邮箱和短信注册了，增加个人邀请码和渠道商邀请码，注册时可填」+ 中途改令「**存量用户不用保留，我会对接好，直接按全新做，不用留一些用不上的优化**」——从兼容性保留改为**绿地删除**：激活码/机器码/有效期语义全部拔除。①**签发机制整体下线（⚠ 勿回退）**：`POST /v1/register`（激活码注册通道，废除第212轮③的保留条目）、`/agent-api/users/batch`（门户批量开码）、`/admin-api/users/batch`（总控批量开码）、门户 unbind/regenerate-key/DELETE user 端点全部删除（404）；门户 batch-op 仅剩 enable/disable/setFeature/setMode；accessKey 降级**纯内部访问凭证**（注册自动生成回传、客户端持久登录用；管理端改称「访问凭证」）。②**用户数据层绿地**（[users.ts](server/src/store/users.ts)）：`machineCode/validityDays/expiresAt` 字段与 bindOrCheckMachine/normValidityDays/createUsersBatch/activateValidity/isUserExpired 全删；登录/心跳/me 响应无 expiresAt；门户「已激活」=`account||lastSeenAt`。③**邀请码=唯一获客通道**：`genInviteCode(prefix)`——`U`（个人）/`A`（渠道商）前缀 + 7 位无易混字符（alphabet 剔 0O1IL）；`Agent.inviteCode`（[agents.ts](server/src/store/agents.ts) 启动回填存量商+createAgent 生成+getAgentByInviteCode）；`User.inviteCode` 懒生成（ensureUserInviteCode，/v1/me 下发+invitedCount）+ `User.invitedBy`；注册 `/v1/register/account` 接 `inviteCode`——**A 码→归属该商（停用商拒）、U 码→记 invitedBy（为邀请奖励预留，配 Q6 赠送机制）、无效→400「邀请码无效，请核对后重试（可留空注册）」（用户特意填的不静默忽略）、留空=平台直属**；⚠ **邀请码校验先于验证码核销**（验证码一次性——先拒完能拒的防用户白烧一枚码）；查码大小写不敏感。④**两控制台**：门户用户页重写（「我的邀请码」chip 点击复制、KPI「名下用户·凭你的邀请码注册」、自助注册徽标、无 激活码/有效期 列、抽屉无 作废/dangerzone）+ /agent-api/me 带 inviteCode；管理端删 批量激活码弹窗链/解绑机器/validityHtml，KPI「已注册账号」，渠道商详情头部显邀请码+复制。⑤**客户端**：[LoginPage.tsx](src/canvas/LoginPage.tsx) 三态（账号登录/注册/找回——激活码注册删除）+ 注册页「邀请码（可选）」字段（placeholder「服务商或好友的邀请码，可留空」）；[managedClient.ts](src/services/managedClient.ts) registerAccount 带 inviteCode、register() 删除；[contract.ts](src/contract.ts) 删 register 端点与 expiresAt、UserStats+inviteCode/invitedCount；[PersonalCenter.tsx](src/components/PersonalCenter.tsx) expiresAt 块换「我的邀请码」（点击复制+已邀请 N 人）。实测：两端 tsc 干净 + **614 vitest 全过** + build 过 + **沙盒冒烟 25/25**（8967 独立 data+迷你 SMTP：存量商启动回填 A 码/门户 me 带码/商码注册归属实锤（小写亦可）/个人码 invitedBy+invitedCount+1/无效码 400 且不烧验证码（同码留空重试成功）/留空平台直属/停用商码 400/签发与机器码遗留端点 6 处全 404/登录响应无 expiresAt·machineCode）+ **浏览器端到端**（vite5199+沙盒 8967：注册页图形码+倒计时+邀请码字段渲染、**UI 全流程注册带商邀请码→归属 P1 测试商实锤**、个人中心「我的邀请码 U 码+已邀请 0 人」、门户用户页/抽屉、管理端用户页/渠道商详情邀请码；三端零控制台报错）。**须重新部署服务端 + 客户端重新打包**（旧客户端登录/生成照常，其激活码注册页因端点 404 失效引导升级）。**P3 状态：⏸ 待用户决策**——用户问「relay 能否减轻源站负担/两个源站是否等效」，已答复（relay=商务/密钥隔离非负载分摊；两源站=上游密钥外泄+无积分池约束，不可行）并建议缓做，记录于方案 §6-P3。待做：P4 清理收尾。）

上一轮：2026-08-09（第212轮：**商业化改造 P2——注册体系（邮箱/手机验证码）+ 机器码退役 + 同时在线设备限制·两端**——接第211轮 P1，方案见 [docs/商业化改造方案.md](docs/商业化改造方案.md) §4。①**注册体系（服务端）**：新公开端点 `GET /v1/captcha`（自绘 SVG 图形码，内存态一次性）/ `POST /v1/register/send-code` / `POST /v1/register/account`（邮箱/手机号+验证码+密码→建号平台直属→回传 accessKey 直接登录态）/ `POST /v1/password/send-code|reset`（找回密码，**目标不存在也返 ok 不暴露存在性**）；防线全配套（[regGuard.ts](server/src/store/regGuard.ts) SQLite reg_guard 表重启不丢）：图形码前置发码、同目标 60s 冷却（**跨用途共用**）、6 位码 10 分钟错 5 次作废、IP 发码时/日上限、IP 注册日上限、内置+可配临时邮箱域黑名单、全局注册开关、注册赠送积分（默认 0 机制保留）；邮件=nodemailer SMTP（[mailer.ts](server/src/services/mailer.ts)，**新依赖 nodemailer——部署机须 `cd server && npm i`**）、短信=阿里云适配器（[smsAliyun.ts](server/src/services/smsAliyun.ts) 纯 RPC 签名零 SDK，未配置=手机通道「暂未开放」）；配置全在管理端新「**注册与安全**」页（settings.register，密钥脱敏回显+空串不改语义+SMTP 测试邮件端点）。②**机器码退役（⚠ 勿回退硬绑定）**：bindOrCheckMachine 删除；auth.ts requireAccessKey 改**同时在线设备限制**——x-machine-code 头降级为设备标识（**客户端继续采集发送，勿删**——新旧双向兼容的关键）；[users.ts](server/src/store/users.ts) 新 `User.devices/deviceLimit` + checkDeviceAccess（在表刷活跃/有空位当场收编=存量已登录客户端无感过渡/表满陌生设备 403）+ registerDeviceOnLogin（**登录抢占制**：默认 1 台（settings.deviceLimit 可配 0=不限）+ 按用户覆盖（管理端用户抽屉「设备上限」），被顶设备 403→客户端既有 401/403 立即登出）；validityDays 激活改**首次成功登录/注册**起算（activateValidity）；账号格式放宽三形态（邮箱/手机号/旧式用户名，validateAccount）；门户「已激活」判定改 `machineCode||account||lastSeenAt`（isActivated 一把尺——防机器码退役后已用码被作废退面额）。③**旧激活码注册通道保留**（/v1/register 绑账号语义不变仅去绑机——渠道商发码在 P3 渠道节点上线前仍是其用户入口）。④**客户端**：[LoginPage.tsx](src/canvas/LoginPage.tsx) 重做四态（账号登录/邮箱·手机注册（图形码+发码倒计时）/激活码注册/找回密码）+ 删机器码展示 + 用户协议占位；[managedClient.ts](src/services/managedClient.ts) 新 getCaptcha/sendVerifyCode/registerAccount/resetPassword；contract Endpoints 新五端点。实测：两端 tsc 干净 + **614 vitest 全过** + build 过 + **沙盒冒烟 32/32**（8967 独立 data + **内嵌迷你 SMTP 实收验证码邮件**（nodemailer 中文正文 base64 需解码——测试教训）：注册全流程/图形码答错·一次性/黑名单/非法目标/60s 冷却/错码拒/重复注册拒/赠送 0/邮箱登录/**设备抢占三段**（默认 1 台陌生设备 403→B 登录顶 A→按用户覆盖 2 台共存）/找回密码全流程（旧密码失效）/不存在账号发码不暴露/激活码直登不绑机+validityDays 首登激活/激活码注册通道兼容/注册开关 403/测试邮件端点实发）+ **浏览器端到端**（vite5199+沙盒 8967：登录页四态渲染、机器码展示已删、图形码 data-URI 渲染、**UI 全流程注册**（填码→发码→迷你 SMTP 取码→注册并登录→进应用，accessKey/account 落地）、管理端「注册与安全」3 卡渲染+密钥不回显+设备上限保存往返；两端零控制台报错；测试 localStorage 已清）。**须重新部署服务端（`npm i` 装 nodemailer）+ 客户端重新打包**（只部署服务端=旧客户端登录/生成照常（绑机校验取消对其无感），无新注册页；部署后到管理端「注册与安全」配 SMTP 才开放邮箱注册）。已知边界：①短信通道代码就位但**未真机验证**（待阿里云签名/模板资质，见方案 §8-Q3）；②图形码为内存态（重启失效，重取即可）；③浏览器 dev 的设备 id=localStorage 随机 UUID（清存储=新设备，占一个名额）。待做：P3 渠道商独立部署（relay 模式）、P4 清理收尾。）

上一轮：2026-08-09（第211轮：**商业化改造 P1——经济模型翻转（统一定价 + 分发实扣 + 免费码 + 渠道商链拍平）·纯服务端**——总方案见 [docs/商业化改造方案.md](docs/商业化改造方案.md)（P1–P4 四期，本轮=P1；用户已逐条拍板 8 项决议）。①**统一定价（⚠ 勿回退链式双扣费）**：[routes.ts](server/src/routes.ts) planBilling 删归属链进货价段（settle agents 恒空、logs.agentCosts 停写；settle/reverse 框架保留——切换前旧 billing 快照的退款仍两侧同退）；[catalog.ts](server/src/catalog.ts) 去渠道商换价（价格恒平台价；开放范围过滤 modelAllowedForAgent 与 改名 agentModelLabel 保留；.p 段沿用 pricingVersion 旧名承载 改名/启停/换分组 热更）。②**分发实扣（⚠ 与①是原子包，勿单独回退任一半——免费面额+无链式结算=渠道商无限铸造用户积分的经济漏洞）**：门户签发 兑换码/激活码 按 面额×数量 从商 credits 池真实划转（不足 402），作废未用兑换码/未激活激活码原路退回；logCodeIssue 留痕（退回记负数 cost，门户统计天然净额）。③**退役**：开码积分（codeCredits 全部函数/端点/UI）、激活码签发价四档（settings.codePricing/CODE_PRICE_TIERS）、渠道商 售价/结算价/分组价/默认下级价 全部定价维度、下游渠道商体系（sub-agents 全组端点+门户两页签）、团队码收费（改**仅源站免费签发**，门户端点删）；扩容卡机制原样、签发免费（spec.price 字段保留待后续升级）。④**链拍平**：Agent.parentAgentId 退役，agentChain/audienceChain/chainPricingVersion 退化单级（函数形态保留——feature 闸/禁用/改名消费点以链遍历书写，拍平后语义不变）。⑤**启动迁移**（[agents.ts](server/src/store/agents.ts) 模块加载段，按字段存在性幂等）：首次剥离前备份 agents.json/agent-groups.json（`.bak-p1-unified-pricing`，已存在不覆盖）→ 剥离 8 个旧字段（modelPricing/costPricing/codePricing/codeSalePricing/defaultSub 两表/codeCredits 清零舍弃/parentAgentId 拍平）+ 分组 costPricing 剥离。实测：server tsc 干净 + **沙盒冒烟 36/36**（scratchpad p1sand 复制 src+junction node_modules+独立 data 零触碰真实库：迁移剥离/备份含旧字段/二启幂等（hash 不变）、实扣/退回/超额 402、九个定价端点全 404、门户团队码 404+源站免费、扩容卡免费余额不变、echo 模型端到端**用户扣 5 商积分不动**（链式结算废除实锤）、商属用户 catalog=平台价、发码日志净额精确、改名/禁用保留且 catalog 跟随）+ **两控制台浏览器端到端**（管理端：渠道商页无开码/激活码价格入口、详情 4 列只读+禁用管控 94 键、团队页仅源站签发、分组弹窗无分组定价、配额页免费文案；门户：九页签、划转预览、余额不足红条+禁用签发、模型页统一价横幅 111 行零价格输入框；两端零控制台报错）。**须重新部署服务端（首启自动迁移，生产部署前建议手动再备份 agents.json）**；客户端零改动无需打包。本机 dev 8787 未运行=真实库未迁移（下次启动自动跑）。待做：P2 注册体系（邮箱验证码+机器码退役+同时在线 1 台）、P3 渠道商独立部署（relay 模式）、P4 清理收尾。）

上一轮：2026-08-07（第210轮：**画布分集下拉可删单集（自定义下拉替代原生 select）·纯客户端**——用户报「画布模式下的分集只增不减，不够完善」。原分集切换是原生 `<select>`（选项里放不了按钮）→ [AssetPanel.tsx](src/canvas/AssetPanel.tsx) 改自定义下拉 `EpisodeSwitcher`：按钮显当前集名+ChevronDown，展开面板逐行列分集（点行=switchCanvas 切画布并收起；激活行高亮），**每行悬停显 × 删除键**（仅剩一集时隐藏=末集保护第一道闸）——删除语义与表格模式 Frame161195 deleteEp 完全一致：confirmDialog 确认（同款文案「将一并移除本集全部分镜、提示词与该集画布，操作不可撤销」）→ projectStore.deleteEpisode（store 层至少保留一集第二道闸；正看其画布则先切走再删；连同该集 canvases 数据一并丢弃）；外点（mousedown）/Esc 关闭。⚠ **顺手加固 deleteEpisode「正看其画布」判定**（[projectStore.ts](src/store/projectStore.ts)）：裸比 `canvasEpisodeId === id` 改 `resolveCanvasKey(canvasEpisodeId, episodes) === id`——canvasEpisodeId 为 null 时实际激活的是第一集，裸比会漏切：删第一集后活动画布内容仍是被删集、下次快照会错写进新的第一集画布（表格模式同路径一并受益，勿回退）。实测：client tsc 干净 + **614 vitest 全过** + **浏览器端到端**（5199+dev8787 临时账号真实注册登录建项目进画布：注入 3 集→下拉 3 行各带删除键→点行切换（active 变更+旧画布快照入 canvases+下拉收起）→删非激活集（confirmDialog 文案精确→销集+下拉实时更新）→**删激活集→自动切回余集**（switcher 标签跟随）→仅剩一集时删除键隐藏且 store 层 deleteEpisode no-op→外点 mousedown/Esc 关闭双实锤；零控制台报错；临时账号/localStorage 全清；截图受「Browser pane 不合成帧」环境限制以 DOM 断言为准——历轮同款）。**须客户端重新打包**；服务端零改动。多窗口同步天然兼容（episodes 属 SHARED_PROJECT_FIELDS，对端收到后 pruneCanvasesToEpisodes 自动清画布）；删集时该集在途任务由请求台账两次确认后转找回通知（真删除语义，第205轮补充2 既有行为）。）

上一轮：2026-08-07（第209轮：**垫图探活误报修复——「直链已失效」误报归零（探活三态 + 三次重试）·纯服务端**——用户实报：生成频报「垫图无法获取：第1张「…」（TP00026268）——直链已失效且台账无可用直链」，实际直链正常。根因=[openai.ts](server/src/translators/openai.ts) `urlAlive` **单次探活**：HEAD 一发超时/网络抖动/被限流拦下就判死 → resolveEditRefs 记 missing → 整单报错。修复（⚠ 勿回退）：①**探活改三态**（新 urlProbeOnce：alive/dead/transient）——只有 **404/410** 才能断言死链，超时/5xx/403/网络异常一律 transient 不判死；②**最多探 3 次**（退避 1.5s/3s），明确死才提前终止；真死链仍明确报错（图例按位对齐绝不静默丢图的 §9 语义不变）。⚠ **首版曾加第③层「真实下载最后一搏」**（探活全空时真下载候选直链、下到字节 multipart 直传）——**用户令删除**（「我们的对象存储百分百可以访问、也不会请求其他图片链接」，探测被 WAF 拦而 GET 放行的场景在我方 OSS 上不存在），**勿再加回**。实测：server tsc 干净 + **沙盒冒烟 11/11**（scratchpad junction 沙盒零触碰真实库：瞬断×2+第三次成功→重试判活走 JSON 直链模式提交成功且零兜底下载、真 404→HEAD 仅 1 次快速判死（<3s）仍报错带资产 id 与名称、三轮全瞬断→探满 3 次后明确报错）。**须重新部署服务端**；客户端零改动无需打包。已知边界：对「永远瞬断」的直链，失败路径最长约 3×30s 探测 + 4.5s 退避后才报错（换取瞬断误报归零）。）

上一轮：2026-08-07（第208轮：**3D 导演台「场景透视开关」（透视 ⇄ 正交平行投影）·纯客户端**——用户令「增加场景透视开关」。①**双相机架构**（[modelStage.ts](src/lib/modelStage.ts)）：PerspectiveCamera（主）+ OrthographicCamera 并存，`this.camera` 恒指当前生效者；新 `projection: "persp"|"ortho"` + `setProjection()`；**正交视锥按「目标距离处透视等效视野」换算**（halfH=dist·tan(fov/2)，切换时画面主体尺寸不跳变、dist 滚轮仍有缩放手感）；全景宿主恒透视（球面背景无正交语义，setProjection 忽略+开关不显示）；aspect 收敛为实例字段（setSize/captureWith/capturePose 原来直写 `camera.aspect`——正交相机没有该属性，统一走 updateCamera 分派）；投影模式随 `getCameraState/setCameraState` 持久化（[directorStore.ts](src/store/directorStore.ts) camera 类型加 projection?——涂鸦嵌入/产物节点再编辑还原）。⚠ **顺手修真 bug（E2E 平行光线断言实锤，勿删）**：updateCamera 末尾必须 `camera.updateMatrixWorld()`——切投影/改机位后未渲染前就 raycast（拾取/拉边/视平面交点）会用陈旧 matrixWorld（首版正交切换后首个 ray 全偏：位移比 0.263 而非 1）。②**属性栏「视图」区**（[DirectorStageModal.tsx](src/components/DirectorStageModal.tsx) 右栏顶部，pano 隐藏）：复选框「场景透视（关=正交平行投影）」默认开，title 说明两模式语义；再编辑按 camera.projection 还原勾选态。实测：client tsc 干净 + 613 vitest 全过 + vite build 过 + **浏览器端到端**（5199+dev8787 临时账号：**独立数学断言**——rayOnViewPlanePoint 在近/远两锚面各测 100px 屏幕位移的世界位移：透视 0.5223/0.8628（ratio 1.652 近大远小）、**正交 0.5223/0.5223（ratio 1.000 平行光线精确）**且切换时目标距离处位移一致=等效视野换算实锤；正交下 captureComposite/captureDepth 非空（6.3KB/3.2KB）；getCameraState 投影往返 ortho→persp；**弹窗 UI**——「视图」区+开关渲染、默认勾选透视、切正交后画面 100% 非黑正常渲染、加人偶/选中照常；零控制台报错；临时账号/localStorage 全清）。**须客户端重新打包**；服务端零改动。已知边界：①正交下拖模型的水平面交点/拉边缩放全部照常（raycaster 对正交=平行射线，数学天然成立）；②深度图产物在正交下深度按平行投影分布（作 ControlNet 输入语义不变）；③嵌入涂鸦的模型层与产物图按当前投影渲染（所见即所得）。**待真机 QA**：正交模式摆位/对齐观感、切换手感、再编辑还原。）

上一轮：2026-08-07（第207轮：**3D 导演台三交互优化——功能按键入属性栏 + 选中框拉边缩放（等比开关）+ 边悬停滚轮旋转（三轴旋转参数）·纯客户端**——用户三点（截图标注）全落地：①**顶部功能按键全部迁入右侧属性栏**（[DirectorStageModal.tsx](src/components/DirectorStageModal.tsx)）：工具栏行整删——「添加模型」组（加人偶/加道具（内联展开五形状）/导入GLB）置右栏顶部，姿势模式/重置姿势收进选中区（仅人偶显示）、删除选中恒在选中区尾；错误文案移顶栏（截断+title）；右栏 252→262px；顶栏提示词补「选中框：拖边=缩放 · 边上滚轮=旋转」。②**拉边缩放**：[modelStage.ts](src/lib/modelStage.ts) 新 `edgePick(px,py)`（选中模型世界 AABB 12 条边投影屏幕、点到线段距离 ≤10px 取最近，返回边平行世界轴+框中心）+ `worldAxisToLocalAxis`（模型有旋转时世界轴→主导本地轴）+ `rayOnViewPlanePoint` 纯数组版（⚠ 弹窗不能静态引 three）；**等比**（属性栏「等比缩放（拉边时）」开关，默认开）=指针到框中心距离比→整体 scale；**非等比**=边的两个固定世界轴各按位移分量比→**新数据字段 `stretch`**（[stageScene.ts](src/lib/stageScene.ts) StageModelBase.stretch 本地 xyz 拉伸倍率叠乘 scale、`sanitizeStretch` 逐轴夹 0.05..20、≈[1,1,1] 省略落盘；applyObjScale 统一入口）；属性栏拉伸读数行（有 stretch 才显示）+「重置」；边悬停十字光标提示（onCanvasHover）。③**边滚轮旋转**：滚轮命中选中框边=绕该边轴向转 5°/格（worldAxisToLocalAxis 映射本地轴，空白处照旧 距离/视场）；属性栏旋转参数补全**三轴**：朝向 rot[1]/俯仰 rot[0]/侧倾 rot[2]（±180° 步 5，滚轮旋转实时跟随）。实测：client tsc 干净 + **613 vitest(+1：stretch 夹取/≈1省略/坏形状丢弃) 全过** + vite build 过 + **浏览器端到端**（5199+dev8787 临时账号进画布开导演台：三按键 x=1031/1121（右栏起点 1018）=已入属性栏实锤、顶部无工具栏行；加人偶→选中区 俯仰/侧倾/等比缩放/姿势模式/删除选中 全渲染；**悬停扫描 401 个边命中点（十字光标）**；竖直边滚轮 2 格→**朝向 0→10° 精确且俯仰/侧倾不动**（按边轴向实锤）、空白滚轮不误旋；等比拉边外拖→**缩放 1→1.08×**；关等比拉边→**拉伸 [1.45/0.95/1] 分轴生效**、点重置→清除；零控制台报错；临时账号/localStorage 全清）。**须客户端重新打包**；服务端零改动。已知边界：①边拾取基于世界轴对齐 AABB——模型大角度旋转后拉边/滚轮按「最接近的本地轴」映射（worldAxisToLocalAxis 主导轴近似）；②非等比拉伸对 GLB 整体变形（子网格不单独拉伸）；③拉伸挂在 scale 之外的独立倍率，旧客户端打开带 stretch 的场景=按无拉伸渲染（sanitize 兼容不炸）。**待真机 QA**：拉边手感（10px 命中带宽度）、旋转后模型的拉边方向直觉、涂鸦/全景两宿主下同交互。）

上一轮：2026-08-07（第206轮：**视频转深度——转深度从图片扩展到视频（逐帧本地推理 + WebCodecs 编码回 mp4）·纯客户端零服务端零计费**——用户令「转深度不仅仅是图片可以转深度，视频也应该可以」，AskUserQuestion 三答定案：**16fps 默认+节点「帧率」参数可调（8/12/16/24，重跑生效）**、**防闪烁两模式做全（设置→生成偏好 切换 时间平滑/每帧独立）**、**专用 `video.depth` 节点**（与 image.depth 同规矩：kind script/capability null/**永不走生视频管线**，杜绝「失败后点生成=真调视频模型扣费」）。①**管线**：fetch 字节→blob objectURL（同源解码免跨域污染）→`<video>` 逐帧 seek 抽帧（seeked+rVFC、全程超时兜底；**MediaRecorder webm「时长 Infinity」修正**（seek 1e7 强制算时长）——尾段截取产物就是这种）→逐帧本地深度推理（**复用图片侧模型单例**，[depthEstimator.ts](src/lib/depthEstimator.ts) 新导出 preloadDepthModel/estimateDepthRawFrame（吐原始深度不归一化）；推理输入长边压 720——模型内部 518 系，整帧喂入纯浪费）→归一化（新纯逻辑层 [videoDepthCore.ts](src/lib/videoDepthCore.ts) 可单测：**时间平滑=相邻帧 min/max 标定范围 EMA α=0.8**（防闪烁，转场约半秒渐变适应属已知边界）/每帧独立=与图片同逻辑；帧计划 帧心采样/末帧内钳）→**WebCodecs VideoEncoder（H.264 档位逐级探测）+ mp4-muxer 封装灰度 mp4（无音轨）**（新 [videoDepthEncode.ts](src/lib/videoDepthEncode.ts)；⚠ **不走 Rust/ffmpeg**——内置 ffmpeg 只有单帧/片段两命令，逐帧抽帧=O(n²) 从头重解码；WebCodecs 让 dev 浏览器与打包版走同一条代码路径；新依赖 mp4-muxer@5 与本模块都只在视频转深度时动态加载）。②**编排** [videoDepthify.ts](src/canvas/videoDepthify.ts) 与 depthify.ts 完全同构：depthifyVideoNode（右侧建节点+连线承接原视频）/depthifyVideoUri（灯箱，视口中心）/rerunVideoDepthNode（原视频解析 上游连线视频结果>params.depthSrcAssetId>depthSrcUri；运行即清场旧深度片进堆叠；running 时忽略重复运行）；节点被删=逐帧循环当场中断丢产物（比图片侧更省——推理不再烧完）；**帧数上限 2400 超限明确报错**（提示降帧率/截片段，合 §9 绝不静默截断）；进度 模型加载1-14/逐帧15-96/编码97-99。③**接入面**：nodeSpecs 新 `video.depth`（display video/inPalette false/params=帧率枚举——脚本节点面板天然显示参数 pill 隐藏模型下拉）+ pluginRegistry runScriptNode 分支 + BaseNode 视频悬浮工具栏「转深度」（video.depth 自身=「重新转深度」dispatch run）+ ContextMenu 视频右键同款 + Lightbox 视频「◑ 转深度」；settingsStore 新 `depthVideoSmooth`（默认 true 持久化+读盘归一）+ SettingsModal「本地推理」卡扩展第二复选框。实测：client tsc 干净 + **600 vitest(+15) 全过**（帧率归一/帧计划 数量·递增·末帧钳·超限不截断/EMA 数学与收敛/固定范围灰度映射与钳位/退化 128/偶数化/降采样）+ vite build 过 + **浏览器端到端**（5199+dev8787 临时账号真实注册登录建项目进画布；测试面板 rAF 冻结（第131/184轮同款假象）改用自家编码器 rAF-free 合成 1.2s 320×240 动态测试 mp4=**编码器独立实锤**（28KB video/mp4）→ depthifyVideoNode → 节点 type/标题「深度-测试片段2」/depthFps=16/连线/params 全对 → **success：产物 19帧@16fps=1.1875s 精确、320×240 原尺寸、三帧采样 100% 灰度、动态范围 0-255、逐帧均值变化**；帧率改 8fps 重跑=**1.25s=10帧精确**+旧片归档堆叠 3 层；「每帧独立」切换落库生效；设置 UI 复选框渲染+点击双向落库；**服务端该用户 0 条请求日志（0 积分账号全程零扣费零 402）**；零控制台报错；临时账号/localStorage 全清）。**须客户端重新打包**；服务端零改动。已知边界：①WebView2 打包版 WebCodecs H.264 编码待真机实锤（Chromium 系应可用；不支持=明确报错，后续可加 ffmpeg 图序列兜底命令）；②防闪烁只治「归一化标定跳动」，模型逐帧内容抖动是 per-frame 推理固有（Video Depth Anything 类时序模型属后续可选，注意 license 只有 Small 可商用）；③产物无音轨（深度视频=垫图/控制输入用途）；④旧版打包客户端打开含 video.depth 节点的项目会被 sanitizeCanvas 清除该节点（新节点类型惯例，pano.view/image.depth 同）。**待真机 QA**：Tauri 打包后真实成片转深度全链路（WebGPU 逐帧耗时、H.264 编码可用性、1080p/15s 片观感与防闪烁效果）、尾段截取的 webm 产物转深度（Infinity 时长修正路径）、灯箱视频入口。
　**补充（用户两点反馈：①「直接原帧率吧，不抽帧了」②「转出来的深度视频精度很差，提升一点」· client tsc 干净 + 607 vitest(+7) 全过 + build 过 + 浏览器端到端复验全过（临时账号/数据全清、零控制台报错、8787 tsx 僵尸树顺手清杀））**——①**原帧率逐帧（帧率参数整体移除）**：新纯解析层 [mp4Meta.ts](src/lib/mp4Meta.ts)（零依赖可单测）——`<video>` 元素不暴露帧率，从 **mp4 容器 moov>trak>mdia 直接算**：fps=视频轨采样数（stsz/stts 求和兜底）÷ 轨时长（mdhd duration/timescale），mdhd v0/v1、64 位 largesize、坏 size 防死循环全处理；videoDepthify 提交前解析源字节 → `clampSourceFps`（解析失败（webm/碎片化 mp4）回退 30、>60 夹 60——深度视频无增益纯烧时）；nodeSpecs 删 depthFps 参数、normalizeDepthFps/帧率档常量删除，三处入口文案改「按原帧率」。②**精度提升双修**：⑴**编码码率 0.15→0.45 bit/像素/帧（下限 1M→6M、上限 12M→30M）**——深度图是大面积平滑渐变，低码率 H.264 压出色带/宏块=「精度差」主观感受的主因（⚠ 勿回退保守码率）；⑵推理输入降采样上限 720→**1036（=2×518 供采样上限）**——模型内部 518 系，1036 内不再多降一道采样（1080p 帧原尺寸直喂，精度零损失内存有界）。实测：**607 vitest 全过**（+7：mp4 解析 跳音频轨/29.97 分数帧率/stts 兜底/四种 null/坏 size 终止；clampSourceFps 三态；inferSize 默认 1036）+ 浏览器端到端（临时账号：自家 muxer 合成 30fps/36 帧测试片 → **parseMp4VideoInfo 解析 {fps:30, frames:36} 精确** → 转深度 success → **产物 36 帧 @30fps、时长 1.19998s 与源逐帧一致（原帧率实锤）**、实际码率 3.3Mbps（旧公式目标仅 ~0.35M）、竖列渐变区 **122 个灰阶无色带塌陷**、100% 灰度、动态范围 0-255；服务端 0 条请求）。已知边界：webm 源（浏览器 dev 尾段截取）解析不出容器帧率回退 30fps 采样；VFR 源按平均帧率均匀采样。
　**补充2（用户反馈「GPU调度太温柔，才调用了20%，我需要完全调用」——抽帧/推理并行流水 + GPU 批量推理 + 双批在飞 · client tsc 干净 + 612 vitest(+5) 全过 + build 过 + 浏览器端到端复验（90帧/60帧两轮 success、批量路径实锤、临时数据全清））**——GPU 只吃 20% 的根因=**串行流水线**：seek 抽帧（CPU/解码）→推理（GPU）→编码逐帧轮着来，GPU 大部分时间在等 CPU。三层改造（[videoDepthify.ts](src/canvas/videoDepthify.ts) 帧循环重写为生产者-消费者）：①**并行流水**：新 `FrameQueue` 有界异步队列（[videoDepthCore.ts](src/lib/videoDepthCore.ts) 纯逻辑：push 队满背压等待/close 排空/fail 两端即刻终止，5 条单测）——生产者持续 seek 抽帧入队，消费者并行取帧推理，seek/解码耗时被 GPU 计算完全遮蔽；②**批量推理**：[depthEstimator.ts](src/lib/depthEstimator.ts) 新 `estimateDepthRawFrames(frames[])` **整批拼一个 batch 张量一次推理**（518 系 ViT-S 单帧算子太小喂不满 GPU），GPU 批 8（`DEPTH_INFER_BATCH_GPU`）/WASM 恒 1；凑批用 pullImmediate **非阻塞自适应**（生产快=满批、生产慢=小批不干等）；⚠ 模型批维若为静态（batch>1 报错）自动回退逐帧并记忆（`batchSupported` memo + `depthBatchSupported()` 只读探针供 QA）；③**双批在飞**（MAX_INFLIGHT=2）：GPU 算上一批时下一批的 CPU 预处理同时进行（两个 pipe() 调用的 JS 段互相穿插），批间 GPU 不空转；drainOne FIFO 保帧序，inflight promise 预挂 catch 防 unhandled rejection。取消语义升级：节点被删=queue.fail(CANCELLED) 两端即刻终止（含队满阻塞中的生产者）。实测：**612 vitest 全过**（FrameQueue FIFO/背压/先等后到/非阻塞取/fail 终止）+ 浏览器端到端两轮（90帧@30fps success + **batchSupported=true（onnx 批维动态、8 帧批量真实生效）**；双批在飞重构后 60帧@30fps success 产物 60帧/2.0s 精确；测试面板 rAF 冻结环境下耗时被抽帧 250ms 兜底钳制=推理完全被并行隐藏的旁证；微基准：该机单帧推理 38ms 批量≈串行（GPU 本就吃满单帧）——**批量收益在大 GPU 上体现**，小 GPU 上流水线重叠即是主增益）+ 零控制台报错 + 服务端零请求。**须客户端重新打包**；服务端零改动。**待真机 QA：可视窗口下任务管理器看 GPU 占用应大幅上升（抽帧几十 ms/帧不再钳制吞吐）**；若仍不满可把 DEPTH_INFER_BATCH_GPU 上调（16）再试。）

上一轮：2026-08-06（第205轮：**多窗口实时同步——单实例多窗口 + 同项目状态实时镜像 + 单写者写盘·客户端 + Rust**——用户报「客户端多开相互独立，同项目在另一窗口新建节点原窗口不同步」，AskUserQuestion 定稿：**档2 实时同步**、同设备单人操作、**按分集画布分工**（一块画布同一时刻一个窗口在编、其它窗口实时看）。⚠ **先说清改造前的真问题：多开不只是不同步，是整文件互相覆盖丢数据**（两个进程都对 project.Qiji 做原子保存，last-writer-wins——B 窗口新建的节点会被 A 窗口下一次自动保存从磁盘上抹掉）。①**单实例多窗口**（[lib.rs](src-tauri/src/lib.rs) 新增 tauri-plugin-single-instance@2.4.3，⚠ 须注册在 Builder 最前）：二次启动同一 exe（用户的「多开」）不再另起独立进程，改为本进程新开完整应用窗口 `main-<millis>`（decorations false + disable_drag_drop_handler + additionalBrowserArgs 与主窗同参——⚠ 该参数会整体替换 wry 默认，见第199轮）；[capabilities/default.json](src-tauri/capabilities/default.json) windows 加 `main-*`。②**同步模型=快照广播（非命令重放，契合画布分工假设，⚠ 勿改成广播整张 canvases 表——会把别的窗口正在编辑的画布用本窗口陈旧快照盖掉）**：纯逻辑层 [projectSyncCore.ts](src/lib/projectSyncCore.ts)（SHARED_PROJECT_FIELDS 共享字段清单（⚠ 刻意不含 savePath/canvasEpisodeId/canvases/uiSnapshot——窗口本地态）/diffByRef 引用级 diff/pickLeader 确定性选举/prunePeers/pruneCanvasesToEpisodes/mergeLedgerForPersist）+ 传输在场层 [windowSync.ts](src/services/windowSync.ts)（Tauri 事件系统 / 浏览器 dev BroadcastChannel 双通道；5s 心跳携带 {windowId,openedAt,projectPath,activeCanvasKey}，3 周期超时剔除；**写者 isProjectWriter=同项目窗口中最先开的（唯一写盘方）、主窗口 isPrimaryWindow=全部窗口最先开的（请求台账全局兜底方）**——确定性无协商各窗口独立算同一结论，关窗自动顺延；⚠ 本模块零 store 静态依赖（projectStore 要静态引它做写盘门禁），上下文由 projectSync 经 setSyncContext 喂入；未初始化=单窗口语义行为不变）+ 引擎 [projectSync.ts](src/services/projectSync.ts)：发送侧只广播**自己激活画布**快照（canvasStore 订阅 250ms 去抖，含 runtime——**对方窗口「生成中」实时可见**，用户点名要求）+ 共享字段 diff + libraryStore 媒体库 + 切画布时立即广播离开画布的最终快照（归属交接）+ 打开项目 hello 报到（写者回 full 全量镜像——磁盘载入可能落后写者内存 3s 去抖窗口）；接收侧 applyingRemote 防回声，画布快照按「正是我激活画布→直写 canvasStore（保留本窗口 viewport/undo）/否则写 canvases 快照+runtime 存 remoteRuntimes 切过去补挂」，episodes 变化随手 pruneCanvases，写者收到任何镜像 scheduleAutoSave 替全体落盘。③**单写者写盘**（[projectStore.ts](src/store/projectStore.ts) save 门禁，⚠ 勿回退）：非写者窗口不写盘（isDirty 置 false——镜像已在写者手里）；手动保存转发 save-request 由写者执行。④**请求台账多窗口分工**（[requestLedgerStore.ts](src/store/requestLedgerStore.ts)）：⚠ localStorage 台账在窗口间共享，整体覆盖写会清掉对方登记——persistLedger 改**合并写**（mergeLedgerForPersist：内存为准+保留没见过的外来条目+见过且已删的不复活，knownTaskIds 追踪）+ tick 前 mergeFromStorage 吸收其它窗口登记；条目归属 windowOwnsEntry（本窗口项目→写者处理/别的窗口打开的项目→让给对方/谁都没开→主窗口兜底），终态回执带 forceTaskId 强制本窗口立即投递（结果在手不等写者周期）；投递写进非激活画布后 broadcastCanvasSnapshot 收敛各窗口（防日后被陈旧快照盖掉）。实测：client tsc 干净 + **585 vitest(+9) 全过**（diff/选举与顺延/在场清理/画布清理/消息过滤/台账合并写三规则）+ vite build 过 + **cargo check 干净**（single-instance 编译+窗口创建代码）+ **浏览器双标签页端到端**（5199+dev8787 临时账号，BroadcastChannel 通道=同一套引擎：选举（窗1 writer+primary/窗2 均 false）、hello→full 全量对齐（窗2 陈旧 scriptText 被写者镜像覆盖实锤）、**窗2 建节点+运行态→窗1 快照收到→切过去节点可见+「生成中 42%」（remoteRuntimes 补挂实锤）**、同画布实时（窗2 再建节点→窗1 canvasStore 直写即见）、共享字段同步、非写者手动保存被门禁拦下+save-request 到达写者实锤、窗1 离开→窗2 心跳超时后自动接任 writer+primary（让位实锤）；两标签页零控制台报错；临时账号/localStorage 测试数据全清）。**须客户端重新打包**（Rust 插件+同步引擎都随包）；服务端零改动。已知边界：①快照广播粒度=整画布（大画布高频拖动时每 250ms 全量 nodes 序列化，同机 IPC 可承受；后续可差量化）；②接收窗口不产生 undo（远端改动直写 store）——本窗口 undo 只回退自己的操作；③新开窗口（main-*）release 版 devtools 未像主窗禁用（与 popout 同现状）；④若用户真的同画布同时操作（违反分工约定），250ms 去抖内的交叉编辑 last-writer-wins（单人操作承诺下不发生）；⑤重启后存量孤儿通知可能在多个窗口各显示一份（各自可关，互不影响）。**待真机 QA**：Tauri 打包后双击 exe 二次启动=同进程新窗口实锤；两窗口同项目各开一块分集画布：A 建节点/跑生成→B 切过去即见（含生成中态）；关 A 窗口→B 接任写者后改动正常落盘；popout 弹出窗口不受影响。
　**补充2（⚠ 找回通知误报修复：换画布/换项目被误判「已删除」·纯客户端 · client tsc 干净 + 614 vitest 全过 + 浏览器端到端 3 场景全过（临时账号已删））**——用户实报：单开时换了个画布，生成图片弹「您的任务已完成，但X已被您删除…」，实际只是切换（原画布被释放≠删除）。**根因=登记时机竞态**：台账身份（项目路径/画布 key）在**提交确认回执**（onTaskConfirmed，服务端返 taskId 时）才现读——用户点完生成立刻切画布/切项目，回执到达时读到的是**新画布/新项目**的 key，身份记错位 → 完成后按错身份找不到节点 → 误判删除（第204轮 E2E 直接注入正确身份条目，恰好绕开登记竞态没抓到）。三层修复（⚠ 均勿回退）：①**身份在执行开始那一刻捕获**（[pluginRegistry.ts](src/nodes/pluginRegistry.ts) defaultNodeExecute 顶部 ledgerIdentity 快照——节点此刻必然在激活画布上；registerCanvasLedgerTask 新 identity 参数，缺省现读仅作兼容）；②**全画布自愈搜索**（[requestLedgerCore.ts](src/lib/requestLedgerCore.ts) resolveDeliveryTarget：登记的画布 key 只是线索不是判据——按 key 找不到时按 nodeId 搜 激活画布+全部画布快照（节点 id 全局唯一），搜到即按真实位置投递并纠正 key（inactive-node 目标改携带实际 canvasKey，[requestLedgerStore.ts](src/store/requestLedgerStore.ts) 投递/广播全用纠正后 key；patchTargetNode 改「实时层有节点就写实时层」自愈语义），搜遍仍无才算删除）；③**孤儿两次确认**（markOrphanConfirmed：单次判定绝不弹通知——切换瞬态里一次落空不代表删除；间隔 ≥5s 的第二次仍搜不到才转找回通知；找到目标即清嫌疑。真删除通知最多晚一个 tick ≤60s，误报归零）。实测：**614 vitest 全过**（新增自愈搜索 3 用例：key 错位指向激活画布/反向/画布整个不存在但节点在别处——全部纠正投递；存量断言随 inactive-node 带 canvasKey 更新）+ **浏览器端到端**（5199+dev8787 临时账号：**原报错场景复现修复**——身份错位条目（canvasKey=激活画布、节点实际在 ep-2）→ 自愈投递到真实画布+task 清除+销账+零通知卡实锤；**两次确认**——第一击后两条均 done 零弹窗、瞬态「节点回来」者第二轮正常投递（resultAssetId 写入）、真删除者两次确认后转 orphaned/node 且仅 1 张通知卡；零控制台报错；临时账号/localStorage 全清）。**须客户端重新打包**（随第204/205轮同一次发版）；服务端零改动。）

上一轮：2026-08-06（第204轮：**请求台账 requestLedger——请求独立运行空间（项目/画布/节点身份 + 全局兜底轮询 + 删除找回通知）·纯客户端**——用户报「客户端关闭/切项目/切画布后失去请求状态，服务端已成功、客户端永远停在进行中/初始态」。根因：请求状态只有两种存活形态——内存轮询（taskCenter，关软件即丢）与 node.data.task（随项目落盘，但第150轮找回只扫**当前激活画布**），请求没有项目/画布身份、失去目标即失联。①**台账**（新 [requestLedgerCore.ts](src/lib/requestLedgerCore.ts) 纯逻辑可单测 + [requestLedgerStore.ts](src/store/requestLedgerStore.ts)）：画布节点提交确认即登记 {taskId, adapterKey, **projectPath（savePath 即项目身份）/canvasKey（分集 id）/nodeId** + 标题/displayKind/assetName/idPrefix}，localStorage `Qiji:requestLedger` **应用级持久化**（独立于任何项目文件；上限 150 条、pending 72h/终态 30 天 TTL、文本结果截 5 万字）。②**全局兜底轮询**：登录后 initRequestLedger（App，幂等）+ 60s 周期 tick + 项目加载/切画布触发（onProjectContextChanged，延后 800ms 让画布找回先认领）——对「画布流程够不着」的在途任务（非激活画布/项目未打开/节点已删）重挂 taskCenter 集中轮询；⚠ **绝不与画布流程双挂**（新导出 pluginRegistry.isCanvasTaskTracked + 目标为激活画布节点的一律让给 resumeCanvasNodeTasks——taskCenter handler 按 taskId 唯一，双挂互相顶掉，勿回退）。③**按身份投递**（resolveDeliveryTarget 纯函数）：激活画布节点=让既有找回路径完整落盘（含裂变）；同项目非激活画布=**直写 canvases 快照**（媒体走 persistLedgerMediaAsset：本地副本→rawLink 接力转存 OSS→rehost 兜底→三元映射/媒体库登记/资产名写回，与 defaultNodeExecute 媒体完成分支同语义；文本写 resultText+清 task 标记；写入时**现算**激活画布防切换竞态，写失败不销账留下轮重判）；项目未打开=结果缓存台账待其加载；⚠ **能证实删除才判孤儿**（勿回退）——项目没打开一律 defer 绝不误判，Tauri exists() 确认项目文件已删才转孤儿（检查按路径 10 分钟节流）。④**找回通知**（[TaskRecoveryNotice.tsx](src/components/TaskRecoveryNotice.tsx) App 根挂载自订阅）：目标已删（节点/画布/项目三种原因分别措辞）→ 右上角通知「您的任务已完成，但X已被您删除，无法找到落盘位置，特此通知。以下是找回的任务/视频|图片|音频链接：…」+ 复制链接/复制文本内容，**不自动消失**（花了积分的结果必须用户亲见），逐条关闭即销账。⑤**挂钩**：defaultNodeExecute onTaskConfirmed/找回分支登记（存量 node.data.task 在途任务经找回路径**补登台账**，台账上线前的旧单也获身份）、终态回执 ledgerOnCanvasTerminal（成功且节点在激活画布=销账；节点不在（切画布/被删）=结果缓存立即按身份投递；失败=销账——失败已自动退款无可找回）；节点被重新提交（task.taskId 换人）旧结果作废销账；投递幂等防重（节点 resultAssetId/history 已含同 id 只清标记销账）。实测：client tsc 干净 + **576 vitest(+15) 全过**（sanitize 清洗/TTL/容量/upsert 保留提交时间/投递目标七分支/孤儿文案关键句）+ vite build 过 + **浏览器端到端**（5199+dev8787 临时账号真实注册登录：done 条目→非激活画布快照 resultText 直写+task 清除+销账实锤；节点删/画布删→两条孤儿通知渲染（措辞与链接精确）；关闭→销账+localStorage 同步落盘；pending 条目→台账认领集中轮询（适配器缺失经轮询层报错销账=接线实锤）；**刷新页面（模拟重启客户端）→台账从 localStorage 恢复、孤儿通知重现**；零控制台报错；临时账号/测试数据/假项目痕迹全清）。**须客户端重新打包**；服务端零改动（服务端任务落盘+终态留 48h 第89轮已有，是台账的前提）。已知边界：①表格模式（pendingGens/inferTasks）不经台账——其断连保护随项目落盘已有，落点是资产不是节点，删资产场景后续可循同范式接入；②跨画布投递的拆分/推理类文本结果**不补裂变**（裂变需激活画布布局计算；正文已写入节点，用户切回后可「二次解析」补建子节点）；③服务端终态只留 48h——超 48h 未开机的在途任务无从找回（台账 pending 72h 自动清理）；④「项目已删」判定仅 Tauri 生效（浏览器 dev 无文件系统，永远 defer）。**待真机 QA**：Tauri 打包后真单四场景——生成中切画布/切项目/关软件重开（结果自动落回原节点）、生成中删节点（弹找回通知且链接可播）。）

上一轮：2026-08-06（第203轮：**LibTV/即梦 本地 CLI 模型更新——LibTV 加 MiniMax H3、即梦内置 CLI 升 1.4.15 加 Seedance 2.5·纯客户端**——用户令「更新 libtv 和即梦 cli，主要看 seedance 2.5 和 minimax H3」。**真机情报（2026-08-06 实测）**：LibTV CLI 1.1.1 已是最新，`model search --type video` 实锤线上有 **MiniMax-Hailuo-H3（Minimax H3）**、**没有 Seedance 2.5**（Seedance 最高仍 2.0 家族 + 1.5 Pro；另有 Kling 3.0/O3、Hailuo 2.3、Vidu Q2/Q3、Wan 2.7 等未上架款）；即梦上游 version.json 实拉 **1.4.15（2026-08-01）新增 seedance2.5**（内置为 1.4.10）。①**内置 dreamina.exe 1.4.10→1.4.15**（commit a857341，官方 bytednsdoc 直链下载后 --version/multimodal2video -h 实证）：新 CLI `--video_resolution` 变**必填**（[dreaminaCli.ts](src/services/dreaminaCli.ts) 封装一直恒发不受影响）+ **参数严格校验**（非法值明确报错不静默调整，与 §9 同哲学）；顺带新增 `seedance2.0mini` 档（首版未上架，用户随即令加上——CHOICES 加 `dreamina-seedance-2-mini`，走 2.0 家族默认档：720p、时长 4-15、需图或视频，561 vitest 全过）。②**即梦 +Seedance 2.5**（[dreaminaAdapter.ts](src/services/adapters/dreaminaAdapter.ts) CHOICES 加 `dreamina-seedance-2-5`，model_version=`seedance2.5`，即梦侧 **VIP 专属**）：时长 **4-30s**、分辨率 **480p/720p**（默认 720p）、素材上限 **图30/视10/音10（总≤50）**、**允许纯音频参考**（参考音视频单条与合计 2-30s）——REF_LIMITS 按款分派（REF_LIMITS_SD25）、入参底线放宽为「至少一条素材（图/视/音均可）」；⚠ **新款遵守 §9 不夹钳**：duration/ratio/resolution 缺省补默认、非法值原样发出由 CLI 明确报错（clampDreamina* 只服务存量四款，历史收敛行为分毫未动）。③**LibTV +MiniMax H3**（[libtvAdapter.ts](src/services/adapters/libtvAdapter.ts) CHOICES 加 `libtv-minimax-h3`，[libtvCli.ts](src/services/libtvCli.ts) 变体表加 `minimaxH3`→modelKey `MiniMax-Hailuo-H3`）：schema 实拉（`libtv model MiniMax-Hailuo-H3`）分辨率 **768P/2K（默认 2K）**、时长 **5-15**、比例七档同 Seedance、mixed2video 图9/视3/音3 同尺；⚠ **H3 schema 无 enableSound 键**——libtvRunVideoNode 改按变体分发（仅 Seedance 系发 `-s enableSound=on`，勿回退成恒发）；H3 参数同样不夹钳（h3Params 缺省补默认）。④**家族归属修正**：H3 非 Seedance——LIBTV_MODEL_CHOICES 每款自带 familyId/familyName（H3=`fam-minimax`「MiniMax」，客户端侧 id；服务端将来注册同 id 家族则显示名自动跟随），[ModelPicker.tsx](src/components/ModelPicker.tsx)/[channelAdapter.ts](src/services/adapters/channelAdapter.ts) 两注入点改按款取家族（⚠ 勿再把本地 CLI 全员钉 fam-seedance）。实测：client tsc 干净 + **560 vitest(+3) 全过**（H3 独立家族分组/2.5 在列且 flag 值精确/存量四款 model_version 锁定）+ vite build 过 + CLI 真机（H3 schema 全文、新 CLI 版本与 2.5 档位实证）。**须客户端重新打包**（新 CLI 二进制与新模型都随包）；服务端零改动。已知边界：①2.5 即梦侧 VIP 专属、部分模型首用可能返 `AigcComplianceConfirmationRequired`（需先到即梦 Web 端用该模型完成一次生成）；②H3 未真单验证（提交/轮询链路与 Seedance 完全共用，参数档按 schema 实拉）。**待真机 QA**：H3 真单（text2video 与带素材 mixed2video 各一）、2.5 真单（VIP 账号：30s 档、纯音频参考）、模型下拉家族分组观感（MiniMax 独立组）。）

上一轮：2026-08-06（第202轮：**转深度完全嵌入客户端（模型进安装包，零网络零服务端依赖）+ 设置「启用 GPU」开关·纯客户端**——用户令「将转深度图的功能完全嵌入客户端，无需服务端就能使用，在设置中增加启用GPU设置」。①**模型内置**：Depth Anything V2-Small 四文件（config/preprocessor_config + q4f16 19MB/q8 26MB 双档 onnx，从 server/data/depth-model 网关缓存复制）落 **`public/depth-model/onnx-community/depth-anything-v2-small/`**（vite public 原样进 dist → Tauri 打进安装包，**安装包增重 ~45MB**）；[depthEstimator.ts](src/lib/depthEstimator.ts) 改 `env.allowLocalModels=true + allowRemoteModels=false + localModelPath="/depth-model/"`——第187轮的 remoteHost 网关下发（syncRemoteHost/connectionStore 依赖）整体删除，**断网/未登录都能转深度**；服务端 `/v1/depth-model` 路由**保留**（旧版打包客户端仍在用，本轮服务端零改动）。⚠ **踩坑（勿回退）：localModelPath 必须是相对路径勿 absUrl**——transformers v4 `get_file_metadata` 对 URL 形态 localPath **跳过本地存在性检查**（`if (!isURL)` 才查），叠加 allowRemoteModels=false 即判 preprocessor_config.json 不存在 → 管线不装 processor →「this.processor is not a function」（浏览器实测复现两次后源码定位）；相对路径 fetch 同源解析 dev/打包通用（ort wasmPaths 的绝对 URL 要求是另一机制（blob 模块导入），互不冲突）。②**设置「启用 GPU」**：[settingsStore.ts](src/store/settingsStore.ts) 新 `depthGpu`（默认 true，持久化+读盘归一）+ `setDepthGpu`；[SettingsModal.tsx](src/canvas/SettingsModal.tsx)「生成偏好」页新「本地推理（转深度）」卡——复选框「启用 GPU 加速（WebGPU）」+ 本机 WebGPU 支持徽标 + 说明（模型已内置无需联网；GPU ~0.2–0.5s / CPU ~1–3s；切换对下一次转深度生效）；[depthEstimator.ts](src/lib/depthEstimator.ts) getPipe 按开关分派（关=跳过 WebGPU 直走 WASM q8），**开关变化后下次调用自动丢弃旧管线重建**（dispose 尽力释放）。③三处「首次使用需下载模型约27MB」文案改「模型已内置，无需联网」（BaseNode/ContextMenu/depthify 失败提示）。实测：client tsc 干净 + 557 vitest 全过 + vite build 过（dist/depth-model 四文件实锤）+ **浏览器端到端（服务端全程未启动=零服务端实锤）**：清 Cache API 后 320×240 测试图推理 SUCCESS（100% 灰度、动态范围 0–255），transformers-cache 键全部 `localhost:5199/depth-model/*` 同源（q4f16=WebGPU 路径生效）；**GPU 开关双向实锤**——setDepthGpu(false)→重建加载 q8 WASM 推理成功、切回 true→q4f16 再成功，settings.json 双向持久化；登录后设置 UI「生成偏好」页卡片渲染（含「本机支持 WebGPU」徽标）、复选框点击双向落盘；零控制台报错（临时账号已删）。**须客户端重新打包**（模型随包分发是本轮核心；不打包=旧客户端继续走网关下发不受影响）；服务端零改动无需部署。已知边界：①安装包 +45MB（q4f16+q8 双档都内置——GPU/CPU 两路径各用各的档）；②转深度不再需要浏览器 Cache API 首跑下载，但 transformers 仍会把同源响应写进 cache（无害）；③WebView2 WebGPU flag（第199轮 additionalBrowserArgs）仍是 GPU 快路径的前提，设置开关只控制「是否尝试 GPU」。）

上一轮：2026-08-05（第201轮：**简一助手上下文上限（6万提示 / 8万封锁 + 双总结按钮）·两端**——用户需求全落地：①会话上下文字数=全部消息正文+可读附件正文（新 [jianyiContext.ts](src/lib/jianyiContext.ts) `sessionContextChars`，纯函数可单测）——达 **6 万字**输入区上方黄条提示「建议新开窗口」；达 **8 万字**红条封锁（textarea/发送/上传图片/上传文件/粘贴素材/重新回答全禁；回退/复制仍可用），只留两按钮：**【总结本次对话】**（注意力 首30%/中20%/尾50%，总结性语言，流式落回当前会话为助手消息）与**【总结至新窗口继续】**（生成**提示性交接语言**→自动新建会话「原名 · 续」并继承原会话模型、交接词预填输入框——用户确认后发送即无缝续聊，不自动发送不额外扣费）。②⚠ **「完整请求」定稿（用户两次纠偏后最终定稿，勿回退）：零采样零截断**——客户端把整段对话转录（`formatConversation`，含附件正文）**原样完整**作 `{{对话内容}}` 变量提交（服务端 bodyLimit 25MB 足够），服务端 [jianyiSummaryPrompt.ts](server/src/translators/jianyiSummaryPrompt.ts)（buildPrompt 按 purpose 分支，转视角同构）**不做任何采样/压缩**，完整填入模板（曾按「注意力 首/中/尾 权重采样」实现过两版——先客户端采样、后服务端采样——均被用户否决删除，勿再加回任何「智能压缩」）；该分支的存在价值=模板被管理端删/清空/禁用时回退代码兜底正文（功能不断）；`对话内容` 缺失=形状不对走普通路径。③**提示词由管理端保存**：新 Purpose **`chat.summarize`**（[contract.ts](src/contract.ts)+purposeRegistry+logs PURPOSE_LABELS「对话总结」）+ 两条服务端模板 **`jianyi.summary`/`jianyi.handoff`**（[templates.ts](server/src/store/templates.ts) **补种**入库（勿 bump 版本号）、分类「简一助手」，管理端「提示词模板」页可调优；带 purpose=第200轮统一规则天然 **catalog 正文/预览零下发**）；客户端 runPurpose 只带 templateId+variables（兜底正文在服务端代码，客户端零提示词）。管理端模板页 PURPOSES/CATEGORIES（+「简一助手」）与两控制台 PURPOSE_LABELS 同步。实测：client+server tsc 干净 + **557 vitest(+6) 全过** + vite build 过（dist 无服务端正文泄漏）+ 沙盒冒烟 **16/16**（全新库种子/catalog 零泄漏/buildPrompt 展开与缺失兜底）+ **7/7**（零采样定稿版：完整 10 万字原样进提示词、无省略标注、handoff 同规、模板删除兜底仍完整填入、缺变量走普通路径、种子正文无采样措辞）+ **5/5**（复制真实 dev 形状库：补种 17→19、存量 17 条正文一字未动、二启幂等）+ dev 8787 tsx 热迁移实锤（19 条落库；零采样定稿后两条正文已经管理端 API 更新到位）+ **浏览器端到端**（5199+8787 临时账号真实注册登录：注入 8.5 万字会话=红条「已达 8.5 万字（上限 8 万）」+双按钮+输入/上传/粘贴/重答全禁实锤、6.5 万字=黄条提示不封锁；echo 模型实测两按钮全链路——**请求记录①段实锤客户端进参 对话内容=85,083 字完整**、总结=助手消息流式落回、交接=新会话「· 续」+模型继承+输入框预填交接词且新会话不封锁；请求记录 `chat.summarize` success 归属正确；临时账号/echo 模型已删、测试会话已清、零控制台报错）。**须客户端重新打包 + 服务端重新部署**（首启自动补种两模板；只部署服务端=旧客户端无上限提示但功能不受影响；只打包客户端=模板缺失走客户端兜底文案，仍可用）。已知边界：①字数按**整段会话**计（日常对话发给模型的记忆窗口仍是最近 12 条——上限防的是会话无限膨胀与总结质量）；②总结/交接动作走正常文本模型计费，且完整对话原样进提示词——超长会话（尤其带大附件）可能触及所选文本模型自身的上下文上限，届时上游明确报错（不静默截断，合 §9「绝不静默改写」）；③弹出窗口（popout）同组件天然生效。）

上一轮：2026-08-05（第200轮：**catalog 停发可执行模板 bodyPreview（提示词预览泄露修复）·纯服务端**——用户发现客户端悬浮提示词模板下拉会显示提示词开头（智能推理「## 1. 角色定位 (Role)…」），疑客户端泄露提示词正文。核实：正文全文从未下发（带 purpose 模板 `body: undefined` 一贯如此），泄露面=catalog 下发的 `bodyPreview`（正文前 200 字，[OperationPanel.tsx](src/panel/OperationPanel.tsx):560 放进下拉选项 `title` 悬浮显示）。⚠ 修法（勿回退）：[catalog.ts](server/src/catalog.ts) buildTemplates 改统一规则——**带 purpose 的可执行模板 bodyPreview 一律不下发**（与 body 同规则；客户端选用只需 id/name，提交带 templateId 服务端展开正文）；原 viewangle.*/panorama.* 前缀特判（第193/194轮保密占位文案）随之收编删除（四条保密模板均带 purpose，统一规则天然覆盖）。**无 purpose 的预设/画风类模板全文照发不受影响**（客户端要本地展开胶囊；消费点 Frame164/SettingsModal/presetSchemes 全部核对无误伤）；catalog.presets 独立预设库不动。客户端零改动（`title={t.bodyPreview ?? ""}` 变空=悬浮无提示，属预期观感）。server tsc 干净 + dev 8787 端到端冒烟 10/10（临时账号建→login 绑机→拉 catalog：13 条可执行模板 body/bodyPreview 零下发、响应全文无「角色定位/超级影视制作大脑」正文片段、6 条无 purpose 模板与独立预设库 6 条正文完好、viewangle/panorama 4 条零正文零预览；临时账号已删）。**须重新部署服务端**（catalog 热更 ≤30s 对新旧客户端一并生效）；客户端零改动无需打包。）

上一轮：2026-08-04（第199轮：**转深度占位节点改专用 `image.depth` 节点——杜绝「点生成=误调生图模型扣费」·纯客户端**——真机事故：用户反馈「转深度调用了生图模型且不是深度图」（截图：山水风景=空提示词纯文生图典型输出、堆叠2层、手动连线）。⚠ 排查结论（勿再怀疑打包）：**wasm 依赖在安装包里**（0803 安装包 app.exe 二进制扫描实锤内嵌 `/assets/DMmc6YqF.wasm` 23.5MB + `/assets/CVnZHRES.mjs` ort 运行时；?url 导入=缺资产 build 必失败，转深度按钮在=依赖在）；转深度代码纯本地绝不会调生图——生图请求全部来自用户在 image.gen 占位节点上点「生成」（多半是转深度失败/等待中当成重试按钮）。**真正要查的是服务端**：模型 19–27MB 设计上不在安装包、运行时经 `/v1/depth-model` 网关下发——生产服务端若没部署第187轮版本该路由 404=转深度必失败（核查：curl `/v1/depth-model/onnx-community/depth-anything-v2-small/resolve/main/config.json` 是否 200）。修法（用户定稿「不给生图机会，点击就是重新转深度」）：新 NodeSpec **`image.depth`**（[nodeSpecs.ts](src/nodes/nodeSpecs.ts)：capability null/purpose null/**kind "script"**/display image/inPalette false）——**永不走生图管线**（OperationPanel isScript=无模型下拉、运行不要求模型）；「运行」=**重新转深度**（[pluginRegistry.ts](src/nodes/pluginRegistry.ts) runScriptNode 顶部按类型分派 → [depthify.ts](src/canvas/depthify.ts) 新 `rerunDepthNode`：原图解析 上游连线图片结果（可换图重跑）> params.depthSrcAssetId（fetchUriOf 现查活映射）> params.depthSrcUri；运行即清场旧深度图进堆叠）；depthifyNode 改 spawnNodes **带连线承接原图** + params 记录 depthSrcAssetId/depthSrcName；BaseNode 悬浮按钮/ContextMenu 对 image.depth 显示「重新转深度」=dispatch run（不再套娃新建）；失败文案改「可再点『运行』重试」。client tsc 干净 + 551 vitest 全过 + vite build 过 + 浏览器端到端全实锤（vite5199+dev8787 临时账号真实注册登录：depthifyNode→节点 type=image.depth+连线+params 正确→推理 success→**像素断言 320×240 全灰度 0-255 近白211/远黑146**→dispatch run 重跑 success+堆叠2层→**服务端日志该用户 0 条生成请求**（0积分账号全程零扣费零 402）；临时账号已删）。**须客户端重新打包**；服务端零改动。已知边界：①存量项目里旧「深度-」image.gen 占位节点不迁移（无从可靠识别，仍是普通生图节点）；②旧版打包客户端打开含 image.depth 节点的项目，sanitizeCanvas 会当未知类型清除该节点（新节点类型惯例，pano.view 同）；③WebView2 WebGPU flag 已随本轮补充配上（见下）。
　**补充（生产「no available backend found」根因修复：CSP 拦 blob: 模块导入 ·客户端 tauri.conf.json + depthEstimator 双保险 · 打包版真机 CDP 探针 修复前后对照实锤）**——用户报「测试环境能转深度图，生产环境不行」，错误「no available backend found. ERR: [webgpu] TypeError: Failed to fetch…」。排查路径（后续排障可复用）：给 tauri.conf.json 临时加 `additionalBrowserArgs --remote-debugging-port=9333` 重建 app.exe → Node 内置 WebSocket 写 CDP 探针（scratchpad cdp-probe.mjs，Runtime.evaluate awaitPromise）在**真打包环境**逐项实测——wasm fetch 200/application/wasm ✓、mjs fetch 200/text/javascript ✓、mjs 动态 import ✓、navigator.gpu=true（WebView2 151 **WebGPU 默认开**），但 import 打包分块直跑 `estimateDepthPng` 复现完整错误：**`Failed to fetch dynamically imported module: blob:http://tauri.localhost/…`**。⚠ **根因（勿回退）**：打包版 WebView2 里 SharedArrayBuffer 可用 → ort 走多线程路径，对**判不出同源**的 wasmPaths.mjs（我们传的是相对路径 `/assets/xx.mjs`）做「fetch 预载→blob: URL→动态 import」——而 CSP `script-src` 无 `blob:` → 全部后端初始化被拦。dev 浏览器 SAB 不可用（无跨源隔离）=单线程直接 import 相对路径不触发 blob，故「测试环境好用」。**双保险修法（勿删任一）**：① [tauri.conf.json](src-tauri/tauri.conf.json) CSP `script-src` 追加 `blob:`；② [depthEstimator.ts](src/lib/depthEstimator.ts) wasmPaths 经 `absUrl`（new URL(u, location.href)）转**绝对同源 URL**——ort 同源判定成立直接 import 压根不走 blob。同轮顺手：**第187轮 WebGPU flag 待办落地**——additionalBrowserArgs 配 `--enable-features=WebGPU`（⚠ 该字段会**整体替换** wry 默认参数，必须显式带上 `--disable-features=msWebOOUI,msPdfOOUI,msSmartScreenProtection --autoplay-policy=no-user-gesture-required`；新 WebView2 本就默认开 WebGPU，flag 只为老版本兜底）。实测：打包版修复前后对照（同机同服务器 64.90.2.120:8787）——修复前=用户同款报错、修复后=**SUCCESS 真实出深度图**；该服务器 `/v1/depth-model/...config.json` HTTP 200=服务端路由在役；dev 浏览器修复后照常 SUCCESS；tsc 干净+551 vitest 全过。**须客户端重新打包**（与本轮 image.depth 节点同一次发版）；服务端零改动。）

上一轮：2026-08-04（第198轮：**请求记录按渠道商分类（归属落笔固化 `ownerId`）·纯服务端**——用户忧「渠道商能不能拿到全部请求记录」+ 令「将渠道商请求记录分开，按渠道商分类」。核实：门户日志端点本就服务端强制过滤（chainUserIds+详情剥 ③④/agentCosts，无既有越权面），但归属是**查询期从用户表反推**——用户被删/改归属后历史记录归属漂移。改造（⚠ 语义定稿勿回退）：**归属=请求落笔那一刻的归属，固化进记录本身**。① [logs.ts](server/src/store/logs.ts) `LogMeta.ownerId`（用户日志=直属商 id、开码日志=签发商、平台直属=空）+ SQLite logs 表补 `owner` 列（ALTER 幂等）+ `idx_logs_owner` 索引，UPSERT 随写；`filterLogs` 新 `owners` 过滤（哨兵 `PLATFORM_OWNER="platform"`；与 userIds 为 **AND**）；② [routes.ts](server/src/routes.ts) 两处 startLog 传 `ownerId: user.agentId`、客户端 `/v1/logs/:id` 剥 ownerId；③ 门户 [agent.ts](server/src/routes/agent.ts) 日志四端点+详情改 `owners: chainAgentIds`（本商+下游链）——单用户视图=owners+userIds AND（范围外 userId 天然空集）、详情 404 判定=ownerId∈链；④ 管理端 [admin.ts](server/src/routes/admin.ts) logScope：商属=`owners:[agentId]`、源站=`owners:[platform]`（excludeUserIds/excludeAgentScoped 弃用，LogFilter 字段保留）；⑤ 启动一次性回填 `backfillLogOwners`（index.ts 注入 getUser 防 logs↔users 循环引用；flag 落 DATA_DIR——第183轮教训）。**行为变化**：用户改归属/被删后其历史记录留在**原归属商**视图；用户挂别家期间的记录本商永不可见（含门户 ?userId 单用户视图）；已删商的开码日志随归属离开源站视图。实测：server tsc 干净 + 沙盒冒烟 A 20/20（全新库：落笔固化/owner 列与索引/owners 过滤与 AND/summary·facets 口径/回填幂等）+ B 10/10（旧形状库：模块加载补列、回填 用户按直属商/开码按签发商/无主留源站、flag 幂等）+ dev 8787 tsx watch 热迁移实锤（flag 落地；`__all` 5238 vs 源站 5227，差 11 条=各商开码日志逐商核对 + owner 分布 SQL 只读复核）。**须重新部署服务端**（首启自动补列+回填，生产 39 万行首启阻塞数秒属一次性）；客户端零改动无需打包；管理端/门户页面零改动（查询参数不变，服务端映射改按归属列）。同轮追加运维脚本 [purge-agent-logs.mjs](server/scripts/purge-agent-logs.mjs)（清理渠道商归属的请求记录：无参=只列归属分布；`--owner=<id>`/`--all-agents` 定范围 + 可选 `--before=YYYY-MM-DD`；默认 dry-run 只读打开、`--apply` 真删（自动备份 qiji.db + 事务内 logs/log_details 同删）；⚠ **须停服后跑（或跑完立刻重启）**——索引常驻服务端内存，运行中删库会被内存回写；owner='' 源站直属记录永不触碰；dev 三场景 dry-run 实测过）。）

上一轮：2026-08-03（第197轮：**转存资产 `.bin` 后缀修复（Content-Type 防御 + 存量修复脚本）**——用户报 overseas 渠道成片转存 OSS 后成了 `video00024621.bin`。根因实锤（HEAD 验证）：上游成片托管站 minioapi.meaicc.com（MinIO）对成片返回 `Content-Type: binary/octet-stream`（对象元数据没配），rehostVideo 只在「头缺失」时兜底 video/mp4，垃圾值照单全收 → extFor 映射不到兜底 `.bin`，OSS 直链也以 octet-stream 服务（webview `<video>` 拒播）。⚠ 修法（勿回退）：新 [contentType.ts](server/src/translators/contentType.ts) `resolveContentType(raw, fallback)`——剥 `;charset` 段、**头缺失或泛型二进制类型（application/binary octet-stream）一律视为「上游没说」走调用方兜底**，具体类型原样保留；应用于四个「下载字节→落资产」取型点：rehostVideo（video/mp4，全视频渠道收口）、index.ts 图/音异步下载、jmh.ts 结果下载、volc.ts 增强结果下载（custom.ts 通用层媒体种类未知刻意不动）。存量修复=新脚本 [fix-octet-assets.mjs](server/scripts/fix-octet-assets.mjs)（P0 脚本惯例：默认 dry-run、`--apply` 真执行、`--id=` 定向）：扫台账 octet-stream 行 → 桶内 GET 字节 → **魔数嗅探真实类型**（mp4/webm/png/jpg/gif/webp/mp3/wav/ogg，嗅不出按 type 列兜底）→ PUT 新键（后缀换真实 ext + 正确 ContentType）→ 更新台账 content_type/oss_key/url；**旧对象保留不删**（已下发的旧直链继续可用）。server tsc 干净 + resolveContentType 冒烟 9/9 + 脚本对 dev 库 dry-run 全链路过（text 类型无法判定正确跳过）+ 生产成片头部字节 Range 实测嗅探 video/mp4 ✅。**须重新部署服务端；部署后在服务器上跑 `node scripts/fix-octet-assets.mjs`（先 dry-run 核对再 `--apply`）修存量 `video00024621` 等**；客户端零改动无需打包。）

上一轮：2026-08-03（第196轮：**画布新功能⑤「可嵌入 3D 模型系统」（任务⑤定稿形态）**——用户改定稿：不做孤立 3D 导演台，做一套**可嵌入的 3D 模型系统**，三宿主共用：①**独立 3D 导演台**（画布空白右键/图片节点右键「3D导演台…」）：网格舞台/图片垫底，放置**可动人偶**（自研骨骼 21 关节，拖关节=转父骨骼 open-pose-editor 手感）+**基础几何道具**（方块/球/圆柱/圆锥/板）+**用户导入 GLB/GLTF**（懒上传+自动归一尺寸落地），产物三件套=**合成图/OpenPose 姿势图/深度图（近白远黑）** 逐张落 image.gen 节点（懒上传 LC），合成图节点带 `data.stage3d`（场景 JSON，零位图）可**再编辑**；②**涂鸦嵌入**：涂鸦工具栏「3D模型」→ 底图垫底摆模型 → 透明模型层嵌回画面成新元素 kind "model3d"（doc 只存场景 JSON+相机；位图离屏重渲缓存；可移动/变形/擦除；选中浮条「编辑3D」再改）；③**720°全景嵌入**：全景查看器「3D模型」→ 模型放进全景三维空间（equirect 内视球背景，**转视角透视跟随**）。技术：**纯 three.js**（不引 r3f，~600KB 独立懒加载分块、主包仅+10KB）；骨骼 FK/姿势序列化/OpenPose 映射=纯逻辑层可单测（551 vitest(+16) 全过）；⚠ 三坑（勿回退）：three 的 scene.background equirect 采样与 panoRender **镜像**不可旋转救→改内视球面网格 rotation.y=-π/2；**flipY 对 ImageBitmap 被规范忽略**→UV 变换 repeat.y=-1 翻垂直（全景球+垫底图两处）；E2E 合成事件坐标须除以 **canvas dpr**。tsc 干净+551 测试+build 过+浏览器端到端全实锤（人偶渲染/拖动换位/关节拖拽 pose 落盘/三产物像素断言/涂鸦嵌入烙图/再编辑还原/全景方位色带 中心绿·右红·天顶白·地底黑 逐点精确）。**须客户端重新打包**；服务端零改动。详见 §8。

上一轮：2026-08-03（第195轮：**720°全景节点化 + 倒置修复 + 转视角球形轮廓恢复**——①**「转全景」改为搭链不自动跑**：点击后 spawn「生成节点（默认 gpt-image-2 家族 + `resolution:"4k"` + `aspect:"16:9"` + purpose/panorama 标记）→ 新节点类型 `pano.view`（720全景查看，440×260）」两节点两连线，**用户自选模型/要求后自己点生成**；结果一出查看节点**直接进入全景交互查看**（节点上拖动转视角/滚轮视场/HUD，双击放大全屏查看器，跳过扭曲平面图展示）；②⚠ **倒置修复（勿回退）**：[panoRender.ts](src/lib/panoRender.ts) shader `up = cross(fwd, right)` **不取负**（原 `*-1.0` 使画面上下颠倒；中心像素测试抓不到该 bug，回归须测非中心像素——fov110 下屏上=天白/屏下=地黑实锤）；③**PanoNodeView 首绘用 `setTimeout(draw,0)` 不用 rAF**（后台标签 rAF 冻结=黑画布）；④**转视角编辑器恢复线框球轮廓**（用户定稿观感：静态 SVG 球壳（圆 r110+经纬椭圆）包着 190×142 的 3D 画面卡，**相机固定语义不变**——球只是可视化外壳，取景框/WASD/滚轮/拖动逻辑未动）。tsc 干净 + 535 vitest 全过 + build 过 + dist 零密语 + 浏览器端到端（链结构/不自动跑/节点内拖动/倒置像素/双击放大/球轮廓渲染/拖动 15° 吸附联动全实锤）。**须客户端重新打包**；服务端零改动（第194轮部署要求仍在）。详见 §8。

上一轮：2026-08-03（第194轮：**画布新功能④「720°全景」+ 全局懒上传规则**——①**转全景**：图片节点悬浮/右键「转全景」→ 右侧 image.gen 节点生成 equirect 2:1 全景（purpose "image.panorama"，前置提示词=服务端模板 `panorama.main` 分类「转全景」，与转视角同规**保密不下发**）；②**全景查看器**（全屏，App 根 lazy）：**原生 WebGL2 equirect 渲染器**（[panoRender.ts](src/lib/panoRender.ts) 一个 fragment shader，零依赖零体积；水平 REPEAT=左右无缝回卷；关闭 loseContext 防 WebView2 上限）——拖动转视角/滚轮视场缩放/姿态 HUD；**截取当前视角**（1600×900 落新图片节点）+ **四/六/八/十二视图**批量截取（四=前后左右 fov90、六=+上下、八=每45° fov60、十二=平视/俯45/仰45×四向；1024² 方图落**分镜组节点**宫格承载）；③**全局懒上传**（用户定的新上传规则）：`uploadMediaToCanvasAsset` 改为**本地暂存**（LC- 本地资产 id，落盘+注册映射零网络，不再立即传 OSS）——涂鸦/转深度/截帧/上传节点/素材添加/全景切图全部自动获得；提交生成请求时由既有 `ensurePublicUrl` 补传 TP 并**把 OSS url 回填进 LC 映射**（第二次引用直接命中缓存零重传）；collectMedia 过滤 LC id 绝不发服务端（补传后凭 srcUri 换真 TP id）。client+server tsc 干净 + 535 vitest(+4) 全过 + build 过 + 浏览器像素级端到端（查看器中心色/四视图前绿右红后蓝左黄/六视图上白下黑逐张精确；LC 落地零网络→ensurePublicUrl 补传 TP+url 回填+二次命中缓存零重传 全链实锤）。**须客户端重新打包 + 服务端重新部署（panorama.main 模板补种+渲染分支）**。详见 §8。

上一轮：2026-08-03（第193轮：**转视角提示词服务端化（用户要求对客户端保密）**——客户端**零提示词文案**：节点只带 `purpose:"image.viewangle"` + 机位参数 `params.viewAngle{az,el,shot,panX,panY,lens,custom}`，可见 prompt 仅短参数标签（「转视角：水平180° 俯仰-30° 近景 · 鱼眼」）；服务端 buildPrompt 按 purpose 分支（变体前缀同款先例）经新 [viewAnglePrompt.ts](server/src/translators/viewAnglePrompt.ts) 渲染——机位/俯仰/景别/平移/镜头短语=参数化代码文本，**主骨架+未见区域两档条款=管理端「提示词模板」新分类「转视角」三模板**（viewangle.main/.backfill.far/.backfill.mid，补种入库、可调优、删空自动回退代码兜底）；⚠ **catalog 对 viewangle.\* 连 bodyPreview 都不下发**（原 bodyPreview 会漏前 200 字）。新 Purpose "image.viewangle" 两端注册（purposeRegistry/PURPOSE_LABELS「转视角」）。**放「提示词模板」而非「预设」的原因：预设是全文下发客户端的（藏不住），模板正文本就只留服务端——这正是两个实体的分界**。client+server tsc 干净 + 531 vitest 全过（客户端提示词用例随迁移删除）+ build 过 + 冒烟实锤（服务端渲染三例正确、坏参数回退、catalog 三模板 bodyPreview=保密占位 body=null、节点 params 结构化、**dist 产物 grep 零提示词文案**）。**须客户端重新打包 + 服务端重新部署（首启自动补种三模板）**。详见 §8。

上一轮：2026-08-03（第192轮：**转视角提示词重写为纯镜头语言（真机崩法反馈）**——用户真单实测：无人场景 180° 出图**凭空造了个背对镜头的人、场景还是正面**。根因=提示词「背面视角/展示背面/发型/服装」是**拟人措辞**，模型把「背面」理解成「给我一个背面的人」。⚠ 重写（勿回退）：[viewAngle.ts](src/lib/viewAngle.ts) 全部短语只讲**相机的位置与朝向**——180°=「相机绕场景中心水平环绕180度移动到场景正后方，镜头转向回望原拍摄方向——从场景的另一侧往回拍」；未见区域条款不提任何人物词，只讲「新机位下哪些区域原图没拍到（各物体的背侧、场景的另一面），按物体形状/材质/结构/空间布局推理生成，前后关系相应反转」+ **明令「画面里只能出现原图已有的物体，绝不要凭空添加原图中不存在的物体或人物」**（直接反制造人崩法）；一致性句「人物与主体的形象、服装」→「原图里已有的一切（场景、物体，如有人物也包括人物）」条件化措辞；panPhrase「主体偏向」→「画面内容整体偏向」。538 vitest 全过（断言随句式更新，含「无背面措辞」反向断言）+ tsc + build 过。**须客户端重新打包**；服务端零改动。详见 §8。

上一轮：2026-08-03（第191轮：**转视角·取景器交互重做（用户三点反馈）**——①**背面提示词优化**：|环绕|≥135° 自动附「背面推断条款」（背面是原图**未展现**区域，须基于发型/服装结构/体型/场景延伸合理生成，**禁止镜像正面充当背面**），90–135° 附侧后方补全条款；②**相机固定面向屏幕，拖动转的是画面**：弹窗左侧改为**取景器**——源图为 CSS 3D 卡片，拖动改水平环绕/垂直俯仰（自由角度实时预览，松手吸附 15° 档），线框球删除；③**固定取景框 + 滚轮距离 + WASD 平移 + 镜头差异化**：取景框固定不动（标准/荷兰角=矩形三分线框（荷兰角画面转15°）、**鱼眼=圆形框 + canvas 逐像素桶形畸变简易预览**（r→r^1.6 重采样、圆外留空；实际效果仍靠模型生成；⚠ 位图 fetch→createImageBitmap 防画布污染，主源失败走服务端 /raw 兜底，再失败回退 img 无畸变）），滚轮=图像缩放=等效相机距离（40%–200%，shotFromScale 换算景别档），**WASD=相机平移取景**（±40%、4%/步，输入框聚焦放行；入 prompt 为「主体偏向画面X方」构图短语，8% 内忽略/22% 分轻重）。取景器与右侧滑杆双向同步。538 vitest(+4) 全过 + 浏览器端到端（拖动 +65°/+35° 吸附 60°/30°、滚轮 175%→「特写」联动、WASD 12%/8%、鱼眼圆框+角落透明中心实像素、背面+平移 prompt 三条款全中——假模型零花费；实测坑：HMR 后注入脚本经裸 URL 拿到旧 store 实例=读不到会话，测试注入需防）。**须客户端重新打包**；服务端零改动。详见 §8。

上一轮：2026-08-03（第190轮：**涂鸦工具栏「资产名称」下拉——快速把资产名（主体）作为文字落画面**——[AnnotationEditor.tsx](src/components/AnnotationEditor.tsx) 工具栏新增「资产名称」下拉（Tags 图标，虚线橡皮右侧）：数据源=projectStore 五类资产（角色/群像/场景/生物/物品）已命名者按类分组成名字胶囊。**点击名字=画面中心落文字元素**（本质普通 text 元素，颜色/字号跟当前设置，按 canvas measureText 实测宽度居中）**+ 收起面板 + 自动切「选择」工具并选中**（可立刻拖动调位）；**把名字拖到画面=在落点居中放置且面板不收起**（可连续拖多个；HTML5 DnD 自定义 MIME `application/x-qiji-asset-name`，不拦外部文件拖入）。纯客户端零新依赖零数据层改动；client tsc 干净 + 534 vitest 全过 + 浏览器端到端实锤（登录页已改账号密码+机器码无法自动登录→临时 vite harness 直挂编辑器验证，已删）。**须客户端重新打包**；服务端零改动。详见 §8。

上一轮：2026-08-03（第189轮：**画布新功能③「转视角」——多角度编辑器（纯提示词路线，零新上游）**——用户定稿：不接 LoRA（怕限制模型），用通用图像编辑模型（**默认 gpt-image-2 家族**）+ 标准化视角句式实现。**多角度编辑器弹窗**（对齐用户参考截图）：预设行（自定义/鱼眼/倾斜/正面俯拍/正面仰拍/全景俯拍/背面视角）+ SVG 线框球机位可视化（球心源图缩略+相机标记+四向微调，纯装饰）+ 水平环绕(0-345°)/垂直俯仰(±90°)/景别(特写..远景) 三滑杆 **15° 档位吸附**（无 LoRA 硬监督，句式标准化+档位化降歧义）+ 提示词开关（自由补充）+ 模型下拉（订阅 catalog 响应式、默认锁 fam-gpt-image-2）。提交=源节点右侧新建 image.gen 节点+**连线承接源图**（图像超分同构）+ 图例前缀 + 模板句式提示词 + dispatch run 走标准生成管线（计费/进度/历史/换模型重跑全继承，**服务端零改动**）；三入口（悬浮「转视角」/右键/灯箱「⟳ 转视角」，灯箱无节点=产物节点自带素材落视口中心）。纯模板层 [viewAngle.ts](src/lib/viewAngle.ts) 可单测（方位/俯仰/景别/镜头特效句式 + 一致性约束句）。534 vitest(+5) 全过 + 浏览器端到端（弹窗/预设联动/默认模型/提交管线假模型验证——节点+连线+图例+提示词+run 全对，未发真实请求零花费）。**须客户端重新打包**。详见 §8。

上一轮：2026-08-03（第188轮：**涂鸦「选中框变形」——移动/旋转/翻转/Alt复制/四角透视**——统一 **quad（四角）模型**：`AnnoElement.xform{quad}`=几何外接矩形四角变形后的位置，移动=平移、旋转=绕质心、翻转=镜像、透视=拖单角，一种表示全覆盖；渲染凭 基准矩形→quad 的**单应矩阵 + 网格纹理贴图**（canvas 2D 无透视——元素含蒙版先离屏渲成源画布，仿射 1 格数学精确、透视 6–14 格细分逼近；编辑器 Shape sceneFunc 与导出共用）。交互（选择工具）：选中框虚线四角框、**四角手柄拖动=透视**、**四角外缘环区=旋转**（悬停双向弯箭头光标，Figma 式；用户实测指出顶部手柄会被浮条挡住后改此形态）、**浮条 ↔/↕ 翻转 + 复位**（quad 回原位自动摘除 xform）、**Alt+拖动=复制**（原元素不动落平移副本，三类渲染路径统一）。变形与既有系统闭环：蒙版随组一起变形、变形后再擦经**逆单应**存回基准空间、拖动时 quad 同步平移、doc 落盘可再编辑；顺带修「点选元素立刻被清选」bug（DOM 冒泡层清选→改 Stage 空白命中才清）。529 vitest(+9) 全过 + 浏览器端到端像素实锤（翻转箭头头部换边、旋转角点数学精确、透视角烙进导出、副本继承变形）。**须客户端重新打包**；服务端零改动。详见 §8。

上一轮：2026-08-03（第187轮：**「标注」改名「涂鸦」+ 虚线橡皮滚轮调密度 + 画布新功能②「转深度」**——①全部用户可见文案 标注→**涂鸦**（三入口/编辑器标题/弹窗/产物文件名「涂鸦-」；代码标识符与 `node.data.annotation` 字段**不改名**，旧项目零迁移）；②虚线橡皮模式下**滚轮调虚线密度**（上滚更密/下滚更疏，0.25–4×，涂抹中实时生效；`AnnoMask.dashScale` 随蒙版落盘、=1 省略）；③**转深度**：图片→黑白深度图（**近白远黑**）**纯客户端本地推理**（transformers.js v4 + Depth Anything V2-Small ONNX，WebGPU q4f16 → WASM q8 自动降级；⚠ license 只有 Small 可商用）——三入口同涂鸦（悬浮「转深度」/右键/灯箱「◑ 转深度」），点击即落**占位节点**显真实进度（下载 0-88%/推理/合成），完成传 TP 资产为主图；**模型经自家网关 `/v1/depth-model` 下发**（新服务端路由：白名单代理 hf-mirror + data/depth-model/ 磁盘缓存，客户端不出外网）；ort wasm 运行时（**必须 asyncify 变体**）经 ?url 打进产物不走 CDN。client+server tsc 干净 + 520 vitest(+7) 全过 + build 过 + 浏览器端到端实锤（深度图 640×480 全灰度 0-255、近 251/远 13；dashScale=e^-0.5 精确落盘）。**须客户端重新打包 + 服务端重新部署（模型下发路由）**。详见 §8。

上一轮：2026-08-03（第186轮：**出海营（overseas）视频渠道接入**——新模式「overseas」（api.aiid.edu.kg · Seedance 任务格式，Bearer sk-）：新协议 `overseas-video`（提交 `POST /api/v3/contents/generations/tasks`，素材经 **content 数组混排**、name=ImageN/VideoN/AudioN 与 @tag 图例一一对应，生成配置顶层 duration/ratio/resolution；首尾帧=兼容 mode i2v_first_last）+ 5 模型种子（os933-sd2.0 720p/1080p/4k 分辨率 routes 重定向上游后缀款 / -fast / -mini / os933-sd2.5 / os-gemini-omni 同接口 t2v/r2v/edit 按素材自动定+时长 4/6/8/10 就档）。全按秒**占位价**。server tsc 干净 + 沙盒冒烟 22/22。第187/188轮补充：**时长改「原样透传」**（第186/187轮的翻译器夹钳把请求 30s 静默砍成 15s、按秒计费却按 30 扣=多扣钱；第188轮用户定稿**「静默修改永远禁止」入 §9 全局规则**——夹钳/就档/兜底默认全删，档位由管理端参数把关、非法值上游明确报错；④c 迁移 sd2.5 参数档 max→30 仍有效）。**须重新部署服务端（首启自动补种+迁移；部署后管理端「overseas（出海营）」渠道填 sk- 密钥）**；客户端零改动无需打包。详见 §8。

上一轮：2026-08-03（第185轮：**标注编辑器真机反馈修复 + 纹章/双橡皮**——①文字「无法输入」修复（autoFocus 在 mousedown 同拍建元素场景不可靠→改 effect 强制夺焦 + mousedown preventDefault + 确定/取消按钮）；②「残影」根因=在 setState 更新器里嵌 setState 被 StrictMode 双调用→元素双份提交，改 draftRef/elementsRef 事件回调提交 + 按 id 去重（⚠ 勿回退）；③新增**纹章**（下拉章库：内置 人形/星形/爱心 矢量章 + PNG 自定义章存本机 localStorage 章库；指针即预览、滚轮缩放、点击盖章；doc 只存章引用绝不存 base64）；④橡皮/虚线橡皮=**「依附在笔画上的擦除蒙版」**（四易其稿最终定稿：**擦的是笔画不是画布**——笔刷分发为相交元素的锚点相对蒙版，destination-out 挖孔露出**当前位置**原图；**移动笔画时擦除效果自动跟随**；虚线橡皮=**沿笔刷路径方向间隔挖孔**（dash 分段）；涂抹时**半透明红色预览**+**吸附线条**（沿线擦除自动修正到线上，粘滞同一条线）；对一切元素类型统一生效）。513 vitest 全过 + 浏览器端到端像素级实锤（拖动跟随、虚线段长、吸附贴线）。**须客户端重新打包**。详见 §8。

上一轮：2026-08-02（第184轮：**画布新功能① 图片标注**——konva 自建标注编辑器（笔·停顿拟合平滑曲线/箭头/矩形/圆·无填充|有填充/文字/颜色，三档粗细），三入口（灯箱「✎ 标注」/图片节点悬停「标注」/右键「标注…」），完成=合成图上传 TP 资产→新建 image.gen 节点承载、矢量 doc 随节点落盘可**再编辑**（产物节点再点标注=带矢量继续改，写回原节点旧图进堆叠）。⚠ 矢量只存坐标 JSON 绝不存 base64。同轮：管理端「存储」页 保留策略+容量总览 改双栏。**须客户端重新打包 + 服务端重新部署（存储页双栏）**。详见 §8。画布新功能②-⑤（深度图/转视角/全景/3D导演台）见 docs/画布新功能方案调研.md，按任务清单逐个实现。

上一轮：2026-08-01（第180轮：**P2 观察期**——保留策略三档天数可配（tmp/unref/ref，默认 7/7/14）+ 管理端「保留策略 · 清理预览」卡。⚠ **只算不删**：模块与页面刻意都没有任何删除能力，防观察期内手滑开闸；≥30 天后才进 P3。须重新部署服务端，客户端零改动。详见 §8。

上一轮：2026-08-01（第179轮：**P1 全部完成**——收藏服务端化（按 assetId，替代换机即失效的 localStorage 版）+ 个人 200MB／团队 2GB 配额 + 扩容卡（门户开码积分实扣签发、客户端核销、容量叠加各自计时）+ 256px WebP 缩略图 + 管理端「收藏与配额」页 + 门户「扩容卡」页。⚠ **收藏＝永久保留额度，超额只挡新增收藏；取消收藏绝不立即删**。须两端一起发。详见 §8。

上一轮：2026-08-01（第178轮：**P1a 引用上报**——`POST /v1/assets/ref` + 客户端打开项目/保存时上报 `assetBlobs` 的 id → 刷 `last_ref_at`。⚠ **两端都上线那天才是 P2 观察期第 1 天**（≥30 天后才能进 P3 清理）。同轮记录了 P0 的生产对账实测结果与容量画像。详见 §8。

上一轮：2026-07-31（第177轮：**存储改造 P0**——台账加 7 列 4 索引（**启动自动迁移**，无「代码先上线、迁移后跑」空窗）+ 存储档 storage profile（换桶/换布局＝改配置不改代码；`reputAsset`/`ossDelete` 改按行的档定位）+ 资产归属记账（`size_bytes`/`user_id`/`agent_id`，走 AsyncLocalStorage 覆盖全部翻译器）+ 管理端「存储」页（容量 KPI，纯 SQLite 零 OSS 请求）+ 三个运维脚本（migrate/reconcile/verify，默认 dry-run）。**须重新部署服务端**，客户端零改动。详见 §8 与 [docs/存储与数据结构改造方案.md](docs/存储与数据结构改造方案.md)。

上一轮：2026-07-30（第176轮：**提示词模板开放范围改按渠道分组按键 + 模型管理同组内拖动排序·服务端 + 管理端**——①模板开放范围与模型侧同构：`TemplateDef.shareGroupIds` + `templateVisibleToAgent` 按受众生效分组判定（旧受众清单存量兼容），管理端编辑器**直接平铺分组按键**（源站/VIP…按亮灭启用禁止，去掉「全部受众/指定/不开放」——全亮=all、全灭=none、部分亮=select）；②模型卡加 ⠿ 拖动柄**同组内排序**（`ModelDef.order` + `reorderModels` 槽位置换 + catalog 排序键 模式 order→模型 order），影响客户端模型下拉顺序（≤30s 热更）。**须重新部署服务端**；客户端零改动无需打包。详见 §8。

> 📌 **历轮细节已压缩**：完整的逐轮记录见 git 历史中的旧版本（`CLAUDE.md.bak-before-compact`）。
>    所有「⚠ 勿回退」规则已提取到 **§9A**，改代码前先查那里。


---

## 进度快照（新会话先读这里）

> 🚧 **进行中的大改（2026-07-30 起）**：存储与数据结构改造（OSS 分层+保留策略+配额 / 服务端热路径迁 SQLite / 客户端项目文件换 SQLite）。
> **完整方案见 [docs/存储与数据结构改造方案.md](docs/存储与数据结构改造方案.md)** —— 该文档是这项改造的唯一事实来源，含实测数据、表结构、分阶段计划（P0–P5）。

**架构**：用户端(Tauri/React, 项目根) → 管理端网关(`server/`, Fastify+TS) → 上游（g-aisc 聚合网关 `https://sub.g-aisc.com`，一把 sk- + Bearer，兼容 OpenAI/Anthropic/Gemini 三协议；视频另有 简梦/喵-Mega 独立渠道）。

**当前状态（主链路全通）**
- 用户端：登录+30s 心跳（掉线容差：401/403 立即登出，瞬时失败连续 10 次≈5 分钟才登出）→ catalog 热更 → 剧本编辑（剧集拆分/资产拆分，流式）→ 五类资产出图（角色/群像/场景/生物/物品，变体图生图）→ 视频界面（智能推理/拆分出分镜+故事板提示词+视频提示词，垫素材自动匹配，故事板图、视频生成，导出）→ 画布模式（9 类节点、裂变、每分集独立画布、手动投影仅剧集/分镜——资产不投影防卡）。
- 管理端 `/admin`（ADMIN_TOKEN，自包含单页）：用户（增删改查/启停/重置 API 密钥/统计图表可导出；获客走自助注册+邀请码，P2b 起无激活码）、渠道、模型（协议/重定向/参数/计费）、提示词模板、兑换码、注册与安全、OSS 存储、请求记录（四段全留+筛选）。
- 积分闭环：按字段计费（视频按秒 `costField×costPerUnit`，路由按档覆盖）、`/generate`+`/batch` 前置校验（402）、异步失败自动退款、兑换码（`/v1/redeem`）、个人中心（`/v1/me`）。
- 项目文件安全：**原子保存 + `.bak` 滚动备份 + 损坏自动恢复/明确报错**（第79轮）。

**质量**（2026-07-07 实测）：client `tsc --noEmit` 干净 + **180 vitest 全过（24 文件）** + `vite build` 过；server `tsc --noEmit` 干净（2026-07-03）。

**⚠ 版本控制现状**：最新 commit `082e4bf`；工作区仍有 **~268 项未提交**（第49轮起的客户端功能迭代 + 第70轮死文件清理 + server 增量）。**建议尽快分阶段提交固化**。

**运行**：`cd server && npm i && cp .env.example .env`（设 ADMIN_TOKEN、GATEWAY_API_KEY）→ `npm run dev`（/admin 默认 admin-dev）；项目根 `npm run dev`（登录填 http://localhost:8787 + accessKey 首启默认 dev-key）。server 跑 `tsx watch`，改 .ts 自动重启。

**待办/已知问题**（详见 §7）：genMeta 落盘 base64 撑爆项目文件（161MB 事故根源，已立修复任务）；Postgres/批量拓扑未做（任务表已 JSON 落盘+启动对账，第89轮）；客户端需重新打包（第79轮 capabilities 变更）；**服务端需重新部署**（第89轮启动对账须上线生效，首启会清扫存量僵尸日志并按日志退款）。

---

## 0. 合作方式 ✅

- 中文沟通。
- **先把需求/协议/格式完全确定，再一次性开发**（用户端 + 管理端同时做）。
- 本文档代替"长期记忆"（用户未开启 Claude 记忆功能），作为跨轮次的共识载体。
- 用户邀请：知识面有限，遇到更好的方案请主动提出建议。
执行任务时，不要只按字面要求生成结果
请同步运用以下四象限：

1. 共同已知：先确认任务目标、已有背景、交付标准和明确边界。信息充分时直接执行，不要重复询问。

2. 我的已知、你的未知：识别可能只存在于我脑中的真实语境、审美偏好、判断标准和现实限制。若缺失信息会显著改变结果，最多提出3个关键问题；若不影响推进，则明确你的合理假设，先完成探索版本。

3. 我的未知、你的已知：主动补充我可能没考虑到的知识、方法、风险和替代路径。不要局限于我的原始方案；如果我的前提可能错误，请直接指出，并给出更优建议及取舍依据。

4. 共同未知：识别无法仅靠现有信息确定的问题，把它们转化为可验证的假设，必要时设计最小实验，说明要改变的单一变量、成功或失败信号，以及后续需要的数据。

---

## 1. 总体架构 ✅

```
用户端(Tauri 桌面)  →  管理端(Web 网关/BFF，部署在服务器)  →  第三方API(OpenAI/Claude/Gemini/简梦…)
   只认识"服务器地址"      持有所有真 key                       各家私有协议
   不碰第三方 key          负责翻译/转发/计费/对象存储
   catalog 远程下发        规范化请求/状态/结果
   声明式 JSON 插件
```

- 用户端**只与管理端通信**，绝不直连第三方 API；提示词正文只存服务端（客户端按 templateId 调用）。
- 管理端负责：翻译/转发、计费、对象存储（OSS）、全局唯一资产 id 分配、四态状态机归一。
- 模型/模板/节点均从 `catalog` 下发并本地缓存，热更新、零发版。

---

## 2. 用户端 ↔ 管理端 通信协议 v1 ✅

鉴权：所有请求 `Authorization: Bearer <API 密钥>`（accessKey，用户级身份凭证——注册自动签发、个人中心/管理端可重置；第218轮起为登录与外部 API 对接的唯一凭证，无激活码/机器码语义）。设备区分另经 `x-device-id` 头（随机 UUID，仅用于同时在线设备限制；旧客户端 x-machine-code 头兼容收下）。渠道节点（P3 relay）另用 `ank-` 节点密钥走同协议白名单端点。

### 2.1 目录
```
GET /v1/catalog?since=<version>
→ { version, models[], templates[], nodes[], schemas[] }
```
- `models`: { id, capability, params(schema), cost, costField/costPerUnit/costRules }
- `templates`: { id, name, purpose?, category?, nodeTypes, isDefault, body?(预设类), images? } ← 正文存管理端
- `schemas`: 输出契约 JSON Schema（按 id 引用）

### 2.2 提交生成
```
POST /v1/generate
{ purpose, model, templateId, variables, inputs:{texts/images/videos/audios}, params,
  output:{format,schemaId}, clientTaskId, scheduledAt? }
→ 同步(文本)：{ status:"success", result:{ text, json } }
→ 异步(文本长任务/图/视频)：{ taskId }
```

### 2.3 轮询（四态归一）
```
GET /v1/tasks/{taskId}
→ { status:"queued|running|success|failed", progress, partialText?, result:{ assets[], json? }, error }
```

### 2.4 批量
```
POST /v1/batch → { batchId, taskIds[] }　GET /v1/batch/{batchId}
```
（拓扑排期/幂等/断点续传 ⬜未做，当前逐任务受理+计费）

### 2.5 素材上传
```
POST /v1/assets?prefix=&name=  → { id, url }
```

### 其它端点
`POST /v1/login|heartbeat`（心跳回传积分）、`GET /v1/me`（积分+消耗）、`POST /v1/redeem`（兑换码）、公开 `GET /v1/assets/:id/raw`（供 `<img>` 直读，有 OSS 则 302 直链）。

### 要点 ✅
- **id 是真理，url 是缓存**：生成资产由管理端落 OSS、分配全局唯一单调 id（类型前缀 C/A/G/M/S/P/video/audio/TP + 8 位）；上游临时链接（简梦 6h）由管理端下载转存为永久资产。
- 项目隔离；id 由管理端分配，客户端当不透明值。
- 未完成素材可声明 `{fromTask, part}`（⬜服务端排期未做）。

---

## 3. 数据模型与资产体系 ✅

- **资产分层**：基础资产 + 变体资产（继承、锁 DNA、只变指定项）；编号 C 核心/A 配角/G 群像/M 怪物异兽/S 场景/P 道具（`C01`、`C01A` 变体）。**编号 code 是项目内人读编号；assetId（C00000123）是全局二进制资产 id，引用一律存 assetId。**
- **用户可见五类界面**：角色 / 群像（G，存 `crowds`）/ 场景 / 生物 / 物品；剧本分析按编号前缀自动分流。
- **项目视觉圣经**：项目级风格锚点，所有资产继承；**资产状态账本**：变体新增/作废/临时过滤。
- **资产是项目级实体，分集/分镜只持有 id 引用**（删分镜不删资产本体）。
- 角色带 `voiceHint`（音色提示）+ `voiceUri/voiceAssetId/voiceName`（已实现本地上传音频→OSS 绑定；匹配资产时自动把音频加入素材区并在图例写「@ImageN的声音参考@AudioM」）。
- **变体生成 = 图生图**：LLM 直接产整段变体模板（继承来源+DNA不可变+唯一变化项）；管理端 catalog 配四类各一套变体前缀。
- 自定义类型兜底桶：无映射的资产进表格「自定义类型」分类。

---

## 4. 提示词模板与输出契约 ✅

**模板数据化（权威来源=管理端）**：正文存 `server/data/templates.json`（`TemplateDef`：body/purpose/category/nodeTypes/variables/schemaId/chainNextId 链式/images/isDefault/order），经 catalog 下发；客户端 `catalogStore` 按 `templatesByNode/templatesByPurpose/templatesByCategory/artStyles` 取用。**客户端绝不含提示词正文**。

**模板库（运行时以管理端数据为准，管理端可增删改）**：2026-07-03 实测 version 43 共 9 条——功能 4：`asset.extract.basic`(script.analyze 资产提取·白羊)、`smart.infer.multi`/`smart.infer.single`(storyboard.toVideoPrompt 智能推理 多/单卡)、`script.episodes.basic`(script.analyze 自动分集·内部)；画风预设 3：`style.3d-guoman/2d-hand/realistic`；用户自建 2：`629mumu`(script.analyze)、`zhinengchaifen701`(storyboard.split 智能拆分)。⚠ `asset.prompt.optimize`（出图提示词优化）已不存在（模板数据与客户端引用均无，第85轮审计确认）。⚠ 模板 `nodeTypes` 白名单数据仍是旧节点类型（如 "text"），客户端已弃用该字段、一律按 purpose 过滤（第84轮补充）。启动迁移机制：`补种缺失` + `RESET_VERSION` 裁剪 + `SKILL_REFRESH_VERSION` 定向从 md 强刷指定模板正文 + 删除内置项走墓碑（不复活）。

**`server/skills/`**（提示词 md 原稿，仅 seed/SKILL_REFRESH 时经 `readSkill` 读入；不进客户端打包）。⚠ **2026-07-19 用户定（勿回退）：skills md 已全部过时——提示词正文一律在管理端「提示词模板」页维护（服务器 `data/templates.json` 为唯一权威）；永远不要再 bump `SKILL_REFRESH_VERSION`/`SEED_VERSION`（会用过时 md 覆盖用户线上调好的正文；[templates.ts](server/src/store/templates.ts) 常量处有同款警告）；新增内置模板走补种（只加缺失、不动存量）**。md 仅作历史留档（全新环境首启种子会产出过时正文，需在管理端重调）。

**输出契约 v1**（server catalog schemas）：
- ① `asset.extract.v1`（script.analyze）——每个资产带 **`imagePrompt` 整段出图模板**（LLM 直接产，非槽位合成）；含 visualBible/characters/scenes/creatures/props/episodes/ledger；运行时也接受扁平 `assets[]` 形态（解析器两者都吃，按编号前缀分流）。
- ② `storyboard.v1`（storyboard.split）——`{ episodeIndex, shots:[{index, scriptContent, durationSec}] }`。
- ③ `videoPrompt.v1`（storyboard.toVideoPrompt）——`visualDescription` 保留 `{角色:名}{场景:名}{音频:名}` 公式供客户端正则抽取自动填垫素材。
- 智能推理实际输出 4 字段卡 JSON `[{card_number, original_script, storyboard_prompts, video_prompts}]`，客户端 `parseInferCards` 容错解析（不依赖整体 JSON 合法，支持流式未闭合尾卡）。

**变量约定**：`{{原文}}{{视觉风格}}{{角色列表}}{{场景列表}}{{物品列表}}{{生物列表}}{{群像列表}}`（客户端 `buildAssetListVars()` 统一下发；模板未引用则忽略）。

---

## 5. 业务工作流（用户端）✅

1. 新建项目：项目名/封面/画风（catalog 画风预设）；选「表格创作」或「画布创作」打开（仅默认视图不同，同一份数据）。
2. 剧本推理：放入剧本 → 剧集拆分（LLM 或快拆：空行/n-1/n-n/场n-n）+ 资产拆分（两按钮独立运行）→ 角色/群像/场景/生物/物品 + 出图提示词推送各界面。
3. 资产界面（AssetWorkbench 五页共用）：生成基础形象/造型变体、提示词优化、上传本地图、绑定音色、主图历史。
4. 视频界面：分集列表 → 智能推理（整集出 分镜+故事板提示词+视频提示词）/智能拆分（只拆原文）/单镜推理；提取资产自动匹配垫素材（优先资产助手选中造型）；故事板图 → 视频生成 → 导出。
5. 画布模式：8 类节点（见 §11，第87轮删「生成故事板」），裂变自动建下游节点，每分集一块独立画布。

**树结构 ✅**：`小说/剧本 → 分集 → 分镜 → 垫素材(资产 id 引用) → 视频`。

**表格 ↔ 画布 = 单数据源双视图 ✅**：底层一份项目数据；表格按键 ⇄ 画布节点一一映射（`purposeRegistry` 固化 `按键→purpose→capability→节点`）；节点自带素材格子，连线是补充。资产模式→画布为**手动单向投影**（视频界面「同步本集到画布」按钮，幂等、按 sourceRef 去重、不触碰手建节点）；**资产不投影**（第81轮：几十个资产节点是卡顿主因，只投 全文→剧集→本集分镜流水线；历史残留资产投影节点随下次同步清理；需要单个资产图时从资产助手拖入）。

---

## 7. 待办清单

1. **genMeta/assetRefImages 净化**：垫图 data: base64 整段落盘 → project.Qiji 可膨胀到百 MB（「三姐妹」161MB 损坏事故根源：保存窗口被拉长，中断即截断；加载也会内存暴涨）。方案：落盘前把 data:/blob: uri 替换为公网 url/缩略图 + 存量项目加载时净化。**已立后台任务**。
2. 服务端任务已 JSON 落盘+启动对账（第89轮，重启不丢计费/视频可续轮询）；**资产 id SEQUENCE 已由 SQLite `asset_seqs` 提供（第105轮，`node:sqlite` 内置）**；logs/tasks/users 等可循同范式后续逐表迁 SQLite（非急）；多实例共享状态才需 Postgres（现单机不需要）；批量拓扑排期（fromTask/part）/幂等/断点续传仍待。
3. 简梦音频上游（待文档）；`audio.tts`、`script.toScenes`、场景/生物/物品 variant 按键未接线（`unwiredPurposes()` 可盘点）。
4. 客户端重新打包发布（第79轮 `fs:allow-rename` capabilities 变更须随安装包生效）。
5. 小项：`jszip` 依赖未用可移除；`font.css` @font-face 未接入待修；`registry.unregister()` 未实现（下架模型不主动注销 adapter）；模板链式仅两段。
6. 规范变量集命名的进一步统一 ⬜。
7. 火山引擎 超分/去字幕已全链路接入（第90轮二期：`volc-mediakit` 协议+5 内置模型+两模式真提交）；**待生产配置**：管理端「火山引擎 MediaKit」渠道填 API Key（或环境 `VOLC_API_KEY`）+ 重新部署服务端；计费为按次固定价（40/10/15/25/20 积分，管理端可调）。⚠ 上游产出 video_url 仅 24h 有效，已由 rehostVideo 转存 OSS——未配 OSS 时回退上游直链会过期。

---

## 9. 关键设计决定（锁定）

- **整段模板 ✅**（推翻早期"槽位拆分"）：提取 LLM 直接产完整 `imagePrompt`；catalog 出图模板休眠。
- **模型解析零兜底 ✅**：`resolveActiveModelKey`=节点自带→设置面板→空；空则报错，绝不退 mock；无假数据兜底（报错优于编造）。
- **请求参数绝不静默改写 ✅（第188轮用户定稿，全局规则勿回退）**：翻译器对用户请求的 时长/比例/分辨率 等参数**原样透传**——禁止任何 夹钳/就近取档/兜底改值 类「自动判断」（第186/187轮 overseas 时长 Math.min 夹钳把请求 30s 静默砍成 15s、按秒计费却按 30 扣=多扣钱，即反例）。可选档位由管理端模型参数（catalog params）把关，非法值由上游明确报错（失败自动退款）。~~已有渠道的历史夹钳属存量行为~~（**第215轮起豁免废止**：13 个视频翻译器的存量夹钳已全部根除，统一走 [paramPass.ts](server/src/translators/paramPass.ts) 透传），**全部翻译器一律遵守本条**。
- **唯一请求路径 ✅**：表格按键与画布节点共用 `runPurpose → taskCenter 单例轮询`（见 §11.1）。
- **提示词正文只在服务端 ✅**：客户端只发 templateId+variables。
- **画布 NodeSpec 单一事实来源 ✅**：一条 spec 完整描述一类节点（端口/能力/purpose/裂变/参数），加节点只加一条 spec；旧节点由 `sanitizeCanvas` 载入时清除。
- **画布渲染性能规则 ✅（第82轮，勿回退）**：① Canvas 本体与 BaseNode **绝不订阅 viewport**（onMove 每帧写 store，订阅=逐帧全量重渲染）——需要跟随视口的 UI 拆成独立小组件自己订阅（SelectionToolbar）、BaseNode 的 zoom 仅悬停时读取；② `rfNodes` wrapper 按「节点对象引用+zIndex」缓存复用（拖动时只有被拖节点重渲染）；③ ResultView 有 **LOD**（zoom<0.35 时文本/对话/视频/音频换轻量占位块，图片保留作地标；布尔选择器只在跨阈值时触发）+ 节点内文本只预览前 3000 字；④ 画布内随视口移动的元素（节点卡/角标/分组容器）**禁用 backdrop-filter**（平移时每元素每帧对背景重采样+模糊=WebView2 闪烁主因；节点背景本就近不透明，改纯色）——毛玻璃只允许出现在单例悬浮 UI（操作面板/悬停工具栏且仅悬停时挂）；⑤ 连线**不做常驻 dash 流动动画**（SVG stroke-dashoffset 不能 GPU 合成→全部边永远逐帧重绘），流动只给 激活/选中/悬停/续传 边；⑥ 小地图比例尺只按节点 bbox（不并视口，否则平移时整图逐帧重标定="游动"），节点矩形子树 useMemo。不做视口裁剪/节点卸载（第72轮教训：连线消失+逐帧裁剪本身更卡）。
- **多画布 ✅**：每分集一块独立画布（各自节点/连线/视口/撤销栈），无主画布、项目至少一集，`switchCanvas` 切换；投影只进当前激活画布。
- **项目文件原子保存 ✅**：写 tmp → 轮换 .bak → rename 生效；加载失败依次试 .tmp/.bak 恢复，彻底失败弹窗明示（不静默）。
- **自动保存绝不新建项目 ✅**：无 savePath 的自动保存直接跳过（防幽灵「未命名项目」）。
- **S3 对象键仅 ASCII ✅**：中文键在 S3 兼容服务上编码不一致致 AccessDenied（实测教训），键=纯资产 id。
- **`http://*.localhost` 直链=本地文件显示态 ✅（第90轮，勿回退）**：convertFileSrc 在 Win 下产 `http://asset.localhost/...`，形式是 http 实为本地——显示层一律原样直用，任何「按 url 换源/自愈下载」逻辑必须用 `isWebviewLocalUri()` 排除它（换源命中失效 blob 映射=「短暂显示后裂开」）；`localhost:8787` 服务端 /raw 不在此列。
- **服务端热路径写盘规则 ✅（第105轮，勿回退）**：Fastify 单线程——**任何在请求路径上被高频触发的落盘，绝不同步全量重写大文件**（`writeFileSync`+`JSON.stringify` 大对象会冻住 event loop→心跳/登录/admin 全超时，且文件越大越卡）。① 高频、可容忍极小窗口丢失的写（日志索引/任务）走 [db.ts](server/src/store/db.ts) `scheduleSave`（防抖合并 + `fs/promises` 异步写 + 原子 rename），进程退出前 `flushPendingSaves`；② 低频、需即时持久的写（配置/用户）用同步 `saveJson`（已改原子 rename，小文件）；③ **大对象拆分**：像请求日志这种「元信息小、报文大」的数据，索引只留元信息常驻内存、重报文按条外置文件按需读（logs.ts 的 LogMeta/LogDetail 即范式），别把重字段堆进被反复重写的索引里。
- 技术栈：Node+TS+Fastify（同仓 `server/`，复用 contract 类型）；存储 OSS(S3 兼容, 雨云 rains3) + 未来 Postgres。

---

## 10. 开发阶段（状态）

- 阶段0 共享契约 ✅（`src/contract.ts`，server 有副本）。
- 阶段1 用户端接入层 ✅（managedAdapter/catalogStore/声明式插件/集中轮询/设置=服务器地址+accessKey）。
- 阶段2 管理端 ✅ 主体（五端点+真翻译器+admin 控制台+持久化+OSS+计费+兑换码+统计）；余下深化见 §7.2。
- 阶段3-5（文本链路/图像/视频分镜）✅ 主体已在客户端落地（§5 工作流全通）；深化项见 §7。

---

## 11. 代码地图（2026-07-03 第80轮实测校准）

### 11.1 用户端请求主链路（唯一路径）
```
表格按键 / 画布节点
  → runPurpose(purpose, input)              src/services/purposeRunner.ts
      ├─ PURPOSE_REGISTRY 取 capability/nodeType   src/lib/purposeRegistry.ts
      ├─ resolveAssetModelKey(cap) | 显式 modelKey  src/services/adapters/channelAdapter.ts
      ├─ getAdapter(modelKey)                       src/services/adapters/registry.ts
      ├─ ManagedAdapter.submit() → POST /v1/generate
      └─ taskCenter.trackTask() 单例集中轮询         src/services/taskCenter.ts → taskTracker.ts
          └─ ManagedAdapter.poll() → GET /v1/tasks/{id}
```
- 画布入口：`pluginRegistry.defaultNodeExecute()`（spec 驱动；文本类写 resultText+流式；媒体类落资产库；裂变 canvasSpawn）。
- 轮询：首轮 2s、常规 5s、**文本流式(有 partialText)期间 1.5s 自适应**；超时 text 20min/video 15min/其余 4min。
- 断连保护：出图/推理在途任务落盘（`pendingGens`/`inferTasks`/`analysisTask` 随项目持久化），项目加载后 `resumePendingGenerations()`/`resumeInferTasks()` 重挂轮询（不重提交不再扣费）；**画布节点**（第150轮）：提交确认把 `{taskId, adapterKey}` 写进 `node.data.task` 随项目落盘、终态清除，项目加载/switchCanvas 后 `resumeCanvasNodeTasks()`（pluginRegistry）重挂轮询、收尾裂变走 buildRespawn 只补缺漏；服务端任务已落盘（第89轮，终态留 48h），重启不丢。

### 11.2 用户端目录（实测）
- `src/contract.ts` — 两端共享协议类型。**Purpose 共 18 个**：script.toScenes、script.analyze、storyboard.split、storyboard.toImagePrompt、storyboard.toVideoPrompt、asset.{character|scene|creature|prop}.{image|variant}、video.generate、**video.upscale、video.desub**（火山超分/去字幕）、audio.tts、**chat.reply**。Capability 共 6 种（+video-enhance/video-erase）。
- `src/lib/`：purposeRegistry（全 purpose 映射+`unwiredPurposes()`）· assetExtraction（资产拆分权威解析）· storyboardParse · smartInferPrompts（`parseInferCards(Stream)` 容错卡解析）· jsonRecover（未闭合 JSON 抢救）· episodeSplit（4 种快拆+场n-n）· assetMatch（画布资产匹配）· assetVars（资产列表变量）· shotMaterials（@tag/图例）· shotMaterialOps · materialValidation · wordDiff（推理后用户改动高亮）· mindmapLayout（画布整理）· canvasSpawn（裂变）· genParams（图/视频参数与计费预估单一来源）· clipboardMedia · saveMedia · publicUrl · projectAssets · fuzzyMatch · mentionResolver · id/clipboard/layout/utils。
- `src/services/`：managedClient（HTTP 全端点）· purposeRunner · taskCenter · taskTracker · generationQueue（在途出图落盘+恢复）· inferRun（智能推理/拆分持久化运行器）· assetPersist（远程资产落本地+三元映射）· canvasProjection（资产模式→画布投影）· projectFile（类型+文件 IO）· libtvCli（LibTV CLI 调用封装，第94轮）· dreaminaCli（即梦 CLI 调用封装，第107轮）· creditLedger · fileStorage · imageEditService · taskBlackbox · webdavSync · modelAdapter；`adapters/`：managedAdapter · libtvAdapter（LibTV·Seedance 2.0 本地适配器，不走管理端）· dreaminaAdapter（即梦·Seedance 2.0 本地适配器，不走管理端，第107轮）· channelAdapter · registry · types · mockAdapter · index · utils。
- `src/store/`：projectStore（项目数据+原子保存+多画布 canvases+pendingGens/inferTasks+libtvCanvasUuid）· canvasStore · commitStore · catalogStore · settingsStore（含 videoTableLayout/videoTableTemplates；channels/apiKeys 休眠）· connectionStore（含 useModeFeatures/useLibtvFeature/useDreaminaFeature）· libtvStore（LibTV 授权会话态）· dreaminaStore（即梦授权会话态）· libraryStore · uiStore · dockStore（2 按钮停靠：资产助手/简一助手）· jianyiAssistantStore · lightboxStore · assetFormStore（资产助手选中造型，会话态）· promptModalStore · uploadStore · history · debouncedSave。
- `src/components/`：AssetWorkbench（五资产页共用工作台）· AssetAssistant（悬浮资产助手：拖拽建节点/造型右键）· JianyiAssistant+JianyiWindow（AI 对话助手：多会话/分身/打字机流式）· Lightbox（全局放大灯箱，右击保存）· PersonalCenter · EditorSidebar/EditorHeader · ModelPicker/TemplatePicker · PromptMentionEditor（@胶囊富文本）· HighlightEditable（原文高亮）· PromptModal · PromptExpandButton · ShotMaterialStrip · AssetImportDropdown。
- `src/nodes/`：nodeSpecs（**8 类节点**：text.seed / asset.split / episode.split(纯脚本4模式) / smart.infer(多·单卡·智能拆分) / image.gen / video.gen / ai.chat(单轮+串联记忆) / upload(自适应)；storyboard.gen 第87轮已删）· pluginRegistry · BaseNode（单输入/输出连接点）· ResultView · NodeMaterialBay（可编辑素材区）· NodeRefMaterials · registry/nodeMetadata（旧映射，spec 派生）。
- `src/canvas/`：Canvas · AssetPanel（分集下拉切画布）· hooks(drag/drop/connect/keyboard/viewport) · nodeFactory（防重合/避让+聚焦）· nodeUpload/nodeMaterials · videoCapture（ffmpeg 帧/片段提取）· TitleBar/StatusBar/SettingsModal/LoginPage/ContextMenu/FloatingToolbar 等。
- `src/popout/`：三助手弹出为独立 Tauri 窗口（只读视图，不写盘）。
- `src-tauri/`：`lib.rs` 含 `run_ffmpeg/extract_video_frame/extract_video_clip/download_url` 命令（ffmpeg 二进制在 `resources/ffmpeg`，`scripts/fetch-ffmpeg.ps1` 获取）；capabilities 含 `fs:allow-rename`（第79轮）。

### 11.3 视图 ⇄ 路由
| 路由 | 视图 | 功能 |
|---|---|---|
| `/` | Frame21 | 大厅/项目列表（进入项目→loadFromPath+快照路由恢复） |
| `/frame164` | Frame164 | 新建项目（名/封面/画风） |
| `/frame1693` | Frame1693 | 剧本编辑器（剧集拆分+资产拆分） |
| `/frame16285` | Frame16285 | 角色资产 |
| `/frame-group` | FrameGroup | 群像资产 |
| `/frame16550` | Frame16550 | 场景资产 |
| `/frame16780` | Frame16780 | 生物资产 |
| `/frame161000` | Frame161000 | 物品资产 |
| `/frame161195` | Frame161195 | 视频/分镜/故事板表格工作台 |
| `/frame-canvas` | FrameCanvas | 画布节点编辑器 |

### 11.4 管理端（`server/`，Fastify+TS）
- **用户 API**（Bearer accessKey）：`POST /v1/login|heartbeat|generate|batch|assets|redeem`、`GET /v1/catalog|tasks/:id|batch/:id|assets/:id|me`、公开 `GET /v1/assets/:id/raw`、`GET /health`。
- **管理 API**（Bearer ADMIN_TOKEN）：`/admin`(单页) + `/admin-api/{users,models,templates,channels,redeem-codes,logs(+/facets),stats,settings/oss|register(+/test),agents(+/:id/node-key),relay/status(仅 relay)}`（P2b 起无批量激活码——获客=自助注册+邀请码）。
- **协议派发** `translators/index.ts` 按 `model.protocol`（**24 种**）：echo / openai-chat / anthropic-messages（文本，SSE 流式+partialText+链式）· openai-image / gemini-image（图，真异步落资产）· jianmeng-video / openai-video / **volc-mediakit**（火山超分/去字幕）/ **sudashui-video**（苏打水，第131轮接入：方法 omni/frames + 官方真人图；第156轮收编 26→7 并三模式合一为「简梦S」）/ **aistars-video**（星辰 AIStartLab 统一生成，第132轮：channel|model|quality 编码 + 线路能力表守卫；第162轮按 2026-07-26 config 重写 LINES：+53 漫剧优选（全系 frames+6 比例）、50 fast 放宽 933、49 线能力翻转、已下架老线守卫移除）/ **aistars-image**（星辰图片，第162轮：POST /generation/create/image 与视频同构（channel/model/prompt/aspectRatio/quality/n+inputImages）、**poll 直接复用 pollAistarsVideo**；质量档钉在编码/routes 价随档变；比例=params.size 就近映射线路 aspects；真机 46 线 1K 小额单 E2E SUCCESS）/ **huaying-video**（画影 AI-Studio，第133轮：模型能力表守卫 + requestId 幂等；⚠ 上游首尾帧字段拒收，全能参考单方法）/ **dimensio-video**（Dimensio jimeng.dimensio.cn，第134轮：functionMode 必填映射 omni_reference/first_last_frames（无素材=纯文生走 frames 形态）+ 模型能力表守卫 + @Image1→@image_file_1 引用转写；统一 `VideoDriver` 轮询+完成下载转存永久资产+断点续轮询）/ **aivide-video**（Aivide 2.0 aivideo.beauty，第139轮：12s 轮询 + 视+音合计≤3 守卫）/ **jianmengp-video**（简梦P Sora/Veo 系，第147轮：模型能力矩阵守卫 + Sora V3 提示词≤2500 前置拦截；⚠ 渠道 Base URL 文档未给、部署后管理端配置）/ **musem-video**（简梦M MuseAI，第151轮：apikey 头鉴权非 Bearer + 真 @tag 引用素材）/ **jmz-video**（简梦Z zexitongxue.com，第152轮：防御式状态族（文档只公布 success 终态）+ 豆包三线首尾帧 start_frame/end_frame）/ **jmz-image**（简梦Z 图片，第153轮：异步 submit+poll 复用视频轮询管线按 image 能力落资产；⚠ 结果链接须带 Bearer 下载 → `VideoPoll.resultHeaders` 带鉴权下载、仅本站域附头）/ **jmt-video**（简梦T llm.chre3.com，第160轮：sd2-c8 单模型 OpenAI 兼容 submit+poll；**原生 @Image1/@Video1/@Audio1 占位符与我方图例注入直通零转写**；合规素材（人脸参数）compliance_mode 白名单命中才发；音频参考须搭配图/视频守卫；5s 轮询）/ **jmf-video**（简梦F new.vosle.xyz，第161轮：**模型 ID 现拼** `seedance-2.0+{比例}+{720}+{5|10|15}`；methods omni+frames（start_frame/end_frame 真支持）；提示词≤8000 前置拦截；**成片下载须带 Bearer + 24h 时效** → poll 附 resultHeaders（仅本站域）由轮询循环带头转存 OSS；5s 轮询）/ **overseas-video**（出海营 api.aiid.edu.kg，第186轮：Seedance 任务格式 `POST /api/v3/contents/generations/tasks`；素材经 **content 数组混排**（image_url/video_url/audio_url 对象 + role + **name=ImageN/VideoN/AudioN 与 @tag 图例编号一一对应**）、生成配置顶层 duration/ratio/resolution；有素材= mode reference_material、首尾帧=兼容 mode i2v_first_last（image_url/end_image_url）；**gemini-omni 同接口** t2v/r2v/edit 按素材自动定 + 时长 4/6/8/10 就档；查询结果 顶层 content.video_url 与 items[] 双形态防御；结果仅本站域附 Bearer）/ **jmh-image**（简梦H ZhengAPI，第154轮：**同步单请求**复用 createImageTask 零轮询——grok 系 images/generations（image=单值纯 base64）、firefly 系 chat/completions stream:false + 模型 ID 现拼 -{res}-{ratio} 后缀）/ **jmh-video**（简梦H 视频，第155轮：chat/completions **SSE 流式单请求**（整流读完提链）复用 createImageTask 按 video 能力落资产；模型 ID 按家族现拼 时长/比例/分辨率 后缀、grok 朝向由比例定；仅图参考 data URI 内联）· stub。上游解析 `upstream.ts`：路由(参数重定向) > 模型覆盖 > 渠道 > 网关默认。
- **store**：db.ts（JSON/JSONL 文件存储）· users（含批量创建/统计/退款/授予）· models（协议/路由/`resolveModelCost` 字段计费/墓碑）· templates（迁移三机制）· channels · settings（OSS 可管+自检）· redeemCodes · assets（**SQLite 台账** `data/qiji.db` 的 assets/asset_seqs 表，类型前缀 id 分前缀单调+OSS 上传；对外 API 不变；首启从旧 `assets.json` 一次性迁移。连接层 [sqlite.ts](server/src/store/sqlite.ts)，`node:sqlite` 内置无原生模块）· tasks（内存索引+**tasks.json 防抖异步落盘**：四态+partialText+billing 退款钩子+视频续轮询信息 resume；终态留 48h/上限 1000 条）· logs（**轻量索引按天分文件 `data/logs-index/<YYYY-MM-DD>.jsonl`（LogMeta，全部常驻内存+防抖落盘，含预抽 resultLink，第129轮起——落盘只重写当天/被裁剪当天文件，不再全量重写）+ 重报文按条外置 `data/logs/<id>.json`（LogDetail：①②③④）**，**纯按时间保留无数量上限、两档**（≤30天完整/>30天整天删文件+详情）、facets/stats/summary/导出/列表全走内存索引、`getLog`/`getRunningLogs` 才读详情、启动一次性迁移旧单文件 `logs.jsonl`→按天文件（旧文件改名 .migrated）+批量收尾 finishLogsBulk）。启动对账 `reconcile.ts`（index.ts 启动后异步跑）：待办视频续轮询/其余判失败退款 + 孤儿 running 日志清扫（④段有 completed+video_url 先补转存救回）。**db.ts 防抖异步写盘 `scheduleSave`/`flushPendingSaves`（§9「热路径写盘」）根治 event loop 阻塞。**
- **持久化** `server/data/`：users/models/**modes（第130轮视频模式注册表）**/**families（第163轮模型家族注册表·纯展示分组）**/templates/channels/settings/redeem-codes 均 .json 带 version（变更自增→catalog `v{m}.t{t}` 热更）+ 请求记录按天 `data/logs-index/<日期>.jsonl`（第129轮）+ `data/logs/`（分条详情）+ tasks.json + **qiji.db（SQLite：资产台账 assets/asset_seqs；旧 assets.json 迁移后留底 .migrated）**。
- **env**：PORT/ADMIN_TOKEN/SEED_ACCESS_KEY/GATEWAY_BASE_URL/GATEWAY_API_KEY + 可选分协议覆盖 + JIANMENG_* + OSS（seed，运行时以 /admin 配置为准）。

### 11.5 遗留 / 休眠（实测）
- 未提交 ~268 项（见快照）。
- `settingsStore.channels/apiKeys`、`projectFile` 旧 `table*/canvas*` 模型键：休眠，仅向后兼容读。
- 残余 mock：Frame1693 本地画风兜底文案（OperationPanel/VideoOperationPanel 的 no-op fallback adapter 已于第83轮删除）。
- `jszip` 未用；`font.css` @font-face 未接入；`registry.unregister()` 未实现。

---

## 12. 用户数据结构 v3（多项目 + OSS）✅

### 12.1 布局（本地镜像 + OSS 权威备份同构）
```
<userDataDir>/user.json / projects.json
<userDataDir>/projects/<projectId>/
  project.Qiji(+.bak 滚动备份/+.tmp 原子写中间态)   # meta+content+资产实体(存 id 引用)
  assets/{character,scene,creature,prop,crowd,video,audio}/<assetId>.<ext>
OSS: qiji/<userId>/<projectId>/...
```
（实际落地：项目在 `AppData\Roaming\com.qiji.canvas\Qiji\projects\<名_时间戳>\`；WebDAV 云同步可选。）

### 12.2 资产 id ✅
- 管理端分配；类型前缀 + 8 位，分前缀单调、永不复用：`C`/`A`→character、`G`→crowd、`M`→creature、`S`→scene、`P`→prop、`video`、`audio`、`TP`(临时)。文件名=id；**OSS 对象键仅 ASCII**。
- `code`(C01，项目内人读) ≠ `assetId`(C00000123，全局)；引用一律存 assetId。

### 12.3 流程
- 生成：管理端跑上游→分配 id→上传 OSS→回 `{id,url}`→客户端 `saveRemoteAsset` 下载本地副本+注册 `assetId↔url↔localPath` 三元映射（显示走本地 `asset://`，请求走公网 url——Tauri CSP 不允许 http(s) 图直显）。
- 上传：客户端 `uploadAsset`（服务端落 OSS+分配 id）→ 本地副本。未配 OSS 时 url 为服务端 `/raw`（上游不可达，属已知限制）。
- 解析 `resolve(assetId)`：本地文件 → 远程 url → 凭 id 重解析。

---

## 9A. 历轮锁定规则速查（⚠ 勿回退清单）

> 从历轮变更记录中机械抽取的全部 ⚠ 标记规则，按轮次倒序。**这是防止改回旧方案的核心清单**，改动相关代码前先在此搜关键词。
> 原始上下文见 §8 对应轮次（已压缩）或 git 历史中的完整版 CLAUDE.md。
> ⚠ **第211轮商业化改造 P1 起，本清单中涉及「链式计费/双扣费/开码积分/签发价/结算价/售价/下游渠道商」的历史规则整体废止**（体系已退役，勿按旧规则恢复任何定价链代码）。

- **第240轮**：**中栏双页签在标题栏 headerExtra，勿搬回内容区整行 nav**（标题栏内部控件一律 draggable=false+mousedown 不冒泡——第236轮拖拽换位规则）；工作台正文分派收口在 RtcCenterStage 的 WorkbenchBody（占位符无 shotRef→RtcFreeGenWorkbench / 其余→RtcShotAiWorkbench），页签自动切换规则 1=**任何占位符**新选中→工作台。
- **第240轮**：**自由结果占位分工定稿：中栏工作台=提示词/垫素材编辑，右栏 RtcFreeGenProps=ai 设置（模型/时间码/开始生成/进度/重试）**——勿把提示词编辑加回右栏；共享件在 freeGenParts.tsx 两处 import 勿复制。
- **第240轮**：**工作台提示词区第二行=本分镜覆盖，写入字段必须与 genShotVideo 读取链一把尺**（overrides.videoModelKey/method/aspect/resolution/officialAssetIndexes + shot.durationSec；档位收敛恒走 videoReqOptions/clampToOptions/clampDurationTo/clampMethod）；**补镜头重排编号唯一来源=lib/shotReindex.reindexShots**（Frame161195/inferRun/RTC 工作台三处共用，勿再写本地实现分叉）。
- **第239轮**：**变速唯一入口=rtcOps.setSegmentSpeed，勿回退成 `commitSegmentPatch({speed})` 纯 patch**——变速必须联动 targetDurationUs 并恒回写 `sourceDurationUs=round(targetDur×speed)` 维持不变量（trim/split/导出都按它换算）；SpeedInput（RtcMediaProps）与属性剪贴板粘贴（rtcAttrClipboard）均已收口，新增变速入口一律走 rtcSegUtils.commitSegmentSpeed；rtcPlayback.sourceTimeSec 的源右缘钳制兼作存量失配 doc 的播放防御，勿删。
- **第239轮**：**中栏双页签「AI 工作台/预览」——工作台=absolute 不透明叠层，RtcSequencePlayer 仍常驻绝不条件卸载**（第236轮红线延续）；AssetPreviewLayer 仅「预览」页渲染；页签自动切换规则全部收口 rtcCenterTabCore 纯函数（单测锁定），新增自动信号在纯函数里加、勿散落组件。
- **第239轮**：**占位符分工定稿（用户定稿勿回退）：中栏工作台=提示词/垫图/原文/动作/历史，右栏=AI 生成属性（模型+参数档）**——生成参数 setter 唯一走 setMediaSettings（项目级、与表格模式同步），档位来源 IMG_SIZE 表/imageResolutionOptions/videoReqOptions 一把尺勿自造；ShotPromptField/JobChips/HistoryGrid 等共享件在 shotWorkbenchParts.tsx，两处 UI 都从那里 import 勿复制第二份。
- **第238轮**：**预览选中框/画面几何=百分比零测量（containFrac），勿回退成 frameRect 像素版**——图层排版是纯 CSS，实测像素在 WebView2 下滞后/取整必错位；实测矩形只允许喂拖动解算且**手势开始现读 getBoundingClientRect**。
- **第238轮**：**复合片段导出格式唯一依据=[docs/剪映复合片段草稿结构.md](docs/剪映复合片段草稿结构.md)**（真机逆向+四轮探针实锤；pyJianYingDraft 不支持复合勿凭它猜）——渲染真源=drafts 素材的内联 draft；复合段 **8 组伴生素材引用缺一=认得出但不渲染**；剪映打开过的草稿会被重存为加密，对照实验必须换新草稿文件夹。
- **第238轮**：**rtc 快捷键=两层表**（RTC_ACTION_DEFS 默认表+localStorage 覆盖层，键位与说明从生效表单一派生）——新增动作必须三处同改：DEFS 条目+RtcTimeline keydown 派发+NEEDS_SELECTION 门控；勿再出现静态速查表。
- **第238轮**：**rtc 编辑动作一律 activeRtcDoc(st)/st.commitActive**（子层编辑感知；主层与 doc/commit 同义零回归）——新写动作勿直用 st.doc/st.commit；**音量（含关键帧与复合宿主乘积）在 rtcPlayback 产出处算好**，播放器只消费 layer/clip.volume，勿在消费端再采样（子层时间基会错）。
- **第238轮补充6**：**转场/特效「所见即所得」红线（用户定稿）：预览做不出与剪映一致观感的款式不上 UI**——选择器/转场页只列 `JY_PREVIEW_TRANSITIONS`（带 previewKind 的 7 款），勿把花式款加回选择器；播放器转场渲染=每轨 `#tr` 幽灵槽（冻结帧/色闪），幽灵层 frozen 永不 play、不占解码位、不参与点选，勿改；将来上特效同一原则（无预览不放出）。
- **第238轮补充10**：**原文=派生只读，绝不是轨道数据（用户定稿勿回退）**——原文实时提取自**主轨**片段 shotRef 对应分镜的 scriptSegment（[rtcScriptLane.ts](src/lib/rtcScriptLane.ts) 唯一派生点：时间轴车道+预览窗参考条共用）：主轨没素材=没原文、分镜原文改了立即变、挪动/分割/伸缩天然一一对应、不参与剪辑不落盘不导出。**勿再写入任何 role:"script" 轨/片段**（补充8-9 的落轨+配对联动形态已废止；存量 doc 的该轨由 rtcOps.pruneScriptTracks 在 rtcStore loadDoc/收编时整轨清除，散落的 role 判定只是未清洗 doc 的防御）；时间轴几何基线=`RULER_H+laneH`（rowTops[0]，车道整行 pointer-events-none 穿透到空白语义）。
- **第238轮补充5**：**RTC 分集化单一真相=projectStore.rtcDocs（按分集分档），旧 rtcDoc 字段只在加载迁移时读**——回写唯一路径 `setRtcEpisodeDoc(ownerEpisodeKey, doc)`（rtcStore 恒写 doc 归属集的档位，勿改成写「当前激活集」——切集竞态会串集）；切分集=`switchRtcEpisode`（rtcStore 模块订阅换档并全量重置，勿手动 loadDoc）；**占位符生成的落笔必须能写非激活分集档位**（rtcGenSink patchInactiveSlots，勿删——删了「生成中切集」结果就丢）。
- **第236轮**：**rtcStore 项目身份守卫勿删**——commit/undo/redo 入口比对 ownerProjectId（=projectStore.projectInstanceId），不一致=丢弃回写并收编当前项目（stale commit 单测锁定）；同项目外部 rtcDoc 变更收编**不回写**（selfWriting 防回声）；换项目全量重置时无 rtcDoc 要建空文档勿传 null（时间轴引导 effect 只认 null→非 null 变化）。placeholderSwap 的双重身份校验（arm 时+异步落笔前）同理勿删。
- **第236轮**：**RTC 面板换位=CSS order（恒定 JSX 顺序），勿改成重排 children**——重排会卸载重建面板子树，播放中的 video 断流（RtcPanelFrame 注释）；中栏素材预览=叠层显隐，**勿条件卸载 RtcSequencePlayer**；splitter 拖动本地预览、pointerup 才写 store/localStorage，勿逐帧落盘。
- **第236轮**：**RTC 全流程工作台复用既有链路勿另起管线**——剧集拆分/资产拆分/智能推理直接 import Frame1693/Frame161195 同款（runPurpose+analysisTask 断连+owner 防串台 / startInfer+inferTasks 互斥 / mergeApply+attachSplitPresets 预设胶囊两处必带）；与资产模式共享 analysisRunning 互斥属预期非 bug。
- **第235轮**：**rtcDoc 的 segment=素材纯时间窗口引用（assetId 唯一源头），split/裁剪/复制绝不产生新素材实体**（用户明令）——splitSegment 两段同 assetId、source 窗口互补（rtcOps.test.ts 锁定）；**导出剪映时 materials 按 assetId 严格去重**（同素材多段/多轨只落一条 material、素材文件只拷一份，jianyingDraft.test.ts 锁定）。勿改成「每段独立素材」。
- **第235轮**：**rtc 数据变更唯一路径=`rtcStore.commit(mutator)`**（推 undo 栈+回写 projectStore 去抖落盘）——面板/播放器等组件只读 doc、需要改片段属性用 commit 内联不可变更新，勿绕过 commit 直写、勿在拖裁过程中每帧 commit（本地 previewDoc 预览、pointerup 才 commit 一次=undo 粒度一次手势）。
- **第235轮**：**模式注册已表驱动，勿回退硬编码**——App.tsx `MODE_ROUTE_TABLE`（路由→模式→home）与 TitleBar `modeTabs` 数组：新增第四模式只在表里加一行；`editorMode` 缺省=开（`!== false` 与 canvasMode 同尺）、「三主模式（资产/画布/实时剪辑）不能全关」守卫在 服务端 updateAgent+两批量 setFeature+两控制台 toggleFeat 五处一致，改一处须五处同改。
- **第235轮**：**实时剪辑的 AI 生成只走 startShotGeneration()/startInfer() 唯一路径**（参数组装对齐 Frame161195），占位符成片替换**只动 kind/assetId/uri/source，target 位置时长不动**（时长不符留给用户裁剪）；资产面板拖拽恒走 HTML5 MIME `application/x-qiji-asset`（payload 与 AssetAssistant 同构只加不改）——时间轴 drop 端按 MIME 解析是硬契约，勿改原生 startDrag。
- **第235轮**：**剪映草稿导出=自包含复制**（素材拷进草稿 assets\，勿改回引用 Qiji 项目路径——用户清缓存/删项目即媒体丢失）；Rust 四命令写入面**双向限定**（目标 canonicalize 恒在草稿根下、源恒在项目 assets 下、JSON 白名单）勿放开任意路径；上游模型名/uuid 形态照 pyJianYingDraft 模板（platform 5.9.0 骨架），剪映不识别时先对照模板 diff 勿凭感觉改字段。
- **第234轮**：**autodl 鉴权=`Authorization: <Token>` 原样令牌，勿加 Bearer 前缀**（官方文档明写 `{"Authorization": 您的Token令牌}`；加了 Bearer 上游 401）——提交与轮询两处同规，勿照抄其它翻译器。
- **第234轮**：**autodl 的 workflow_id 守卫勿删**——「上游模型名」=各工作流的 workflow_id（一个工作流=一个模型），种子是占位符「请填写workflow_id」；翻译器对 占位符/回退成模型 id（`^adl-`）两种「未配置」形态**前置明确报错不发请求不扣费**。改模型 id 前缀时同步改该正则。
- **第234轮**：**errorScrub 域名隐藏必须在「模式名占位符保护」之前，勿回退**——autodl 是首个「模式名（autodl）是上游域名（autodl.art）子串」的渠道：先打占位符会把裸域名拆成 `files.␀N␀.art` 逃过隐藏；域名正则须整段命中 host，独立出现的模式名词不会被它误吞，先跑域名严格安全（沙盒 D4-D7 实锤：autodl 裸域名隐藏 + 简梦S/聪宸 既有语义零回归）。
- **第233轮**：**congge 两侧各自「不发某字段」勿回退**——图片侧**绝不发 `size`**（文档明写与 `resolution` 冲突），客户端的像素串 `size` 由翻译器换算成 `aspect_ratio`（10 档就近）+ `resolution`（长边反推 ≤1280→1K/≤2560→2K/否则 4K；显式 `params.resolution` 优先且档外原样透传）；视频侧**绝不发 `size`/`resolution`**（分辨率已编码在上游模型名里，routes 重定向），只发 `ratio`。`quality` **仅 gpt-image-2** 发（Gemini 传了上游明确报错），我方 `auto`=不发走上游默认。
- **第233轮**：**congge 上游视频模型名带空格且大小写敏感**（`seedance2.0 Mini-480p`、`seedance2.5 720p`）——逐字照抄勿"顺手规范化"，改一个字符即模型不存在；能力守卫按**前缀**判定（seedance2.5→图30/视10/音10·30s；seedance2.0→图9/视3/音3·15s；未知名不守卫直发），站方加分辨率后缀时管理端建模型即接。**文档无首/尾帧字段 → 不声明 methods**（仅全能参考），勿凭 Seedance 惯例补 frames。
- **第233轮**：**congge 图生图必须走 `/v1/images/edits`**（塞进 generations 无效，文档「七」明写）；垫图**硬上限 4 张**（image+images+reference_images 合计）前置明确报错**绝不静默丢**；全公网直链走 JSON（单张 `image`/多张 `images`），只剩字节/直链已死走 **multipart 兜底**（经 openai.ts 新导出的 `buildEditBlobs` 统一取字节——⚠ JSON 与 multipart 不能混合表达，混排时一律转字节，任一张取不到即明确报错不缺张）。
- **第220轮**：**logCostFor 消耗视角统一「带 agentCosts 取对应那份（agent=本商/platform=根级）、无链取 cost（用户实扣）」，勿回退成「无链=undefined」**——P1 后生成请求 agentCosts 停写、积分实时扣用户，回退=门户「我的消耗」与源站商属视图 KPI 恒 0/「—」（渠道商与源站都无法统计用户消耗）；第214轮节点日志落笔定式（cost:0+agentCosts 池实扣）与发码日志（cost+agentCosts 同值）在新规则下**带链优先**语义不变勿动；管理端「渠道商详情」的「源站实收」列文案已随改（统一定价下=用户实扣），勿再恢复「旧口径/仅旧记录有值」措辞。
- **第219轮**：**P4 起商业化改造迁移三件套（P1 agents 剥离 / P2b 邀请码回填 / P4 users 剥离）在生产首次部署前一律保留勿删**——生产尚未部署时删任何一段=部署即坏数据；生产部署并确认迁移落地后才可作为历史代码清理。users 剥离的备份名 `users.json.bak-p4-cleanup` 勿改（与 P1 `.bak-p1-unified-pricing` 同规：已存在不覆盖=保住首次剥离前原貌）。
- **第218轮**：**机器码/硬件指纹整体退役，勿回退**——身份=API 密钥（accessKey，Bearer），设备区分=前端随机 UUID（`x-device-id` 头；服务端 `deviceIdOf` 兼容收旧 `x-machine-code` 头**勿删**——旧客户端全靠它）；第137轮硬件指纹三条规则（材料格式/启动时序/Rust 命令）**废止**，勿以任何理由恢复硬件采集。客户端 `getDeviceId()` 的旧键回退链（Qiji:hwCode/Qiji:machineCode）勿删——老用户升级设备身份连续性的唯一保障。
- **第218轮**：**API 密钥重置语义**——`/v1/api-key/regenerate` 换新钥必须同时把设备表重置为仅本机（旧钥失效+旧设备表一起作废）；重置入口两处（用户个人中心自助 + 管理端用户抽屉），确认弹窗明示「其它设备/外部对接将失效」勿去掉。对外 API 对接=同一把密钥走 /v1 协议，无独立「对接密钥」实体——勿另造第二套密钥体系（真需要多密钥/细粒度授权时是新需求，先问用户）。
- **第217轮**：**算力（suanli-video）素材引用=`<<<N>>>` 0 基全局下标**（materials 图/视/音混排数组，图在前视次之音在后；`suanliTags` 单点转写勿另写）；帯故事板/追加素材一律**只追加 materials 末尾**（前插=全部既有下标错位——比 @tag 体系更脆弱）；「参考音频须伴随图或视频」守卫勿删（上游明言）。
- **第217轮**：**动 modes/channels/models 种子代码前先停本机 dev tsx watch，编辑完 tsc 过后再重启**——第216轮教训的操作化（停服=版本号与条目分两次编辑也无竞态；生产部署天然是最终代码不受影响）。
- **第216轮**：**classifyFamily 新家族判定必须放在 seedance 数字兜底正则（/933|431|403|900|903/）之前**——minimax/happyhorse 规则若排在其后，xc903-/xc900- 前缀模型会被数字段误吞进 fam-seedance（沙盒实锤）；模型 id 带素材量数字的新家族一律注意此序。
- **第216轮**：**编辑 models.ts 种子/迁移代码前记住本机 8787 是 tsx watch**——编辑途中每次保存都热重启并把「半成品代码」的补种/迁移跑进真实 dev 库（本轮 h3/hh familyId 被中间态钉错，admin API 修正）；生产部署=最终代码一次跑对不受影响；dev 库沾了中间态后，沙盒冒烟必须「先剥离痕迹还原部署前形状」再跑迁移，否则测的不是部署路径。
- **第216轮**：21 线 Gemini 重接用**新 id `xc600-gemini-omni`**——旧 `xc600-gemini-flash` 在墓碑，按「删除内置项走墓碑不复活」惯例不清墓碑不复用旧 id；勿为「恢复旧模型」清墓碑。
- **第215轮**：**参数透传统一 helper（[paramPass.ts](server/src/translators/paramPass.ts)）勿绕过**——13 个视频翻译器的 时长/比例/分辨率 一律经 `numberParam`/`stringParam`（显式原样透传、缺省补默认），勿再引入 `Math.min` 夹钳/`nearestDuration` 就近取档/`RATIOS.has` 白名单回退；§9「绝不静默改写」条目里「已有渠道的历史夹钳属存量行为」的豁免**自本轮废止**（存量已全部根除，任何翻译器一律遵守）。保留的三类**不是**夹钳勿误删：volc `clampEnum`（管理端实时参数硬闸）、业务前置守卫（jmz grok 1080p 单图、jmh grok 朝向段=明确报错）、图片侧 size→比例语义映射。
- **第214轮**：**relay 计费镜像时序勿改**——节点转发前只按目录预估价**预检**，真扣按**源站响应回传的实扣 cost**（generate 附 cost / batch 附 costs[]，仅对 ank- 凭证下发）；改成「先扣本地再转发」会在参考视频按秒加权等场景打破金额恒等。源站拒单=本地一分不动。
- **第214轮**：**节点日志落笔定式勿改**——源站对节点请求记 `cost:0 + agentCosts:[池实扣] + ownerId:商 + userId:nu: 留痕`：logCostFor 两视角天然取池实扣，且启动对账（reconcile.ts）孤儿退款按 agentCosts 原路退池、userAmount 0 对留痕 id 天然 no-op——**零改动兼容**。若改成 cost=实扣，reconcile 会对不存在的 `nu:` 用户退款失败、连池的退款一起丢。
- **第214轮**：**relay 模式勿启用 reconcileOnStartup**——它会把仍在源站运行的任务当孤儿就地退款（任务随后完成=用户白拿）；节点的孤儿兜底是 relay-ledger 清扫（轮询透传结算 + 10 分钟周期 + 源站 404 超 72h 按中断退款）。重置 nodeKey=旧密钥立即失效（管理端/门户确认弹窗已明示，勿去掉确认）。
- **第213轮**：**激活码签发机制整体退役（绿地），勿回退**——`/v1/register`、`/agent-api/users/batch`、`/admin-api/users/batch`、门户 unbind/regenerate-key/DELETE user 已删；获客唯一通道=邀请码（A 商码→归属、U 个人码→invitedBy）。第212轮③「激活码注册通道保留」条目**废止**。用户明令「直接按全新做」——勿以兼容为名恢复 machineCode/validityDays/expiresAt 任何读写路径。
- **第213轮**：**邀请码校验必须先于验证码核销**（[routes.ts](server/src/routes.ts) /v1/register/account）——验证码一次性，先拒完能拒的（无效邀请码/停用商/密码太短）再核销，防用户白烧一枚验证码；填了无效邀请码明确 400 不静默忽略（用户特意填的）。查码大小写不敏感（getAgentByInviteCode/getUserByInviteCode 内部 upper 归一）。
- **第213轮**：accessKey 降级为**内部访问凭证**（注册自动生成、回传客户端持久登录；管理端显示「访问凭证」）——勿恢复任何「激活码」语义 UI；invitedBy 与注册赠送机制（Q6）是后续邀请奖励的预留接口，勿清理。
- **第212轮**：**机器码硬绑定退役勿回退**——x-machine-code 头降级为「同时在线设备限制」的设备标识，**客户端采集与发头勿删**（旧服务端仍校验绑定、新服务端做设备表，双向兼容全靠它）；checkDeviceAccess「有空位当场收编」是存量已登录客户端无感过渡的关键，勿改成「必须先登录」。
- **第212轮**：找回密码发码对**不存在的账号也返回 ok**（不真发）——防账号存在性探测，勿改成明确报「账号不存在」；发码 60s 冷却按**目标**跨用途共用（注册/找回同池），属产品预期勿拆分。
- **第212轮**：管理端注册设置的密钥语义——GET 只回 hasPass/hasSecret（绝不回显明文），PUT 空串=不改、null=清除；SMTP 新依赖 nodemailer，部署机须 `cd server && npm i`。
- **第211轮**：**统一定价 + 分发实扣是原子包，勿单独回退任一半**——免费面额 + 无链式结算 = 渠道商可无限给用户铸积分白嫖源站；planBilling 勿回退链式双扣费（settle agents 恒空）；门户发码（兑换码/激活码面额）必须从商 credits 池实扣、作废退回。
- **第211轮**：settle/reverse 结算框架保留——P1 切换前的历史 billing 快照（带 agentCosts）退款/对账仍原路两侧同退，勿删 agents.ts 的 applyAgentCreditsDelta/persistAgents 原语。
- **第211轮**：P1 迁移按**字段存在性**幂等（无 flag 文件）；备份 `agents.json.bak-p1-unified-pricing` 已存在则不覆盖（保住首次剥离前的原貌），勿改备份命名。
- **第211轮**：agentChain/audienceChain/chainPricingVersion 拍平后保留函数形态（单级）——feature 闸/模型禁用/显示名消费点以链遍历书写；pricingVersion 旧名保留（现承载 改名/启停/换分组 的 catalog .p 段热更），勿改名触发存量数据失配。

- **第176轮**：未动过按键且存量是旧受众清单 → 返回 null=保存时不带开放范围字段**（绝不把存量语义抹成 none，沙盒+浏览器双验）；
- **第176轮**：顺带修撞名（第175轮引入）**：模板页「按渠道商查看」下拉的设置器与开放范围旧设置器同名 `setTplScope`（后定义者胜=下拉失灵）→ 视图筛选改名 **`setTplViewScope`**，旧同名函数随分段控件一并删除（注释留告警勿再撞名）。
- **第176轮**：语义勿改成 reorderModes 那种整表重排：管理端是「按渠道/模式/家族分组 + 状态筛选」的视图，整表重排会把被筛掉的模型甩到末尾、破坏其相对次序）；
- **第176轮**：须注册在 `/admin-api/models/:id` 之前）；
- **第176轮**：须重新部署服务端**（管理端页面随部署生效；
- **第175轮**：保留**运行时自定义协议引擎（store/protocols.ts、translators/custom.ts）+ `GET /admin-api/protocols` + 前端 `fetchProtocols`/`protoOptions`——**模型「协议」下拉依赖它**（存量自定义协议的模型仍可正常显示/选中/生成）；
- **第175轮**：按 leaderId 过滤须在 `adminTeamView` 映射**之前**）/shared-libs/**models（按可用性 `modelAllowedForAgent` 而非创建者——模型全归平台）**/templates（`listPlatformTemplates` + 各商自营，带 agentName 投影）逐页按范围返回。
- **第175轮**：须重新部署服务端**（管理端页面随部署生效）。
- **第174轮**：勿回退）：**拆分推理只产资产正文，固定前后缀由客户端按配置自动挂「预设胶囊」，不再消耗 token 不等推理；
- **第174轮**：客户端全量升级后可删）**：预设同时以「模板形状」附加进 catalog.templates 的 画风/预设方案 分类——旧打包客户端的 预设下拉/画风选择/胶囊展开 照旧可用（不补这层，只部署服务端会让旧客户端预设下拉变空、已插胶囊以标记文本漏发上游）；
- **第174轮**：装饰器注册=splitPresetAttach 模块体自注册 + pluginRegistry/ContextMenu 裂变前动态 import**——绝不可在 pluginRegistry 静态 import（它在 debouncedSave→useCanvasDrag 加载链上，静态引 projectStore 会成 TDZ 循环，vitest 实测炸过，勿回退）。
- **第174轮**：须重新部署服务端（首启自动迁移 templates→presets，生产部署前建议备份 data/templates.json）+ 客户端重新打包**（只部署服务端=旧客户端凭兼容投影照旧用预设/画风，仅无 自动挂胶囊/autoAttach 能力，拆分行为与现状一致）。
- **第173轮**：勿回退成「读进内存不落盘」）：**导入 = 新建项目并复制文件，被导入的源文件/源项目分毫不动**。
- **第172轮**：核心语义勿回退）**：`teamPayerFor(user)`——shared 团队的非团长成员 → **扣款人=团长**（共享池=团长余额）；
- **第172轮**：须重新部署服务端 + 客户端重新打包**（只部署服务端=旧客户端无团队入口，但共享模式对团员的池扣费/池余额下发已生效（credits 口径随心跳自动变）；
- **第172轮**：经济漏洞修复：绑定改「邀请-同意制」+ 收回上限=分发净额台账 + 退团自动结算·两端 · server/client tsc 干净 + 456 vitest 全过 + vite build 过 + dev 8787 专项冒烟 23/23 全过（临时数据全清））**——用户指出首版是**严重经济 bug**：团长可单方面按账号强拉任何人入团，再用「收回」收缴对方**自有积分**。
- **第172轮**：三条均为经济安全语义，勿回退）： ①**入团必须对方同意**（[teams.ts](server/src/store/teams.ts)）：`Team.invites` 待接受邀请表——团长「绑定」改为**发出邀请**（`inviteToTeam`，满员判定含待接受防超发；重复邀请拒）；被邀请人在自己团队页**接受**（`acceptInvite`=唯一入团路径，接受时再验 不在团/未满员，入团即清其在所有团的其它邀请+加入团队共享库）或**拒绝**；团长可**撤销**（`removeInvite` 共用）；**7 天未处理自动过期**（sanitizeTeams 懒清）。端点：PO
- **第172轮**：dev 库现存用户自建测试团队「测试」（团长=用户「简一」，源站码开团）——真实数据未触碰。
- **第172轮**：定稿勿再收紧）**：开团（[teams.ts](server/src/store/teams.ts) createTeam 删归属校验——任何用户凭任意团队码开团）与邀请（[routes.ts](server/src/routes.ts) POST /v1/team/members 删同归属校验——跨渠道商/平台直属互邀）全放开。
- **第172轮**：开码积分改「实扣制」+ 共享素材库全面开放·服务端 + 两控制台 + 客户端一行文案 · server/client tsc 干净 + 474 vitest 全过 + 两控制台 script 语法过 + dev 8787 专项冒烟 17/17 全过（临时数据全清））**——用户两点定稿：①「共享库有搜索和密码限制，不需要隔离，开放，只要是本软件就能搜索到和使用共享」；
- **第172轮**：定稿勿恢复）**：[sharedLibs.ts](server/src/store/sharedLibs.ts) `searchLibraries(q)` 改**全局搜索**（去 audience 参数）；
- **第172轮**：定稿勿回退成 mint+链式）**：开码积分从此是**真实划转的配额**，与通用积分（mint 分发+消耗时逐笔链式结算）刻意不同——⑴**分发=划转**（[agent.ts](server/src/routes/agent.ts) `POST /agent-api/sub-agents/:id/code-credits` 改 [agents.ts](server/src/store/agents.ts) 新 `transferAgentCodeCredits`：分发扣上级自身（不足拒绝）、扣回反向划回；
- **第172轮**：同轮用户追问确认：**消费积分（通用积分）保持链式不动**——每笔消耗仍沿归属链逐级按结算价扣各商积分池、分发给下游仍是 mint（约束在逐笔结算）；
- **第171轮**：须重新部署服务端**（管理端页面 readFileSync 每请求现读，部署即生效；
- **第170轮**：勿删：新版 SDK 默认把「空体 CRC32」校验参数钉进预签名 URL，rains3 宽松忽略但严格 S3 会拒真实字节）**；
- **第170轮**：带空 body "{}" 防 Fastify 拒空 JSON——第101轮同款坑）；
- **第170轮**：须重新部署服务端（部署机须 `cd server && npm i` 安装 @aws-sdk/s3-request-presigner）+ 客户端重新打包**（只部署服务端=旧客户端仍走 multipart 一切如旧；
- **第169轮**：勿回退成短超时）：`SUBMIT_TIMEOUT_MS`（env 可调；
- **第169轮**：勿顺手放大）**：各家轮询 30s（瞬时失败=继续轮询，不亏钱）、结果下载/垫图取字节（openai 图 2×30s+fallbackUrl 客户端接力=第158轮定稿、jmh/gemini 垫图 60s、rehost 下载 180s）、openai 图像上游 1h / gemini 图像 1h（本就够长）。
- **第169轮**：配套勿删）：Node 内置全局 fetch 的 undici 缺省 headersTimeout/bodyTimeout=300s 会在各请求自己的 AbortSignal 之前掐断慢上游——只放宽 ① 也会在 5 分钟处被掐。
- **第169轮**：须重新部署服务端（部署机须 `cd server && npm i` 安装 undici，否则启动报模块缺失）**；
- **第168轮**：勿回退）：完整链接→「（链接已隐藏）」；上游域名（静态清单 + 渠道 baseUrl host **动态并入**，子域一并吞）→「（已隐藏）」；品牌词（静态清单：Dimensio/星辰/画影/苏打水/AIStartLab/MuseAI/ZhengAPI/Aivide/火山/**简梦X 系渠道别名**/协议 id 品牌段 dimensio·huaying·aistars·sudashui 等 + 渠道显示名**动态并入**）→「渠道」；**「当前模式名」经 NUL 占位符保护绝不擦**（模式名=客户端下拉可见的对外品牌——模式仍叫「星辰」时保留、改名后旧品牌词自动开始被擦；占位符而非逐词豁免，
- **第168轮**：教训：**源码里勿放裸 NUL 字节**（首版模板串内写真实 \0 被编辑工具吞写坏，改 `String.fromCharCode(0)`）；
- **第168轮**：须重新部署服务端**（首启自动：logs 一次性迁移擦存量 + tasks 每启幂等擦；
- **第167轮**：下游商不受分组价影响**——其进货价=上级售价，分组价不越级；
- **第167轮**：设置弹窗内打开时挂进 `<dialog>` 顶层防遮挡；
- **第167轮**：须重新部署服务端**（首启自动种默认分组；
- **第166轮**：会经 catalog 下发给用户，勿写接入信息**；
- **第166轮**：须重新部署服务端（管理端卡片页 + note 字段/catalog 下发随部署生效）+ 客户端重新打包**（不打包=旧客户端无悬浮提示、面板仍 680px；
- **第165轮**：必须剥——客户端 familyName 回查不到会裸显 id；
- **第165轮**：拖动期间绝不重渲染网格——HTML5 DnD 里重建被拖元素会当场取消拖动）；
- **第165轮**：冒烟脚本教训：**无 body 的 DELETE 不能带 Content-Type: application/json**（Fastify 拒空 JSON body，第101轮 POST 同款坑））+ **浏览器端到端**（tsx watch 热重启后真 UI：13 模式卡/11 家族卡 3 列网格、卡高一致 150px、UI 表单建临时模式、合成 DragEvent 拖到最前→预览+落库 order=1、开关停用→卡片 .off+「已停用」badge+toast、恢复+confirmDo 删除→列表复原、零控制台报错；
- **第165轮**：须重新部署服务端**（存量无 enabled 字段=全启用，行为不变）；
- **第165轮**：渠道排序仅影响管理端显示顺序**（渠道不下发客户端——与模式/家族"排序影响客户端"不同，卡片 tooltip 与提示文案已注明）：[channels.ts](server/src/store/channels.ts) `ChannelDef.order?`（缺省=按加入顺序排后；
- **第165轮**：占位/草稿卡无 data-id 无 ⠿：不参与拖动，`wireMfSort` 排序提交 `.filter(Boolean)` 剔除（勿删这层守卫——否则 reorder 会带 undefined）。
- **第165轮**：列间相对序对客户端无意义（下拉按能力分场景各看各列的序），唯一边界：图视混合模式在图像下拉里的位置被列块拼接钉死、不能与纯图像模式交叉排）。
- **第165轮**：勿回退成派生归列 · admin script 语法过 + 浏览器实测过（1900px 三栏 905/240/452、模式单列 13 卡+末尾「＋」、拖 qiji-img 置顶落库后已恢复原序、家族两列 4/7 不受影响、零控制台报错））**：补充3 的 `modeColOf` 按模型能力派生归列**整体删除**（图视混合/文本模式归不进两列语义站不住）——模式面板改**单竖列**（.chpage 配比调 渠道 2.4fr / 模式 minmax(240px,.6fr) 窄列 / 家族 1.2fr；
- **第165轮**：规则：窄栏内的卡片网格轨道一律显式 `minmax(0,1fr)`，配 `.mfcard{min-width:0}`，缺一不可）**：`.mfcol .mfgrid`/`.mfcols` 轨道全改 `minmax(0,1fr)`；
- **第164轮**：勿删）：原生 `<select>` 下拉是 OS 级弹窗，指针移入会触发 mouseleave——BaseNode `handleMouseLeave` 对「焦点在悬停工具栏内（hoverbarRef.contains(activeElement)）」不收起；
- **第164轮**：已知局限（分析定稿）：圆角面板留四个小白角（只裁到内容外接矩形）；
- **第164轮**：迁移实锤发现并收编：**9grid 首版种子笔误「数字1-6」**（第117轮补充5 从 6grid 复制漏改，dev 库 07-10 种子即此形态、生产同源）——该变体也算原样，升级顺带修正为 1-9。
- **第163轮**：与模式分工锁定（勿混）**：模式=渠道源头+用户/渠道商门禁开关（关渠道用它，仍是第二级「渠道/线路」显示名来源）；
- **第163轮**：8787 又跑着**不带 watch 的陈旧 tsx 实例**（第145/148轮同款，还停在第160轮代码=161/162 补种一直没落库的真因）——已备份 models/modes/channels.json（.bak-r163）后清杀并以 npm run dev 重启：94→101 模型、161/162 补种随本轮一并落库、87 个视频/图像模型全带 familyId、families.json 11 条）+ **浏览器端到端**（管理端：家族面板 11 行计数全对（seedance 51）/模型页「家族」列+「按家族」分组/零报错；
- **第163轮**：须重新部署服务端**（首启自动：种 11 家族 + 全模型归类；
- **第162轮**：撞名雷（冒烟抓到，勿再踩）**：新 id 与第148轮 ⑤d 删除的旧 37 线模型 `xc933-sd2.0-fast-c` **同名**——生产库墓碑仍在、③ 补种会被挡住；
- **第162轮**：须重新部署服务端**（首启自动：⑤h 迁移改名 + 补种 6 新款；
- **第161轮**：模型 ID 动态拼接**（文档「模型」节）：`seedance-2.0+{16:9|9:16}+{720}+{5|10|15}`——种子 upstreamModel 只存基名，翻译器按请求参数现拼（时长就近取档并列取小、比例越档回退 16:9、分辨率取 params.resolution 剥 p——上游放开 1080 后管理端给 resolution 加档即放行）；
- **第161轮**：成片下载须带同一 API Key + 24h 时效**（文档「下载视频/注意事项」）→ poll completed 附 `resultHeaders`（**仅结果域=本站域才附 Bearer**，防密钥外泄给第三方 CDN——第153轮 jmz-image 同款；
- **第161轮**：rehost 失败回退上游直链时客户端多半下载不了（要鉴权头）——**须配好 OSS**，属已知边界。
- **第161轮**：须重新部署服务端**（首启自动补种 简梦F 模式+模型+渠道；
- **第160轮**：该家 prompt 原生就用 @Image1/@Video1/@Audio1 占位符**（编号 1 起、与三 refs 数组顺序一一对应）——与 injectReferenceTags 注入约定完全一致，**直通零转写**（区别于苏打水小写/Dimensio @image_file_N）。
- **第160轮**：须重新部署服务端**（首启自动补种 简梦T 模式+模型+渠道；
- **第159轮**：已知行为（与 ⑤d/⑤e/⑤f 同款、非 bug）：**全新库首启走快速初始化路径不进迁移段**——清单首启即正确（种子本就无这两款），墓碑+版本号由第二次启动补齐。
- **第159轮**：本机 8787 未在运行（无进程非僵尸，dev 未启动——下次 `npm run dev` 首启自动 补种+删除）。
- **第158轮**：须重新部署服务端 + 客户端重新打包**（只部署服务端=已止损不退款：旧客户端对 rehosted:false 结果走原生 180s 下载+rehost 兜底，下载成功=正常显示但不回传 OSS；
- **第157轮**：两个部署要点**：⑴渠道密钥**必须**在管理端「云雾」渠道填——openai-chat 协议的环境兜底是网关 GATEWAY_API_KEY（共享协议无法按渠道走环境变量），渠道留空会把网关密钥错发给 yunwu.ai 得 401（明确报错无静默风险）；
- **第156轮**：须重新部署服务端**（首启自动迁移：删 19+归 jms+删三模式；
- **第155轮**：文档矛盾裁决（勿回退）**：旧版「grok-imagine-1.0-video 终端对接文档」（2026-05-31）说"模型名固定不拼后缀 + video_config + stream:false 推荐"，与新「视频生成接口文档」（统一 6 家族，"所有视频模型统一 chat/completions 流式 + 模型 ID 动态拼接"）正面冲突——**以新统一文档为准**（拼后缀+顶层 aspect_ratio/video_length/resolution+stream:true）；
- **第155轮**：须重新部署服务端**（与第154轮同一次部署即可；
- **第154轮**：范围：只接图片**（用户指令明确；同站视频系（grok-imagine-1.0-video / firefly-sora2/veo31/kling3/runway45，chat 流式+模型 ID 拼时长比例分辨率）文档已在手、要接另起一轮）。 ①**上游（Base `https://zhengapi.top`，Bearer sk-）——同步单请求渠道，两种形态**（[jmh.ts](server/src/translators/jmh.ts) 按上游名前缀分流；**无轮询**，协议 `jmh-image` 复用 createImageTask 图片管线，任务后台阻塞出图→当场落资产）：
- **第154轮**：`image` 是**单值纯 base64 无 data: 前缀**（文档注意事项②，与 firefly 相反勿混）→ 图1 上限（>1 明确拒）、-edit 图生图专用（无图明确拒）；
- **第154轮**：无任何价格情报，建议先小额单摸底）。 ③**注册面**：新模式 jmh「简梦H」（[modes.ts](server/src/store/modes.ts) `MODES_SEED_VERSION=11`——**版本+条目一次编辑，零竞态**）+ 渠道 `ch-jmh` + 6 模型种子（`jmh()` 工厂经补种入库）+ 协议 `jmh-image` 六处注册（Protocol 联合/BUILTIN_PROTOCOLS/**dispatch case=createImageTask**（无 VIDEO_DRIVERS——同步渠道不进轮询表）/upstream fallback `con
- **第154轮**：须重新部署服务端**（首启自动补种 简梦H 模式+6 模型+渠道；
- **第153轮**：本轮唯一动共享代码处，勿回退）**：该家**结果链接非公开**——`GET /v1/images/tasks/{id}/content?index=0` 下载须带 Bearer、成功结果仅保留约 2 小时。
- **第153轮**：图片查询是 信封+大写状态+"100%"字符串进度，与同站视频查询（扁平+小写）形状不同，翻译器信封/扁平双容忍；
- **第153轮**：按同站视频 API「素材与兼容字段」多图别名族推定，待真机实锤——多图垫图是我方核心场景，首个真单必验）**；
- **第153轮**：1K/2K/4K 实际能力由 API Key 所在分组决定**（default=1K原生+2K/4K超分 / image2=仅1K / image2 4k=真4K）——要 4K 让站方控制台把 Key 分到「image2 4k」分组，我方不传任何上游线路信息）；
- **第153轮**：须重新部署服务端**（与第152轮同一次部署即可；
- **第152轮**：本轮情报关键（后续维护先做这一步）：实时模型目录 `GET https://zexitongxue.com/ai-api/models?type=video` 免鉴权可拉、逐模型带 note/duration_profile/duration_rules/resolution_profile/max_reference_images/can_use——比文档正文细得多**（豆包线 note 明示「支持首尾帧+真人参考图已真实出片」——文档正文「真人照片暂不支持」已过时；
- **第152轮**：渠道 Base URL 填根域**不带 /v1**——翻译器自拼 `/v1/videos`，防 /v1/v1 双拼）**：提交 `POST /v1/videos` body `{model, prompt, aspect_ratio, duration, reference_image_urls?, reference_videos?, audio_urls?, start_frame?/end_frame?(首尾帧), resolution?(仅 grok)}`——字段别名众多（image_url/imageUrl/images/seconds/ratio…）**只发一套主字段**（第14
- **第152轮**：文档只公布 status:"success" 一种终态**——[jmz.ts](server/src/translators/jmz.ts) 防御式状态族：success/succeeded/completed=成功（result_url 主字段、video_url 兜底；
- **第152轮**：须重新部署服务端**（首启自动补种 简梦Z 模式+14 模型+渠道；
- **第151轮**：鉴权=`apikey: MUSE-...` 请求头，非 Bearer——勿照抄其它翻译器）**：提交 `POST /api/v2/videos` body `{model, prompt, ratio(6 比例含 21:9), duration(4-15 整数), resolution?(480p/720p/1080p), images:[{tag,url}](schema 必填恒发，空数组也发), audios?/videos?:[{tag,url}]}` → `{code, msg, data}`（**data=任务号数字**，转字符串保存）；
- **第151轮**：文档 schema 把状态枚举误标在 data.id、examples 在 data.status——翻译器 `pickState` 两字段防御式都探；
- **第151轮**：排查记录：**再踩第134轮记过的 modes.ts 分两次编辑竞态**——tsx watch 在 bump 与加条目之间重启，seedVersion 已抬 9 但 jmm 错过版本闸（dev 已按惯例 POST /admin-api/modes 补上；
- **第151轮**：须重新部署服务端**（首启自动补种 简梦M 模式+4 模型+渠道；
- **第150轮**：勿回退成 IncrementalSpawner）：关软件前流式期间可能已裂变过部分子节点（已随项目持久化），整批重裂变必重复——找回模式不建 spawner，成功后走「二次解析」同款 buildRespawn 与画布已有子节点比对（幂等，全齐=零新增）；
- **第150轮**：须客户端重新打包**；服务端零改动（第89轮任务落盘+终态留 48h 是天然前提）。**已知边界**：①提交确认到 canvas 档去抖落盘之间强杀进程 → 标记未写盘=不可找回（同「提交未确认」类，窗口极窄）；②生成中途切走画布再切回：结果由找回接住，但原进程若已在节点缺席时落过库资产，媒体类可能多一条重复历史记录（极角落）；③ai.chat 对话节点未纳入（对话丢一轮重问即可，且其完成逻辑在 runChatNode 闭包内，纳入需另行抽取）；④浏览器 dev 无项目文件持久化，「重开软件」完整链路（落盘→重启→loadFromPath 自动找回）待 Tauri 真机验证。**待真机
- **第149轮**：未知家族：**不声明 methods**（extra methods:undefined 覆盖工厂默认→客户端无「方法」下拉仅全能参考）、不设 matLimits，归 jm933））；
- **第149轮**：关键确认——gf 拒视频守卫是 `/^sdas-gf-/`、hn 时长档守卫是 `/^sdas-hn-/`，**`sdas-gf2-` 不命中 gf 守卫**（有意：豆包线是否拒视频未知，不本地硬拦上游兜底）；
- **第149轮**：教训：块注释里写 `sdas-gf2-*/` 的 `*/` 会提前闭合注释——注释中的上游 id 通配写法勿带星杠）+ **沙盒冒烟 C 31/31**（用 `models.json.bak-r148`（第148轮前备份）→ **xc+sds 两轮迁移同一次启动连跑**：苏打水 26/死链墓碑/星辰共存仍 6/存活字段不刷+补缺 matLimits/gf2/wf/pd431 双档/xinghe 无 methods/jy 未接/计费（gf2 1080p 10s=30、wf 480p 10s=15、xinghe 10s=20、存活 bf 价保留）/catalog 26 下发+零泄漏上游真名+ma
- **第149轮**：须重新部署服务端**（首启自动迁移；**生产部署前建议备份 data/models.json**——第148+149 两轮迁移一次跑完）；**客户端零改动无需打包**。**运营注意**：①26 模型全为**占位价**（新线按秒 1-3/秒），上线前管理端定真价——**新线路无文档无价格情报**，建议先小额单摸底再定价；②迁移把苏打水全员置 enabled=true（此前 16 个全禁用）——不想上架的在管理端关；③**新线路零真单**（能力全按命名推断）：gf2/xinghe 尤其未知（xinghe 连底层模型是什么都不知道），首批用户单观察请求记录③④段实锤能力后，按需回填 matLi
- **第148轮**：与「只补缺」迁移不同，勿仿此模式做常规迁移）**+ enabled=true 重新上架**（存量 14 个全被管理端禁用=旧线过时下架，换新线即上架，运营可再关）+ 2 新模型走补种机制。
- **第148轮**：顺带清杀：本机 8787 又跑着**不带 watch 的 `tsx src/index.ts`**（第145轮同款、永不热重启=「改代码不生效」元凶），已杀树并以 `npm run dev`（tsx watch）重启常驻。
- **第148轮**：须重新部署服务端**（首启自动迁移：删10+刷4+补2+上架；
- **第147轮**：文档未给 Base URL**：渠道 `ch-jianmengp` baseUrl 留空下种，缺地址/缺密钥时提交明确报错不发请求。
- **第147轮**：校验时须按 UTF-8 读 html，PS 默认编码会把中文读坏产生假语法错）+ **本机 dev 热迁移实测**（8787 tsx watch 本轮正常自动重启：10 模式含 jmp=简梦P（用户改名过的 dimensio=「简梦」正确保留）、7 模型 协议/渠道/单价/matLimits 全对、ch-jianmengp 就位、served /admin 含 jianmengp-video 协议项）。
- **第147轮**：须重新部署服务端**（首启自动补种 简梦P 模式+7 模型+渠道；
- **第146轮**：勿回退原生 controls——进度条 nodrag 占满整行吞指针事件即「粘鼠标」根因，与视频节点「节点内不放原生控制条、双击灯箱」同哲学）：**真实解码波形**（fetch 字节→decodeAudioData→48 桶峰值+γ 归一，模块级按 uri 缓存；
- **第146轮**：本轮环境截图工具再次卡死（第135轮同款），以 DOM/JS 断言为准。
- **第146轮**：勿回退）**：⑴ 本地副本改 `saveUploadedLocal` **直接写手上的 File 字节**（零网络零失败面，Tauri 下 blob: 兜底只剩磁盘异常一种极端）；
- **第146轮**：环境截图工具再次卡死，以 DOM 断言为准）。
- **第146轮**：与 ResultView/LibTile 同尺，勿回退）**：⑴ 枚举源头 id 现查——上游素材 `uri = blobs[sid]?.localUri || a.uri`（预览快）、`url = blobs[sid]?.url || a.uri`（提交快：ensurePublicUrl 直接拿到真公网 url 走 isPublicUrl 直通，死快照不再导致该素材被 collectMedia 静默跳过；
- **第146轮**：勿回退）**：显示层不再信「库记录/节点里存的 uri 快照」（写入时的死照片=丢失观感的载体），改为**按资产 id 现查三元映射的活本地副本优先**——[ResultView.tsx](src/nodes/ResultView.tsx) 与 [AssetPanel.tsx](src/canvas/AssetPanel.tsx) LibTile 各加 `liveLocalUri = assetBlobs[serverAssetId||id]?.localUri` 单值选择器（仅该值变化才重渲染，合 §9），`useDisplayUri(liveLocalUri ?? 快照 uri)`；
- **第146轮**：运营/用户须知（重要）**：已损坏的存量素材（旧 OSS 链接已死、本地无字节）任何代码无从取回——**删掉重新上传一次**即永久修复（新代码下重传是真重传；
- **第145轮**：不入 `MODEL_REFRESH_FIELDS`**（管理端自有配置，seed 刷新不冲掉——同改价待遇）。
- **第145轮**：判定与文案与服务端两处同步）在 [managedAdapter.ts](src/services/adapters/managedAdapter.ts) `submit` 组装完 req.inputs 后预检——**唯一请求路径（§11.1）单点收口=表格按键/画布节点全部提交入口一处覆盖**，超限请求不发出、明确报错（错误经既有 submit 报错链路呈现）。
- **第145轮**：排查记录（tsx watch 僵尸·历史之最）**：本机 8787 同时存在 **3 个 `tsx watch` 僵尸树 + 1 个不带 watch 的 `tsx src/index.ts`**（正占着 8787、永不热重启——首轮 API 冒烟全跑在旧代码上现形）；
- **第145轮**：须重新部署服务端 + 客户端重新打包**（不打包=旧客户端缺提交前预检，超限单发到服务端被 400 拦、文案相同，仅少「不发请求」的快速反馈）。
- **第144轮**：曾试 vite `define` 与 `process.env.VITE_*` 两条注入链均"不生效"，根因见②；
- **第144轮**：重大隐患根治（vite.config.js 遮蔽，勿回退）**：排查 define 不生效时发现仓库根残留 **tsc 编译产物 `vite.config.js`（+.d.ts）**——vite 配置解析 **.js 优先于 .ts**，即 vite.config.ts 的一切修改（本轮 define/插件，以及未来任何配置改动）都被旧 .js **静默遮蔽**；
- **第143轮**：核心设计，勿另起定价维度）**：新 `Model.refVideoSecondsWeight`（[models.ts](server/src/store/models.ts)，按秒视频模型专用；
- **第143轮**：须重新部署服务端**（首启自动：assets 表补列 + dm 系补系数）；
- **第143轮**：另记：dev 库 `av933-2.0`（第139轮 Aivide）已被管理端手动删除进墓碑（deletedSeedIds）——按设计不复活；
- **第143轮**：须客户端重新打包**（不打包=旧客户端角标仍少显示参考视频部分，实扣/402 不受影响）。
- **第142轮**：核心规则：任何改动素材集合/顺序的路径必须经带图例重建的统一入口，勿再裸写 materials/input）**：①**表格页七条路径收口 [shotMaterialOps](src/lib/shotMaterialOps.ts)**——拖拽重排（新增 `reorderShotMaterial`：图例整体重建 + **三份提示词正文内联 @ 引用按新旧编号重映射**，新纯函数 [shotMaterials.ts](src/lib/shotMaterials.ts) `remapBodyTags`＝单次扫描置换、交换类映射不串连改写）、右键删除/＋添加资产/跨分镜拖拽复制/资产 JSON 拖入
- **第141轮**：不含本商自禁——门户列表/售价/改名/启停守卫全走它：**本商禁用的模型仍显示、仍可改价改名、可再启用**，上级禁的/未开放的才从门户消失；
- **第141轮**：须重新部署服务端**（客户端零改动无需打包）。
- **第140轮**：排查记录（本轮虚惊）：临时商删除后 agents 列表为空曾疑数据丢失——实为 **dev 库已于 07-19 07:08 被用户主动重置**（旧 users/agents 带 `-bak-0719` 后缀备份留存 `server/data/`；
- **第140轮**：须重新部署服务端**（客户端零改动无需打包——label 走 catalog 既有字段）。
- **第139轮**：提交不幂等**（文档明言）：超时/网络错误不自动重试（可能已建任务重复计费），明确报错交用户确认；
- **第139轮**：失败响应里的非空 video_url **不得当结果**（文档 §4.3）；
- **第139轮**：与 dm933「各自 3」不同，该家**视+音合计≤3**，守卫在翻译器）：分辨率 480p/720p（默认 720p）、时长 4-15s（默认 15）、比例 16:9/9:16（文档只推荐这两档）；
- **第139轮**：须重新部署服务端**（首启自动补种 Aivide 模式 + av933-2.0 + ch-aivide 渠道；
- **第137轮**：材料格式被单测锁定，改动=全体机器码漂移集体 403，勿动）——废值过滤（UUID 全零/全F/著名 OEM 重复默认值 `03000200-…`；
- **第137轮**：时序规则：机器码随每个请求 `x-machine-code` 头发出，初始化前发请求会绑错机器）；
- **第137轮**：客户端须重新打包**（Rust 命令 + 启动时序；
- **第136轮**：勿回退回商名）**：[agent/index.html](server/src/agent/index.html) `<title>`「灵创工场 · 渠道商控制台」+ favicon（mark data-URI）+ 侧栏白底磁贴+mark 图+「灵创工场」+ env 徽标「门户」→「渠道商」+ 登录卡横版全 logo+「渠道商控制台」；
- **第136轮**：测试残留：一条「激活码签发」消耗日志（临时商已删，归属回落源站视图，无害）。
- **第136轮**：须重新部署服务端**（门户 HTML + agent.ts；
- **第135轮**：技术标识未动**（productName「Qiji」/identifier com.qiji.canvas/CSS 类名 Qiji-*/localStorage 键——改 identifier 会换 appData 目录丢用户数据，勿动）。
- **第135轮**：须重新部署服务端（管理端品牌生效）+ 客户端重新打包（登录页/标题栏/窗口标题/应用图标）**。
- **第134轮**：错误可能带 HTTP 200 返回 `{code,message}`（文档明言勿只看状态码），task_id 才是提交成功判据**；
- **第134轮**：dev 侧排查记录两条（后续排障参考）**：⑴ tsx watch 老毛病升级版——**本机曾同时存在两个 watch 父进程抢 8787**（改代码触发重启时新进程 EADDRINUSE 崩、旧进程带旧代码继续服务，观感=改了不生效）；
- **第134轮**：须重新部署服务端**（首启自动补种 Dimensio 模式+3 模型+渠道；
- **第134轮**：定稿规则（勿回退）：兜底默认按最高——`cost = costPerUnit × 该模型最长时长`**（数字型时长取 max、枚举取最大档、缺省 15；
- **第134轮**：顺带修存量小毛病：「按渠道/按模式」分组 chips 此前高亮不随点击刷新（静态渲染一次）——两组 chips 一并改为随 `renderModels` 重渲染（`#m_toolbar` 容器），高亮/计数恒实时。
- **第134轮**：另注：seedance-2.0/fast/videos-standard 三件套已被用户在本轮前自行启用，第132轮「本就 enabled=false」的记录已过时））：默认视图 55 行无禁用项、「已禁用」页签恰 3 行按组显示、dm933-sd2.0-mini 开关关→行当场消失+chips 变 54/4→已禁用页签点开关还原→55/3（数据零残留）、按模式分组高亮跟点、零控制台报错。
- **第133轮**：与文档三处出入，勿按文档回退）**：提交 `POST /videos/generations`（body `{model,prompt,duration,resolution,aspectRatio,requestId?,images?,videos?,audios?}`）→ 201 `{id:"job_xxx",status:"processing",usage:{cost,reservedCost},balance:{available,reserved}}`，错误 `{error:{code,message,type,request_id}}`（invalid_input/insuff
- **第133轮**：须重新部署服务端**（首启自动补种画影模式+6 模型+渠道；
- **第132轮**：锁15s+必须带图）；xc900-sd2.0（ch37 特价，5-15s）；**xc431-sd2.0/-fast（ch18——全上游唯一支持首尾帧，methods=omni+frames）**；xc700-grok1.5（ch24）/xc700-grok1.0（ch27，⚠时长仅 6/10 两档）/xc100-grok1.5（ch27，⚠仅1图）/xc600-gemini-flash（ch21，⚠固定10s，可纯文生）；全部按秒占位价（2/1.5/3/3.5/13每秒档）+ modeId=xingchen；模型经**补种机制**入库（不 bump MODELS_SEED_VERSION、不冲
- **第132轮**：时长仅 6/10 两档）/xc100-grok1.5（ch27，⚠仅1图）/xc600-gemini-flash（ch21，⚠固定10s，可纯文生）；
- **第132轮**：仅1图）/xc600-gemini-flash（ch21，⚠固定10s，可纯文生）；
- **第132轮**：固定10s，可纯文生）；全部按秒占位价（2/1.5/3/3.5/13每秒档）+ modeId=xingchen；模型经**补种机制**入库（不 bump MODELS_SEED_VERSION、不冲管理端存量改动）。 **③新协议 `aistars-video`**（[aistars.ts](server/src/translators/aistars.ts)）五处注册（Protocol 联合/BUILTIN_PROTOCOLS/VIDEO_DRIVERS/dispatch case/upstream fallback `config.aistars` env AISTARS_*）+ 渠道
- **第132轮**：同一上游模型编码横跨多线且每线一个质量档，凭编码反查线路**不唯一**——线路必须钉在模型定义/routes 里，勿改成动态反查）。
- **第132轮**：反转第110轮「未配模型=报错引导设置」）**：设置弹窗**整删「模型」页**（[SettingsModal.tsx](src/canvas/SettingsModal.tsx) 5 页签；
- **第132轮**：须重新部署服务端**（首启自动补种星辰模式+14 模型；
- **第131轮**：与文档出入，勿按文档回退）**：`GET /v1/models` 24 上游模型；
- **第131轮**：bf 线路当轮对素材恒 invalid_asset**（同 payload 在 my 成功=上游线路问题非我方格式，需运营知悉）。
- **第131轮**：须重新部署服务端**（首启自动迁移；**部署后到管理端「渠道」给「苏打水（简梦）」填 sk- 密钥**（或环境 SUDASHUI_API_KEY）——不填该 16 模型提交报「未配置上游密钥」）**+ 客户端重新打包**（四级下拉/方法/真人图/模式隐藏）。**运营注意**：16 模型为按秒占位价（2/1.5 每秒、gf 高档 4.5-9），上线前在管理端定真实价（可用新「计费方式」切按次）；bf 线路（jm933-sd2.0/fast/real/fast-real）当轮上游对素材报 invalid_asset，恢复前建议先用 my/hn/gf/mo 线路。**待真机 QA**：视频下拉
- **第131轮**：新守卫（勿回退）**：generate/batch 模式 403 门禁对 **hidden 内部计费模型豁免**（[routes.ts](server/src/routes.ts) `md.modeId && !md.hidden`）——防管理端误把 fee-thirdparty 归进模式后、被禁该模式的用户连 LibTV/即梦手续费扣费都 403（同第121轮「hidden 不可禁」规则）；
- **第131轮**：实测环境备注：浏览器测试面板后台标签 rAF 冻结（visibility=hidden）→ framer-motion 退出动画不跑、下拉观感上"不关"——**环境假象非代码 bug**（探针 rafTicksIn1s=0 实锤；
- **第131轮**：只删 UI 不删过滤会留下"存量勾选仍在过滤却无处可改"的坑，故连根拔）：①[settingsStore.ts](src/store/settingsStore.ts) 删 `selectedModels` 状态/`SelectedModels` 类型/`toggleSelectedModel`/`pruneSelectedModels`/save 持久化字段，init 读盘 `delete parsed.selectedModels` 剥存量残留（下次保存即从 settings.json 消失）；
- **第131轮**：跨页守卫：`renderModels` 加 `#m_body` 存在性守卫（模式增删改在渠道页触发它刷新分组时无模型表、原会 null.innerHTML 崩）；
- **第130轮**：须重新部署服务端**（管理端随部署即生效；
- **第130轮**：客户端待做（本轮未做，需重新打包）**：客户端下拉按 `session.user.features.modes` + `catalogModel.modeId` **隐藏被禁模式的模型**（现服务端 403 已拦，未打包=模型仍显示、点了报 403；
- **第129轮**：须重新部署服务端**（首启自动迁移 `logs.jsonl`→`logs-index/`；
- **第118轮**：一张都不许静默丢**：垫图与提示词图例 @ImageN 按位对齐，丢一张=整段图例错位（「只读第一张」观感的帮凶）；
- **第118轮**：排查记录：本机 8787 tsx watch 又跑陈旧代码（第107/112/115/116轮同款），已杀进程树代为重启常驻。
- **第117轮**：规则：**预设正文只在服务端维护，客户端只持有 id + catalog 副本、提交时展开**（与画风同构）。
- **第117轮**：边界：`inferAllShots` 并行触发、快照时各镜结果刚被清空 → 邻镜多回退原文（提供剧本级上下文，连贯结果由手动逐镜推理承载）；
- **第117轮**：规则：**画布任何隐形入参都应有对应明示胶囊；
- **第117轮**：规则：**宫格补丁只补图片，不动视频；已手动含宫格指令则不补**。实测：两端 tsc 干净 + **298 vitest 全过（presetSchemes 13 测，新增互斥组/gridPresetForShotCount 档位+缺档回退/countUnifiedShots/hasGridInstruction）** + vite build 过 + 服务端 catalog 交付实测（4/6/9 宫格 full body 272 字下发，验证后还原用户 data）。**上线前提**：重新部署服务端（补种 4/9 宫格）+ 客户端重新打包。**待真机 QA**：设置「预设方案」加自定义预设并填
- **第117轮**：规则：**连上游文本=明示胶囊（不隐形匹配）；
- **第115轮**：排查记录：用户本机 `npm run dev`（8787）进程又死（第107/112轮同款），已代为重启（后台常驻）。
- **第114轮**：规则：**新增画布文件导入入口一律走 importFilesToCanvas；
- **第114轮**：规则：**素材的资产身份识别一律走 findProjectAssetByImage**（勿再按名字匹配单腿走路）。
- **第113轮**：两条锁定规则：①**同源节点只写 params.prompt、绝不写 resultText**（下游 collectUpstreamText 优先 resultText，写了它编辑 prompt 后下游仍拿旧值）；
- **第113轮**：设计纠偏记录（用户指正，勿回退）：首版曾把同段 unifiedPrompt **复制**进图片/视频两节点的 params.prompt——用户判定不对：同源提示词必须**独立成节点**、图/视频不内置提示词直接吃上游，「修改一个提示词=同时改图片和视频」；
- **第112轮**：语义须与 catalog 售价投影三方一致）；
- **第112轮**：排查记录：用户本机 `npm run dev`（8787）tsx watch 又现「父进程在、子进程不随改动重启」跑旧代码（第107轮同款）——已杀进程树代为重启（后台常驻），冒烟首轮误在旧代码上跑出假失败并留残留（已按 `__tmp112`/`tmp112` 标记定向清理复核）。
- **第110轮**：规则：**任何模型解析路径不得回退 adapters[0]、不得注册假适配器；
- **第109轮**：语义须与客户端 estimateCost、catalog 投影三方一致，函数注释已锁）；
- **第109轮**：模型 id 含点号如 seedance-2.0，DOM 取值一律 getElementById 不走 querySelector）。
- **第109轮**：冒烟事故记录（教训）**：第一轮测试用户挂在已删旧临时商名下 → 覆盖价未生效、一单 480p×15s 空提示词 seedance 真请求按平台价 465 过了 402 预检、真下发到简梦上游（属主已删，失败自动退款为 no-op，成功则产一条无主废片，无法取消）——**计费冒烟一律用小额码让 402 预检拦截，绝不让请求真下发**。
- **第108轮**：规则：**弹窗「匹配资产」永远走宿主 onMatchAssets 委托，不得在 PromptModal 内重写匹配逻辑**（首版曾在弹窗内用 matchAssetsInText+onImport 自拼图例，用户指出重复实现且丢音色，已推翻）。
- **第108轮**：同日曾先按「模板套系配对」（seriesKey/shotMode 标注+按节点语义选变体）实现并全链路实测通过，**用户判定逻辑有误整体回退**（代码零残留；
- **第107轮**：规则：面板组件 hooks 必须全部在 early return 之前）。
- **第107轮**：排查时发现用户本机 `npm run dev`（8787）的 tsx watch 父进程已死（孤儿子进程跑旧代码不再热重启）——已代为重启同命令（后台常驻）。
- **第107轮**：规则：本地 CLI 模型加新款只改 CHOICES 清单一处）。
- **第104轮**：修复**：`mergeTextNodes` 起初只加进 GEOMETRY_COMMANDS 漏了 STRUCTURAL_COMMANDS，导致撤销不回源节点（实测发现，已补 STRUCTURAL_COMMANDS）。
- **第103轮**：事故记录（教训）**：清理测试数据时**误删了一个我没创建的渠道商账号「奇迹/qiji」**（疑似用户自建测试账号，0 用户/模板附着）——违反「不删非自己创建的数据」原则；
- **第103轮**：上轮误删的「奇迹/qiji」渠道商用户已重建，本轮清理测试数据严格按前缀/机器码标记定向删、核对保留了它**。
- **第100轮**：实测临时账号已删。**待真机 QA**：整理后大画布观感、运行环在深色画布上的亮度、拖动挤开手感（枝干范围/级联是否符合直觉）、吸附阈值手感、网格明显度。 - **补充（严格不重叠升级为全局硬约束·浏览器端到端实测过）**：用户定：不开「重叠」时**整个画布任何时刻不允许重叠**——冲突按**节点生成时间**裁决（晚生成留位、早生成让位）、同枝干整体走、允许多级级联（A 挤开 B 时把 C/D/E/F 链条一起挤开）。①新纯函数 `enforceNoOverlap`（[branchPush.ts](src/lib/branchPush.ts)）：顶层节点两两查**实际相交**，每轮取一对
- **第100轮**：已知边界：媒体加载自动贴合比例的 resize 走非命令路径（§9
- **第100轮**：echo 模型/模板/临时账号已删。**待真机 QA**：真实推理下多行裂变把下方旧流水线逐批推开的观感、新视频节点面板时长显示 15s。 - **补充（吸附参考线「看不到」修复·真机报障→浏览器埋点定位）**：用户报吸附生效但看不到对齐参考线。**主根因**（埋点实锤：写入日志 `x@400, null, x@400, null…` 交替）：React Flow 同一次拖动移动会发**两拨 onNodesChange**（第二拨无位置变更），[useCanvasDrag.ts](src/canvas/hooks/useCanvasDrag.ts) 此前每次回调都无条件 `setGuid
- **第100轮**：临时账号已删。**待真机 QA**：真实画布上拖动对齐的线是否够醒目、低缩放（<0.5）下粗细观感。 - **补充（拖动挤开「节点丢失」修复·真机截图报障→浏览器复现定位）**：用户报「拖动挤开时可能丢失节点不显示」。**根因**（纯函数复现实锤）：拖动矩形（多选整行/大节点很容易）**同时压住两行**时——主目标行与追加目标行都按同一「让出拖动矩形」公式（`clearDy`）计算落点 → **两行被推到完全相同的坐标**（实测 r2/r3 两整行全部叠在 y=214），而级联只处理「已让位 vs 未让位」的碰撞、**两条已让位枝干互相重叠永远不会被分开**——完全重叠的行看起来就是"丢
- **第100轮**：存量项目里已被叠住的行：节点没丢，框选拖开或「整理」即可复原。
- **第100轮**：实测临时账号已删。**待真机 QA**：Tauri（WebView2）下 QQ/微信截图直接粘贴进简一助手、画布 Ctrl+C 图片节点后粘贴到简一助手/外部软件（微信/画图）能出图、asset:// 本地图节点复制后可粘贴。 - **补充（分镜原文节点自跑重推理·echo 模型端到端实测过）**：「分镜n原文」节点（标题匹配 `SHOT_SCRIPT_TITLE_RE` 且上游也是智能推理节点）再次运行推理时——它自己就是原文节点：①卡片**不再建原文节点**，只裂变 分镜n故事板/分镜n视频 **接到自身**（n 沿用自己标题里的分镜号；[canvasSpawn.ts](src/li
- **第100轮**：存量画布已有的 text.seed 原文行不自动改型（改型=删行重跑或整节点重新推理）。
- **第100轮**：绝不给受控 edges 传 selected / 调 addSelectedEdges**：RF 内部选择状态与 prop 里的 selected 互相覆盖 → StoreUpdater setEdges 的 Maximum update depth 死循环（实测踩坑，教训入注释）；
- **第100轮**：实测教训（工具链）：**vite dev 下 HMR 过的模块带 ?t= 时间戳，eval 动态 import 无时间戳路径会拿到旧模块实例（store 双实例假象）——改完 store 代码做浏览器注入实测前必须重启 dev server**。
- **第100轮**：存量画布（改动前裂变的）无主体→变体边，二次解析不补边（节点已存在即跳过）；
- **第99轮**：经验：**投影节点+提交快照=项目文件的隐性备份**，commits 之后字段丢失时可从画布反推；
- **第98轮**：规则：**新增可共享设置只改 SHARED_PARAM_KEYS 一处；
- **第98轮**：端到端出卡依赖服务端模板产出 duration 字段，**待模板更新后真机 QA**：推理后分镜行时长显示各卡值、画布视频节点时长参数=卡值。
- **第98轮**：实测临时账号已删。 - **补充（画布尾卡「漏图漏视频」根因修复·真机截图定位）**：智能推理裂变最后一卡只出文本节点、图片/视频节点缺失。根因=**尾卡假闭合竞态**（第84轮起就有，窗口窄这次被踩中）：闭合判定「两次有增长的快照间签名不变=已闭合」，但卡片字段有顺序（原文在前、提示词在后）——尾卡在「原文已闭合、`storyboard_prompts` **键已出、值未出**」的空窗里签名恰好不变 → 误判闭合当场裂变（只有原文→只建文本节点），且 key 进 `spawnedKeys` 去重集后 **final 补齐也永远跳过**。资产拆分天然免疫（imagePrompt 是最后字
- **第98轮**：已裂变残缺的存量画布行不会自动补齐（key 无从追溯），该排节点手动补建或整节点重跑（重跑=整排重新裂变）。
- **第98轮**：维护注意：canvasSpawn.ts 的 key/sig 模板串里有**不可见 `\x01` 分隔符**（防字段拼接歧义），编辑工具精确匹配这两行会失败，需用脚本替换。
- **第98轮**：实测用临时账号（admin API 建/删，已清理；
- **第97轮**：规则：**媒体加载触发的自动布局调整绝不走结构命令**。
- **第95轮**：规则：**节点体内 absolute 出界元素会被裁，堆叠/角饰类观感一律画在 `.Qiji-node` 卡片层**。
- **第95轮**：用户侧注意：**dev 会话经 HMR 半更新（zustand store 模块被替换）可能出现新旧 store 分裂，改完代码后需整页刷新/重启 dev 再测**。
- **第95轮**：性能规则：**store 批量变更必须单次 setState**）：拖 N 个节点时 rAF 批次此前逐个 moveNode=每帧 N 次 store 通知（Canvas/小地图/多选工具栏各重算 N 遍）→ 合并单次 setState（[useCanvasDrag.ts](src/canvas/hooks/useCanvasDrag.ts)）；
- **第94轮**：规则：**新增模型下拉一律用 useCapModelOptions / getChannelModelsForNodeType，不得直接读 catalog.models**。
- **第94轮**：排查时在用户账号建了一张「Qiji-测试-可删除」画布（CLI 无画布删除命令），请到 LibTV 网页端手动删。
- **第94轮**：规则：**对 LibTV 永远按 modelKey 定位模型，modelName 只在发命令时即时反查**。
- **第94轮**：规则：**LibTV 结果以节点 data.url 为权威，--run stdout 只当快路径**。
- **第94轮**：规则：**LibTV 资源节点名必须带 tag 编号稳定可反查；
- **第93轮**：规则：**受控 React Flow 的 select 变化必须在 onNodesChange 落地、wrapper 必须携带 selected**，任何丢弃都会让选中态滞后/脱同步。
- **第90轮**：规则补充：**blob.url 字段只允许真公网 url；
- **第90轮**：规则：**`http://*.localhost` 直链是本地文件的显示态，显示层一律直用，任何「按 url 换源/下载」逻辑必须排除它**（`localhost:8787` 服务端 /raw 不在此列，仍走下载落地）。
- **第89轮**：规则：**带 transform 的容器内不得直接渲染 position:fixed 弹层，一律 portal 到 body**。

---

## 8. 变更记录（第168轮起全文保留；更早只留结论摘要）

- **2026-08-20 第240轮（AI 工作台二轮迭代：双页签入标题栏 + 自由占位编辑移入工作台 + 工作台四栏样式对齐表格模式·纯客户端 · client tsc 干净 + 1264 vitest 全过（1250→1264，+14）+ vite build 过 · 双 worktree 并行子代理（wt/shell 43a6283 + wt/wb bc247b8）顺序合并 · 须客户端重新打包、服务端零改动）**——第239轮真机截图三反馈：①「将顶部AI工作台和预览收到上面的标题栏里面，放这里影响空间」；②「将属性里面的填写提示词的删除」（截图圈=自由占位 RtcFreeGenProps 的提示词+垫素材）；③「将ai工作台增加图2和图3所示四栏样式」（图3=表格模式分镜行提示词区实拍样板；箭头语义=原文气泡下移原文栏、故事板图归左上）。
  ①**页签入标题栏**：RtcCenterStage 顶部整行 h-8 nav 整删；新 [RtcCenterTabSwitch.tsx](src/rtc/panel/RtcCenterTabSwitch.tsx)（h-5 segmented 双按钮）经 [FrameEditor.tsx](src/views/FrameEditor.tsx) 中栏 `headerExtra={<><RtcEpisodeSwitcher/><RtcCenterTabSwitch/></>}` 挂进标题栏；⚠ 内部控件 draggable=false + mousedown 不冒泡（照 RtcEpisodeSwitcher，防标题栏拖拽换位冲突）。叠层显隐/初始页签/自动切换 hook 原样。
  ②**自由结果占位收敛**：[RtcFreeGenProps.tsx](src/rtc/panel/RtcFreeGenProps.tsx) 删 提示词 DraftArea+垫素材 RefStrip，保留 占位身份/RtcTimeFields 时间码/originSegId 提示/音频不支持块/ModelPicker/参数说明/开始生成/进度/失败重试 + 紫色引导小字；编辑面移入新 [RtcFreeGenWorkbench.tsx](src/rtc/panel/RtcFreeGenWorkbench.tsx)（左窄列=身份/时间窗/状态/参数摘要，右大列=提示词 DraftArea minHeight 30vh 灯箱式+PromptExpandButton+RefStrip 垫素材（application/x-qiji-asset 拖拽+本地文件懒上传）+开始生成/重试）——逻辑零重写（useRtcFreeGenStore.patch/startFreeGen/retryFreeGen/片段自身 status·progress 驱动原样），DraftArea/RefStrip 抽 [freeGenParts.tsx](src/rtc/panel/freeGenParts.tsx) 两处共用。**分派接入**：RtcCenterStage 新 WorkbenchBody——placeholder 且无 shotRef → RtcFreeGenWorkbench（key=segId），其余（含无选中）→ RtcShotAiWorkbench。**页签规则 1 扩展**（rtcCenterTabCore）：isShotPlaceholder 判定改 `segKind==="placeholder"`（任何占位符新选中→工作台）；自由占位成片替换→预览由规则 3 天然覆盖（补显式单测）。
  ③**工作台四栏样式**（[RtcShotAiWorkbench.tsx](src/rtc/panel/RtcShotAiWorkbench.tsx) 主改）：
  　⑴**提示词区两行头**（与 Frame161195 1768-1850 行同构）：第一行=同源胶囊/故事板·视频页签 + ▦预设方案（ShotPromptField 新 renderHeader/presetLabel prop 挪位，下拉逻辑原线）+ **补镜头**开关（切 shot.isSupplement → setEpisodeShots(reindexShots(...))+save，与表格模式 commitShots 同尺）；第二行=**仅本分镜**参数条（title 注明）：家族→线路→模型（写 `overrides.videoModelKey`，换模型旧覆盖按新档 clamp 收敛同 Frame161195 算法）/方法（modelMethods>1 才显，`overrides.method`）/时长（**shot.durationSec** 非 overrides——照表格模式）/比例·分辨率（`overrides.aspect/resolution`）/真人图（officialAssets+omni+有图，`overrides.officialAssetIndexes`）+ 行尾 PromptExpandButton——⚠ **写入字段=genShotVideo 读取字段逐项核实**，档位收敛走 videoReqOptions/clampToOptions/clampDurationTo/clampMethod 一把尺。右栏「AI 生成属性」仍是项目级默认，两层语义与表格模式一致。
  　⑵**补镜头重排收编**：算法抽 [shotReindex.ts](src/lib/shotReindex.ts)（普通镜 1,2,3…/补镜派生 3-1、3-2/首镜为补降级主镜），[Frame161195.tsx](src/views/Frame161195.tsx) 与 [inferRun.ts](src/services/inferRun.ts) 的本地实现替换为共用 import——三处不再分叉。
  　⑶**原文气泡渲染**：新纯逻辑 [scriptBubbles.ts](src/lib/scriptBubbles.ts)——按行拆（空行跳过），`▲/△/（/(` 开头=action（整行浅灰卡）、`人名：台词`（≤6 字中文/字母数字+全半角冒号）=dialogue（人名着色加粗，10 色亮系 charCode 滚动哈希同名恒同色）、其余 plain；工作台原文栏改气泡列表，**右键进 textarea 编辑整段**语义保留、空态保留。
  　⑷左列 故事板(上)/原文(下) **分界可拖**（10px 横柄 pointer capture，30-70% 夹取，会话本地态不持久化）。
  实测：集成后 client tsc 干净 + **1264 vitest 全过**（118 文件；+1 页签规则 +9 scriptBubbles +4 shotReindex）+ vite build 过；两分支文件面零交叠零冲突。⚠ **须客户端重新打包**；服务端零改动。**已知边界**：①工作台时长下拉写 durationSec 不改占位符片段 target 时长（第235轮语义：时长不符留给用户裁剪）；②气泡人名判定对「英文词+冒号」前缀会误判 dialogue（剧本文本罕见）；③分界拖动/页签均会话态不持久化。**待真机 QA**：标题栏页签与分集切换器并排观感（窄面板换行？）、两行头改参数后真单提交 overrides 生效实锤、补镜头重排在时间轴/表格两侧联动、自由占位在工作台编辑提示词→开始生成全链、原文气泡着色观感。
　**补充（工作台默认宽度三调·用户截图定稿 · client tsc 干净 + 1265 vitest(+1) 全过 + vite build 过）**——①**素材面板默认宽 300→380**（[rtcLayoutCore.ts](src/rtc/layout/rtcLayoutCore.ts) ASSET_W def=380/max=640；⚠ **存量迁移**：normalizeLayout 对落盘值恰为老默认 300 的 assetWidth 自动升级新默认（ASSET_W_LEGACY_DEFS 清单，改默认值时把旧默认追加进去），用户手动拖过的值原样保留——单测锁定）；②**工作台左列拉宽**：`flex-basis: clamp(300px, 42%, 560px)` 随工作台宽自适应；③**右列不整列滑动**：ShotPromptField 新 `fill` 模式（[shotWorkbenchParts.tsx](src/rtc/panel/shotWorkbenchParts.tsx)——栏位 flex:1 吃满剩余高、编辑区有界 overflowY:auto=**超长提示词框内滚动收起**，右栏「AI 生成属性」不传 fill 观感不变）+ 视频历史条 maxHeight 100 内部滚；右列 overflowY:auto 仅作极小视口兜底。
　**补充5（自由占位工作台统一三栏·用户定稿「视频占位和图片占位也要三栏，三栏是 AI 工作台的基本布局，不要出现其他布局，没有原文就空着」 · client tsc 干净 + 1268 vitest 全过 + vite build 过）**——参照列外壳抽共享件 [shotWorkbenchParts.tsx](src/rtc/panel/shotWorkbenchParts.tsx) `WorkbenchRefColumn`（clamp 宽/order:2 居右/可拖分界 30-70%，分镜与自由占位两工作台共用同一壳，⚠ 勿再出现两栏等其它布局）；[RtcFreeGenWorkbench.tsx](src/rtc/panel/RtcFreeGenWorkbench.tsx) 重排为与分镜工作台同骨架：提示词列（头部身份→垫素材→提示词 fill 大编辑→动作/进度/失败状态→参数说明）+ 参照列（上=「结果预览」空态说明（含新版本占位提示）、下=「原文对照」**空着**——自由占位无关联分镜，栏位保留）；freeGenParts.DraftArea 加 fill 模式（吃满剩余高框内滚动收起）；原两栏（身份窄列）布局删除。音频占位（无生成能力）警示块在提示词列，三栏骨架不变。
　**补充4（⚠ 时间轨滚轮快捷键「过一段时间失效、重开才恢复」根因修复·用户实报 · client tsc 干净 + 1268 vitest 全过 + vite build 过）**——根因：[RtcTimeline.tsx](src/rtc/RtcTimeline.tsx) 的 Ctrl/Alt+滚轮监听与视口宽 ResizeObserver 都是 `useEffect([])` **一次性**绑在 `scrollRef.current` 上，而「正在载入剪辑文档…」早退分支会把滚动容器**卸载重建**（doc 瞬时为空：切分集/换项目/子层瞬态）——监听留在死节点上=滚轮快捷键永久失效、重开页面才恢复。⚠ 修法（勿回退成 `[]` 一次性绑定）：回调 ref `setScrollNode` 同步 scrollRef+`scrollEl` state，wheel 与 ResizeObserver 两个 effect 改依赖 `[scrollEl]`——节点重建即自动重挂；scrollRef 保留供事件时现读（attentionRef/eventUs 等，React 重挂自动更新不受影响）。全库扫 addEventListener 仅此一处此模式。
　**补充3（工作台跟随播放头·用户定稿「默认显示当前时间的 ai 界面：这个时间点有结果就显示结果，没有结果就显示 AI 工作台（三栏常显、原文肯定有，不要黑屏）」 · client tsc 干净 + 1268 vitest(+3) 全过 + vite build 过）**——①**页签规则 6「播放头跟随」**（[rtcCenterTabCore.ts](src/rtc/panel/rtcCenterTabCore.ts) 新纯函数 `mainTrackSegAt`（主轨=第一条 video 轨、区间右开）+ 快照新增 phSegId/phSegKind）：播放头下主轨片段变化（进入新片段/所在占位被成片原位替换）→ 占位符=切工作台、media/compound=切预览；空白处不动；**选择类信号（规则 1/2/3）同拍先判**；初始页签（规则 4）也改为优先按播放头处片段定、空白按 doc 有无可播片段兜底；②**工作台绑定回退播放头**（[useRtcSelected.ts](src/rtc/panel/useRtcSelected.ts) 新 `useWorkbenchTarget`：选中占位符优先、无选中回退播放头下主轨占位符——⚠ 播放头选择器只返回 doc 里的稳定 seg 引用/标量，帧级 playheadUs 变化零重渲染）：RtcCenterStage.WorkbenchBody 分派与 RtcShotAiWorkbench 绑定都改走它——播放头停在待生成片段上即直接三栏显示该镜（原文/故事板/提示词），「未选中」引导只在播放头处无占位符且无选中时出现。
　**补充2（工作台左右两列互换·用户定稿「提示词放左边，和素材靠近点」 · client tsc 干净 + 1265 vitest 全过 + vite build 过）**——[RtcShotAiWorkbench.tsx](src/rtc/panel/RtcShotAiWorkbench.tsx) 提示词工作区列 `order:1` 居左（紧邻素材面板）、参照列（故事板预览+原文气泡，LeftColumn 改名 RefColumn）`order:2` 居右；[RtcFreeGenWorkbench.tsx](src/rtc/panel/RtcFreeGenWorkbench.tsx) 同规则（提示词大列居左、身份/状态窄列居右）。**CSS order 互换、JSX 顺序不动**（与面板换位同款手法，零子树重建）。
- **2026-08-19 第239轮（实时剪辑两连：倍速联动时长修复 + 中栏「AI 工作台 / 预览」双页签·纯客户端 · client tsc 干净 + 1250 vitest 全过（1231→1250，+19）+ vite build 过 · 双 worktree 并行子代理（wt/speed + wt/aiwb）顺序合并 · 须客户端重新打包、服务端零改动）**——用户两点：①「把倍速的时长对准——目前 10 秒倍数 2 还是占用 10 秒时间轨，播放到 5 秒后面就出错；10 秒的 2 倍应该就是 5 秒长度」；②实时剪辑 AI 生成设计定稿（中栏双模式 + 工作台三栏 + 右栏收敛为生成属性），并令多 agent 并行处理。
  ①**倍速联动时长**（commit a580ae6）：根因=变速走 `commitSegmentPatch({speed})` 纯 patch，targetDurationUs 不动、`sourceDur≈targetDur×speed` 不变量被破坏（trim/split/导出都按它换算），源耗尽后播放出错。⚠ 修法（勿回退）：[rtcOps.ts](src/lib/rtcOps.ts) 新纯函数 **`setSegmentSpeed(doc, segId, speed)`**——speed 夹 0.1–5、=1 删字段（默认不落键）、同值/非 media/未命中=原 doc 引用；**带 source 窗口**：newTargetDur=round(sourceDur/speed) → 钳位（MIN_SEGMENT_US 下限 + 右缘不越后一片段左缘）→ **恒回写 sourceDurationUs=round(newTargetDur×speed)**（未钳位也回写，round 往返以不变量精确为准；减速被空隙钳短=尾部源内容暂不展示，trim 右缘可拉回，sourceStartUs 不动）；**无 source 窗口**（图片）targetDur 按 oldSpeed/newSpeed 等比缩放同钳位；keyframes 不动（t 相对 target 起点，超出新时长渲染时按端值钳制——变速回去帧还在，比删帧可逆）。写入路径全收口：SpeedInput（[RtcMediaProps.tsx](src/rtc/panel/RtcMediaProps.tsx)）走新 `commitSegmentSpeed`（[rtcSegUtils.ts](src/rtc/panel/rtcSegUtils.ts)，经 commitActive 感知复合子层）；属性剪贴板（[rtcAttrClipboard.ts](src/rtc/rtcAttrClipboard.ts)）speed 从 patchNum 摘出逐 id 链 setSegmentSpeed（行为变化：粘到 compound 上 speed 不再生效——setSegmentSpeed 只认 media，与面板一致）；其余 grep 命中均为创建时一致性复制（audioSegmentFor/片段剪贴板，speed 与 source 窗口成对复制不破坏不变量）或只读消费，不动。播放器防御：[rtcPlayback.ts](src/rtc/rtcPlayback.ts) sourceTimeSec 本就钳源右缘 + RtcSequencePlayer 150ms 漂移校正 → 存量失配片段（bug 期间改过速的旧 doc）源耗尽后定住尾帧不出错，补注释固化（勿删此钳）+ 失配场景测试。导出核对：jianyingDraft 直接消费 seg.speed 与两窗口，不变量成立即自洽，零改动。+10 测试（setSegmentSpeed 7 / 剪贴板 2 / 播放防御 1）。
  ②**中栏双页签「AI 工作台 / 预览」**（commit f9d311f）：
  　⑴**页签壳**（[RtcCenterStage.tsx](src/rtc/RtcCenterStage.tsx)）：顶部双页签 nav（观感对齐右栏三页签）；⚠ **RtcSequencePlayer 保持常驻挂载**（第236轮红线）——「AI 工作台」=absolute inset 不透明叠层，AssetPreviewLayer 只在「预览」页条件渲染。自动切换纯函数核 [rtcCenterTabCore.ts](src/rtc/panel/rtcCenterTabCore.ts)（+store，会话级不持久化；9 单测锁定）：新选中占位符（placeholder+shotRef）→工作台（同一选中不重复切/取消不切/自由占位与 media 不切）；新选中左栏资产卡或媒体卡→预览（两通道各自比较）；同 segId placeholder→media（成片替换保 id）→预览（最先判）；初始=doc 有任何 media/compound 片段?预览:工作台（用户定「当没有结果时默认显示 ai 工作台」）；手动点页签永远生效。同拍双新选中以时间轴片段为准。
  　⑵**工作台正文**（新 [RtcShotAiWorkbench.tsx](src/rtc/panel/RtcShotAiWorkbench.tsx)，绑定选中占位符；无选中=引导文案）三栏：左列 上=**故事板预览**（点击 openLightbox + HistoryGrid「设为当前」→update({storyboardUri})）、下=**原文对照**（**锁定只读、onContextMenu 右键进编辑**（textarea+保存/取消→updateShot({scriptSegment})，标题注明「右键编辑」——用户定稿语义）；右列（灯箱式，参考 PromptModal）：垫图素材（RtcMaterialStrip+匹配资产）→ 提示词页签（imgVideoSameSource 同源=单栏「同源提示词」/否则 故事板·视频 小页签）→ ShotPromptField（editorMinHeight 36vh；@素材/#导入/▦预设/放大弹窗全委托一字不变）→ 动作行 推理提示词/生成故事板/生成视频（genShotVideo 带 swapSegId）+ JobChips×2 → 视频历史（设为当前→update({videoUri, videoActiveKey})）。生成红线不变：只走 shotGenActions 三函数。
  　⑶**右栏收敛「AI 生成属性」**（[RtcShotWorkbench.tsx](src/rtc/panel/RtcShotWorkbench.tsx) 重写，原文/提示词/垫图/动作/历史区块全部移入中栏）：分镜身份头+引导小字；**生图要求**=ModelPicker cap=image + 比例（档位=IMG_SIZE 表键，shotGenActions 加 export 作单一来源）+ 分辨率（imageResolutionOptions(生效模型) catalog 控档+clampImageResolution，照抄 Frame161195）+ 画质；**生视频要求**=ModelPicker cap=video + 方法（modelMethods>1 才显）+ 时长/分辨率/比例（videoReqOptions(生效模型)，模型无档不显示）+ 附带项（genWithAsset/genWithStory）；图视同源开关（注明项目级、与表格模式同步）；JobChips。**setter 全走 setMediaSettings**（Frame161195 同一函数零新状态）；刻意不做换模型回写收敛 effect（表格页已承担，防双处回写；本页显示层 clamp+提交层 shotGenActions 同尺 clamp，无越档风险）。
  　⑷**共享件抽取**：[shotWorkbenchParts.tsx](src/rtc/panel/shotWorkbenchParts.tsx)——ShotPromptField/JobChips/HistoryGrid/DraftArea/useShotJobs/useShotInferring 自原 RtcShotWorkbench **原样抽出零重写**，中栏工作台与右栏都从这里 import（唯一扩展=ShotPromptField 可选 editorMinHeight，缺省 92 保旧观感）。
  实测：集成后（merge ade73a7）client tsc 干净 + **1250 vitest 全过**（116 文件）+ vite build 过；两分支文件面零交叠零冲突。⚠ **须客户端重新打包**；服务端零改动无需部署。**已知边界**：①自由结果占位（无 shotRef，RtcFreeGenProps）未纳入中栏工作台——仍在右栏属性页编辑，选中它不触发页签自动切换；②页签初始规则会话内只定一次（与 rtcPropsTabStore 同现状）；③属性剪贴板粘 speed 到 compound 不再生效（防同 bug 复发，单测锁定）。**待真机 QA**：双页签切换手感与播放不断流实锤、右键编辑原文在 WebView2 的表现、变速后时间轴块长度与播放对齐、存量失配片段（bug 期间改过速的）定尾帧观感、右栏参数改动在表格模式的同步观感。
- **2026-08-19 第238轮（实时剪辑大版本：选中框零测量 + 复合片段逆向 + 四批并行交付 · client tsc 干净 + 1189 vitest 全过（+170）+ vite build 过 + cargo check 过 · 须客户端重新打包、服务端零改动）**——本轮三段式：修复→逆向→并行开发。
  ①**预览选中框错位修复（用户截图报障「预览白框与实际边界不一致」）**：图层画面是纯 CSS 排版（容器查询画幅框+object-fit:contain+百分比 transform），选中框却按 ResizeObserver 实测像素画——WebView2 测量滞后/取整 → 框比画面大一圈（左上贴合右下悬空）。⚠ 修法（勿回退）：[rtcTransform.ts](src/rtc/rtcTransform.ts) 新 `containFrac(画幅比, 自然尺寸)` 零测量比例基准；SelectionOverlay 全百分比几何（与画面同一套 CSS 排版逐像素恒等）；拖动解算改**手势开始现读 getBoundingClientRect**（勿用缓存态）；frameRect 只剩缩放读数一个消费点。
  ②**剪映复合片段格式逆向（探针 v1→v4 真机二分，全绿灯）**：格式定稿见 [docs/剪映复合片段草稿结构.md](docs/剪映复合片段草稿结构.md)（唯一事实来源，pyJianYingDraft 不支持复合片段勿凭它猜）。要点：主时间轴复合段=普通 video 片段（12 键精简形态+source:"segmentsourcenormal"）+ materials.videos 虚拟素材（path 空+extra_type_option:2）+ extra_material_refs **8 组伴生素材**（drafts→speeds→placeholder_infos→hsl→canvases→sound→color→vocal；v1/v2 只挂 speeds=「认得出但不渲染」的病根）+ drafts 素材内联完整子时间轴（渲染真源；subdraft 文件夹服务双击编辑）；子草稿全明文（主草稿加密无碍——我们写明文剪映认）；⚠ 剪映打开过的草稿会**重存为加密**，对照实验必须换新草稿文件夹。顺带：导出器补 **draft_meta_info 素材登记**（[jianyingDraft.ts](src/lib/jianyingDraft.ts)，type:0 桶逐素材；metetype 是剪映原文拼写勿改）——修「导出草稿素材栏为空」。
  ③**四批并行开发与集成**（用户令「调用子agent多线开工1234」）：临时索引快照基线 `7e1f32d`（不动工作区不动分支）+ 4 个 git worktree（junction node_modules）各跑一个子代理，契约=keymap/工具条/右键菜单归批1独占、批2-4 交「待接线清单」、共享文件只追加；集成 worktree 顺序合并（1→2→3→4）+ 统一接线。交付清单：
  　**批1**：[rtcKeymap.ts](src/rtc/timeline/rtcKeymap.ts) 重写**两层表**（RTC_ACTION_DEFS+localStorage 覆盖层，键位与说明从生效表单一派生）；[RtcSettingsModal.tsx](src/rtc/settings/RtcSettingsModal.tsx) 设置二级界面（快捷键三分组点击录制改键/冲突自动改绑/恢复默认 + 剪辑参数 bigStepFrames=10·bigValueStep=10·imageDefaultSec=3.0 + 预览五项收编；打开期间快捷键挂起）；新动作全套（批量分割/两侧全选/裁到播放头/分割点跳转（专用 cutPoints 不混整秒刻度）/大幅移动/镜像/旋转90°/组合 groupId 选中扩散/属性剪贴板/替换片段右键入口）；90s 初始轴、Alt+滚轮水平滚动。整组拖动未做（选中/删除/复制对整组生效）。
  　**批2**：doc 级标记 markers（6 色小旗、标尺双击添加/右键换色备注删）；定格 freezeAtPlayhead（抽帧→LC 资产→原片段切开插 3s 图片段，insertFreezeFrame 关键帧分账）；**关键帧**（RtcSegment.keyframes：x/y/scale/rotation/opacity/volume，t=相对 target 起点；[rtcKeyframes.ts](src/lib/rtcKeyframes.ts) 线性插值+effectiveTransformAt/VolumeAt；播放器逐帧生效+选中框跟随；面板菱形按钮=有帧时改值写帧；导出 common_keyframes 与 toJyClip 同一把换算尺）。
  　**批3**：倒放（Rust 新 `reverse_media` ffmpeg 转码副本+会话缓存+还原镜像换算；桌面限定）；画面裁剪（RtcCrop 四边内缩+[RtcCropEditor.tsx](src/rtc/panel/RtcCropEditor.tsx) 拖动编辑器+预览 clip-path contain 折算+导出 material 克隆条目（去重例外：带 crop 片段一段一条、文件仍只拷一份））；**字幕轨真可用**（RtcSubtitle+时间轴角块「＋字幕」/text 轨双击+RtcTextProps 编辑+预览字幕层+texts material 真导出）；转场（[jyTransitions.ts](src/lib/jyTransitions.ts) 18 条资源表+面板下拉+导出；预览不渲染导出生效）。
  　**批4**：**复合片段全链路**——kind:"compound"+RtcDoc.subDocs 嵌套模型（深度 1；split 共享 subDocId 绝不复制）；[rtcCompound.ts](src/lib/rtcCompound.ts) createCompound/dissolveCompound；rtcStore editingSubDocId+enterCompound/exitCompound+**commitActive**（子层 mutator 写回 subDocs，undo/落盘/项目守卫共用一条链）；双击进入+面包屑返回；[rtcPlayback.ts](src/rtc/rtcPlayback.ts) 递归展开（槽位=`${compoundSegId}/${子trackId}` 保图层元素常驻复用铁律）；导出照 ② 文档（[jianyingCompound.ts](src/lib/jianyingCompound.ts) 三件套+wrapper+config+松散副本）；Rust `jianying_write_draft_file` 白名单扩展 subdraft 路径（uuid 校验+ensure_under 双向限定）。
  　**集成接线**：keymap 新 10 动作（M/Alt+M/Shift+M/Alt+Shift+M/G/T/D/C/Alt+G/Alt+Shift+G）+keydown 派发+NEEDS_SELECTION；右键菜单补 定格/倒放/裁剪画面/新建复合/进入复合编辑/解除复合；⚠ **rtcEditActions 全部动作改 activeRtcDoc/commitActive**（子层编辑键盘可用；主层同义零回归）；⚠ **音量收口 rtcPlayback 源头**（关键帧采样+复合宿主乘积在图层/音频条目产出处算好，播放器只消费 volume 字段——子层时间基天然正确）；键盘 C 裁剪经新 [cropEditorStore.ts](src/rtc/panel/cropEditorStore.ts) 请求通道打开面板编辑器；工具条加轨改 commitActive。
  ⚠ **须客户端重新打包**；服务端零改动。**已知边界**：组合整组拖动未做；关键帧不可拖动（点击跳转）；通用 splitSegment 不做关键帧分账（两半各带整份，渲染钳制）；倒放桌面限定+长片转码慢；裁剪后选中框不随 crop 收缩；转场/复合导出的部分字段面待真机复核（复合 wrapper 用 5.9 骨架、音频子轨内联形态样本未覆盖）；纯音频复合导出形态未验证。**待真机 QA**：设置弹窗改键往返、全部新键位真机手感、定格/倒放真单、关键帧动画预览与导出剪映实锤、字幕/转场/裁剪导出剪映实锤、复合片段全流程（Alt+G→双击进出→导出草稿剪映识别）、含复合+关键帧+字幕的综合草稿导入。**基建留档**：快照 `7e1f32d`、四批提交 db0cae6/ab090fd/f465a92/94b3ad6、集成 `../qiji-wt/integrate`（worktree 验证后可 `git worktree remove` 清理）。
　**补充（预览框直接点选素材·用户追加 · client tsc 干净 + 1193 vitest(+4) 全过 + vite build 过）**——画幅框内点按=自上而下命中检测（[rtcTransform.ts](src/rtc/rtcTransform.ts) 新纯函数 `pointInSegBox`，与选中框同一套几何；忽略 crop 热区收缩）：普通画面片段=选中并**同一手势直接进入移动拖拽**（DragCtx 同构）；复合子层命中=选中**宿主复合段**（只选不拖）；锁定轨只选不拖；点空白=清空选中。⚠ 顺带修集成引入的时间基错配：渲染/选中框对复合子层的关键帧相对时刻原用 `frameUs−子层targetStartUs`（主时间基减子起点）——[rtcPlayback.ts](src/rtc/rtcPlayback.ts) RtcVideoLayer 新增 **`kfRelUs`**（顶层=主时间基、子层=子时间基，产出处算好），渲染/选中框/命中测试三处统一消费，勿在消费端自算。
　**补充2（时间轴滚轮三态定稿 + 主轨常驻·用户定 · client tsc 干净 + 1193 vitest 全过 + vite build 过）**——[RtcTimeline.tsx](src/rtc/RtcTimeline.tsx)：**裸滚轮=轨道区上下滚动**（原生）、**Ctrl+滚轮=缩放时间轴**（指针为锚，既有）、**Alt+滚轮=时间线前后滚动**（既有）；⚠ 裸滚轮=**显式接管只做纵向**（勿回退成放行原生——Chromium 对只有横向溢出的容器会把竖轮映射成横滚，轨道少时裸滚轮就横着跑，用户实测打回过一次）；**主轨 sticky 常驻**（top=RULER_H/bottom:0/z-22 介于轨道头20与标尺30之间、不透明底、行内自带一段播放头线——全局线 z-10 在钉住的主轨之下）。已知边界：主轨被钉离原位时，拖放素材/缝隙建轨的命中按布局位置算（视觉位移期间往主轨拖放以其原布局行为准）。
　**补充3（拖动跟手+白色落点预览 + 空轨自动回收·用户定 · client tsc 干净 + 1198 vitest(+5) 全过 + vite build 过）**——①**拖动观感重做**（[RtcTimeline.tsx](src/rtc/RtcTimeline.tsx)）：「闪来闪去」病根=把 moveSegment 的夹隙结果直接当预览。改三件套：原片段从预览「抬起」（removeSegments 只作预览、落笔仍从真 doc 算）+ **幽灵条跟手**（吸附后的指针时间横向 + **纵向逐像素跟指针**——首版按行量化+钳行被用户实测打回「还是闪来闪去」，勿回退成 ROW_H 取整）+ **白色半透明落点预览**（moveSegment 试算的合法位置=松手真正落下的地方；悬停缝隙建新轨时不画；z 23/24 盖过 sticky 主轨 22）。②**空轨自动回收**（[rtcOps.ts](src/lib/rtcOps.ts) 新纯函数 `pruneEmptyTracks`，5 单测）：片段被 移走/删除/剪切/波纹删/并入复合/解散复合 后留下的空轨自动消失；⚠ 三类空轨保留——**主轨（第一条 video 轨）**、**锁定轨**、全删剩零时留第一条；⚠ **只挂在移出/删除类动作收尾**（rtcEditActions 三处、RtcTimeline 拖动落笔两处+右键删除、compoundActions 两处），**勿挂进 commit 管道**——工具栏「+轨道」刚建的空轨会被当场收走。
　**补充5（RTC 分集化 + 左面板五分页·用户追加大改 · client tsc 干净 + 1204 vitest 全过（+6：分集换档 4 + 分集过滤 2）+ vite build 过 · 纯客户端须重新打包）**——用户参照剪映定稿两件事：①**每分集独立时间轴（分集化）**：projectStore `rtcDoc` 单文档退役 → **`rtcDocs: Record<分集id, RtcDoc>` 单一真相** + `rtcEpisodeId`（窗口本地、随项目持久化）；⚠ **回写唯一路径 `setRtcEpisodeDoc(epId, doc)`**——rtcStore 记 `ownerEpisodeKey`（loadDoc 时定），commit/undo/redo 恒写**doc 归属的分集档位**（即便已切集也写旧集档位=天然不串集；分集已删则丢弃不建孤儿档）；rtcStore 模块订阅三分派（换项目/换分集=全量重置换档（选区/播放头/undo 全清）、同集档位外部变化=收编）；旧项目 `rtcDoc` 加载时迁入第一集（`migrateRtcDocs`，保存只写 rtcDocs）；deleteEpisode 连删该集档位+激活集回退；SHARED_PROJECT_FIELDS `rtcDoc`→`rtcDocs`（整表镜像，两窗口同时编辑不同分集 last-writer-wins 边界已注释）；⚠ **rtcGenSink 三原语（commitPatch/silentPatch/liveSegment）加非激活档位回退**——「A 集提交生成→切到 B 集→A 集结果回来」直写 A 集档位（非激活档无 undo 栈直写安全），占位符跨集落笔不丢；`appendEpisodeToTimeline` 先 `switchRtcEpisode` 再 commit（占位入该集自己的时间轴）；中栏标题栏新 [RtcEpisodeSwitcher.tsx](src/rtc/RtcEpisodeSwitcher.tsx)（RtcPanelFrame 新 `headerExtra` 插槽：分集下拉（切换/删除（末集保护+确认）/＋新建分集）+ **当前分集名行内可编辑**（updateEpisode，三模式共享实体全局生效）；⚠ 标题栏可拖换位——内部控件 draggable=false+mousedown 不冒泡）；导出剪映草稿名=`项目名·分集名`。②**左面板五分页**（[RtcAssetPanel.tsx](src/rtc/RtcAssetPanel.tsx) 重写）：顶部 素材/资产/转场/字幕/特效 分页（红框位）+ 左缘 52px 竖栏二级分类（绿框位，参考剪映左列）——素材页=视频/音频/图片/收藏/共享（**分集过滤**：分镜成片只列当前集、素材库经 `filterLibraryByEpisode`（当前集导入的+未打标旧素材）；「本地导入」与时间轴外部文件拖入都给新素材打 `Asset.episodeId` 当前分集标记）；资产页=角色/场景/生物/群像/道具（选中出图/占位符/造型右键语义原样）；转场页=18 条剪映转场卡点击应用到选中视频片段（同 transitionAfter 字段，无选中给引导文案）；字幕页=播放头添加+字幕列表（点行选中跳转，正文右栏编辑）；特效页=占位（导出剪映后加）。数据读取/拖拽 MIME/选中语义全部与旧版一致。
　**补充7（画布生成物入素材页·按画布分集归属 · client tsc 干净 + 1219 vitest 全过（+4）+ vite build 过）**——用户定「给画布生成物也加进来，分集是三模同步的」（分集列表本就三模共享零改动）。补充6 ② 的「素材库只收 origin=upload」放开为**本地导入 + 画布生成物都收、一律按分集过滤**：⑴**新产物打标**——[pluginRegistry.ts](src/nodes/pluginRegistry.ts) defaultNodeExecute 与 [requestLedgerStore.ts](src/store/requestLedgerStore.ts) 投递的 addAsset 带 `episodeId`（=ledgerIdentity/entry 的 canvasKey——执行开始时所在画布的分集，身份快照惯例）；⑵**存量反查**——`buildCanvasAssetEpisodeMap`（[rtcAssetData.ts](src/rtc/asset/rtcAssetData.ts)）从各分集画布快照+激活画布实时节点的 resultAssetId/resultHistory 反查资产→分集（激活画布先收；激活集快照跳过防陈旧），`filterLibraryByEpisode` 加 attribution 参数（Asset.episodeId 打标优先于反查；两处都查不到=各集都显示）；⑶图片栏 `collectLibraryImageItems` 加 `includeGenerated` 选项（缺省 false=画布本地素材库旧语义零回归）。
　**补充10（⚠ 原文语义终稿：派生只读，非独立数据·用户再纠偏 · client tsc 干净 + 1231 vitest 全过（配对联动 3 测删除 + pruneScriptTracks 1 + rtcScriptLane 5）+ vite build 过）**——用户定稿：「原文轨是**实时提取主轨中素材对应的原文**，不是独立存在的——主轨素材里的原文被编辑修改后跟着变、主轨没素材原文轨也没素材、主轨素材拉长/分割/挪动一一对应、不参与剪辑只是参考文字」。⚠ **推翻补充8-9 的「落轨+配对联动」形态（勿恢复）**：①新纯函数层 [rtcScriptLane.ts](src/lib/rtcScriptLane.ts)——`scriptLaneItems(doc, episode)` 从主轨片段 shotRef **现查现取**分镜 scriptSegment 派生参考块（placeholder/media 一视同仁；空原文/无 shotRef/异分集不产生块；分割=两块同文）+ `activeScriptLaneTexts` 按播放头取活动项；②时间轴渲染**派生只读车道**（RtcTimeline：标尺下 sticky 半高参考行 z-21、整行 pointer-events-none 不参与任何剪辑交互；车道从**真 doc** 派生——拖动预览期间不闪跳、commit 后即时对齐；几何基线 rowTops[0]=`RULER_H+laneH`，gapFromY/slotTop/主轨 sticky top 全随；无块时整行不出现）；③预览窗参考条改同源派生（RtcTextLayer 订阅激活分集 episodes → scriptLaneItems，O 开关语义不变）；④**删除**：appendScriptSegments 落轨、rtcOps scriptPairIdOf/moveSegment/trimSegment 配对联动（派生后自动跟随，联动机制整体多余）、rtcTextCore.activeScriptTexts、RtcTrackHeader「原文」默认名；⑤存量清理：`rtcOps.pruneScriptTracks`——rtcStore loadDoc/外部收编时把补充8 落过盘的 role:"script" 轨**整轨剔除**（role 字段留作遗留标识，散落判定（导出跳过/字幕动作绕开/collectShotRefKeys 排除）保留为未清洗 doc 的防御）。
　**补充9（原文轨四改·用户实测纠偏 · client tsc 干净 + 1230 vitest 全过（+5：配对联动 3 + 框选 2）+ vite build 过）**——①~~原文↔视频配对联动~~（**已被补充10 废止**——原文改派生只读后自动跟随主轨，配对机制连同 scriptPairIdOf 全部删除）：rtcOps `moveSegment`/`trimSegment` 内建 **shotRef 配对联动**（scriptPairIdOf：同 shotRef 的 视频侧片段↔原文轨片段）——挪/裁任一侧，按**实际生效量**同量镜像到配对侧（配对侧按自身轨道约束再钳位；锁定轨不联动；一层不递归）；全部调用面（拖动/Q/W/裁到播放头）自动获得，拖动预览也实时跟随。②**开关语义纠偏**：O/工具条「原文」控制的是**预览窗原文参考条**的显隐（不是时间轨轨道显隐——补充8 的 renderDoc 过滤/visibleTracksOf/gapInsertIndexVisible 已回退删除，原文轨在时间轴恒显示）；原文以**顶部参考条样式**渲染进预览窗（rtcTextCore 新 `activeScriptTexts` + RtcTextLayer 顶部半透明底小字、截 3 行，与成片字幕明确区分；恒不导出不变）。③**Ctrl+A 全选 + 框选**：keymap 新 `selectAll`（Ctrl+A，**注意力门控**——window 捕获阶段记录最近一次按下是否落在时间轴里，不在则不劫持让浏览器原生全选；rtcEditActions.selectAllSegments=当前编辑层全部未锁轨片段）；时间轴空白**点按拖动=框选多选**（blank 手势拖过 3px 起框：虚线矩形实时显示+实时选中相交片段（`marqueeSelectIds` 纯函数：显示行×时间窗相交、锁定轨不参与、整组扩散）；原地点击=清选不变）。④**文本轨半高**（ROW_H_TEXT=32）：时间轴几何全链改 **rowTops 变高口径**（`rowHeightOf` 唯一行高来源：trackIdFromY/gapFromY 逐边界找最近/幽灵落点/框选/行渲染/轨道头（半高=单行紧凑布局，静音钮省略）/片段块（半高=去标题条））+ **文本片段块直接显示内容文字**（truncate 截断）。
　**补充8（原文轨道 + 素材预览自动播放·用户追加 · client tsc 干净 + 1225 vitest 全过（+6：原文轨铺设/幂等/对齐现位置/refKeys 排除/预览排除/导出跳过）+ vite build 过）**——①~~原文轨道~~（**落轨形态已被补充10 废止**——原文改派生只读，存量 role:"script" 轨加载即清）：RtcTrack 新 `role:"script"`（文本类参考轨）——「占位入轨」时把各镜原文（shot.scriptSegment）按其视频侧片段的**当前时间窗**自动铺上（appendScriptSegments：自己一套 shotRef 幂等、空原文不铺、轨道缺失才建（名「原文」text 组头部）、addSegment 夹隙）；⚠ **collectShotRefKeys 排除原文轨**（原文片段也带 shotRef，不排除会让视频占位整批误跳过）；**不导出**（jianyingDraft 整轨静默跳过）、**不进预览画面**（activeTextSegments 排除——不导出的不上画，所见即所得）、字幕动作挑轨绕开它（落上去会静默丢）；工具条「原文」按钮 + 快捷键 **O** 显隐（rtcStore.scriptTrackVisible 会话态；⚠ 隐藏=renderDoc 级过滤——时间轴几何全链同一把尺：`trackIndexFromY` 重构为 **trackIdFromY**（显示行→轨道 id，消费方按 id 查各自 doc，杜绝「显示序下标×真数组」错位）+ `gapInsertIndexVisible`（缝隙号按可见轨换算再折回真数组下标）+ 幽灵落点 rows 走 visibleTracksOf）。②素材页点击预览**自动播放**（AssetPreviewLayer video/audio 加 autoPlay；点击即手势+打包版 autoplay-policy 参数双保险）。
　**补充6（用户实测四反馈：共享分层浏览 / 视频分集过滤收紧 / 素材页点击全预览 / **转场真预览**·client tsc 干净 + 1215 vitest 全过（+11 转场预览）+ vite build 过）**——①**素材-共享改 库→文件夹→素材 两级导航**（参考资产助手，不再摊平全显示）：面包屑 + 库列表（folderCount/assetCount）+ 文件夹列表（已下载计数）+ 进文件夹才出素材网格（`collectSharedFolderItems`）；获取/加入入口仍在资产助手。②**视频/音频栏素材库口径收紧为 origin="upload"**——画布节点生成产物（video.gen_output_* 等 origin=generated）不再混入素材页（用户报「视频没分集筛选」的真凶：这批无分集标记的生成物在每集都显示；分镜成片走 shots 本就按集过滤）。③**素材页各栏（视频/音频/图片/收藏/共享）点击一律进中栏预览**——RtcMediaSel.media 扩展 image，AssetPreviewLayer 加图片大图分支。④⚠ **转场真预览（用户定稿勿回退）：「没有预览的转场不上 UI——不预览直接导出到剪映效果不一致，还不如没有；特效同理」**——[jyTransitions.ts](src/lib/jyTransitions.ts) 加 `previewKind`，**选择器/转场页只列 `JY_PREVIEW_TRANSITIONS`（7 款：叠化/闪黑/闪白/左移/右移/上移/下移）**，11 款花式转场（雾化/马赛克/遮罩等做不出一致预览）保留总表仅供旧文档导出兼容；[rtcPlayback.ts](src/rtc/rtcPlayback.ts) 每条视频轨静态多一个**转场幽灵槽**（`${trackId}#tr`，与 videoLayerSlotsBottomUp 严格同序）：叠化=另一侧片段冻结帧交叉淡化（切点前 B 淡入 α=p、切点后 A 淡出 α=1−p，切点两态同 0.5 视觉连续）、闪黑/闪白=纯色填充层（fill 层：uri 空只画背景色，切点 α=1 两端归 0）、推移=切点前双画面同步推挤（主层 fx 位移 + 幽灵反向推入，切点恰好交接）；窗口钳进两侧片段跨度、只对同轨严格相邻对生效；RtcVideoLayer 新 `fx/ghost/frozen/fill` 字段——**幽灵层永不 play（frozen 定格）、不占解码位、不参与画面点选/选中框、恒静音**；播放器 fx 位移前置在片段变换外、alphaMul 与片段 opacity 相乘（无转场恒缺省=零回归）。编辑复合片段时子层间转场同样预览（子视图即顶层）；复合段相邻的叠化/推移退化（定格不了复合帧）、闪黑闪白照常。
　**补充4（Alt+拖动复制 + 外部文件拖入时间轨同时入素材库·用户追加 · client tsc 干净 + 1198 vitest 全过 + vite build 过）**——①**Alt+拖动=复制**（[RtcTimeline.tsx](src/rtc/RtcTimeline.tsx)，与涂鸦编辑器第188轮/剪映同款语义）：手势开始按 altKey 定（中途按放不变）；复制拖动**不「抬起」原片段**（原片段留在原地），幽灵条带「· 副本」标签，落点预览与松手 commit 同一把尺（复制=addSegment 夹隙试算——原片段在场参与夹隙，与移动的 moveSegment 试算区分）；副本经 `dragCopyClone`（copiedSegTemplate 剥 id/在途状态 + **刻意剥 groupId**——单段复制不并入原组；assetId/subDocId 原样共享=素材唯一性红线不破）落地即选中；悬停缝隙建新轨路径同样支持。②**外部文件（OS）拖入时间轨**：dragover 认 `Files` 类型亮落点轨道提示；drop 逐文件走 `importDroppedFiles`——先 uploadMediaToCanvasAsset（LC- 懒上传）+ libraryStore.addAsset（**与左栏「本地导入」完全同链=素材库同时出现**），再 placeDroppedAsset 按落点入轨（时长探测/轨道匹配/夹隙与内部拖放同路）；⚠ doc 里的 uri 不收 data:/blob:（浏览器 dev 的 objectURL 只作 probeUri，不入落盘数据）；非媒体文件跳过、单个失败不阻断其余。
- **2026-08-17 第236轮（实时剪辑二轮迭代：面板自由布局 + 中栏单一视口 + 四步全流程工作台 + 资产出图 + 项目级隔离·纯客户端 · client tsc 干净 + 722 vitest 全过（682→722，+40：rtcLayoutCore 11/assetGenActions 8/flowCore 10/rtcStore 隔离 8/rtcAssetSelStore 2/资产面板净增 1）+ vite build 过 · 须客户端重新打包、服务端零改动）**——第235轮实机跑通后用户五条反馈+一条扩展需求全落地。
  ①**面板自由布局**（新 [src/rtc/layout/](src/rtc/layout/)：rtcLayoutCore 纯逻辑+rtcLayoutStore+RtcSplitter+RtcPanelFrame；[FrameEditor.tsx](src/views/FrameEditor.tsx) 布局壳重写）：三面板（素材/结果展示/属性）标题栏拖拽互相换位（MIME `application/x-qiji-rtc-panel`+吸附高亮）；左右栏宽（220–520/260–560）、时间轴高（20–60vh）、AI 子栏宽（240–480）全部 splitter 可拖（⚠ 拖动本地预览 pointerup 才落 store→localStorage）；布局持久化 `Qiji:rtcLayout`（normalizeLayout 坏值回默认）+「重置布局」按钮。⚠ **换位用 CSS order（恒定 JSX 顺序）勿改成重排 children**——零 DOM 移动零子树重建，播放中换位不断（RtcPanelFrame 内有注释）。[RtcTimeline.tsx](src/rtc/RtcTimeline.tsx) 高度 h-[35vh]→h-full 跟随父容器（本轮唯一时间轴改动）。
  ②**中栏单一预览视口**（[RtcCenterStage.tsx](src/rtc/RtcCenterStage.tsx) 重构）：底层常驻顺序播放器（时间指针预览）；选中左栏素材（五类资产卡→主图、视频/音频卡→video/audio 预览，[rtcAssetSelStore.ts](src/rtc/rtcAssetSelStore.ts) selected/mediaSel 互斥）→ **素材预览为绝对定位叠层只做显隐**（⚠ 勿条件卸载播放器——切换选中不打断播放）；时间轴选片段不再切换中栏（原 MediaStage/ShotResultStage 删除，片段详情在右栏）。素材网格改 `repeat(auto-fill, minmax(96px,1fr))` 自适应列数（面板拖宽自动增列，缩略图收小）。
  ③**四步全流程工作台**（新 [src/rtc/flow/](src/rtc/flow/)：flowCore+flowActions+RtcAiFlow，挂 AI 子栏）——**实时剪辑独立闭环，不依赖资产模式**（用户明令）：①剧本（编辑回写 setScriptText）→ ②剧集拆分（episodeSplit 本地快拆四模式 + catalog LLM 模板，重拆 confirmDialog 覆盖确认）→ ③资产拆分（与 Frame1693 同模板源/同 analysisTask 断连保护/同 owner 防串台，流式 mergeApply 落库，⚠ **attachSplitPresets 预设胶囊装饰两处都带上**，完成显示五类计数+续提+截断抢救提醒）→ ④逐集智能推理/智能拆分（startInfer 同 Frame161195 模板与互斥锁语义，流式「已出 N 镜」）+ 生成占位入轨（复用 appendEpisodePlaceholders）。⚠ 全部**直接 import 复用既有链路**（runPurpose/startInfer/mergeApply/splitEpisodes/attachSplitPresets…），勿另起管线。
  ④**资产出图**（[RtcAssetProps.tsx](src/rtc/asset/RtcAssetProps.tsx)+[assetGenActions.ts](src/rtc/panel/assetGenActions.ts)）：左栏点资产卡→右栏资产属性视图（提示词编辑回写/ModelPicker/生成基础形象/AssetJobChips（pendingGens 同源断连重连）/主图+历史+设为主图）；参数组装逐字段对齐 AssetWorkbench（五类 purpose 映射、size/quality 同默认、分辨率按 catalog 收敛）；卡片生成中角标。片段选中优先于资产选中。变体/图生图未做（后续）。
  ⑤⚠ **项目级隔离（用户明令「不要串项目」，六项风险四项实锤后修复）**（[rtcStore.ts](src/store/rtcStore.ts)+rtcAssetSelStore+[placeholderSwap.ts](src/rtc/panel/placeholderSwap.ts)；projectStore **零改动**——身份用现成 `projectInstanceId`）：⑴rtcStore 记录 ownerProjectId，**commit/undo/redo 入口身份守卫**——身份不符丢弃该次回写（warn 留痕）并从当前项目收编，杜绝旧项目 doc 写进新项目文件（stale commit 专项单测锁定）；⑵模块级订阅：换项目→全量重置（doc/selection/playhead/undo 栈；无 rtcDoc 建空文档勿传 null）；同项目外部 rtcDoc 变更（多窗口镜像/找回投递）且非自写→收编（selection 剪枝/playhead 钳位/清 undo 栈，**不回写防回声**，selfWriting 标记同 projectSync applyingRemote 惯例）；⑶资产选中态换项目即清；⑷placeholderSwap arm 时记 owner、回调与 probeVideoDuration 异步落笔前双重验身份，切项目立即 disarm。
  **补充（同日实机反馈第二批 · client tsc 干净 + 736 vitest 全过（722→736，+14）· 随同轮打包）**——⑥**右栏合并三页签**（用户定稿「合成一个窗口，不做展开，顶部分页：属性/剧本/分镜」）：中栏「AI 生成」子栏整体删除（rtcLayoutCore/store 的 guideWidth/guideCollapsed 清理，⚠ normalizeLayout 只挑已知键——旧 localStorage 残留 guide 键忽略不炸，单测锁定）；[RtcPropertyPanel.tsx](src/rtc/RtcPropertyPanel.tsx) 单一窗口+三页签（属性=原分派/剧本=工作台①②③/分镜=④；RtcAiFlow 拆为 RtcFlowScriptPage/RtcFlowShotsPage 两区块导出；resumeAnalysisTask 断连重连挪到右栏顶层常驻）；⚠ **自动切回属性页签规则**（rtcPropsTabCore.shouldAutoSwitchToProps 单测锁定）：时间轴 segId 与左栏资产 cat:id 两通道各自比较、**新的非空值才切**——同一选中不重复切、取消选中不切、片段取消露出既有资产选中不切（勿改成合并签名）；右栏宽上限 560→640。⑦**中栏预览视口撑满整列**（用户报「还是分割状态」）：[RtcSequencePlayer.tsx](src/rtc/RtcSequencePlayer.tsx) 根容器 flexShrink:0+固定 clamp 高 → flex:1 撑满（画面 letterbox 居中、控制条贴底），有片段时不再渲染下方空态提示块；**素材预览右缘竖排「生成历史」缩略条**（AssetPreviewLayer 内 84px 窄列：主图+历史、点击=会话级预览 override 不改主图、悬停设为主图同 RtcAssetProps 调用；仅五类资产预览且有图时显示）。⑧**左栏无图资产占位符卡**（用户令「拆分后增加角色占位符」；核实根因=collectProjectImageItems 原本把无图资产**整个过滤**，拆分后左栏无处点选出图）：[rtcAssetData.ts](src/rtc/asset/rtcAssetData.ts) 加 **opt-in `{placeholders:true}`**（⚠ 缺省保持旧语义勿改——RtcCenterStage 依赖「无图返回空」走自身兜底）；占位符卡=虚线紫边+名字首字+「未出图」徽标、排分类最前、点选→右栏出图、不可拖拽（draggable=false+preventDefault 双保险）、出图后同 key 自动变正常卡；**「N造型」角标+右键造型选单**（写同一 useAssetFormStore 与资产助手选中互通；核实五类本就一资产一卡未平铺变体）。⑨**点击时间轨清素材选中**（用户报「素材选中占住中栏取消不了」）：[RtcTimeline.tsx](src/rtc/RtcTimeline.tsx) 根容器 onPointerDownCapture → rtcAssetSelStore.clear()——点轨道/标尺/片段/轨道头任意处，中栏预览让回时间指针。
  ⚠ **须客户端重新打包**；服务端零改动无需部署（本轮纯 src/ 客户端）。**已知边界**：①资产变体（图生图）与音色绑定未进 RTC（资产模式仍可用，数据共享）；②工作台③的资产拆分与资产模式共享 analysisRunning——两边同时开一个任务会互斥（同一保护，属预期）；③布局为应用级偏好（跨项目共享，刻意不随项目）。**待真机 QA**：面板拖拽换位/拖边手感（WebView2 DnD）、播放中换位与切素材不断流实锤、四步工作台全流程真单（剧本→拆分→资产→推理→占位→生成→导出）、切项目后时间轴/选中态清零与 stale 写入防护实机复核、`Qiji:rtcLayout` 跨重启还原。
- **2026-08-17 第235轮（新增第三模式【实时剪辑】：剪映式多轨时间轴 + AI 生成占位符工作流 + 剪映草稿导出·两端 + Rust · server/client tsc 干净 + 682 vitest 全过（602→682，+80：rtcOps 24/rtcAssetData 12/jianyingDraft 20/rtcShotPlaceholders 12/rtcPlayback 12）+ vite build 过 + admin/agent script 语法过 + cargo check 过 + dist 产物 grep 实锤（生产分支 1/DEV 分支 0）· 须重新部署服务端 + 客户端重新打包）**——用户需求定稿（需求评审三答：轻量顺序预览 / 首版仅导出剪映草稿 / 复合片段+分离音轨放 P1）：与【资产模式】【画布模式】并列的第三模式【实时剪辑】，布局=左资产/中生成结果/右属性面板（AI 生成主区）/下多轨时间轴/中间快捷条，**最终产物=剪映草稿**（写明文 draft_content.json 草稿文件夹进 `JianyingPro/User Data/Projects/com.lveditor.draft/`，格式参考 GuanYixuan/pyJianYingDraft，TS 自研零 Python 依赖）。
  ①**模式注册（两端）**：`features.editorMode`（镜像 canvasMode：缺省=开 `!== false`、渠道商整商硬闸 AND 合成、「三主模式不能全关」五处守卫——[agents.ts](server/src/store/agents.ts) updateAgent/applyAgentFeatureGate、admin/agent 两路由批量 setFeature、两控制台 toggleFeat；admin/agent index.html 各 9/7 处加「实时剪辑」chips/开关/批量项）；客户端 [routes.ts](src/router/routes.ts) `/frame-editor`（guid `16:editor`）→ [FrameEditor.tsx](src/views/FrameEditor.tsx) 薄外壳；⚠ **[App.tsx](src/App.tsx) ModeGuard 重构为表驱动 `MODE_ROUTE_TABLE`**（当前路由所属模式被关→跳第一个可用模式 home；新增模式只加一行）；[TitleBar.tsx](src/canvas/TitleBar.tsx) 按钮组 `modeTabs` 数组驱动（只渲染已开启，<2 隐藏组）；[Frame164.tsx](src/views/Frame164.tsx) 第三入口。
  ②**数据模型 `rtcDoc`**（新 [rtc.ts](src/types/rtc.ts)）：RtcDoc/RtcTrack/RtcSegment 微秒时间基，与剪映 target/source_timerange+speed 无损映射；⚠ **segment=素材的纯时间窗口引用（assetId 唯一源头）——splitSegment 两段同 assetId、source 窗口互补，绝不产生新素材实体**（用户明令，单测锁定）；kind:"placeholder"+shotRef{episodeId,shotId} 占位符语义；groupId 预留 P1 复合片段。持久化 6 处（[projectFile.ts](src/services/projectFile.ts) QijiProject.rtcDoc / [projectStore.ts](src/store/projectStore.ts) state+setter+save 两分支+loadFromPath+newProject / [projectSyncCore.ts](src/lib/projectSyncCore.ts) SHARED_PROJECT_FIELDS），缺省即默认不 bump 版本。纯函数 [rtcOps.ts](src/lib/rtcOps.ts)（addSegment 夹隙/moveSegment/trimSegment 联动 source 按 speed 换算/splitSegment/snapCandidates/nearestSnap/rulerStepUs/formatTimecode）+ 运行时 [rtcStore.ts](src/store/rtcStore.ts)（⚠ **rtc 数据变更唯一路径=`rtcStore.commit(mutator)`**：推 undo 栈+回写 projectStore 触发去抖落盘；面板组件不往 rtcOps 加函数、用 commit 内联不可变更新）。
  ③**时间轴 UI**（[RtcTimeline.tsx](src/rtc/RtcTimeline.tsx)+[RtcToolbar.tsx](src/rtc/RtcToolbar.tsx)+src/rtc/timeline/，纯 DOM+CSS transform 自研，不用 React Flow/konva/第三方 timeline 库）：轨道头（静音/锁定/删轨）、自适应刻度标尺、播放头、片段拖动/两端 trim/播放头分割/Delete/Ctrl+Z/多选、磁吸（8px 阈值，snapCandidates）、Ctrl+滚轮锚点缩放、拖放入轨（`application/x-qiji-asset` MIME，视频/音频先探真实时长，媒体类型↔轨道类型匹配）；⚠ **拖裁全程本地 previewDoc 预览、pointerup 才 commit 一次**（undo 粒度=一次手势）；重叠一律夹到最近空隙绝不推挤。工具条：撤销重做/分割/删除/吸附/缩放滑杆/加轨/时间码/「导出剪映草稿」（已接线 exportRtcDocToJianying，忙碌态+结果提示）。
  ④**左资产面板**（[RtcAssetPanel.tsx](src/rtc/RtcAssetPanel.tsx)+src/rtc/asset/）：AssetAssistant 数据层内嵌版（项目/收藏/共享 + 六分类），**新增视频/音频媒体分类**（分镜成片 videoUri/videoUris/videoDerived ∪ 素材库，台账 id 优先去重；音色 voiceUri ∪ audio 资产）、搜索、本地导入（复用 LC- 懒上传零网络）、音频试听；⚠ **拖拽 payload 与 AssetAssistant 逐字段同构且只加不改（media/durationSec 新增字段），恒走 HTML5 MIME 拖拽不走原生 startDrag**——时间轴按 MIME 解析是硬契约。
  ⑤**右属性面板+中央舞台**（[RtcPropertyPanel.tsx](src/rtc/RtcPropertyPanel.tsx)/[RtcCenterStage.tsx](src/rtc/RtcCenterStage.tsx)+src/rtc/panel/ 七模块）：占位符=分镜行属性化视图（原文分段回写/提示词区（同源单栏、双结果两栏）/垫图条走 shotMaterialOps/ModelPicker 双能力/生成故事板/生成视频/重新生成/历史结果灯箱+设为当前）；⚠ **生成只走 startShotGeneration()/startInfer() 唯一路径，参数组装逐行对齐 Frame161195**（IMG_SIZE 表、videoReqOptions 档位收敛、首尾帧校验、officialAssetIndexes 透传）；⚠ **成片自动原位替换占位符**（[placeholderSwap.ts](src/rtc/panel/placeholderSwap.ts) 订阅「pending 销账且该镜 videoUris 增长」→ kind→media、assetId 经 blobByUri 反查、source=[0,probeVideoDuration]，**target 位置/时长不动**，片段已删/已非占位符 no-op；监听为会话级——生成期间重启客户端结果仍由 generationQueue 找回写入分镜历史，仅不再自动换占位）；media 片段=speed/volume/muted 编辑（commitSegmentPatch）。
  ⑥**分镜→时间轴占位**（[rtcShotPlaceholders.ts](src/rtc/rtcShotPlaceholders.ts)）：中央舞台无选中态「按分镜生成占位」入口——选分集一键把 shots 按 durationSec（缺省 3s）逐镜接续追加到首条视频轨（无则新建）；⚠ **幂等键=episodeId+shotId 扫全部轨道（含已被替换成 media 的），命中即 skipped 绝不重复入轨**；已有成片的镜直接落 media 片段。
  ⑦**轻量顺序预览**（[RtcSequencePlayer.tsx](src/rtc/RtcSequencePlayer.tsx)+[rtcPlayback.ts](src/rtc/rtcPlayback.ts)）：主视频轨+音频轨按播放头顺序播放（rAF 推进 setPlayhead、视频 currentTime >150ms 漂移校正、`<video>` 按 uri 作 key——split 同源片段复用元素不换 src、音频元素池进出区间 play/pause）；自绘控制条无原生 controls；**刻意不做多视频轨叠加合成**（完整效果在剪映里看，用户定稿）。
  ⑧**剪映草稿导出**（[jianyingDraft.ts](src/lib/jianyingDraft.ts) 纯构建+[jianyingExport.ts](src/services/jianyingExport.ts) 编排+[lib.rs](src-tauri/src/lib.rs) 4 新命令）：格式实锤自 pyJianYingDraft 模板（platform 5.9.0 骨架、微秒直通、speed→materials.speeds 经 extra_material_refs、图片 photo 3h 时长、音频 extract_music、muted→volume 0）；⚠ **materials 按 assetId 严格去重**（同素材多段/多轨引用只落一条 material、素材文件只拷一份，单测锁定）；placeholder/text 轨跳过记 warning；⚠ **素材复制进草稿文件夹 assets\（自包含，不引用 Qiji 项目路径——防用户清缓存后剪映媒体丢失）**；Rust `jianying_draft_root`（探测 %LOCALAPPDATA% 草稿根，不存在明确报错附路径）/`jianying_prepare_draft`（消毒名+重名 -2/-3 避让）/`jianying_copy_assets`/`jianying_write_draft_file`——⚠ **写入面双向限定**（目标恒草稿根下 canonicalize 校验、源恒项目 assets 下、JSON 文件名白名单），不向前端开放任意路径写。
  ⚠ **过程记录**：第二批三 agent 曾因会话限额中断，从断点续跑完成（半成品在盘上未丢）；时间轴任务重构 rtcOps 测试 34→24 条（「分割不复制素材」锁定用例保留）。
  ⚠ **须重新部署服务端**（editorMode 随部署生效，缺省=开：存量用户部署后即见「实时剪辑」入口，不想放开先在管理端关）**+ 客户端重新打包**（模式页面+Rust 命令都随包）。**已知边界**：①P1 待做：复合片段（导出时展平）、分离音轨（模型层+ffmpeg 抽音频）；②文本轨预留未实现、导出跳过；③占位符替换监听会话级（见⑤）；④导出仅桌面版（Rust 命令）；⑤应用内 mp4 成片导出不做（P2，导出走剪映）。**待真机 QA**：全链路未做浏览器/真机端到端（本轮全部静态验证+单测）——重点：拖放入轨/拖裁分手感、分镜占位→生成→原位替换真单、导出草稿被剪映识别并可编辑（素材路径/时间轴还原）、顺序预览 seek 手感、两控制台 editorMode 开关往返、多窗口 rtcDoc 同步。
- **2026-08-16 第234轮（autodl（autodl.art·ComfyUI 工作流平台）视频渠道接入 · 3 模型/新模式「autodl」·纯服务端 · server tsc 干净 + admin script 语法过 + 沙盒冒烟 54/54 + errorScrub 回归 3/3 + 二启幂等（data 文件零变化；scratchpad 复制 src + junction node_modules + 独立 data，stub fetch 零真实上游、真实库零触碰）· 客户端零改动无需打包）**——用户提供 autodl 三份工作流文档（多图参考生成视频/文生视频/首尾帧生成视频）+ 通用 ComfyUI API 文档，令「新增渠道 autodl」；提交端点与鉴权经官方 docs 实拉 + 用户补发确认。
  ①**上游（Base `https://autodl.art`）**（新 [autodl.ts](server/src/translators/autodl.ts)，协议 `autodl-video`，异步 submit+poll 走统一 VideoDriver，轮询缺省 8s）：提交 `POST /api/v1/comfyui/comfyui_workflow/{workflow_id}`（立即返回 task_id）→ 查询 `POST /api/v1/comfyui/comfyui_workflow/result/{task_id}`（无 body）。⚠ **鉴权 `Authorization: <Token令牌>` 原样令牌不带 Bearer**（文档明写；令牌在控制台「令牌管理」创建、分组选 ComfyUI，勿照抄其它翻译器的 Bearer 写法）。⚠ **状态防御式**：文档只公布 QUEUED/RUNNING/SUCCESS 三态、**未公布失败状态词** → 成功族（success/succeeded/completed/done…，兼容工作流文档的信封形态 `{msg,code:"Success",data:{status:"completed",results}}` 与扁平形态双收）/ 失败族（failed/error/cancelled/timeout… + `code≠Success`）/ **未知词一律不终态继续轮询**（任务 2h 总超时兜底）；「无状态词但 results 已有成品」也判完成（提交即完成形态防御）。成片 `results[].url`（优先 type=video/file_type mp4，首个 image 作封面）**有效期较短**（文档明言「获取后请尽快下载保存」）→ 轮询循环完成即转存 OSS；⚠ 结果下载**防御式只对本站域附 Authorization**（文档未说需鉴权；密钥绝不外发第三方 CDN，第153轮规则）。
  ②⚠ **workflow_id=模型「上游模型名」（一个工作流=一个模型）**：三条工作流各有自己的 workflow_id（autodl 控制台工作流页可查、右侧抽屉可看入参）——种子 upstreamModel=占位符「请填写workflow_id」，**部署后必须在管理端逐模型填入真实 ID**；翻译器对 占位符/回退成模型 id 两种未配置形态**前置明确报错不发请求不扣费**。三种请求形态按 method+素材自动分派（一个翻译器覆盖三工作流）：首尾帧（method=frames）`{prompt, first_frame, last_frame}`（首=带故事板>素材第 1 图、尾=下一张，与算力/苏打水同尺）；多图参考（有图素材）`ref_image_0..ref_image_8`（**@Image1→ref_image_0 顺序与图例编号严格一致**；文档硬限 9 张超限明确报错绝不静默丢；带故事板整体参考图**追加末尾**+提示词说明行，不前插防编号错位）；文生视频（无素材）纯 prompt。素材只收图片（三份文档均无视频/音频字段，前置明确拒）；无 @tag 引用语法 → 图例注入照常（纯文本说明）。
  ③⚠ **参数透传（§9）**：duration（1-10 整数秒）/resolution（**中文档位串** "768p竖" 一类原样发）显式原样透传（数字串归一、越档不夹钳）、**缺省不发该字段**（走上游默认 时长 5s/768p竖）；多图参考多 1080p竖/1080p横 两档（文档差异），文生/首尾帧只有 480p/768p 四档。
  ④**注册面**：新模式 autodl「autodl」（[modes.ts](server/src/store/modes.ts) v21）+ 渠道 `ch-autodl`「autodl（autodl.art）」+ 3 模型种子（`adl()` 工厂经补种入库）：`adl-minimax-h3`（多图参考，图9，3/秒占位）/`adl-minimax-h3-t2v`（文生，零素材，2/秒）/`adl-minimax-h3-flf`（首尾帧，图2，methods=["frames"]，2/秒）——底层模型按官方示例（`"workflow": "H3文生视频"`）判定 MiniMax H3，id 带 minimax 自动归 **fam-minimax**（classifyFamily minimax 规则在 seedance 数字兜底之前，第216轮）；兜底价=每秒价×最长 10s（「默认按最高」）。协议六处注册（Protocol 联合/BUILTIN_PROTOCOLS/VIDEO_DRIVERS/dispatch case/upstream fallback `config.autodl` env `AUTODL_BASE_URL|AUTODL_API_KEY`/admin PROTOCOLS 下拉）+ errorScrub（品牌词 autodl（=模式对外名经占位符保护）+ ComfyUI；域名 autodl.art）+ REHOST 白名单 `.autodl.art`（⚠ 成片实际托管域未知——真单转存失败时到请求记录 ④ 段看 results[].url 域名增补）+ .env.example。
  ⑤⚠ **顺带修 errorScrub 真缺陷（沙盒 D4 抓到，勿回退）**：autodl 是首个「模式名是上游域名子串」的渠道——原顺序先给模式名打占位符保护，裸域名 `files.autodl.art` 被拆成 `files.␀N␀.art` 逃过域名隐藏；**「域名隐藏」挪到「模式名保护」之前**（域名正则须整段命中 host，独立模式名词不会被误吞，严格安全）；回归实锤 简梦S 模式名仍受保护、聪宸/congchen.top 仍被擦、二次擦除幂等。
  实测：server tsc 干净 + admin script 语法过 + **沙盒冒烟 54/54**（种子 12：模式/渠道/3 模型 协议·素材上限·分辨率档差异·计费·fam-minimax·占位符/resolveUpstream；提交 22：未配密钥/占位符与回退 id 零请求/URL 含 workflow_id/**Authorization 无 Bearer**/缺省不发 duration·resolution/数字串归一与 20s 越档原样透传/ref_image_0..2 顺序+图例注入/10 张超限/视频素材拒/首尾帧三例/带故事板追加+说明行/空提示词（含 `"{}"` 兜底形态）拦截/信封与扁平回执/code≠Success/HTTP 400/无 task_id/网络异常不重试；轮询 15：POST 无 body 原样 token/QUEUED·RUNNING/SUCCESS 取成片+第三方 CDN 不附头+本站域附头/封面提取/信封 completed/成功无链接明确失败/FAILED 透出/code≠Success/未知词不终态/500·429 running/404 failed/网络抖动/无状态词但有成品；errorScrub 4+回归 3）+ **二启幂等**（10 个 data 文件 hash 零变化）。
  ⚠ **须重新部署服务端**（首启自动补种 autodl 模式+渠道+3 模型；**部署后到管理端：①「autodl（autodl.art）」渠道填 Token（或环境 AUTODL_API_KEY）；②三个模型的「上游模型名」逐个改成对应工作流的真实 workflow_id**——两者未配置提交都明确报错不扣费）；**客户端零改动无需打包**（模式/家族/方法下拉全走 catalog 热更 ≤30s）。**运营注意**：①3 模型全为按秒**占位价**（上游未公布单价，且 ComfyUI 平台可能按 GPU 时长计费与出片秒数非线性——**务必先小额真单对账再定真价**）；②多图参考工作流 ref_image_0 上游必填——用户选该模型不带图会被上游明确报错（我方无从区分该模型与文生工作流，属预期）；③分辨率档位串为中文（"768p竖"）原样发上游，站方改枚举时管理端同步改参数档即可。**待真机 QA**：三工作流各一张小额真单（提交→轮询→成片转存 OSS 全链路）、成片实际托管域（据此补 REHOST 白名单）、轮询无 body 的 POST 是否被上游接受（若要求 Content-Type/空 JSON 体，看请求记录 ③ 段报错即知）、1080p 档在多图参考工作流的真实可用性。
- **2026-08-15 第233轮（congge（congchen.top·聪宸）图片+视频渠道接入 · 图片 3 + 视频 4 模型/新模式「congge」·纯服务端 · server tsc 干净 + client tsc 干净 + admin script 语法过 + 沙盒冒烟 114/114（全新库）与 106/106（真实库副本）全过 + 二启幂等（三库文件零变化）+ 存量库增量核对（+7 模型 / +1 模式 / +1 渠道，**存量条目零改动**）· 客户端零改动无需打包）**——用户提供两份文档（图片 + Seedance 视频），令「新增渠道【congge】，api 如文档」；AskUserQuestion 三答定案：**视频收敛为 4 款 + 分辨率档**、**占位价 图片 10 / mini 30 / fast 40 / 2.0 50 / 2.5 60**（视频=积分/秒）；图生图字段问答中用户**补发了带 `/v1/images/edits` 章节的第二版图片文档**（首版只写文生图）。
  ①**上游（Base `https://congchen.top`，Bearer sk-；图片与视频同一把 Key）**（新 [congge.ts](server/src/translators/congge.ts)，两条协议）：
  　⑴ **`congge-image` 同步单请求**（复用 createImageTask 图片管线，无轮询）：文生图 `POST /v1/images/generations`、图生图 `POST /v1/images/edits` → `{created, data:[{url|b64_json}]}`。⚠ **垫图必须走 edits 不能塞进 generations**（文档「七」明写）；垫图字段 单张=`image`、多张=`images`（只发主字段，规避「主字段与别名不一致」类失败项）；⚠ **硬上限 4 张**（`image+images+reference_images` 合计，第 5 张上游报 `at most 4 reference images are allowed for image edits`）→ 前置明确报错**绝不静默丢**。素材全是公网直链→JSON；**只剩服务端字节/直链已死但台账有字节 → multipart 文件上传兜底**（文档「八、方式1/2」最推荐形态；⚠ JSON 与 multipart 无法混合表达 → 混排时经 openai.ts 新导出的 `buildEditBlobs` 统一取字节，任一张取不到即明确报错不缺张）。
  　⑵ **`congge-video` 异步 submit+poll**（走统一 VideoDriver）：提交 `POST /v1/videos`（`{model, prompt, seconds, ratio, images?/videos?/audios?}`）→ 查询 `GET /v1/videos/{task_id}`（queued/in_progress·processing/completed/failed）→ 成片 `GET /v1/videos/{task_id}/content`。轮询 5s（文档「接入注意」建议 3-8 秒）。
  ②⚠ **两条「不发某字段」的判断（勿回退）**：⑴ **图片侧不发 `size`**——文档「十二、常见错误④」明写 `resolution` 与 `size` 冲突，而我方客户端出图只发像素串 `size`（genParams.resolveSize）→ 翻译器把它按**跨参数语义映射**换算成 `aspect_ratio`（10 档就近）+ `resolution`（长边反推 ≤1280→1K / ≤2560→2K / 否则 4K，显式 `params.resolution` 优先且档外原样透传），用户选的分辨率档才到得了上游（§9 允许的图片侧 size→比例映射，与 jmh/aistars/jmz image 同规）；⑵ **视频侧不发 `size`/`resolution`**——分辨率已编码在上游模型名里（routes 重定向），再发只会打架，只发 `ratio` 定朝向。另：`quality` **仅 gpt-image-2** 发（Gemini 传了上游明确报错），我方 `auto`=不发走上游默认 medium。
  ③⚠ **上游视频模型名带空格且大小写敏感**（`seedance2.0 Mini-480p`、`seedance2.5 720p`），文档明令原样传——**逐字照抄勿"顺手规范化"**；分辨率编在名字里 → 4 个外显模型经 **routes 按 resolution 重定向到 9 个上游真名**（真名对用户隐藏，与 overseas/星辰同尺）。能力守卫按**前缀**判定（`seedance2.5`→图30/视10/音10、最长 30s；`seedance2.0`→图9/视3/音3、4-15s；未知名=管理端自建，不守卫直发）——站方将来加分辨率后缀，管理端建模型即接，无需改代码。**首尾帧：文档无首/尾帧字段（只有 `input_reference` 单参考图）→ 不声明 methods（仅全能参考）**。
  ④**注册面**：新模式 congge「congge」（[modes.ts](server/src/store/modes.ts) v20）+ 渠道 `ch-congge`「congge（聪宸）」（[channels.ts](server/src/store/channels.ts)，baseUrl 填根域不带 /v1）+ **7 模型种子**（`cgImg()`/`cgVid()` 工厂经补种入库）+ 两协议六处注册（Protocol 联合 / BUILTIN_PROTOCOLS / VIDEO_DRIVERS（仅 video）/ dispatch case（image 与 yali-image 并列走 createImageTask、video 与 suanli-video 并列走 createVideoPollingTask）/ upstream fallback `config.congge` env `CONGGE_BASE_URL|CONGGE_API_KEY` / admin PROTOCOLS 下拉）+ BUILTIN_POLL_INTERVALS 5s + errorScrub（品牌词 聪宸·congchen + 域名 congchen.top；⚠ 「congge」=模式对外名经占位符保护不擦，改名后自动开始被擦）+ REHOST 白名单 `.congchen.top` + .env.example。家族经 classifyFamily 自动归 fam-gpt-image-2 / fam-nano-banana / fam-seedance（零改动）。
  ⑤**7 款清单与占位价**（用户定；上线前管理端可再调）：图片 `cg-gpt-image-2`（1K/2K，⚠ 不支持 4K）/ `cg-gemini-3-pro-image` / `cg-gemini-3.1-flash-image`（1K/2K/4K）**统一 10 积分/次**——文档为按次 元/次（gpt low/medium 0.03 · high 0.06；Gemini 1K/2K 0.04 · 4K 0.06），10 分已覆盖最贵档故**不按档分价**、无亏本风险；视频按秒，**720p 档为用户给定锚点、其余档按上游元/秒比例折算**：`cg933-sd2.0-mini` 480p 20 · 720p 30 / `cg933-sd2.0-fast` 25 · 40 / `cg933-sd2.0` 42 · 50 · 1080p 71 / `cg-sd2.5` 480p 37 · 720p 60（积分/秒；兜底价=每秒价×该档最长时长，「默认按最高」规则第134轮补充2）。
  ⚠ **运营须知**：⑴ **同款模型在本渠道比 Skylee 便宜约 7 倍**（congge gpt-image-2 0.03 元/次 vs Skylee 0.23 元/次）——两边并存时注意别把用户流量定价定反了；⑵ 视频比例档只种了文档明写的 16:9/9:16/1:1，需要 21:9/4:3 等在管理端加档即可（翻译器原样透传）；⑶ **结果图/成片的 CDN 域未知**（文档只给 `https://xxxx/xxx.png` 占位）——白名单先放行 `.congchen.top`，真单转存失败（日志见「下载结果资产」报错）时到请求记录 ④ 段看实际域名并在 [routes.ts](server/src/routes.ts) `REHOST_ALLOW_SUFFIXES` 增补后缀。
  实测：server tsc 干净 + client tsc 干净 + admin script `node --check` 过 + **沙盒冒烟 114/114**（scratchpad 独立 data + junction node_modules，stub fetch 零真实上游调用、真实库零触碰。注册面 33：模式/渠道/7 模型/**上游名逐字含空格**/家族归类/素材上限/参数档/按秒与按次计费 6 组合/resolveUpstream 路由真名；图片 27：generations vs edits 分流/size→比例与分辨率反推三档/显式 resolution 优先且档外原样/**绝不发 size**/gpt 发 quality·gemini 不发·auto 不发/单张 image·多张 images 顺序保持/@Image1 图例注入/5 张超限报错且不发请求/视频素材拒单/未配密钥/400·401·402 错误映射/b64_json 兜底/**multipart 兜底 8 例**（单张 image、多张 images、混排不缺张、死链且无字节明确报错且不发请求）；视频 26：提交路径与模型名原样/seconds·ratio 透传/**绝不发 size·resolution**/三类素材数组/图例全注入/缺省补默认（2.5=30·2.0=15）/**30s 越档原样透传不夹钳**/数字串归一/档外比例原样/素材超限三例/2.5 系 30 图放行/带故事板追加末尾+编号正确/空提示词（含 `"{}"` 兜底形态）拦截/watermark 仅显式才发/无任务号与上游 400；轮询 16：四态+未知态继续轮询/video_url·metadata 内链接·信封形态三路提取/**无地址回退 /content 下载端点**/相对地址拼本站域/**第三方 CDN 绝不附鉴权头**·本站域才附/failed 透出原因/5xx·429 不终态/404 终态；errorScrub 4）+ **真实库副本沙盒 106/106**（F 段 multipart 用例在配了 OSS 的沙盒天然不可达，按环境跳过）+ **二启幂等**（models/modes/channels 三库 md5 零变化）+ **存量库增量核对**（135→142 模型 / 19→20 模式 / 24→25 渠道，**逐条 diff 存量零改动**——用户已定的真价分毫未动）。
  ⚠ **须重新部署服务端**（首启自动补种 congge 模式+7 模型+渠道；**部署后到管理端「congge（聪宸）」渠道填 sk- 密钥**，或环境 `CONGGE_API_KEY`——不填提交明确报「未配置上游密钥」）；**客户端零改动无需打包**（模式/家族/模型下拉全走 catalog 热更 ≤30s）。**待真机 QA**：⑴ 图片三款小额真单（文生图 + 1 张/多张垫图的 edits）；⑵ 视频四款各一单（尤其 2.5 的 30s 与大素材），确认**只发 ratio 不发 size** 上游能正确出对应分辨率——若不行，按文档「推荐分辨率对照」补发 `size`；⑶ 成片与结果图的实际 CDN 域，据此补 REHOST 白名单；⑷ 图生图 4 张上限与我方 matLimits 是否一致（上游报错文案已可在请求记录 ④ 段确认）。

- **2026-08-15 第232轮（产物批量下载器：清单导出（按时间/用户/类型/保存情况）+ 三个消费端（管理端页 / 门户页 / 客户端面板 / 独立 exe）·两端 + 管理端 + 门户 + Rust · server/client tsc 干净 + 602 vitest 全过 + vite build 过 + cargo check 干净（主程序 + 新 crate）+ 两控制台 script 语法过 + 沙盒冒烟 57/57（清单模块）+ 下载器冒烟 28/28（本地假 HTTP 源）+ dev 8787 真实数据端到端（管理端 70 条产物全链路、门户归属隔离、客户端 UI 全分支）· 须重新部署服务端 + 客户端重新打包）**——用户需求：「批量下载器，把服务端请求记录内的链接按时间/用户/类型/保存情况（OSS 还是原链）一键导出，批量下载所有资产。**主要是原链**——OSS 的用户一般自己下好了，原链是服务器重试几次都没存到 OSS 才返回的，用户下载也慢」。AskUserQuestion 三答定案：**下载器直连原链下载到本地**（非服务端代下）、**管理端与客户端两边都做**、**三种形态都要**（客户端内置面板 + 管理端网页 + 独立 exe）。
  ⚠ **两条硬约束（已在需求确认时向用户明示，用户据此选择直连方案）**：⑴ **原链有时效且很短**——各渠道 2 小时（简梦Z 图片）到 24 小时（火山/简梦F）不等，请求记录留 30 天但隔夜原链多半已 404 → **本功能定位是「抢救」不是「归档」**，清单一律按时间倒序、下载按清单顺序（新的先抓）；⑵ **部分原链下载须带上游 Bearer**（简梦F/简梦Z 图片/出海营/Skylee，见各翻译器 `resultHeaders`），而我方硬规则是「密钥只对本站域附头、绝不外发」（第153轮）→ 这类链接**直连必然 401/403**，清单以 `authRequired` 明确标注，各消费端一律「如实跳过并计数」，**绝不反复重试、绝不假装成功**。
  ①**数据源选型（核心判断）**：清单**从请求记录出发而非资产台账**——转存成功的产物在 assets 台账里有行（OSS 永久直链，用户本就能下），而**转存失败回退的上游原链压根不入台账**（[translators/index.ts](server/src/translators/index.ts) 的 `rehosted:false` 分支，那条结果的 id 是 `vid-<上游任务号>` 伪 id），它只存在于日志的 `resultLink` 里。用户真正要抢救的恰是后者。
  ②**服务端地基**（新 [assetExport.ts](server/src/store/assetExport.ts)；[logs.ts](server/src/store/logs.ts) 仅把 `filterLogs` 改为导出）：`classifyLink` 四态（**oss**=命中任一存储档 publicBase / **oss-old**=旧桶已舍弃 / **local**=未配 OSS 的 `/v1/assets/:id/raw` 兜底 / **raw**=上游原链）；`kindOf` 媒体大类（扩展名优先，取不到按 purpose 推——原链常是 `/content` 这类无扩展名端点）；**多链接摊平**（多图任务一条记录的 `resultLink` 是 `" | "` 拼接，逐个摊成独立条目带 seq/total）；`expiryRisk` 四档（原链按记录年龄：≤2h low / ≤24h high / 更久 expired；oss 恒 none）；**建议落盘路径** `<用户>/<日期>/<步骤>_<logId>[_序号].<ext>`（文件名安全化保留中文）。⚠ **`authRequired` 判定不靠静态域名清单**，而是「结果域 == 该模型所属渠道的 baseUrl 域」——与四个翻译器附鉴权头的判据完全同构，**新接渠道自动覆盖、无需维护第二张表**（dev 真实数据实锤：`api.pixellelabs.com`=简梦P 渠道域→标需代下、`edu888.top`=第三方托管→可直连）。⚠ **绝不静默截断**：上限 50000，命中即 `truncated:true` + `matched` 报实际匹配数，三个消费端全部据此显式告警。
  ③**三端点**（归属隔离一律由入口强制补，`parseDownloadQuery` **刻意只解析产物维度参数、不含 owners/userIds**——越权范围绝不能由查询串决定）：`/admin-api/downloads/manifest|summary`（复用 logScope，源站看直属/切渠道商看该商）、`/agent-api/downloads/manifest|summary`（`owners` 恒本商）、`/v1/downloads/manifest`（`userIds` 恒本人，另剥内部 userId 字段）。实测越权：临时用户查询串塞 `?userIds=<别人>&owners=platform` → total=0。
  ④**管理端「批量下载」页**（[admin/index.html](server/src/admin/index.html) 系统组）：范围下拉（复用第168轮 scopeSelect）+ 时间段 + 用户/步骤/模型 + **保存情况**（默认「仅上游原链」——那才是要抢救的）+ **类型**；KPI（产物总数/上游原链/需代下/已存 OSS）；三条自动告警（截断/已过期数/需代下数）；**导出清单 JSON**（喂独立下载器）/ 导出 CSV / **浏览器内小批量直下**（≤50，fetch→blob 带文件名，跨域被拒回退新窗口；明示「不会自动分目录，大批量请用独立下载器」）。渠道商门户 [agent/index.html](server/src/agent/index.html) 同尺（无范围下拉，范围恒本商）。
  ⑤**客户端内置面板**（个人中心新「批量下载」页签，仅 Tauri 显示 + 新 [batchDownload.ts](src/services/batchDownload.ts) 并发引擎）：时间/保存情况/类型三行 chips 筛选 → 选目标文件夹（dialog）→ 并发 4 下载、失败重试 1 次、按建议路径自动分目录、实时进度（成功/跳过/需代下/失败/字节）、可中途取消、**失败清单逐条列出**（过期的额外注明「该链接已超 24 小时，多半是过期了」）。⚠ **取消标记必须走 `useRef`**——下载 worker 循环里读 state 拿到的是启动那一刻的闭包旧值，点了取消不会生效（首版即此 bug，已修）。新 Rust 命令 **`download_to`**（[lib.rs](src-tauri/src/lib.rs)，`reqwest` 加 `stream` feature + 新依赖 `futures-util`）：直落用户选定路径、**流式写入**（几百 MB 成片不整个进内存）、⚠ **先写 `.part` 完成才 rename**（中断不留半截文件冒充成品，`skipExisting` 才判得准）。
  ⑥**独立 exe**（新 crate [tools/downloader](tools/downloader)，Rust + tokio，release 838KB 单文件无运行时依赖）：吃管理端导出的 JSON 清单跑批，`--concurrency/--retries/--timeout/--overwrite/--include-blocked/--raw-only/--dry-run`；⚠ **失败清单 `_failed.json` 与输入清单同形状——直接当 `--manifest` 再跑一次即只重试失败项**；⚠ **防目录穿越**（清单可被手改，路径里的 `..` 段一律剔除）；有失败时退出码非 0。
  实测：server/client tsc 干净 + **602 vitest 全过** + vite build 过 + **cargo check 干净**（主程序 + 新 crate）+ 两控制台 script 语法过 + **沙盒冒烟 57/57**（scratchpad 独立 data + junction node_modules，真实库零触碰：链接四态分类/媒体类型六例/多链接摊平与 seq/needAuth 三例/过期风险四档/筛选十二例/失败记录不入清单/最新在前/截断如实标注/统计聚合自洽/查询串解析五例含「不含归属字段」/路径安全化五例）+ **下载器冒烟 28/28**（本地假 HTTP 源：分目录落盘/内容正确/无 .part 残留/重跑跳过已存在/--overwrite/404 与需代下进失败清单且原因如实/失败清单可直接重跑/--raw-only/--dry-run/截断告警/**目录穿越防护**/--include-blocked 真发请求拿 401/坏输入明确报错）+ **dev 8787 真实数据端到端**（管理端：70 个产物、旧桶 66 + 原链 4、需代下 2 判定精准、筛选组合全对（36 图 + 34 视频 = 70）、导出 JSON 70 条字段齐全、CSV 71 行、中文建议路径正确；门户：临时商登录 → 页面渲染 + **归属隔离实锤**（同期源站 70 个、该商 0 个）+ 空清单导出明确提示；客户端：临时账号真实登录 → 空态与三行筛选 chips 全渲染 + **拦截响应喂构造清单验满态**（KPI/三条告警/徽标计数/预览列表全对）+ 越权隔离实锤；三端零业务控制台报错（仅 favicon 404）；临时 用户/渠道商/localStorage 全清）。
  ⚠ **须重新部署服务端 + 客户端重新打包**（只部署服务端=管理端与门户页立即可用、独立 exe 可用；旧客户端无「批量下载」页签但不受影响）。**独立 exe 分发**：`tools/downloader/target/release/qiji-downloader.exe` + [README](tools/downloader/README.md)（含两条硬约束说明与失败处理指引）。**已知边界**：①**过期原链救不回来**——这是上游时效使然，本功能只能抢救还活着的（清单已按风险标注）；②**需代下的条目本方案下无解**——服务端代下通道未做（用户本轮选择直连），清单已把这批标出来，将来要开代下随时能接上；③浏览器直下受 CORS 限制（我方 OSS 全开、上游 CDN 未必），失败自动回退新窗口打开；④客户端面板仅 Tauri 可用（`download_to` 是原生命令）。**待真机 QA**：打包后客户端选文件夹批量下载真实产物（含大视频的流式落盘与进度）、取消与重跑跳过、独立 exe 在运维机上跑真实清单。
  ⚠ **文档轮次说明**：头部「最后更新」行仍停在第228轮——第229/230/231 轮为并行会话（记录均在本节），本轮同样未动该行以免互相覆盖，**待统一收口**。

- **2026-08-15 第231轮（算力（xienlive）新增请求字段 `up_resolution:"off"`（关闭上游超分）·纯服务端 · server tsc 干净 + 沙盒冒烟 12/12 全过（scratchpad 复制 src + junction node_modules + 独立 data，真实库零触碰实锤；stub fetch 零真实上游调用）· 客户端零改动无需打包）**——用户按上游最新文档令「算力模式增加新请求字段：关闭超分 `up_resolution:"off"`」，两端点（`/api/v1/video/generate` 参考生视频 + `/api/v1/video/generate-flf` 首尾帧）示例均带该字段。
  改法（[suanli.ts](server/src/translators/suanli.ts) 单点）：`const upResolution = stringParam(req.params?.up_resolution, "off")` → 两个 body 各加一行 `up_resolution`。⚠ **缺省恒发 `"off"`，但显式 `params.up_resolution` 按 §9 原样透传**（不写死常量）——将来运营要放开超分，只需在管理端给 `sl933-minimax-h3` 加一档 `up_resolution` 参数（客户端下拉即出），翻译器零改动；写死 "off" 则关不掉。
  ⚠ **运营须知（与 4k 档的关系）**：该模型 4k 档=720p 生成后超分（第217轮注释）——关闭超分后选 4k 会怎样由**上游裁决**（可能报错→失败自动退款，也可能自行降级），我方**不改写参数、不本地拦截**（§9）。若真机确认 4k 与 off 互斥，请在管理端把该模型的 4k 档收掉（或加 up_resolution 参数让用户按需开），**勿在翻译器里加「4k 时自动改发 on」这类静默改写**。
  实测：server tsc 干净 + **沙盒冒烟 12/12**（omni 与 frames 两端点各：提交成功 / 路径正确 / 带 `up_resolution:"off"` / **既有字段（model·aspectRatio·durationSeconds·resolution·materials·firstFrameUrl·lastFrameUrl）分毫未变**；显式 `"on"` 与 `"ON "`（trim）原样透传；空串回落默认；缺 resolution 时仍不发该字段=既有语义不变）。⚠ **须重新部署服务端**；客户端零改动无需打包。**待真机 QA**：算力小额真单（参考生视频 + 首尾帧各一）确认上游接受该字段且成片非超分档；4k 档在 off 下的实际行为。
  （⚠ 轮次号说明：本轮与第229/230轮为**并行会话**，文档头部「最后更新」行仍停在第228轮、待其中一方统一收口——本轮未动该行以免与并行会话互相覆盖。）

- **2026-08-15 第230轮（Skylee（api.808relay.com）图片渠道接入 · 12 模型/新模式「Skylee」·纯服务端 · server tsc 干净 + admin script 语法过 + 沙盒冒烟 78/78 + 二启幂等 6/6（scratchpad 独立 data + junction node_modules，真实库零触碰、stub fetch 零真实上游调用）· 客户端零改动无需打包）**——用户令「接入 api.808relay.com 的图片生成模型」，AskUserQuestion 三答定案：**12 款全接 / 异步轮询 / 新建独立模式**（模式名先定「Skylee」=站点自称，管理端「模式管理」一键可改）。
  ⚠ **并行开发提醒**：本轮与另一会话的「第229轮 Yali AI Studio 图片渠道接入」**同时进行**、改同一批注册面文件（config/channels/models/modes/protocols/errorScrub/upstream/translators-index/routes/admin）。两边均为精确匹配编辑、互不覆盖，但轮次号原本都占了 229 → 本轮顺延为 230、`MODES_SEED_VERSION` 顺延为 19。**文档头部「最后更新」行两边都未改，需其中一方落地后统一收口。**
  ①**上游（Base `https://api.808relay.com`，Bearer；备用入口 api2.808relay.com 参数全同）**（新 [relay808.ts](server/src/translators/relay808.ts)，协议 `skylee-image`，异步 submit+poll 复用视频轮询管线按 image 能力落资产）：提交 `POST /v1/images/generations?async=true` body `{model, prompt, n, response_format:"url", size?, quality?, image_urls?[], extra_body?}` → 回执 `{id, task_id, status:"queued"}`（文档明示「同时兼容 id 和 task_id」→ 防御式 `task_id||id`）；轮询 `GET /v1/images/tasks/{id}?response_format=url` → `data[0].url` / `data[0].b64_json`。文档**未公布图片任务状态词枚举** → 防御式状态族（success/succeeded/completed…=成功、failed/error/cancelled…=失败、**未知词=生成中继续轮询**）；信封与扁平两种回执形态都吃；5xx/429/网络抖动不终态。
  ⚠ **同步回执短路（勿删）**：文档没承诺所有款式都认 `?async=true` —— 若上游忽略该参数直接返成品（回执已带 `data[0].url` 而无 task_id），提交段就地把结果寄存进模块级 `SYNC_DONE`（30 分钟 TTL）并返回合成 `sync:` 任务号，首次 poll 立刻取回。**无论上游认不认 async，管线都走同一条路**；寄存被取走/进程重启后再 poll → 明确失败以触发退款（不静默挂死）。
  ⚠ **参考图字段与既有 openai-image 不同，故独立协议不复用**：这家输入一律走 **`image_urls` 数组**（公网 URL 与 `data:image/...;base64,` Data URL 同一字段），而第118轮定稿的 openai-image 发的是 `images[].image_url`；`response_format` **只控结果**（url/b64_json），绝不用它描述输入。本地文件才用 `/v1/images/edits` 的 multipart `image[]`——我方素材恒是 OSS 公网直链，不走该形态。结果只有 b64_json 时合成 `data:` URL 交给通用轮询循环取字节（**undici fetch 支持 data: 协议已实测**）。
  ②**款式形态（KIND，按上游名分派）**：gpt=像素尺寸 size + quality（auto/low/medium/high，非枚举像素→同比例最近档）；gemini=比例串 size + quality 1K/2K/4K（我方 resolution 档映射，4K 补 `extra_body.google.image_config`——与简梦Z 图片同尺，上游不认会明确报错无静默风险）；mj=仅比例串。素材数量闸交管理端 matLimits 前置硬闸，翻译器只拦「图片接口收到视频/音频素材」（**明确报错绝不静默丢**——丢一条即 @ImageN 图例整段错位）。
  ③**注册面**：新模式 skylee「Skylee」（[modes.ts](server/src/store/modes.ts) v19）+ 渠道 `ch-skylee`（[channels.ts](server/src/store/channels.ts)，baseUrl 填根域不带 /v1）+ **12 模型种子**（`sky()` 工厂经补种入库，外显 id=`sky-`+站点公开名）+ 协议六处注册（Protocol 联合 / BUILTIN_PROTOCOLS / VIDEO_DRIVERS / dispatch case（与 jmz-image·aistars-image 并列走 `createVideoPollingTask(..., "image")`）/ upstream fallback `config.skylee` env `SKYLEE_BASE_URL|SKYLEE_API_KEY` / admin PROTOCOLS 下拉）+ errorScrub（品牌词 Skylee·808relay + 域名 808relay.com；⚠ 「Skylee」=模式对外名经占位符保护不擦，改名后自动开始被擦）+ REHOST 白名单 `.808relay.com` + .env.example。家族经 classifyFamily 自动归 fam-gpt-image-2 / fam-nano-banana / **fam-mj（第163轮预留的空家族本轮首次有模型入驻）**。
  ④**12 款清单与占位价**（站点价为**元/次** → 占位价 ≈ 元价 × 100，与简梦Z 图片同一折算尺；上线前管理端定真价）：`sky-gpt-image-2` 23 / `-low` 7 / `-compact`（gpt-image-2-openai-compact）15 / `-token`（上游按 Token，与次数非线性 → 按最贵档保守占位）23 / `sky-gemini-3-pro-image` 16 / `sky-gemini-3.1-flash-image` 16 / **`[zz]` 廉价平行线三款**（同款 Gemini 的另一条站方线路，便宜 25%~47%）12 / 9 / 7 / **`sky-midjourney-v7|v8.1|v8.2`** 各 40。
  ⚠ **上游名逐字照抄，勿"顺手规范化"**：`[zz]gemini-3-pro-image-preview`（无空格）与 `[zz] gemini-3.1-flash-lite-image`（**`[zz]` 后有一个空格**）是站点原样写法，改一个字符即模型不存在。
  ⚠ **三条已知边界（运营须知）**：⑴ **`[zz] gemini-3.1-flash-lite-image` 在站点 `/llms.txt` 里只挂 chat/generateContent、未列 images 接口** → 走本协议可能被上游拒（明确报错+失败自动退款，无静默风险），**先接入、用小额真单复核，不通就在管理端停用它**；⑵ **Midjourney 三款「一次任务返回四张图」，本轮管线一个任务只落一个资产（取 data[0]）**，张数记进请求记录 ④ 段——多图落盘需扩展通用轮询管线（`VideoPoll` 只有单 videoUrl），属后续可选项；其价目分四档（Relax 标准 0.40 / Relax HD 1.20 / Fast 标准 0.80 / Fast HD 2.40 元）由**上游账号档位**决定、文档未给请求字段 → 我方按 Relax 标准单档占位，运营按实际档位定真价；⑶ **结果图的 CDN 域未知**（无 Key 无从实测）——白名单先放行 `.808relay.com`，若真单转存失败（日志见「下载结果资产」报错）到请求记录 ④ 段看 `data[0].url` 的域名，在 [routes.ts](server/src/routes.ts) `REHOST_ALLOW_SUFFIXES` 增补后缀即可（转存失败会回退上游直链，功能不断但链接可能有时效）。
  ⑤**文档自身两处错**（读文档时发现，接入按 `/llms.txt` 为准）：站点「图像生成/图像编辑」章节的接口标签写成 `/v1/chat/completions`，但 llms.txt 里每个图片模型的推荐场景都是 `image-generations-json-url: POST /v1/images/generations`；「Grok Imagine 图片」示例用的 `grok-imagine-image-pro` **不在 12 款图片模型清单里**（本轮未接）。
  实测：server tsc 干净 + admin script `node --check` 过 + **沙盒冒烟 78/78**（注册面 26：模式/渠道/12 模型/上游名逐字/家族归类/占位价/resolveUpstream；catalog 6：下发 12 款、**不泄漏 `[zz]` 内部线路标记**、三形态参数档位；submit 26：async 路径/image_urls 数组/response_format 只控结果/像素→最近档/gemini 4K extra_body/mj 只发比例/`[zz]` 空格名原样/视频素材拒单/未配密钥/402·400 错误透传/网络异常不重试/**同步短路三态**/无任务号无成品；poll 15：queued/进度 "37%" 提取/data[0].url/**第三方 CDN 绝不附鉴权头**·本站域才附/信封+大写状态/b64_json→data: URL/完成无地址明确失败/failed 透传/无状态词但成品已在/5xx·429 不终态/404 终态/网络抖动继续；errorScrub 4：模式名保护+链接隐藏+子域隐藏+渠道显示名擦除）+ **二启幂等 6/6**（模型/模式/渠道均不重复补种 + undici `data:` 协议取字节实锤）。
  ⚠ **须重新部署服务端**（首启自动补种 Skylee 模式+12 模型+渠道；**部署后到管理端「Skylee（808relay）」渠道填 API Key**，或环境 `SKYLEE_API_KEY`——不填提交明确报「未配置上游密钥」）；**客户端零改动无需打包**（模式/家族/模型下拉全走 catalog 热更 ≤30s）。**待真机 QA**：⑴ 各形态小额真单（gpt-image-2 文生+图生、gemini 4K、midjourney）全链路（提交→轮询→结果转存 OSS）；⑵ `?async=true` 是否被上游真正认（不认则走同步短路，请求记录 ③ 段可见回执形态）；⑶ `[zz] lite` 那款能否经 images 接口调通；⑷ Midjourney 返回的是 2×2 宫格单图还是 4 条 data——若是 4 条且需要全落盘，再评估扩展轮询管线。

- **2026-08-15 第229轮（Yali AI Studio（api.yaliai.com）图片渠道接入 · 4 模型/新模式「Yali」·纯服务端 · server tsc 干净 + 沙盒冒烟 46/46 全过（scratchpad 独立 data + junction node_modules，真实库零触碰）+ 二启幂等（落库无重复）· 客户端零改动无需打包）**——用户提供 api.yaliai.com 文档（SPA `/docs`，经浏览器逐页读取），AskUserQuestion 定稿：**只接 OpenAI Images + Banana/Gemini 两类图片接口，Grok Images / Agnes Image / Grok Videos 本轮不接**。
  ①**上游（Base `https://api.yaliai.com`，Bearer；也兼容 X-API-Key）**（[yali.ts](server/src/translators/yali.ts)，协议 `yali-image` 复用 createImageTask 图片管线，**同步单请求无轮询**）：统一走 **OpenAI Images 形态**——`POST /v1/images/generations`（文生图）/ `POST /v1/images/edits`（图生图）。本轮两类接口都接受这一形态（Gemini 类文档明示「同时接受 /v1/images/generations 与 /v1/images/edits，可使用 size/resolution/ratio/image 等 OpenAI 形态字段」）→ **一套代码零分支路径**，不走 `/v1beta/models/{model}:generateContent` 原生路径。响应 `{created, data:[{url}]}` → 复用 openai.ts `readImageResult` 下载落资产（含第158轮 fallbackUrl 止血语义）。
  ⚠ **一把 Key 绑定一种「接口类型」**（文档 §认证）：OpenAI Images 的 Key 调不了 Gemini 模型、反之亦然，错配返回 **403 permission_error** → 种子按接口类型分**两个渠道**（`ch-yali-openai` / `ch-yali-gemini`，同 baseUrl 各填各的 Key）；403 错误文案明确指向「Key 与模型的接口类型不匹配」而非笼统无权限（勿改回笼统文案——这是该家最容易踩的配置坑）。
  ②**规格字段——⚠ 原样透传绝不静默改写（§9 第188/215轮定稿）**：`size`（像素或比例写法）+ `resolution`（1k/2k/4k）显式给了才发、缺省不补默认、非法档位原样发出由上游明确报错（失败自动退款）。⚠ **比例写法必须同时给 resolution 才映射**（文档明示顶层 `ratio`/`aspect_ratio` **不替代 size**）→ 参数表**只给 size+resolution 两项、刻意不给 ratio**（给了会让用户以为能单独控比例）。`quality` **仅 OpenAI Images 类发送**（Gemini 与 Grok 同样不是质量参数、上游会剥掉——翻译器按上游名 `/gemini|banana/` 跳过）。`n` **不发**（网关向上游恒提交 n:1，文档明示不能用它请求多图）；`response_format` **不发**（省略即 url；亦规避 openai.ts 记录的「多发该字段触发 502」教训）。
  ③**参考图**：文档硬限 **最多 6 张**（超限前置明确报错不发请求）；解析复用 openai.ts 新导出的 `resolveEditRefs`（死链探活 → 按资产 id 回查台账当前直链自愈 → 明确报错；⚠ 一张都不许静默丢，垫图与 @ImageN 图例按位对齐）——**优先公网 HTTPS 直链**（文档推荐，不占内联预算），仅剩服务端字节（未配 OSS）时按文档支持的**完整 Data URL** 内联并守住 **单张 12 MiB / 合计 30 MiB**（超限明确报错引导配 OSS，绝不发出去让上游 413）。⚠ 本轮唯一改动既有文件处：[openai.ts](server/src/translators/openai.ts) 把 `resolveEditRefs`/`ResolvedEditRef`/`readImageResult` 三个符号加 `export`（纯导出，语义零改动）。
  ④**4 模型种子**（[models.ts](server/src/store/models.ts) `yali()` 工厂经**补种机制**入库，不 bump MODELS_SEED_VERSION）：`yali-gpt-image-2`（→ ch-yali-openai，resolution 1k/2k/4k + size auto/9 比例/3 像素 + quality）、`yali-gemini-2.5-flash-image`（**仅 1K 档**）、`yali-gemini-3-pro-image`（1K/2K/4K）、`yali-gemini-3.1-flash-image`（比例档最全，含 1:4/1:8/4:1/8:1）——后三款 → ch-yali-gemini。⚠ **Gemini 3.1 Flash 的 512（0.5k）档不开**：文档明示「0.5k 与 512 不是 OpenAI Images 规格，仅属其原生 imageSize」，走本形态时无法表达；要用需另走 `/v1beta/...:generateContent` 原生路径（后续可选项）。素材上限全员 `{img:6, vid:0, aud:0}`；familyId 经 classifyFamily 自动归 fam-gpt-image-2 / fam-nano-banana。上游未公布单价 → 全员按次**占位价**（8/5/10/9），上线前管理端定真价。
  ⑤**注册面**：新模式 `yali`「Yali」（[modes.ts](server/src/store/modes.ts) `MODES_SEED_VERSION=18`）+ 两渠道（[channels.ts](server/src/store/channels.ts)）+ 协议六处注册（Protocol 联合 / BUILTIN_PROTOCOLS / **dispatch case=createImageTask**（无 VIDEO_DRIVERS——同步渠道不进轮询表）/ upstream fallback `config.yali` env `YALI_BASE_URL/YALI_API_KEY` / admin PROTOCOLS 下拉）+ errorScrub（品牌词 Yali AI Studio/YALI/Yali/yaliai + 域名 yaliai.com；**「Yali」=模式对外名经占位符保护不擦**，模式改名后自动开始被擦）+ .env.example 环境段。
  ⑥**异步不用**（有意）：上游支持请求带 `async:true` 走统一任务协议（202 + `query_path` 轮询，查询窗口 ≥30 天）——本轮走同步，因为 createImageTask 已把同步调用包成我方异步任务（客户端体验一致）且 submitSignal() 已取消提交短超时（第169轮）；真机若出现同步长连接被中断，再改走 async:true + 轮询（与 jmz-image 同构走 createVideoPollingTask）。
  实测：server tsc 干净 + **沙盒冒烟 46/46**（种子 15：模式/两渠道/4 模型 protocol·能力·素材上限·家族·渠道归属·各款分辨率档·512 档不开·quality 仅 OpenAI 类·无 ratio 参数；upstream 3；文生图 5：路径/body 字段/不发 n 与 response_format/Bearer 取对应渠道 Key/结果 url 下载落字节；**规格透传 5**：9:16+4k 原样、Gemini 不发 quality、非法档 3:7+9k 原样发出、缺省不补默认、空白值视为未给；图生图 5：edits 路径/image 公网 URL 数组顺序保持/带规格字段/7 张超限报错不发请求/死链明确报错不发请求；错误映射 9：403 指接口类型不匹配·402·413·422·429·503 带 trace·200 无图明确失败·b64_json 兜底·缺密钥前置拦；errorScrub 4：模式名保留/链接隐藏/裸域名隐藏/改名后被擦）+ **二启幂等**（46/46 再过 + models/channels/modes 落库计数无重复）。
  ⚠ **须重新部署服务端**（首启自动补种 Yali 模式+两渠道+4 模型；**部署后到管理端两个 Yali 渠道分别填对应接口类型的 Key**——填错那把会全线 403）；**客户端零改动无需打包**（模式/家族/模型下拉全走 catalog 热更 ≤30s）。**运营注意**：①4 模型全为占位价，上线前定真价；②`n` 对该家无效——需要多图请多次生成；③文档未公布价格与限流细节，建议先小额单摸底。**待真机 QA**：两类 Key 各一张小额真单（文生图 + 图生图各一）、多垫图（2-6 张）按位对齐效果、4K 档实际可用性、Gemini 极端比例（8:1）出图。


- **2026-08-11 第197轮（OSS 转存失败降级 + 后台补传·纯服务端 · server tsc 干净 + 沙盒冒烟 14/14（伪 S3 掐 socket 复现同款 "socket hang up"，独立 data 真库零触碰已删）· 须服务端重新部署、客户端零改动）**——生产大批量报 `socket hang up`：排查实锤是**上游 200 出图后、`createAsset`→`ossPut`（雨云 rains3）PUT 被对端重置**（异步任务客户端早已拿 taskId 走人，与客户端连接无关；aws-sdk 默认 3 次连接重试仍失败）。旧行为一次上传失败=整单报废——图字节明明在服务端内存里，与第158轮 fallbackUrl 防的是同一种平台亏损，但 b64_json 路径无上游直链可兜底。
  ①**降级**（[assets.ts](server/src/store/assets.ts) `createAsset`）：ossPut 失败（含 2s 退避整体重试一次后）→ **台账照落、任务照成功**：url/oss_key 置空 → `assetUrl` 自动回退 `/v1/assets/:id/raw`、字节进 memBytes；⚠ **降级行绝不落 oss_key**——assetUrl 优先按 oss_key 拼公网直链，落了 key 就是一条对象不存在的死链而不是回退 /raw。
  ②**后台补传**：降级字节同时进 `pendingOss` 队列，`retryPendingOssUploads` 每 60s 顺序重试（首个失败即收工防批量刷错；导出供冒烟直接驱动），成功即 `stmtUpdateUrlKey` 回写 url+oss_key 并释放内存——/raw 此后 302 到直链，客户端已保存的 /raw 链接无感恢复。补传按行自己的 storage 档定位（与 reputAsset 同理由）。
  ③**边界**：队列仅内存、重启即失 → /raw 404，由客户端死链自愈（reputAsset）兜底；总量封顶 **1GB**，超出不再降级、抛 `转存 OSS 失败：<原因>`（错因标明是 OSS 段，防再误判成上游；1GB≈近 3 小时产出，防 OSS 长瘫吃爆内存）。
  实测：server tsc 干净 + **沙盒冒烟 14/14**（伪 S3 `socket.destroy()` 复现原错误文案：正常直传回归 2 项 / 降级 6 项（不抛错、url 空、无 oss_key、memBytes 有字节、assetUrl 回退 /raw、台账已落）/ 恢复补传 5 项（url+oss_key 回写、内存释放、assetUrl 变直链、伪 S3 收到完整字节）/ 队列空幂等 1 项）。⚠ 沙盒 settings.json 用 PS5.1 `-Encoding utf8` 写会带 BOM → JSON.parse 静默回退空配置=「OSS 未配置」假象，排查过一次。
  ⚠ **根因（OSS 链路抖动）本身不是代码问题**：部署后若仍成批进补传队列，查管理端「存储」页 OSS 自检 + rains3 服务状态。（画布新功能⑤ 可嵌入 3D 模型系统 ·纯客户端 · client tsc 干净 + 551 vitest(+16) 全过 + vite build 过（three 独立懒加载分块 627KB、主包+10KB）+ 浏览器端到端全过（临时账号已删）· 须客户端重新打包、服务端零改动）**——用户定稿（推翻原「独立 3D 导演台」单品形态）：「不打算单独做一个3d导演台，而是做一个**可嵌入图像中的3d模型系统**，用户可在【涂鸦】【720°全景】中直接加入3d模型摆放位置，也可以单独在3d导演台中布置模型」；AskUserQuestion 三答定案：模型库=**可动人偶+基础几何道具+用户导入 GLB**、产物=**合成图+姿势图+深度图**三件套、全景=**真三维放置**（转视角透视跟随）。
  ①**架构（一套引擎三宿主）**：**纯 three.js 不引 r3f**（项目现状是 konva/原生 WebGL2 命令式两套，ModelStage 类+宿主薄封装更贴合；依赖只加 three@0.180）。分层：纯逻辑层（可单测零 three）[pose3d/vecmath.ts](src/lib/pose3d/vecmath.ts)（手写 vec3/quat：欧拉↔四元数互逆/qFromTo/qRotate）+ [pose3d/skeleton.ts](src/lib/pose3d/skeleton.ts)（**JOINTS 单一来源** 21 关节：腰/脊柱/胸/颈/头+四肢 14 可编辑 + 鼻/眼/耳/腕/踝标记点；computeFK/sanitizePose/**aimJointAt**（拖子关节→解父骨骼旋转，qFromTo 最短旋转，丢捻转属 v1 边界）/OpenPose COCO-18 键点映射）+ [pose3d/openposeDraw.ts](src/lib/pose3d/openposeDraw.ts)（官方 17 肢体连线+18 点色、胶囊椭圆画法、MiniCtx 接口可单测）+ [stageScene.ts](src/lib/stageScene.ts)（场景 JSON：figure{pose}/prop{shape,size}/glb{assetId} 三类模型×pos/rot/scale，sanitize 白名单、上限 30、⚠ **只存引用与变换绝不存位图**）；渲染层 [modelStage.ts](src/lib/modelStage.ts)（**只能动态 import**）：ModelStage 类——人偶按 JOINTS 搭 three 组层级（圆柱骨+球关节+头+躯干胶囊；拖拽手柄=青色球 depthTest:false 仅姿势模式+选中显示）、轨道相机（orbit/pan/dolly）与全景视角（yaw/pitch/fov 与 panoRender 同约定 yaw0=+Z）、拾取（关节手柄优先→模型）、拖模型=水平面射线交点（Shift=升降）、拖关节=过关节的相机正对面交点→aimJointAt、GLB=GLTFLoader.parseAsync+过大过小归一 1.6m+落地居中、**三产物**：captureComposite（可透明背景=涂鸦模型层）/capturePose（键点投影→openposeDraw，相机背后剔除）/captureDepth（MeshDepthMaterial 近白远黑+**near/far 收紧到模型包围盒**保对比度，网格/全景球/选中框全隐藏）；destroy 必 loseContext（WebView2 上限）。
  ②⚠ **全景背景三坑（E2E 色带实锤，勿回退）**：⑴ three `scene.background` 的 equirect 采样 `atan(z,x)` 与 panoRender 的 `lon=atan(x,z)` 是**镜像关系，backgroundRotation 旋转救不回**——改**内视球面网格**（SphereGeometry side:BackSide + `rotation.y=-π/2`）：u=0.5（图像中央）落 +Z=yaw0、u 增大向 +X，与 panoRender 逐点一致；⑵ **`texture.flipY` 对 ImageBitmap 源被 WebGL 规范忽略**——垂直翻转走 UV 变换 `repeat.y=-1, offset.y=1`（全景球+image 垫底 scene.background 两处同修；色带图 天顶白/地底黑/右方红 逐点验证）；⑶ E2E 合成指针事件坐标必须把 GL 缓冲坐标**除以 canvas dpr**（buffer=client×1.25，直接用会点空——首轮选不中的真因）。
  ③**独立导演台**（[DirectorStageModal.tsx](src/components/DirectorStageModal.tsx) App 根 lazy + [directorStore.ts](src/store/directorStore.ts) 会话 + [directorOp.ts](src/canvas/directorOp.ts) 入口/落库）：工具栏（加人偶/加道具五形状/导入GLB/姿势模式/删除选中）+ 右栏模型列表与选中属性（朝向/缩放/高度滑杆+颜色实时改+重置姿势）+ 底栏产物勾选（合成图/姿势图（需人偶）/深度图）。入口：画布空白右键「3D导演台…」（directorStageBlank 网格舞台）/ 图片节点右键（directorStageNode：底图垫底；**产物节点带 stage3d=再编辑**；全景链产物自动 pano 背景）。产物 saveStageOutputs：逐张懒上传（LC）落 image.gen 节点、合成图节点写 `data.stage3d{scene,mode,srcAssetId}`（[types/index.ts](src/types/index.ts) NodeData 新字段，sanitizeCanvas 原样保留）；baseName 剥「-合成图」尾防再编辑标题滚雪球。z=100400（高于涂鸦 100300/全景 100270——两处都能唤起）。
  ④**涂鸦嵌入**（[annotation.ts](src/lib/annotation.ts) 新元素 kind **"model3d"**{x,y,w,h,scene,camera}——doc 存场景 JSON+相机**零位图**，空场景元素丢弃；elementBBox/translateElement/蒙版/quad 变形天然通用）：工具栏「3D模型」→ openDirectorStage embed 模式（底图=当前原图）→ 完成回调交回**透明模型层位图**+场景 → 元素落画面（初始=整幅，可选中移动/翻转/透视/擦除）；渲染=konvaSpecFor "model3d" 分支按 RenderCtx.model3dImages 查位图（缺=青色虚线占位框）；位图=[model3dRender.ts](src/lib/model3dRender.ts) **离屏重渲**（动态 import modelStage、渲完即 destroy、按场景+相机+尺寸签名缓存 24 张）——再编辑打开旧 doc 自动补渲；导出前未就绪的补渲一次、仍失败跳过不烙进成品；选中浮条新「编辑3D」=带场景重开导演台。
  ⑤**全景嵌入**：[PanoViewerModal.tsx](src/components/PanoViewerModal.tsx) 工具条新「3D模型」→ openDirectorStage pano 模式（同源图+源节点 stage3d 场景续编）→ 模型放在全景空间（新模型落在当前视线方向 2.6m），拖动=转视角（模型透视跟随）、保存=当前视角合成图（1600×900）+姿势图+深度图落节点。
  实测：client tsc 干净 + **551 vitest 全过（+16：欧拉四元数互逆/qFromTo 反向向量/FK 静止姿高度对称/抬肩 90° 腕水平/腰转 180° 鼻向-Z/sanitizePose 归一与丢弃/aimJointAt 拖腕肘部朝向精确+根关节 null/OpenPose 18 键点与 17 连线映射/drawOpenPoseFigure null 跳过/sanitizeStageScene 三类清洗·上限·夹取）** + build 过（three=627KB 独立分块，仅 3D 功能首次唤起才加载）+ **浏览器端到端**（5233 临时账号真实注册登录/建项目/进画布）：右键空白「3D导演台…」→ 弹窗+three 加载；加人偶→竖扫中线灰白亮像素（渲染实锤）；点人偶→「选中：人偶1」+拖动→保存场景 pos[-0.14,0,0.49]（换位实锤）；再编辑（合成图节点重开）→人偶还原；姿势模式→青色手柄块（与选中框细线区分的密集块判定）拖踝→**pose.kneeR=[8.4,2.0,26.7] 落盘**（关节链路实锤）；「保存到画布」→合成图（4.4% 灰=人偶+网格）/姿势图（**黑底 0.8% 高饱和彩色**=OpenPose 骨骼）/深度图（纯灰度 1.1%）三节点全 LC 懒上传；涂鸦：绿底图→「3D模型」→垫底图模式（背景绿铺满）→加人偶→「嵌入画面」→Konva 2 图层+浮条「编辑3D」→完成→产物节点 doc **elements=[model3d 全幅+场景1模型]**、合成图 98.8% 绿+1.1% 灰（烙图实锤）；全景：方位色带 equirect→查看器「3D模型」→**中心[0,204,0]绿 / 右转[255,0,0]红 / 天顶[255,255,255]白 / 地底[0,0,0]黑**逐点精确→加人偶（视线方向出现）→保存→1600×900 合成图 72.9% 红+0.5% 灰、节点 stage3d mode="pano"；上白下黑图验垫底方向（屏上白/屏下黑）。临时账号已删、preview 已停。
  ⚠ **须客户端重新打包**；服务端零改动。**已知边界**：①关节拖拽丢捻转（twist）——手臂扭转类姿势做不出（拖向即所得，v1 取舍）；②GLB 材质不跟颜色选择器（外部资产原样渲染）；③涂鸦 model3d 元素跨设备再编辑需 GLB 字节可取（LC 未补传 OSS 前本机可用；引用过一次即有公网自愈）；④姿势图无遮挡剔除（相机背后才剔除，前后交叠的键点都画——ControlNet 惯例可接受）；⑤全景导演台的批量视图截取（四/六/八/十二）未纳入（无模型时用全景查看器截取；带模型的多视图为后续可选项）。**待真机 QA**：WebView2 下 three WebGL 性能与 context 压力（多次开合导演台）、真实 GLB 模型（带贴图/骨骼动画的静态首帧）解析兼容性、姿势图配 ControlNet 真实出图效果、涂鸦大图（4K）嵌入的离屏重渲耗时。
- **2026-08-03 第195轮（720°全景节点化（不自动跑+默认4K）+ 倒置修复 + 转视角球形轮廓恢复 ·纯客户端 · client tsc 干净 + 535 vitest 全过 + vite build 过 + dist 零密语 + 浏览器端到端全过（临时账号已删）· 须客户端重新打包、服务端零改动）**——用户定稿：「720°全景查看做成一个节点，点击生成后自动选用4k画质，连接出720全景查看节点，选好模型和要求后点击生成，结果自动转换成720全景模式查看（节点上可拖动、双击放大），跳过显示扭曲图的步骤；现在的720打开是倒置的，修复」+ 中途追加「转视角那里还是要球形轮廓，但摄像头固定」。
  ①**转全景改「搭链不自动跑」**（[panoramaOp.ts](src/canvas/panoramaOp.ts) panoramaNode 重写）：一次 spawnNodes 落两节点两连线——生成节点（title「全景-原名」，params：默认 gpt-image-2 家族模型 + `purpose:"image.panorama"` + `panorama:{}` 标记 + **`aspect:"16:9"` + `resolution:"4k"`**（自动选 4K；模型未开放 4K 档由既有 clampImageResolution 提交前归一）+ 可见短提示词标签）→ **`pano.view` 查看节点**（440×260，title「720°全景-原名」）。**不 dispatch run**——用户在生成节点上换模型/加要求后自己点生成；第194轮的「点击即 run」语义废除。
  ②**新节点类型 `pano.view`**（[nodeSpecs.ts](src/nodes/nodeSpecs.ts) 新 spec：display **"pano"**（DisplayKind 新增）、kind seed、inputs image、inPalette false）+ [PanoNodeView.tsx](src/nodes/PanoNodeView.tsx)（[ResultView.tsx](src/nodes/ResultView.tsx) display==="pano" 分支挂载；LOD 换轻量占位块）：订阅上游边首个图片结果 resultAssetId → fetch→createImageBitmap（服务端 `/v1/assets/:id/raw` 兜底、LC- id 跳过兜底）→ 节点内 WebGL 全景交互（**拖动转视角**（力度随视场自适应、stopPropagation 防画布拖走节点）/ native 非 passive **滚轮视场 25–110°**（容器 `nodrag nowheel`）/ HUD 徽标（横纵视场+「拖动查看·双击放大」）/ **双击 stopPropagation 开全屏查看器**（openPanoViewer 带 sourceNodeId，四/六/八/十二视图截取全套可用））；上游重跑出新图自动换源。⚠ **首绘用 `setTimeout(draw,0)` 绝不用 rAF**（后台标签 rAF 冻结→黑画布，E2E 实测踩坑）。BaseNode 悬浮按钮/右键菜单：全景链产物节点（isPanoramaNodeParams）显示「全景查看」，普通图片节点显示「转全景」。
  ③⚠ **倒置修复（勿回退；用户真机报障）**：[panoRender.ts](src/lib/panoRender.ts) fragment shader 相机基向量 **`up = cross(fwd, right)`（不取负）**——原代码 `* -1.0` 把 clip space +y（屏幕上方）映射到世界下方=画面上下颠倒。⚠ **中心像素测试对 up 不敏感**（第194轮四/六视图中心色测试全过却没抓到），回归必须测**非中心像素**：fov110 下 屏上=天（白）/屏下=地（黑）已实锤，renderPanoSnapshots 六视图上白下黑复核仍过。
  ④**转视角编辑器恢复球形轮廓**（[ViewAngleModal.tsx](src/components/ViewAngleModal.tsx)；用户中途定稿「还是要球形轮廓，但摄像头固定」）：取景器底层插回**静态 SVG 线框球**（圆 r=110 + 纬线椭圆 ×5 + 经线椭圆 ×5，stroke rgba(255,255,255,0.20)、pointerEvents:none 纯装饰）,3D 画面卡收到 **190×142**（带投影）嵌在球内——**相机固定语义分毫未动**（球只是包着画面的可视化外壳；第191轮删球是配合「相机固定」重做，本轮按用户观感要求恢复形，不恢复第189轮的旧交互）。取景框/荷兰角/鱼眼畸变/WASD/滚轮距离/拖动转画面全部原样。
  实测：client tsc 干净 + **535 vitest 全过** + vite build 过 + **dist 产物 grep 密语零命中**（环绕180度/equirectangular投影/凭空添加）+ **浏览器端到端**（5233 临时账号：点「转全景」→ 链结构实锤（gen params：gpt-image-2/purpose/panorama/16:9/**4k** + runtime idle=不自动跑 + pano.view 440×260 + 两连线）；注入方位色带全景图 → 节点 canvas 中心 [0,204,0] 前方绿带精确、**屏上 [255,255,255] 白天/屏下 [0,0,0] 黑地=倒置修复实锤**、节点上拖动 → HUD 纵73° 且中心变白（真交互）、滚轮 → 场110°、双击 → 全屏查看器开启（四/六/八/十二视图按钮全在）；转视角弹窗 → SVG 球（circle r110 ×1 + ellipse ×10）渲染、画面卡 190×142、拖动 60px→水平+30°/90px→+45°（15° 档吸附精确联动，React 状态异步落地——断言须在下一轮读，同轮读是旧值））；临时账号已删、preview 已停。
  ⚠ **须客户端重新打包**；服务端零改动（第194轮「panorama.main 补种+渲染分支」部署要求仍在，两轮一起发即可）。**已知边界**：①pano.view 节点无自有结果资产（纯查看器，导出走双击全屏的截取按钮落新节点/分镜组）；②非 2:1 图接入查看节点会被水平回卷采样（观感由用户判断，与全屏查看器同规）；③生成节点删除后查看节点显示空态（连线断=无源）。**待真机 QA**：真单 4K 全景出图后节点内交互流畅度（4K 位图纹理上传耗时）、WebView2 下多个 pano.view 节点并存的 WebGL context 压力（已有 loseContext 治理）、双击放大与画布双击开面板的手感区分。
- **2026-08-03 第194轮（画布新功能④ 720°全景 + 全局懒上传 ·两端 · client+server tsc 干净 + 535 vitest(+4) 全过 + vite build 过 + 浏览器像素级端到端全过（临时账号已删）· 须客户端重新打包 + 服务端重新部署）**——用户设想全部落地：全景前置提示词+原图→equirect 平面图→3D 查看器拖动看→单张/四/六/八/十二视图截取；同轮按用户要求改**全局上传规则为懒上传**。
  ①**转全景**（[panoramaOp.ts](src/canvas/panoramaOp.ts)）：图片节点悬浮/右键「转全景」→ 右侧 spawn image.gen（连线承接原图，图像超分同构）+ `purpose:"image.panorama"` + `params.panorama:{}` 标记 + 默认 gpt-image-2 家族模型 + 立即 run。前置提示词**服务端渲染**（[panoramaPrompt.ts](server/src/translators/panoramaPrompt.ts) + 模板 `panorama.main` 分类「转全景」补种；正文=「equirectangular 2:1、水平360°垂直180°、原图为正前方水平中央、左右边缘无缝衔接、天顶地底按投影拉伸、{{补充要求}}」；catalog 保密名单扩为 `viewangle.*|panorama.*` 连预览不下发；模板删空回退代码兜底）。产物节点带 panorama 标记→悬浮按钮自动切「全景查看」。
  ②**全景查看器**（[PanoViewerModal.tsx](src/components/PanoViewerModal.tsx) 全屏 + [panoStore.ts](src/store/panoStore.ts)，App 根 lazy）：渲染器=**原生 WebGL2 单 shader**（[panoRender.ts](src/lib/panoRender.ts)：屏幕像素→相机射线→经纬→equirect 采样；比 three/Photo Sphere Viewer 轻两个数量级）。⚠ 三条硬约束（勿回退）：**WebGL2 必须**（NPOT 纹理在 GL1 不能 REPEAT——水平 REPEAT 是左右无缝回卷采样的关键）；图像字节 fetch→createImageBitmap（asset:// 直喂纹理 CORS 死，涂鸦同款教训）+ /raw 兜底源；关闭必须 `WEBGL_lose_context.loseContext()`（WebView2 WebGL context 数量上限，调研④工程注意）。交互：拖动=转视角（力度随视场角自适应）、滚轮=视场 25–110°、右下 HUD（横/纵/视场）。
  ③**视角截取**（[panoView.ts](src/lib/panoView.ts) 纯定义可单测 + panoRender.renderPanoSnapshots 离屏批渲）：**当前视角**=1600×900 落新图片节点（名「横X纵Y」）；**四视图**=前/右/后/左 fov90、**六视图**=+上/下、**八视图**=每45°八向 fov60、**十二视图**=平视/俯45/仰45 三层×前右后左 fov60——1024² 方图批量落**分镜组节点**（buildShotGroupNode+createShotGroup 命令，宫格可拖排/复用）。
  ④⚠ **全局懒上传（用户定的新规则，勿回退）**：[nodeUpload.ts](src/canvas/nodeUpload.ts) `uploadMediaToCanvasAsset` 语义改为**本地暂存**——分配 `LC-` 本地资产 id、字节直接落盘（saveUploadedLocal）+注册三元映射，**不再立即传 OSS**（sha256 去重保留；非 Tauri 回退 objectURL 会话级显示）；涂鸦/转深度/视频截帧/上传节点/素材添加/画布拖入/全景切图**全部调用点自动获得**。补传=既有 `ensurePublicUrl`（提交生成时 collectMedia 逐素材调用）：上传 TP 后**把 OSS url 回填进 LC 映射**（⚠ 关键坑：blobByUri 会先命中 LC 映射（无 url）导致缓存失效重复上传——回填后第二次直接命中，即用户要求的「拿到的链接可重复利用」）；collectMedia **过滤 LC- id 绝不发服务端**（补传后按 srcUri 精确查真 TP id 随请求带上）。已知边界：LC 资产未被任何请求引用前无 OSS 备份（本地丢=真丢）、跨设备不可见；引用过一次即有公网链接与自愈。
  实测：client+server tsc 干净 + **535 vitest 全过（+4：四档视图数量/角度/上下层/标签唯一/yaw 归一/全景节点判定）** + build 过 + 服务端冒烟（panorama.main 种子+渲染含 equirect/2:1/无缝/补充要求、零残留占位）+ **浏览器像素级端到端**（方位色带测试全景图 1024×512：查看器 yaw0 中心像素=[0,204,0]前方绿带精确；拖动 HUD 横36°；四视图→分镜组 4 资产**前绿[0,204,0]/右红[255,0,0]/后蓝[0,0,255]/左黄[255,255,0]逐张精确**；六视图上白[255,255,255]下黑[0,0,0]；**懒上传全链**：落地即 LC- 零网络→ensurePublicUrl 补传 TP00007708 公网 url→**LC 映射 url 回填**→二次调用命中缓存**blob 零增长零重传**；右键菜单「转全景/全景查看」入口在）；临时账号已删。
  ⚠ **须客户端重新打包 + 服务端重新部署**（panorama.main 补种+渲染分支+catalog 保密名单）。**待真机 QA**：真单转全景出图质量（gpt-image-2 的 equirect 服从度：左右接缝/天地拉伸——不理想优先调管理端「转全景」模板正文）、查看器 WebView2 下 WebGL2 可用性与手感、批量视图落分镜组的宫格观感、懒上传在真机 Tauri 的落盘链路。**转全景补充要求入口未做 UI**（params.panorama.custom 服务端已支持，后续可加弹窗）。
- **2026-08-03 第193轮（转视角提示词服务端化（保密）·两端 · client+server tsc 干净 + 531 vitest 全过 + vite build 过 + 冒烟全过（临时账号已删）· 须客户端重新打包 + 服务端重新部署）**——用户：「需要隐藏提示词，不能直接写在客户端里，放在管理端预设的位置」。**落位说明：放进「提示词模板」库而非「预设」库——预设是 catalog 全文下发客户端的实体（藏不住），模板正文本就「只留服务端」（§4），这正是两个实体的分界**；管理端「提示词模板」页新分类「转视角」可调优。
  ①**契约**：Purpose 新增 `"image.viewangle"`（[contract.ts](src/contract.ts)，两端共享）+ 客户端 purposeRegistry 条目 + 服务端 PURPOSE_LABELS「转视角」（请求记录/统计自动分组）。
  ②**客户端零提示词**（[viewAngle.ts](src/lib/viewAngle.ts) 只剩 UI 参数模型/档位换算/预设；全部短语函数删除）：submitViewAngle（[viewAngleOp.ts](src/canvas/viewAngleOp.ts)）给节点写 `params.purpose="image.viewangle"` + `params.viewAngle{az,el,shot,panX,panY,lens?,custom?}`，可见 prompt=**短参数标签**（「转视角：水平180° 俯仰-30° 近景 · 鱼眼 · 平移(12%,0%)」）——purpose 经 pluginRegistry `params.purpose` 覆盖（图像超分同款）、viewAngle 随节点 params 原样落 `GenerateRequest.params`。⚠ 边界：转视角节点提示词框的手改文本不生效（服务端按 purpose 渲染覆盖），微调入口=多角度编辑器「提示词」开关（viewAngle.custom）。
  ③**服务端渲染**（新 [viewAnglePrompt.ts](server/src/translators/viewAnglePrompt.ts)；[prompt.ts](server/src/translators/prompt.ts) buildPrompt 顶部按 purpose 分支——变体前缀 VARIANT_RE 同款先例，**全部翻译器统一走 buildPrompt=单点生效全渠道**）：`sanitizeViewAngle`（az 归一 0-359、el/shot/pan 夹取、custom 截 500 字；形状不对返回 null 走普通路径）→ 机位/俯仰/景别/平移/镜头短语（参数化代码文本，第192轮纯镜头语言原样移植）→ 主骨架+未见区域条款取**模板库正文**（`viewangle.main`（变量 {{机位描述}}{{未见区域}}{{补充要求}}）/`viewangle.backfill.far`/`.backfill.mid`，[templates.ts](server/src/store/templates.ts) 补种（只加缺失无需版本号）；模板被删/清空/禁用→代码 FALLBACK 兜底功能不断）。
  ④⚠ **catalog 泄漏点堵上**：模板下发本就 `body:undefined`（有 purpose 时），但 `bodyPreview` 会带**前 200 字**——[catalog.ts](server/src/catalog.ts) 对 `viewangle.*` 改发「（保密模板：正文只在服务端渲染，不下发预览）」。
  实测：client+server tsc 干净 + **531 vitest 全过**（客户端提示词用例随迁移删除，保留 UI 档位换算/预设）+ build 过 + 冒烟：**服务端渲染**（临时 tsx 脚本，已删）315°/-60°/平移30%/鱼眼/补充→短语+骨架+补充要求全对、180°→正后方+far 条款、120°→mid 条款、坏参数→null；**catalog**（临时账号+机器码登录）三模板 bodyPreview=保密占位、body=null；**浏览器提交管线**（假模型零花费）节点 params 结构化正确、可见 prompt 零模板文案；**dist 产物 grep「凭空添加/相机绕场景中心/镜头回望」零命中**。临时账号已删。
  ⚠ **须客户端重新打包**（旧包客户端仍带旧文案且不发 viewAngle 参数——旧包提交=variables.prompt 整段旧模板文，服务端 purpose 分支不认 params.viewAngle 缺失会回退普通路径照发，功能不断但文案仍在旧包里，升级即消失）**+ 服务端重新部署**（首启补种三模板+渲染分支）。**待真机 QA**：真单出图（服务端渲染链路全串）、管理端改模板正文后 ≤30s 生效验证。
- **2026-08-03 第192轮（转视角提示词重写为纯镜头语言 ·纯客户端 · client tsc 干净 + 538 vitest 全过（断言随句式更新）+ vite build 过 · 须客户端重新打包、服务端零改动）**——真机崩法：无人场景转 180° 视角，模型**凭空加了一个背对镜头的人，场景仍是正面**。用户点破根因：「提示词应该讲述镜头位置和方向，而不是直接说背面」。
  ①⚠ **拟人措辞是造人诱因（勿回退）**：旧句式「环绕到正后方（背面视角，展示主体的背面）」+ 推断条款里的「人物发型/服装结构/体型/脸部/衣饰」——对无人场景，模型把这些词理解成「需要一个有背面的人」。新句式（[viewAngle.ts](src/lib/viewAngle.ts)）**全程零人物词、零“背面”词**：`azimuthPhrase` 只说相机——0°=「相机保持在原拍摄位置，正对场景拍摄」、<90°=「相机绕场景中心向X水平移动N度（X前方机位），镜头始终对准场景中心」、90°=「环绕到场景正X侧90度，镜头对准场景中心」、>90°=「越过正侧面到达X后方机位，镜头回望场景中心」、180°=「水平环绕180度移动到场景正后方，镜头转向回望原拍摄方向——从场景的另一侧往回拍」。
  ②**未见区域条款重写**（`backfillClause`）：≥135°=「新机位下画面的大部分区域在原图中被遮挡或没有拍到（各物体的**背侧**、场景的另一面）——按原图各物体的形状/材质/结构/空间布局推理并连贯生成；构图必须符合新机位（前后关系相应反转），不要沿用原图的正面构图」+ **明令反制：「画面里只能出现原图中已有的物体，绝不要凭空添加原图中不存在的物体或人物」**；90–135° 同款轻量版。③一致性句条件化：「原图里已有的一切（场景、物体，如有人物也包括人物）」——有人保人、无人不引人；panPhrase「主体偏向」→「画面内容整体偏向」。
  实测：538 vitest 全过（azimuthPhrase/backfillClause/整句 断言全部更新到新句式，并加「`not.toContain("背面")`」反向断言防回归）+ tsc 干净 + build 过（纯模板文本改动，管线上一轮已端到端验证过不重测）。**待真机 QA：同一张无人场景图重跑 180°，确认不再造人**；若仍不稳，下一步是把 180° 档改为双阶段（先生成场景反打描述再出图）或引导用户用「补充要求」注明场景内容。
- **2026-08-03 第191轮（转视角·取景器交互重做（用户三点反馈）·纯客户端 · client tsc 干净 + 538 vitest(+4) 全过 + vite build 过 + 浏览器端到端全过（临时账号已删）· 须客户端重新打包、服务端零改动）**——第189轮多角度编辑器上线后用户三点反馈全落地：背面提示词、相机固定拖动转图、固定取景框+滚轮距离+WASD+镜头差异化。
  ①**背面/侧后方推断条款**（[viewAngle.ts](src/lib/viewAngle.ts) `backfillClause`，自动附进 prompt）：|环绕|≥135° →「目标视角包含原图**未展现**的背面区域——基于发型/服装结构/体型/场景空间延伸合理推断生成背面细节，风格光照与原图统一，**禁止把正面直接镜像当背面**（背面不应出现脸部/正面衣饰）」；90–135° → 侧后方补全条款；≤90° 不附。这是对「背面靠生成」的显式引导——不写模型要么镜像要么拒改。
  ②**取景器（相机固定，操作画面）**（[ViewAngleModal.tsx](src/components/ViewAngleModal.tsx) 左侧整体重做，线框球删除）：源图渲染为 CSS 3D 卡片（`translate(-pan)+scale(距离)+perspective+rotateX/Y`），**拖动=转动画面方向**（0.5°/px 自由角度实时预览，松手吸附 15° 档——句式仍按整档描述）；**滚轮=等效相机距离**（native 非 passive；40%–200%，`shotFromScale` 实时换算景别档，景别滑杆反向 `SHOT_SCALES` 设距离）；**WASD=相机平移取景**（±40%、4%/步，capture 监听、输入框聚焦放行；prompt 短语按「主体在画面中的落位」反向描述（`panPhrase`：8% 内忽略、8–22% 略微、以上明显），模型对落位描述比对相机运动执行得稳）。取景器/滑杆/预设三方双向同步（动任一其余跟随，预设附带 pan 清零+距离对档）。
  ③**固定取景框 + 镜头差异化**：框固定不动（外部超大 box-shadow 压暗）——标准/荷兰角=矩形三分线框（荷兰角把**画面**转 15°，框不动）、**鱼眼=圆形框 + canvas 桶形畸变简易预览**（320×240 逐像素重采样 dst r→src r^1.6、圆外(r>1.15)留空；标注「实际效果由模型生成」）。⚠ 预览位图 fetch→createImageBitmap（asset:// 直喂 canvas 污染教训）+ **双源兜底**：主源（本地副本/OSS url）失败 → 服务端 `/v1/assets/:id/raw`（session.previewFallbackUri，viewAngleOp 组装）→ 再失败回退 `<img>` 平铺（无畸变预览，不阻塞提交）。
  实测：client tsc 干净 + **538 vitest 全过（+4：panPhrase 量化三档/backfillClause 三阈值/shotFromScale 分档与 SHOT_SCALES 自洽/整句含背面条款与平移短语）** + vite build 过 + **浏览器端到端**（5233 临时账号：取景器 canvas 320×240 经 /raw 兜底加载；拖动 +130px/+70px → 滑杆 60°/30°（+65/+35 精确吸附 15° 档）；滚轮 4 格 → 距离 175% 且景别滑杆自动跳「特写」；WASD d×3 s×2 → 平移 12%,8%；鱼眼预设 → 圆形框 + canvas **角落 alpha=0/中心 255**（桶形重采样实锤）+ 镜头标签；提交（az180+panX30，假模型零花费）→ prompt 同时含「背面视角」「禁止把正面直接镜像」「主体略微偏向画面左方」三条款、run 按预期拦在适配器层）；临时账号已删。⚠ 排查记录：**HMR 后测试注入经裸 URL import 拿到的是旧模块实例**（app 图谱被改写成 ?t= 时间戳 URL）——store 读到 null 是双实例假象不是 bug，硬刷新或手工构造参数绕开。
  ⚠ **须客户端重新打包**；服务端零改动。**待真机 QA**：真单出图（重点：背面条款对镜像崩法的抑制效果、WASD 构图短语执行度）、取景器拖动手感/滚轮步进、鱼眼畸变预览观感。
- **2026-08-03 第190轮（涂鸦工具栏「资产名称」下拉·纯客户端 · client tsc 干净 + 534 vitest 全过（零新增，纯 UI 组装复用既有 text 元素管线）+ 浏览器端到端实锤（harness 已删）· 须客户端重新打包、服务端零改动）**——用户需求：涂鸦功能区加【资产名称】下拉，快速把资产名（主体）当文字用；①点名字=收起面板+画面中间落文本+工具切「选择」方便立刻移动；②也可把名字拖到指定位置放下（不收起面板）。
  ①**实现**（全部在 [AnnotationEditor.tsx](src/components/AnnotationEditor.tsx)）：工具栏「资产名称」按钮（Tags 图标+下拉箭头，虚线橡皮右侧、填充开关左侧）挂 `assetPanel` 面板（样式对齐章库面板；与 stampPanel 互斥、Esc 关闭、切任意工具关闭、会话开启复位）；数据=useProjectStore 五类数组（characters/crowds/scenes/organisms/items）`name.trim()` 非空者按 角色/群像/场景/生物/物品 分组渲染胶囊（无资产显示引导文案）。落置统一走 `placeAssetName(name, at?)`：本质 commitElement 一个普通 **text 元素**（颜色/字号跟当前设置→与文字工具产物同构：可双击改字、可变形/擦除/导出，零新元素类型零 doc 变更），位置=at（拖放落点）或画面中心，按 `measureTextWidth`（模块级 canvas measureText，`bold ${fontSize}px Arial` 与 Konva Text 渲染同款）**文本中心对位**并夹取图内；落完 setTool("select")+setSelectedId（点击路径再收面板，拖放路径不收）。受 MAX_ELEMENTS 上限保护。
  ②**拖拽**：胶囊 `draggable` + dragstart 写 `application/x-qiji-asset-name`（+text/plain 兜底）；stageWrap 的 onDragOver **只对该 MIME preventDefault**（不拦外部文件拖入画布的既有链路）+ onDrop 按 getBoundingClientRect/scale 换算原图坐标落置。
  ③**验证方式（可复用）**：登录页已是 账号+密码+机器码绑定（AI 不能代输密码）→ **临时 vite 根 harness**（annoharness.html + src/annoHarness.tsx：直挂 AnnotationEditor + useProjectStore.setState 注入假五类资产 + 运行时 canvas 生成 800×500 测试图 openAnnotation）绕开登录/项目文件验真实组件；`window.Konva.stages[0].find("Text")` 坐标实锤：点击「林小满」→ 文字中心 (400,250)=画面正中、字号 23（=短边500×0.045 中档）、面板收起、选择工具激活、选中浮条出现；DnD（DataTransfer+DragEvent 派发 dragover/drop）「沈青崖」→ 中心 (599.5,399.5)≈目标 (600,400)、**面板保持展开**、同样切选择+选中；控制台零报错。⚠ 派发合成 drop 后 React 状态异步落地——断言前别同步读 Konva（先空后有的假象）。验证后 harness 两文件已删；工作目录级 .claude/launch.json 补 qiji-server 条目（本轮未用上：8787 已被用户 dev 进程占用，直接复用）。
  ⚠ **须客户端重新打包**；服务端零改动。**已知边界**：①名字胶囊 maxWidth 140px 超长截断（title 悬停看全名）；②画面中心已有元素时再点名字会原位叠放（用户可立刻拖开，属预期）；③下拉数据实时读 projectStore——涂鸦中新增资产即时可见。**待真机 QA**：Tauri WebView2 下 HTML5 拖拽（draggable 按钮→Konva 容器）手感与光标、长资产名列表滚动（面板 maxHeight 60vh）。
- **2026-08-03 第189轮（画布新功能③ 转视角：多角度编辑器·纯提示词路线 ·纯客户端 · client tsc 干净 + 534 vitest(+5) 全过 + vite build 过 + 浏览器端到端全过（临时账号已删）· 须客户端重新打包、服务端零改动）**——需求对齐后用户定稿：**不接多角度 LoRA**（「加 LoRA 就会限制模型」），用通用图像编辑模型 gpt-image-2 实现；已向用户讲清参考 UI（Qwen-Edit 多角度 LoRA 的摄像机球）的原理=**参数→固定触发句式映射**（球是假的，LoRA 靠成对多视角数据把句式硬监督成相机控制 token），我们无 LoRA 复刻其架构：**句式标准化 + 15° 档位吸附**尽量降歧义（空间一致性弱于 LoRA 属已知边界，靠一致性约束句压制）。
  ①**纯模板层**（[viewAngle.ts](src/lib/viewAngle.ts)，可单测）：`ViewAngleParams{az 0..359, el ±90, shot 0..4, lens?}`；`normalizeAz`（315°=-45°=左前方）+ `azimuthPhrase`（正面/±45前方/±90正侧/±135后方/180背面 分档句式）+ `elevationPhrase`（俯视=高机位/仰视=低机位）+ `buildViewAnglePrompt`（@Image1 引用 + 方位句 + **一致性约束句**「严格保持内容/形象/材质/光照/空间位置关系，只改拍摄角度与构图」+ 可选自由补充）；六预设（鱼眼=fisheye 镜头短语/倾斜=荷兰角/正面俯拍45°/仰拍45°/全景俯拍75°远景/背面180°）。
  ②**多角度编辑器**（[ViewAngleModal.tsx](src/components/ViewAngleModal.tsx)，App 根 lazy 挂载 + [viewAngleStore.ts](src/store/viewAngleStore.ts) 会话）：预设行 + **SVG 线框球**（纬线/经线椭圆 + 球心源图缩略 + 相机标记按 az/el 正交投影定位 + 四向 15° 微调按钮，纯可视化无 3D 计算）+ 三滑杆（15°/15°/1 档步进）+ 提示词开关 textarea + 模型下拉。⚠ **模型列表必须订阅 catalogStore**——弹窗打开早于 catalog 拉取完成时列表停在打开瞬间快照（实测：登录后立即打开只见 3 个画风模型），订阅后到货即刷新；默认模型锁 `familyId==="fam-gpt-image-2"` 第一个（回退 /gpt[- ]?image/ 再回退列表首个），用户已选且仍在列则不动。
  ③**提交**（[viewAngleOp.ts](src/canvas/viewAngleOp.ts)）：源节点右侧 `makeNode("image.gen")`+`spawnNodes` **连线承接源图**（与图像超分 processImageNodeTo 同构；素材经 listNodeMaterials 自动进请求）+ `syncNodeLegend`（图例前缀+matOrder）+ `dispatch run` 走标准生成管线——**计费/进度/堆叠历史/节点上换模型改提示词重跑全部继承，服务端零改动**；title/assetName=「视角-原名」。灯箱入口（viewAngleUri）无节点=产物节点自带素材（`input.images` 公网 url，无 url 明确拒绝）落视口中心。三入口：悬浮「转视角」（Orbit 图标）/右键「转视角…」/灯箱「⟳ 转视角」。
  实测：client tsc 干净 + **534 vitest 全过（+5：normalizeAz 归一/方位分档句式/俯仰句式/完整 prompt 组装含约束句与自由补充/六预设范围）** + vite build 过 + **浏览器端到端**（5233 临时账号：右键真入口开弹窗 → 7 预设/3 滑杆(0-345·15°、±90·15°、0-4)/线框球/球心缩略图全渲染；点「背面视角」滑杆联动 [180,0,2]；默认模型实测锁定 gpt-image-2（12 个图片模型下拉，含 catalog 订阅修复的回归验证）；提交管线用**假模型 key** 验证：新节点「视角-view-src」+连线+图例「@Image1 是…」+提示词（315°→「向左环绕45度（左前方视角）」、-60→「仰视60度」、中景、约束句、补充要求）+run 派发到适配器层按预期拦下——**零真实请求零花费**）；临时账号已删。⚠ 5199 被另一会话 dev 服务占用 → launch.json（工作目录级）加 qiji-client-b(5233) 备用配置；另一会话的临时 harness（src/annoHarness.tsx）挂 tsc，只修未使用 import 未动其逻辑。
  ⚠ **须客户端重新打包**；服务端零改动。**已知边界**：①无 LoRA 硬监督，空间一致性弱于专用多角度模型（背面/大角度靠模型脑补，UI 已按档位化+约束句压制）；②真实出图效果未验（假模型验管线）——**待真机 QA：gpt-image-2 真单出图各预设/角度的空间一致性表现**，不理想时优先调 buildViewAnglePrompt 句式（单点）；③灯箱入口对无公网 url 的图明确拒绝（引导从节点发起）。二期可选：Seedance 环绕运镜+抽帧做「角色转身图集」（调研③）。
- **2026-08-03 第188轮（涂鸦选中框变形：移动/旋转/翻转/Alt复制/四角透视 ·纯客户端 · client tsc 干净 + 529 vitest(+9) 全过 + vite build 过 + 浏览器端到端像素实锤（临时账号已删）· 须客户端重新打包）**——用户需求「旋转/翻转现有元素；选中框（拖动移动/旋转，Alt+拖动复制，点击翻转，拖动四角透视）」；中途真机反馈「浮条挡住顶部旋转手柄」→ 旋转改**四角外缘环区+双向箭头光标**（Figma 式）。
  ①**统一 quad 模型**（[annotationXform.ts](src/lib/annotationXform.ts) 纯数学可单测 + [annotation.ts](src/lib/annotation.ts) `AnnoXform{quad}`）：变形只存一个 quad=**几何外接矩形四角（TL,TR,BR,BL）变形后的位置**——移动=平移四角、旋转=绕质心转、翻转=绕质心镜像、透视=拖单角；`withElementQuad` 回到原位（每角 <0.5px）自动摘除 xform=复位语义。数学件：`homographyFromQuads`（四点对应 8×8 高斯消元求单应）/`applyH`/`invertH`/`isAffineH`（底行≈0 即仿射）/`rotateQuad`/`flipQuad`/`quadArea`；⚠ **退化维度防奇异**：水平线/竖直线的几何盒撑到最小 1px（geomBBox），单应求解才有解。载入清洗：quad 必须 8 个有限数，坏的整体丢弃（元素回退未变形渲染）。
  ②**渲染=源画布+单应网格贴图**（[annotationWarp.ts](src/lib/annotationWarp.ts)，编辑器 [AnnotationEditor.tsx](src/components/AnnotationEditor.tsx) `WarpNode` 与导出 exportComposite 共用一条管线）：canvas 2D 只有仿射——元素**连同蒙版**先离屏 Konva 渲成源画布（带蒙版走 Group cache 隔离，getClientRect 定源矩形+2px pad），再按 H 网格贴图（每格两三角、每三角一个仿射 `ctx.transform`+clip，clip 向质心外扩 0.4px 盖接缝；**仿射 1 格=数学精确，透视 6–14 格细分**）。源画布按「元素内容 JSON」memo——拖手柄只换贴图矩阵不重渲源；分辨率=显示缩放×变形放大量（0.5 档位化，上限 3）。⚠ **WarpNode 命中区**：hitFunc=显示四边形 + `stroke/strokeWidth` 喂 fillStrokeShape 扩命中带（sceneFunc 全自定义不会真画出描边）——细线/箭头的几何四边形高度≈1px，不加描边带**点不中**（E2E 实测发现）。退化单应（四角拖成线）回退基准渲染防内容消失。
  ③**交互（选择工具）**：选中框=青色虚线四角框；**四角手柄拖动=透视**（单角自由拖，可越出图边，出图自然裁掉）；**旋转=四角外缘环区**（不可见 26px 命中圆垫在角点手柄下层——角点优先命中；悬停/拖动光标切**双向弯箭头**（SVG data-uri `ROTATE_CURSOR`），绕 quad 质心按指针角度旋转）——⚠ 初版顶部旋转手柄被翻转浮条挡住（用户真机截图指出），勿回退；**浮条**（选中元素上方 HTML 条）=「↔ 翻转/↕ 翻转/复位（有 xform 才显）」点击即生效；**Alt+拖动=复制**：三类渲染路径（普通矢量/MaskedNode/WarpNode）dragstart 记 `evt.altKey`，落定统一走 `moveOrCopy`——原元素不动、`translateElement` 副本新 id 入列（受 500 上限保护）。拖动中实时预览走 `xformDraft` 草稿态（松手 `applyQuad` 快照+落定，一步撤销）。
  ④**与既有系统闭环**：⑴蒙版在源画布里**随组一起变形**；⑵变形后再擦：`applyEraseStroke` 按显示位置（quad 外接盒）判交、笔刷点经**逆单应**换回基准空间存蒙版（宽度按 √面积比换算）——移动/再变形时擦除继续跟随；⑶`translateElement` 平移时 quad 与基准几何**同步平移**（相对变形不变）；⑷`snapToStroke` 跳过变形元素（基准几何≠显示位置，已知边界）；⑸doc 落盘含 xform，产物节点再编辑完整还原。⚠ **顺带修选中 bug（勿回退）**：原「选择工具 mousedown 清选」挂在包裹 div 上——元素 `cancelBubble` 拦得住 Konva 冒泡拦不住 DOM 冒泡，**刚点选的元素会被立刻清掉**；改到 `Stage onMouseDown` 且 `e.target===stage`（点空白）才清。
  实测：client tsc 干净 + **529 vitest 全过（+9：单应 平移仿射/透视四角精确映射/正逆往返、rotateQuad 90°/flipQuad 镜像、withElementQuad 落定与复位摘除、translateElement 同步平移 quad、sanitize xform 白名单、变形元素擦除逆映射+按显示位置判交、吸附跳过变形元素）** + vite build 过 + **浏览器端到端像素实锤**（白底 800×500：箭头 ↔ 翻转 → 导出**头部红像素带换到左侧 269 vs 尾 153**；矩形 角外缘拖 30° 旋转（光标实测切到 SVG 双向箭头）+ TL 角拖到 (430,150) 透视 → doc quad 旋转角点与手算逐角吻合（674,310/599,439/426,340）且**非平行四边形=真透视**、导出图 变形角点处有红/原位置零红；Alt+拖动 → 第三个元素=翻转箭头副本 quad 精确平移 (+60,+120)、导出图副本处有红；点选→浮条/选中框出现（选中 bug 修复实锤））；临时账号已删。
  ⚠ **须客户端重新打包**；服务端零改动。**已知边界**：①变形元素渲染是栅格贴图（分辨率跟显示/导出缩放，观感与矢量几乎无差；未变形元素仍纯矢量）；②吸附不作用于变形元素；③透视拖角允许拖成凹四边形（自交时画面翻折，属用户自由）；④文字双击改字后基准盒变化，既有 quad 按新盒重解释（轻微比例变化）。**待真机 QA**：四角透视/旋转环区手感（环区 26px 是否合适）、双向箭头光标在 WebView2 下显示、大元素多蒙版变形的渲染性能、Alt+拖动在触控板的可用性。
- **2026-08-03 第187轮（「标注」改名「涂鸦」+ 虚线橡皮滚轮调密度 + 画布新功能②「转深度」·客户端为主+服务端一条模型下发路由 · client+server tsc 干净 + 520 vitest(+7) 全过 + vite build 过 + 浏览器端到端全过（临时账号已删）· 须客户端重新打包 + 服务端重新部署）**——三件事：改名、虚线密度滚轮、转深度（docs/画布新功能方案调研.md ② 主推路线照做）。
  ①**改名「涂鸦」**：全部**用户可见文案** 标注→涂鸦（[BaseNode.tsx](src/nodes/BaseNode.tsx) 悬浮「涂鸦」/[ContextMenu.tsx](src/canvas/ContextMenu.tsx)「涂鸦…」/[Lightbox.tsx](src/components/Lightbox.tsx)「✎ 涂鸦」/编辑器标题·弹窗·alert/产物文件名「涂鸦-*.png」）。⚠ **代码标识符（annotate*/annotation*）与 `node.data.annotation` 字段名不改**——字段随项目落盘，改名=旧项目迁移成本，纯展示改名不动数据层。
  ②**虚线橡皮滚轮调密度**：`AnnoMask.dashScale?`（0.25–4，[annotation.ts](src/lib/annotation.ts) `DASH_SCALE_MIN/MAX`；**只在虚线蒙版且 ≠1 时落盘**省体积，清洗时非虚线蒙版上的 dashScale 丢弃）；`maskDashFor(width, dashScale)` 段长=max(2, 0.8×直径×倍率)（下限从 4 降 2，容纳高密度档）；编辑器复用纹章的 native 非 passive 滚轮监听（tool==="dasher" 分支，上滚 exp 衰减=更密），**涂抹中滚动实时生效**（dashScaleRef 松手结算读最新值，红色预览 dash 同步变）；会话开启复位 1。
  ③**转深度（`src/canvas/depthify.ts` + `src/lib/depthEstimator.ts` + `src/lib/depthMap.ts`）**：三入口与涂鸦并排（悬浮「转深度」/右键「转深度」/灯箱「◑ 转深度」）。交互=点击即在源节点右侧（灯箱=视口中心）落 **image.gen 占位节点**（title「深度-原名」），`setRuntime` 走既有「生成中 n%」进度语义（**真实进度**：模型下载聚合字节数 0–88% → 推理 90 → 合成 96），完成 `uploadMediaToCanvasAsset("TP")` 传合成 PNG 设为主图；失败置 failed（错误附「首次需下载模型约27MB」提示），用户已删占位节点则静默丢弃结果。**纯客户端零计费**，不走 runPurpose/网关生成路径。
  　**推理**（[depthEstimator.ts](src/lib/depthEstimator.ts)，⚠ 体积重只能被 depthify **动态 import**）：transformers.js v4（@huggingface/transformers@4.2.0）+ `onnx-community/depth-anything-v2-small`（⚠ **license 红线：V2 只有 Small 是 Apache-2.0 可商用**，Base/Large CC-BY-NC 效果再好也不能换——调研②）；WebGPU（dtype q4f16 ~19MB）→ 失败自动降级 WASM（q8 ~27MB），管线单例含并发去重、失败不缓存可重试。**深度语义**：Depth Anything 输出逆深度（值大=近），[depthMap.ts](src/lib/depthMap.ts) 纯函数 min-max 归一化→**近白远黑**（ControlNet 深度图惯例；平坦输入全 128 防除零）+ 灰度→RGBA；推理分辨率与原图不一致时平滑放大回原尺寸。
  　⚠ **模型/运行时下发（大陆网络硬约束，勿回退成直连）**：⑴模型文件走**自家网关** `GET /v1/depth-model/*`（[routes.ts](server/src/routes.ts) 公开路由：**正则白名单只放行该模型仓库路径**防开放代理，代理 hf-mirror.com（redirect follow）+ **data/depth-model/ 磁盘缓存**——首次拉取后全体用户走网关缓存；客户端 `env.remoteHost` 每次取管线前指向 `connectionStore.normalizedUrl()/v1/depth-model`，未登录兜底 hf-mirror 直连）；⑵ort wasm 运行时经 vite `?url` 打进本地产物（transformers 默认指 jsdelivr CDN，必须覆盖）——**相对路径直插 node_modules**（onnxruntime-web exports 表不放行 ./dist/* 深导入）。
  　⚠ **两个实测踩坑（勿回退）**：⑴**ort 运行时必须用 asyncify 变体**（`ort-wasm-simd-threaded.asyncify.{mjs,wasm}`）——transformers v4 web 端就用它（Safari 用 plain）；错用 jsep 变体报「webgpuInit is not a function / no available backend found」；⑵**vite `optimizeDeps.exclude` 必须排除 @huggingface/transformers + onnxruntime-web**（[vite.config.ts](vite.config.ts)）——esbuild 预打包会揉碎 ort 动态 import 自己 .mjs 运行时的机制，症状同上。产物注意：build 会多出两份 ~24MB wasm（我们引的 asyncify + ort 内部 import.meta.url 引用的另一份）——**安装包增重 ~50MB**，后续可 rollup 排除冗余份（待优化，不阻塞）。
  实测：client tsc + server tsc 干净 + **520 vitest 全过（+7：dashScale 落盘/收敛/非虚线丢弃、maskDashFor 倍率与新下限、深度归一化 近白远黑/负值/平坦 128/灰度转 RGBA）** + vite build 过 + **浏览器端到端**（vite5199+dev8787 临时账号：右键菜单实点「转深度」→占位节点 1%→88%（网关代理下载，data/depth-model/ 落 q4f16 18.24MB + q8 26MB 双档缓存）→成功落 TP 资产；**像素实锤：640×480 原尺寸、100% 灰度（非灰像素 0.00%）、动态范围 0–255 全幅、近处采样 251（白）/远处天空 13（黑）**——近白远黑语义正确；涂鸦编辑器全链路：标题「涂鸦 ·」、画线→虚线橡皮**滚轮 5 格**→沿线抖动涂抹→完成→落盘 doc 蒙版 `{dashedLine:true, dashScale:0.6065(=e^-0.5 精确)}` 且吸附仍在（±8px 抖动→相对 y 全 0、RDP 收敛 2 点）、产物节点「涂鸦-」；三入口文案「涂鸦/涂鸦…/转深度」无「标注」残留；控制台仅注入假象报错；临时账号已删、preview 已停）。
  ⚠ **须客户端重新打包**（改名+密度滚轮+转深度）**+ 服务端重新部署**（/v1/depth-model 模型下发路由）。**已知边界**：①首次转深度需联网下载模型（约 19–27MB，经网关；之后浏览器 Cache API+网关磁盘双层缓存离线可用）；②WebView2 的 WebGPU 需 Tauri `additionalBrowserArgs` 注 `--enable-features=WebGPU` 才走快路径（未配则 WASM 1–3s 档，功能不受影响）——**打包时记得配**；③深度节点产物是普通图片节点，可直接作垫图/涂鸦/再转深度。**待真机 QA**：Tauri 下转深度全链路（WebGPU 生效与耗时、真实照片深度质量）、虚线密度滚轮手感、生产域网关模型下发（HTTPS 域名下 fetch 与缓存）。
- **2026-08-03 第186轮（出海营（overseas · api.aiid.edu.kg）视频渠道接入 · 5 模型/新模式「overseas」·纯服务端 · server tsc 干净 + 沙盒冒烟 22/22 全过（scratchpad 独立 data + junction node_modules，真实 dev 库零触碰）+ dev 8787 热迁移核对（模式/渠道经 admin API 补正，见 ④）· 客户端零改动无需打包）**——用户提供「出海营 API 参考文档 · 视频生成Seedance格式」（docs/overseas-seedanceAPI文档.txt），定「增加新渠道新模式 overseas」。
  ①**上游（Base `https://api.aiid.edu.kg`，Bearer sk-；Seedance 任务格式）**（[overseas.ts](server/src/translators/overseas.ts)）：提交 `POST /api/v3/contents/generations/tasks`、查询 `GET .../tasks/{task_id}`，异步 submit+poll 走统一 `VideoDriver` 轮询管线（缺省 8s，文档无轮询频率要求）。**请求按文档「标准格式」**：文本与素材走 `content` 数组（`{type:"text",text}` + `{type:"image_url"|"video_url"|"audio_url", <同名对象>:{url}, role:"reference_image|reference_video|reference_audio", name}`），**name=ImageN/VideoN/AudioN 与 injectReferenceTags 注入的 @tag 图例编号一一对应**（文档：name 可供提示词按名称引用）；生成配置走顶层 `duration/ratio/resolution`；content 首项带与 prompt 同文的 text 项（文档示例即此形态）。分派：seedance 系 有素材= `mode:"reference_material"`、纯文生不带 mode（走上游默认）；**首尾帧=兼容 `mode:"i2v_first_last"` + image_url/end_image_url**（首帧=firstFrameUrl>素材第1图、尾帧=下一张未用图，缺任一/多余图/带视频音频明确报错——与苏打水/星辰/简梦F 同尺）；**gemini-omni 同接口调用**（文档专节）：mode 按素材自动定（带视频=edit / 带图=r2v / 纯文=t2v），时长 4/6/8/10 就近取档（并列取小）。守卫：图9/视3/音3 超限明确拒；prompt 必填非空前置拦截（**buildPrompt 无输入时的 `"{}"` 变量兜底形态也视为空**，冒烟抓到的死角）。查询结果**双形态防御**（文档同时给了顶层 `content.video_url` 与 `items[].content.video_url/items[].video_url`；items[].progress 兼容 "37%" 字符串）；succeeded 但缺 video_url=明确失败不空转；5xx/429/网络抖动=继续轮询；**结果域=本站域才附 Bearer**（文档未说结果需鉴权，防御性带头且绝不发给第三方 CDN——第153轮规则）；REHOST 白名单 +`.aiid.edu.kg`/`.zhongzhuan.chat`（文档素材示例 imageproxy.zhongzhuan.chat 代理域）。
  ②**5 模型种子**（[models.ts](server/src/store/models.ts) `os()` 工厂经**补种机制**入库，不 bump MODELS_SEED_VERSION）：`os933-sd2.0`（doubao-seedance-2-0-260128；720p/1080p/4k **routes 重定向上游后缀款** -1080p/-4k，价随档 3/4.5/6 每秒）、`os933-sd2.0-fast`（fast-260128；480p route→fast-480p，2/1.5 每秒）、`os933-sd2.0-mini`（mini-260615，1.5/秒）、`os933-sd2.5`（sd-2-5；480p route→sd-2-5-480p，3/2 每秒）、`os-gemini-omni`（gemini-omni，时长枚举 4/6/8/10，3/秒）。命名按素材量 933（**文档未给素材上限，933 为 Seedance 惯例值**；gemini 参照 jmp gemini-omni-flash 设 图5视1音0）；seedance 系 methods omni+frames、gemini 不声明（r2v/edit 翻译器自动定，无「方法」下拉）；时长 4-15 整数秒（文档未约束，与同类渠道同尺）；familyId 经 classifyFamily 自动归 fam-seedance / fam-gemini-omni；全按秒**占位价**（上游未公布单价），兜底价=每秒价×15（「默认按最高」规则）。
  ③**注册面**：新模式 overseas「overseas」（[modes.ts](server/src/store/modes.ts) `MODES_SEED_VERSION=16`）+ 渠道 `ch-overseas`「overseas（出海营）」（[channels.ts](server/src/store/channels.ts)，baseUrl 填根域、翻译器自拼完整路径）+ 协议 `overseas-video` 六处注册（Protocol 联合 / BUILTIN_PROTOCOLS / VIDEO_DRIVERS / dispatch case / upstream fallback `config.overseas` env `OVERSEAS_BASE_URL/OVERSEAS_API_KEY` / admin PROTOCOLS 下拉）+ errorScrub（品牌词 出海营/overseas + 域名 aiid.edu.kg/zhongzhuan.chat；**翻译器用户可见错误前缀统一「overseas」=与模式对外名同名**，经模式名占位符保护不被擦——模式改名后旧品牌词自动开始被擦，与简梦F 同构）+ .env.example 环境段。
  ④⚠ **排查记录：又踩 modes.ts「分两次编辑竞态」（第151轮同款，第154轮明令「版本+条目一次编辑」而本轮仍拆了两次 Edit）**——tsx watch 在「bump v16」与「加 overseas 条目」两次写盘之间热重启，dev 库 seedVersion 已抬 16 但条目错过版本闸（渠道名同理停在首版「出海营（aiid）」）。已按惯例 `POST /admin-api/modes` 补建 + `PUT /admin-api/channels/ch-overseas` 补改名；**生产部署不受影响**（那边 15→16 与条目一次跑）。模型 5 条/家族/计费均正常热迁移落库。
  实测：server tsc 干净 + **沙盒冒烟 22/22**（种子入库（模式/渠道/5 模型 模式·家族·方法·上限·计费）/ catalog 下发 5 模型且零泄漏上游真名（⚠ 断言用 260128/260615 版本号段——「doubao-seedance」整词会被简梦Z 自有模型 id `jmz-doubao-seedance-2-0-*` 误命中）/ resolveUpstream 四档路由 / 按秒计费 7 组合含路由档 / submit：omni 全素材 content 形状+@tag 注入+顶层配置+generate_audio 透传、纯文生不带 mode、带故事板追加末图+引用行、gemini t2v/r2v/edit+时长就档、首尾帧正反例、素材超限/缺配置/空提示词、上游错误体透传/缺 id/网络异常 / poll：queued/进度字符串/三种成功形态+本站域 Bearer+相对路径拼接/succeeded 缺 url 明确失败/failed 双形态/5xx=running/404=failed/网络抖动=running / errorScrub 模式名保留+出海营·域名·链接被擦+协议 id 不误擦；二次启动幂等顺带验证）。
  - **补充2（第188轮：⚠ 时长改「原样透传」·「静默修改永远禁止」定稿为全局规则 · server tsc 干净 + 沙盒冒烟 7/7）**——用户实测 sd-2-5-480p 请求 30s 仍被夹 15s（第187轮只把夹钳 15→30，本质仍是静默改写），定稿：**「静默修改永远是禁止的」**（已入 §9 全局规则）。[overseas.ts](server/src/translators/overseas.ts) 时长块重写：**原样透传**（数字/数字串归一为 number 值不变；非数字串也原样发=上游报错可见；缺省不发=走上游默认）——夹钳/gemini 4/6/8/10 就档/兜底默认 15 全部删除（gemini 就档文档明言上游自动完成）。档位把关=管理端模型参数（catalog params 客户端下拉），非法值=上游明确报错+失败自动退款。冒烟 7/7：30/40/3/"30"/缺省/gemini 9/"30s" 全部零改写。第187轮下条「翻译器夹钳放宽到 30…绝对安全夹钳」的说法**已被本条推翻，勿按它恢复任何夹钳**；其 ④c 参数档迁移（max 30）与兜底价修正仍有效。
  - **补充（第187轮：os933-sd2.5 时长 30s 修复·计费与实发不一致 bug · server tsc 干净 + 沙盒冒烟 4/4 + dev 热迁移实锤）**——用户报「sd-2.5 可生成 30s、前端请求 30s，服务端发上游只有 15s」。**双根因都是我第186轮钉的惯例值**：⑴ [overseas.ts](server/src/translators/overseas.ts) 翻译器 `body.duration` 硬夹 `Math.min(15,…)`——30 被静默砍成 15；⑵ 模型时长参数 max=15。⚠ **要害是计费口径**：按秒计费用请求参数里的 30 扣费、实发上游只有 15=多扣一半（此前的 30s 单需人工核补）。修法：①翻译器夹钳放宽到 30（各模型真实档位由 catalog params 把关，这里只做绝对安全夹钳；不支持的时长上游明确报错+自动退款）；②种子 os 工厂加 maxDur 参数、sd2.5=30，兜底价按「默认按最高」改 90/路由 60；③**存量库定向迁移 ④c（osSd25DurVersion）**：只动 duration.max→30；兜底价**仅当仍为旧种子值（45/30）才改**——dev 实锤：用户已把该模型定真价（cost 1875/480p 70每秒），守卫正确生效分文未动、只补了 max。冒烟 4/4：全新种子形状 / 30 直通上游 / 40→30 夹钳·8 原样·缺省 15 / 计费与实发一致。**随第186轮同一次部署即可**（首启自动跑 ④c 迁移）。
  ⚠ **须重新部署服务端**（首启自动补种 overseas 模式+5 模型+渠道 + ④c 时长迁移；**部署后到管理端「overseas（出海营）」渠道填 sk- 密钥**（或环境 OVERSEAS_API_KEY）——不填则提交明确报「未配置上游密钥」）；**客户端零改动无需打包**（模式/模型/方法下拉全走 catalog 热更 ≤30s）。**运营注意**：①5 模型全为按秒**占位价**，上线前管理端定真价（无价格情报，建议先小额单摸底）；②**素材上限/时长档/比例全按文档推断或惯例值**（文档未给硬限制）——首批用户单观察请求记录③④段实锤能力后按需回填 matLimits/params；③gemini-omni 的 r2v/edit 为自动分派，用户带视频素材即触发 edit（视频编辑语义）。**待真机 QA**：小额真单（720p 短时长）全链路（提交→轮询→成片转存 OSS）、1080p/4k 路由档真机可用性、content 数组 name 引用是否被上游正确识别（@ImageN 图例配对）。
- **2026-08-03 第185轮（标注编辑器：真机四反馈修复+纹章+双橡皮·纯客户端 · client tsc 干净 + 504 vitest(+6) 全过 + vite build 过 + 浏览器端到端全过（临时账号已删、真实键盘输入实测）· 须客户端重新打包）**——第184轮真机试用后用户四点反馈（文字不可用/加人形→改纹章/拖动残影/加橡皮擦+虚线橡皮）全落地。
  ①⚠ **「残影/内嵌图」根因修复（勿回退）**：首版在 `setDraft` 更新器里嵌 `setElements` 提交元素——**StrictMode 双调用更新器=元素双份提交**，两份完全重叠、拖走上面那份即露出「残影」（[ShotGroupView.tsx](src/nodes/ShotGroupView.tsx):42 同款已记录的坑，我自己又踩了）。改法：`draftRef`/`elementsRef` 为事件回调的权威数据源（state 仅供渲染），提交/擦除全在事件回调平铺完成 + `commitElement` 唯一入口按 id 去重；`commitText` 同款病（更新器内挂 queueMicrotask）一并修掉。
  ②⚠ **文字「无法输入」修复**：`autoFocus` 在「mousedown 同拍新建元素」场景不可靠（mousedown 默认聚焦行为在 React 提交后落地，把刚 autoFocus 的 textarea 焦点抢走）——改 `useEffect` 里 `setTimeout(0)` 强制 `focus()+select()` + 画布 `mousedown preventDefault`（既保住输入框焦点也防原生选区拖拽）+ 输入框加**确定/取消按钮**（按钮 `onMouseDown preventDefault` 防 blur 竞态）。真机键盘输入经浏览器 CDP 可信键入实测通过。（另记：测试面板 CDP 按键 `e.key` 为空串→Enter 提交在测试环境断言不了，属工具假象；真机 Enter 正常路径未变。）
  ③**纹章（用户由「人形图形」需求升级而来）**：新 `AnnoStamp{x,y,w,h,color,width,fill,builtin?,stampId?}`——**内置矢量章**（[annotationRender.ts](src/lib/annotationRender.ts) `BUILTIN_STAMPS`：人形/星形/爱心，0..100 路径按外接矩形缩放、`strokeScaleEnabled:false` 描边不变形、跟随颜色/填充/粗细）+ **PNG 自定义章**（新 [stampLibrary.ts](src/lib/stampLibrary.ts)：dataURL 存本机 localStorage `Qiji:annoStamps`，单章≤2MB/库≤8MB；⚠ **doc 只存 stampId 引用绝不把 base64 写进项目文件**——导出时已烙进合成图，跨设备再编辑缺章显示虚线占位框且**导出跳过占位不烙进成品**）。交互按用户定稿：工具栏「纹章」下拉章库选章（含 ＋PNG 添加/×删除）→ **指针即半透明预览** → **滚轮缩放**（native 非 passive 监听，0.04–1.5×短边）→ **点击盖章**；PNG 章保持原图宽高比。`konvaSpecFor` 加 `RenderCtx{stampImages}` 参数（编辑/导出共用图像查找表，保持纯函数可测）。
  ④**橡皮/虚线橡皮＝「依附在笔画上的擦除蒙版」**（⚠ 四易其稿的最终定稿，勿回退前三版：v1 整元素删除 → v2 涂抹切段 → v3 画布位置上重画原图 → **v4 用户指正 v3 错位后定稿：擦的对象是笔画不是画布**——v3 把还原画在画布绝对位置上，移动被擦笔画后擦除留在原地=错位。v4：橡皮笔刷分发为**各相交元素自己的蒙版**（锚点相对坐标），渲染时对该元素 destination-out 挖孔——透出的自然是元素**当前位置**下的原图（颜色随位置变化），**移动笔画时擦除效果自动跟随**；虚线橡皮=**沿笔刷路径方向间隔挖孔**（用户后续把 45° 斜纹改为沿路径 dash：destination-out 线直接带 `dash:[0.8w,0.8w]`+平头，实现反而更简——斜纹剪裁机制整体删除）。对一切元素类型统一生效（文字/PNG 章/填充图形都可局部擦），落在空白处的橡皮不产生数据）。
  模型（[annotation.ts](src/lib/annotation.ts)）：`AnnoMask{points,width,stripes?}` 挂在每个元素的 `masks?[]`（上限 40/元素）；`elementAnchor`（pen/arrow=首点、其余=x,y）+ `elementBBox` 粗筛 + `applyEraseStroke`（笔刷外接盒与元素外接盒相交即分发，坐标转锚点相对值；蒙版超出元素的部分渲染时天然无效果，不需精确命中；没碰到任何元素返回 null）；⚠ **translateElement 不动 masks**——相对坐标随锚点平移自动跟随，这是整个语义的核心。渲染（[annotationRender.ts](src/lib/annotationRender.ts)）：`maskSpec`（蒙版→圆帽粗线 destination-out；`dashedLine` 蒙版带 `dash`（maskDashFor：max(4, 0.8×直径) 等分）+平头=沿路径分段挖孔；单点「点擦」退化为 Circle——canvas 零长圆帽线段不出墨）；编辑器 [AnnotationEditor.tsx](src/components/AnnotationEditor.tsx) 新 `MaskedNode` 组件：⚠ **带蒙版元素必须 Group 隔离 + cache（rasterize）后再挖**——不 cache 的 destination-out 会把整层挖穿；cache pixelRatio 跟显示缩放（上限 3），选中投影/拖动挂在 Group 上；**在绘橡皮=半透明红色笔刷带预览**（松手才结算；虚线橡皮预览带 dash 分段观感）+ **吸附线条**（`snapToStroke`：指针在 0.75×笔刷直径内自动吸到最近的 笔迹/箭头/矩形边框/椭圆轮廓 上，`preferId` 1.4× 半径粘滞防被旁线抢走——沿线擦除时路径被修正得干净利落；纹章/文字无线可循不参与），松手 applyEraseStroke 分发+一步撤销；导出侧同构（Group+cache pixelRatio 1）。**v3 的 AnnoErase 独立元素/eraseCapsuleClip 整体删除**；`stripeBandsClip`/`stripeParamsFor` 保留（斜纹剪裁复用）；v2 顺带修下的人形章路径越界修正与 [stampShapes.ts](src/lib/stampShapes.ts) 模块拆分保留。
  实测：client tsc 干净 + **510 vitest 全过（65 文件：masks 清洗（坏蒙版丢弃/dashedLine 只认 true）、**蒙版随元素平移**（相对坐标不改写、绝对位置跟随）、applyEraseStroke（外接盒粗筛/空白返 null/相对坐标与 dashedLine 透传/上限 40/文字与纹章可分发）、maskSpec（destination-out 线/dash 分段与平头/下限/点擦退化 Circle））** + vite build 过 + **浏览器端到端像素级实锤（核心用例）**：纯色底画红粗线 → 橡皮竖扫 + 虚线橡皮扫 → **选择工具拖动整条线（+100,+80）** → 导出合成图：**橡皮缺口跟到新位置精确底色**、线身新位置纯红、旧位置干净——「移动笔画擦除跟随+颜色随位置取原图」实锤；**沿路径虚线**：虚线橡皮顺线横扫 → 线上采样出**规整交替段 红24px/底25px × 5 组**（段长=0.8×笔刷直径精确匹配）、未扫区域连续红；**红色预览+吸附**：橡皮沿线抖动 ±12px 横扫 → 涂抹中红色 0.4 半透明带可见且路径已吸到线上（y≈199.75）、松手蒙版**相对 y 全部=0**（完全贴线，共线点 RDP 简化到 2 点）；doc 元素 masks 随节点落盘。⚠ 排查记录：rAF 冻结环境下 Konva hit canvas 从未绘制 → 合成事件拖不动元素（假象），手动 `layer.draw()` 补齐后拖动正常——**后台标签测拖拽先强制 draw**。
  ⚠ **须客户端重新打包**；服务端零改动。**已知边界**：①自定义章库为**本机级**（localStorage）——跨设备再编辑旧标注时 PNG 章显示占位框（合成图不受影响）；②元素暂无缩放旋转（Transformer 后续）；③蒙版依附分发发生在**擦除落笔那一刻**与元素相交的对象上，之后新画的元素不受既有橡皮影响（符合直觉）；④带蒙版元素经 cache 渲染，数量很多时内存占用上升（上限 40 蒙版/元素兜底）。**待真机 QA**：Tauri 下 文字输入/Enter、纹章滚轮手感、擦除+拖动跟随的真机手感、缓存渲染性能（大图多蒙版）。
- **2026-08-02 第184轮（画布新功能① 图片标注·纯客户端 + 管理端存储页双栏 · client tsc 干净 + 498 vitest(+16) 全过 + vite build 过（konva 懒加载独立 298KB 分块，主包不变）+ 浏览器端到端全过（临时账号已删、零控制台报错）· 须客户端重新打包；存储页双栏须重新部署服务端）**——五个画布新功能（docs/画布新功能方案调研.md）经用户两轮问答定稿需求后的第一个：**图片标注**。
  ①**需求定稿**：目的=图上简单绘画，产物作其他模型的图片输入（本地绘画+OSS 保存，**不做 mask 双通道**）；工具=笔（停顿 350ms 自动 RDP 拟合平滑曲线）/箭头/矩形/圆（无填充|有填充）/文字/颜色（6 色板+自定义）+三档粗细；三入口=灯箱「✎ 标注」+图片节点悬停「标注」+右键「标注…」；产物=**新建 image.gen 节点**（原图不动，合成图 uploadMediaToCanvasAsset 落 TP 资产）；**可再编辑**=矢量 JSON 存 `node.data.annotation{sourceAssetId,doc}` 随项目落盘，产物节点再点标注凭原图+doc 继续改、完成写回本节点（旧图进 resultHistory 堆叠）。
  ②**架构**：纯模型 [annotation.ts](src/lib/annotation.ts)（坐标全程**原图像素空间**；RDP simplifyPoints/normalizedRect/translateElement/sanitizeAnnotationDoc 载入白名单清洗，上限 500 元素/4000 点）+ 渲染映射 [annotationRender.ts](src/lib/annotationRender.ts)（`konvaSpecFor` 元素→Konva 配置，**编辑 JSX 与导出离屏 Stage 共用一份=所见即所得**）+ 会话 [annotationStore.ts](src/store/annotationStore.ts) + 入口/收尾 [annotate.ts](src/canvas/annotate.ts)（annotateNode/annotateUri/finishAnnotation，凭 blobByUri 反查 assetId）+ 编辑器 [AnnotationEditor.tsx](src/components/AnnotationEditor.tsx)（App 根 **lazy 挂载**——konva 不进主包，无会话零成本；绘制走 window 级 move/up 超边不断线；键盘 capture 拦截 Esc/Ctrl+Z/Delete 不漏给画布快捷键）。新依赖 konva@10 + react-konva@18（全 MIT，perfect-freehand 未引入——Line tension+RDP 已覆盖「停顿转曲线」）。
  ③⚠ **规则（勿回退）**：⑴矢量 doc 只存坐标 JSON **绝不存 base64/uri**（§7.1 161MB 事故模式；stripHeavyRefs 不碰 node.data，存了就随项目膨胀）；⑵图像字节一律 fetch(uri)→createImageBitmap（thumbGen/shotGroupOps 同款教训，asset:// 直喂 <img> 两条路都死）；⑶导出用**离屏 Konva.Stage 同步 draw**（rAF 无关），编辑层实时渲染依赖 react-konva batchDraw（走 rAF）。
  ④**实测**：client tsc 干净 + **498 vitest 全过（65 文件，+16：RDP 简化/矩形翻正/平移/载入清洗坏元素丢弃/颜色宽度收敛/数量截断/konvaSpecFor 五类映射）** + vite build 过 + **浏览器端到端**（vite5199+dev8787 临时账号注册登录：注入 data: 底图开编辑器→14 工具按钮/双层 Stage 998×624→合成鼠标事件画 笔/矩形(有填充)/箭头/文字→**完成**→canvasStore 出现 image.gen 节点 `TP00007678`+doc 7 元素 640×400→**服务端 /raw 取回合成图实锤 11,184 个红色标注像素**→灯箱入口 ✎ 标注 开编辑器且灯箱关闭；临时账号已删、client dev 已停）。⚠ **排查记录（环境假象，第131轮同款）**：测试面板后台标签 rAF 冻结→react-konva batchDraw 不跑=「画了不显示」，手动 lay.draw() 当场 3394 采样点实锤渲染路径正常——**后台标签测标注一律以 场景图/导出结果 断言为准**。
  ⑤**同轮顺带**：管理端「存储」页「保留策略·清理预览」+「容量总览」改**双栏**（grid minmax(480px,1fr) 自适应回落单列，OSS 配置卡仍在下方；1920px 实测两卡并排各 814px 无横向溢出零报错）。
  ⚠ **须客户端重新打包**（标注功能）**+ 服务端重新部署**（存储页双栏，页面每请求现读部署即生效）。**已知边界**：①灯箱对无三元映射的图（如纯远程 url）可标注但产物 `sourceAssetId` 为空=不可凭原图再编辑（对合成图重新标注兜底）；②元素支持移动/删除/撤销，暂无缩放旋转（Transformer 后续可加）；③文字双击可改内容、暂不可改字号颜色（删了重打）。**待真机 QA**：Tauri 下三入口全流程（真实项目图片节点标注→新节点垫图可用、产物节点再编辑写回、灯箱从资产页进标注）、WebView2 下笔迹手感与停顿拟合观感、大图（4K）编辑器缩放性能。
- **2026-08-02 第183轮（P4 第二批：结算闸门 credits.settle·纯服务端 · server tsc 干净 + 沙盒冒烟 49/49（含真实多进程崩溃撕裂/作废自愈）+ dev 端到端（扣费/402 拦截/重启对账退款/流水端点）· 客户端零改动无需打包）**——按第182轮收窄后的目标（原子性而非性能）落地。**新增 `store/credits.ts`**：`settle()` 把「用户侧扣款 + 归属链各级渠道商扣款」收成一次结算——全通过才动钱、任一不足一分不动；SQLite `credit_ops` 流水表先写 pending 意图（含每账户 pre/post 余额）→ 改内存 + 两文件各自原子落盘 → 标 done；`reverse()` 原路退回（退款走同一通道也是原子的）；`selfHealCredits()` 启动自愈（`index.ts` 在 reconcileOnStartup **之前**调）：无任何账户到 post → 作废（aborted），部分到 post → 补齐剩余（healed），余额已被手工改动的账户保守跳过留痕。**修掉的两个病灶**：① **跨 await 竞态（每天都在发生）**——旧顺序 planBilling(查余额) → `await dispatchGenerate`(秒级) → applyBilling(扣)，窗口内并发请求都能过预检，回来时 `chargeCreditsAs` 静默 ok:false 且返回值被丢弃 →「用户没扣、渠道商照扣」，异步任务还按 plan 金额登记 billing、失败退款退一笔从未扣过的钱=凭空造币。**现在扣款移到 dispatch 之前**（`chargeBilling`，校验与扣款同一同步块，窗口归零），⚠ **勿再挪回 dispatch 之后**；同步失败 → `reverse` 原路退，异步 → 按**实扣快照 Charged**（不是 plan）登记 billing。② 两文件间崩溃窗口——由 pending 流水 + 启动自愈兜底。**顺手修的三处旧账**：(a) tasks.ts 退款钩子由「用户/渠道商两把分开的尺」合成一把 `setBillingReverseHook`（分两次调等于把原子性在退款侧再漏一遍）；(b) reconcile.ts 孤儿日志退款旧实现**只退用户不退渠道商链**、且团队共享模式会退给没付过款的团员——现走 settle 两侧同退，`LogMeta` 新增 `payerId`（团队共享=团长；存量日志无此字段回退本人=旧行为）；(c) users/agents 拆出「只改内存」原语 `applyUserCreditsDelta`/`applyAgentCreditsDelta` + `persistUsers`/`persistAgents`，由 settle 统一落盘时机（单账户日常增减仍走原带 persist 函数不受影响）。**附带产物**：`GET /admin-api/credit-ops?accountId=&ref=&limit=` 只读积分流水（每单含 pre/post，ref 可传 logId/taskId 回溯）；`pruneCreditOps` 启动时裁 180 天前的 done 行（pending/healed/aborted 永久保留供人工核）。⚠ **刻意不迁 users/agents 整表进 SQLite**（第182轮实测写盘 0.68ms 可忽略，迁移平添「谁是真相之源」风险）——余额真相仍在 JSON，SQLite 只记结算事件。dev 端到端已验：扣费落流水、余额不足 402（含批量语义）、任务在途杀进程重启 → 对账退款走新钩子（credits 15→25、totalSpent 回冲、流水 refund/done ref=taskId）。**须重新部署服务端**；客户端零改动。（P4 第一批：请求记录 logs + log_details 迁 SQLite·纯服务端 · server tsc 干净 + 沙盒冒烟 18/18（对 dev 真实日志 5,224 条 + 224 条详情跑迁移）+ 二次启动幂等 + 写入吞吐实测 · 客户端零改动无需打包）**——解决用户最早提的「服务端越用越卡」。⚠ **核心决策：只换持久层，查询逻辑一行不动** —— 列表/统计/筛选/导出/计费口径（`logCostFor`/`filterLogs`）全部仍在内存 `index` 数组上跑，改它风险大收益小；病灶不在查询，在写盘。
  ①**两个病灶**：⑴索引按天 JSONL —— 当天文件每次 flush **全量 stringify + 重写**（13k 条/天 ≈ 4MB），代价与「当天总条数」成正比，晚高峰每秒冻 event loop 20–50ms；⑵详情按条落 `data/logs/<id>.json` —— 目录堆了 39 万个文件，启动 `readdir` 全扫。
  ②**改法**（[logs.ts](server/src/store/logs.ts)）：新 `logs`(id/day/started_at/meta) + `log_details`(id/detail) 两表。⚠ **meta 整条以 JSON 存一列**（不做列映射）——`toMeta` 与落盘形状保持同一份，不会写着写着漂移。`markDirty(startedAt)` 改 `markDirty(meta)`、`flushIndex` 从「重写这些条所在的整天文件」变成「一个事务 UPSERT 这几条」：**代价从 O(当天条数) 降到 O(改动条数)**。`sweepOrphanDetails` 从「readdir 39 万文件逐个比对 unlink」变成一条 `DELETE ... WHERE id NOT IN (SELECT id FROM logs)`。`deleteDayFile` → `DELETE FROM logs WHERE day=?`。不再用 `scheduleSave`。
  ③**迁移**：`migrateFilesToSqlite()` 幂等（logs 表非空即跳过），把按天 JSONL + 详情文件一次性搬进表，完成后两个目录改名 `.migrated-sqlite` **留底不删**（可回滚）。与第129轮的 `migrateLegacyIfNeeded`（单文件→按天）串成链，老环境逐级迁上来。⚠ **错误擦除 flag 从 `INDEX_DIR/.error-scrub-v2` 移到 `DATA_DIR/.logs-error-scrub-v2`** —— 迁移后 INDEX_DIR 已改名，flag 写在那里会导致每次启动都全量重扫。
  实测（沙盒复制 dev 真实日志）：**18/18 全过** —— 迁移完整性（5,224 条 + 224 详情逐一对得上、抽样字段含 `agentCosts` 相符（第129轮踩过「重启丢渠道商归属」））、目录留底、读写语义不变（脱敏/③增量/②响应/resultLink 预抽/UPSERT 不新增）、统计筛选导出照常；**二次启动幂等**（不重复导入，5,225 条不变，加载 80ms）；**写入吞吐 500 次「提交+收尾」共 65ms = 单次 0.13ms**（旧实现在 13k 条/天的当天文件上单次是几十 ms 量级）。
  ⚠ **须重新部署服务端**（首启自动迁移，生产 39 万详情文件会让启动阻塞若干秒——一次性代价）；**生产部署前建议先备份 `data/qiji.db`**；客户端零改动。**已知边界**：内存索引仍常驻全部 ≤30 天记录（约 39 万条），这是**改造前就有的**、本轮未动——要消除得把查询也迁到 SQL，属后续可选项。**下一批**：users + agents。⚠ **范围已按生产实测收窄，勿再照「整表迁 SQLite 求性能」去做**——2026-08-01 生产实测：`users.json` **132 条 / 113KB / stringify 0.68ms**、`agents.json` 2 条 / 1.7KB / 0.01ms；即便按规划的 500 用户外推也只有约 2.5ms，与 logs 的「4MB × 每请求、单次几十 ms」**不是一个量级**。我最初把 `applyBilling` 与 logs 并列为「三处病灶」是**估高了**：logs 才是「越用越卡」的主因（本轮已解决），users/agents 的写盘开销在此规模下可忽略。**故下一批的目标从「性能」收窄为「原子性」**：`applyBilling` 里 `chargeCreditsAs`（写 users.json）与 `changeAgentCredits`（按链级逐次写 agents.json）之间若进程被杀，会「扣了用户没扣渠道商」。彻底消除需两者进同一事务（=迁 SQLite），但改动面应按这个更小的目标重新设计，不是照搬整表迁移。

- **2026-08-01 第181轮（P1 上线后两个真机 bug 修复：收藏卡裂图 + 缩略图恒为 0·纯客户端 · client tsc 干净 + 482 vitest 全过 + vite build 过 · 须重新打包客户端）**——P0/P1/P2 部署到生产后真机实测暴露两处。**生产验证结果**：引用上报 199 行 ✅（观察期真正开始计时）、收藏条目 2 ✅、size/user_id/storage 全部正常 ✅、缩略图 0 ❌、收藏页裂图 ❌。
  ①⚠ **收藏卡裂图（我踩了自己在 §9 锁定的规则）**：[AssetAssistant.tsx](src/components/AssetAssistant.tsx) 的资产卡是 `background:url(${it.uri})` **直显、不走 `useDisplayUri`**；项目资产的 uri 是本地 `asset://` 所以一直正常，而第179轮我给收藏项填的是服务端返回的 **OSS https 直链** —— **Tauri CSP 不允许 http(s) 图直显**（§9「id 是真理，url 是缓存」条目写死的规则）。修法与第146轮同款：**按 assetId 现查三元映射的本地副本优先**（`assetBlobs[f.assetId]?.localUri || f.url`），并新增一个 effect 对**跨设备收藏**（本机无副本）按 assetId 后台串行 `saveRemoteAsset` 补下载 → 落三元映射 → 卡片自动换源。
  ②⚠ **缩略图恒为 0（首版写法两条路都死）**：[thumbGen.ts](src/services/thumbGen.ts) 首版直接把 `asset://` / `http://asset.localhost/...` 喂给 `new Image()` 并设 `crossOrigin="anonymous"` —— **Tauri 资产协议不返回 CORS 头**：加 `crossOrigin` 则加载失败，不加则 canvas 被污染、`toBlob` 当场抛 SecurityError。改为 **读本地原件字节 → Blob → objectURL**（同源、不污染），`ensureThumb` 签名改 `(assetId, { localPath?, uri? }, contentType?)`，[assetPersist.ts](src/services/assetPersist.ts) 传刚落好的 `dest`；读不到字节才退回 uri（浏览器 dev 无 Tauri fs）。
  ③**部署事故记录（排查参考）**：首次报「收藏 404 / no such table: favorites」但 `size_bytes`/`user_id` 正常 —— 据此判定**服务端跑的是第177轮、客户端是第179轮**（客户端在正确显示服务端的 404）。根因是上传的 tgz 仍是 P0 那次的旧包。**判据可复用**：`ls src/store/favorites.ts` 在容器内是否存在 + `POST /v1/assets/ref` 回 401（已上线）还是 404（旧代码）。
  ⚠ **须重新打包客户端**；服务端零改动。**已知边界**：缩略图只在 `saveRemoteAsset` 路径生成（新下载的资产），存量资产无缩略图 —— `has_thumb` 为空时前端仍用原图，行为不变。

- **2026-08-01 第180轮（P2 观察期：保留策略配置 + 清理预览（只算不删）·纯服务端 · server tsc 干净 + admin script 语法过 + 沙盒冒烟 18/18 + dev 8787 浏览器端到端（真实库 27,833 行，零控制台报错）· 客户端零改动无需打包）**——P0+P1 已部署上线，观察期开始计时。
  ①**保留策略**（新 [retention.ts](server/src/store/retention.ts) → `settings.retentionDays`）：`tmp` / `unref` / `ref` 三档天数**全部可配**（1–3650 收敛），默认 7/7/14。规则：有收藏（用户或平台）或在共享素材库里 → 永久保留；被引用过 → `last_ref_at + ref`；从未引用 → `created_at + unref`；TP 前缀走 `tmp`。
  ②⚠ **本模块没有任何删除能力，这是有意的**：P2 是「只统计不删」的观察期（方案 §10 写死 ≥30 天），管理端刻意不提供任何「立即清理」端点/按钮，防止观察期内手滑开闸。冒烟里有一条断言直接检查模块导出里不含 delete/purge/execute 类函数。
  ③⚠ **修了两个会静默失效的实现缺陷（沙盒实测抓到，勿回退）**：
     ⑴ **TP 的 `tmp` 天数原本只能调长不能调短** —— 首版先按 `unref/ref` 粗筛再在 JS 里按 tmp 复核，于是 `tmp < unref` 时处于两者之间的 TP 永远进不了候选，与「临时资产活得更短」的设计意图相反（今天 tmp=unref=7 看不出来，一旦按最初设想调成 3 天就静默无效）。改为 **SQL 里用 `CASE WHEN id LIKE 'TP%'` 分支各用各的天数**，一次筛到位。
     ⑵ **「因收藏受保护」计数恒为 0** —— `addFavorite` 会把 `last_ref_at` 刷成当下，被收藏的素材根本进不了候选集，在候选循环里数自然是 0。改为**独立统计存活素材中被收藏/在共享库的数量**（分批 900 个查，避开 SQLite 变量数上限）。运营要看的是「永久保留了多少」，不是「差点被删多少」。
  ④⚠ **顺带修一个加载顺序耦合**：[favorites.ts](server/src/store/favorites.ts) 在模块加载期预编译了一条引用 **assets 表**的语句，但它并不 import assets.ts —— 谁先被 import 决定成败（生产只是碰巧 routes 先加载了 assets，沙盒里当场 `no such table: assets`）。改为**首次使用时才编译**，解开耦合且保留预编译复用。
  ⑤**管理端**：「存储」页顶部新增「保留策略 · 清理预览」卡（三档天数可改 + 保存即重算；KPI=将被清理/可释放容量/因收藏受保护/因共享库受保护；按档位表 + 各档抽样 10 条供人工核对）。⚠ 两条自动告警：`size_bytes` 未回填时标注「可释放容量是**低估**的」；将清理占比 >30% 时红字提示「**首次上线必须按存量特赦分批执行**」。端点 `GET/PUT /admin-api/retention`。
  实测：server tsc 干净 + admin script 语法过 + **沙盒冒烟 18/18**（ISO 串比较真的命中「从未引用」档、引用时间覆盖创建时间、tmp3<unref7 时 TP 照样命中且不误伤正式资产、收藏/平台收藏豁免、墓碑不重复计入、汇总口径自洽、无删除函数）+ **dev 8787 浏览器端到端**（卡片顺序、观察期徽标、天数回填 7/7/14、KPI 与档位表渲染、页面上不存在任何删除/清理按钮、零控制台报错）。dev 真实库预览结果：27,791 个待清理（TP 7,644 + 未引用 20,147 + 已超期 0），因共享库受保护 2 个 —— 「已超期 0」符合预期（`last_ref_at` 是 P1a 刚上线的字段，还没有素材攒到 14 天）。
  ⚠ **须重新部署服务端**；客户端零改动无需打包。**观察期使用方式**：每隔几天看一次这个预览，重点看「被引用过但已超期」那一档随时间怎么变——它从 0 开始涨，说明引用数据在正常积累；30 天后这个数字稳定下来，才是判断清理策略是否合理的依据。

- **2026-08-01 第179轮（P1b/c/d：收藏服务端化 + 配额 + 扩容卡 + 缩略图·两端 + 两控制台 · server/client tsc 干净 + 482 vitest 全过 + vite build 过 + 两控制台 script 语法过 + 服务端沙盒冒烟 41/41 + dev 8787 端到端全过（测试数据全清）· 须两端一起发）**——接第178轮 P1a。
  ①**收藏服务端化**（新 [favorites.ts](server/src/store/favorites.ts)，SQLite `favorites` + `quota_grants` 两表）：⚠ **多人收藏取并集**（还有一条记录就永久保留）；⚠ **平台收藏优先级最高且不占任何人配额**（用户全取消、平台还收着＝依然永久保留）；⚠ **取消收藏绝不立即删** —— 一律把 `last_ref_at` 刷成当下、转入「被引用 +N 天」倒计时；⚠ **删用户级联删其收藏与额度，但先刷 last_ref_at**（删用户≠删他的素材，走倒计时而不是掉进「从未引用」档）；⚠ **用量实时 SUM 不做计数器**（计数器不同步是经典 bug，十万行带索引求和微秒级）。端点 `GET/POST /v1/favorites`、`DELETE /v1/favorites/:id`、`POST /v1/favorites/flags`（批量问「这些我收藏了吗」，渲染 ☆ 用，一次查完不逐个问）。
  ②**配额**：`settings.favQuotaBytes`（默认 200MB，可按用户 `User.favQuotaBytes` 覆盖）+ `teamLibQuotaBytes`（默认 2GB）。⚠ **生效配额 = 基础 + SUM(未过期 quota_grants)**；⚠ **配额的本质是「永久保留额度」不是「存不下」** —— 超额只挡新增收藏（409 + 差额字节数），用户照常生成、照常使用；文案硬性要求是「收藏空间已满」而非「存储失败」（冒烟里有断言锁着）。
  ③**扩容卡**（新 [storageCodes.ts](server/src/store/storageCodes.ts)，JSON 与团队码同构）：渠道商门户签发，**开码积分实扣单级**（第172轮补充5 定稿，复用 `changeAgentCodeCredits` + `logCodeIssue` 同一条消耗日志通道）；⚠ **规格冻结进卡**（bytes/days 在签发时写死——事后管理端调档不改变用户已买到手的东西，冒烟有断言）；⚠ **多张卡容量叠加、各自独立计时**（每张一行 grant，不合并；过期的按 `expires_at` 自动不计，不需要清理任务）；未核销可作废（无退回），已核销留档不可删；团队卡须团长核销、不能核销到个人。端点 `POST /v1/storage-codes/redeem` + 门户 `/agent-api/storage-codes` CRUD + 源站 `/admin-api/storage-codes`（源站签发不扣积分，与发激活码同语义）。
  ④**缩略图**（新 [thumbGen.ts](src/services/thumbGen.ts)）：图片资产落本地后顺手 canvas 降采样成 **256px WebP** 直传 `thumb/` 前缀（预签名 PUT，与原图**同存储档**）。⚠ **缩略图不进 assets 台账**（是原图的附属品：无独立 id、不占配额、不参与对账），只在原图行上打 `has_thumb`；⚠ 全程 best-effort + **同一 id 本会话只尝试一次**（失败不反复烧流量）；只处理图片。挂在 [assetPersist.ts](src/services/assetPersist.ts) `saveRemoteAsset` 成功分支，用刚落好的本地副本降采样＝零额外下载。
  ⑤**客户端收藏改造**（新 [favoritesStore.ts](src/store/favoritesStore.ts) + [AssetAssistant.tsx](src/components/AssetAssistant.tsx)）：⚠ 旧收藏是 `Qiji:favoriteAssets` **按显示 uri 存在 localStorage** 的——换机即失效、服务端完全不知情，而清理上线后「有没有收藏」直接决定素材是永久保留还是 14 天后消失，这种本地收藏起不到作用。改为**按 assetId 落服务端**（uri→assetId 经三元映射 `blobByUri` 反查）；带**一次性迁移**（把旧本地收藏反查得到 id 的部分补传上去后清掉本地键，反查不到的只能放弃——本地收藏本就没记 id）；收藏页改从服务端拉、分类按台账 id 前缀（复用 `sharedCatOf`，id 是真理）；面板加**配额条**（用量/上限/扩容部分，满了变红）。⚠ **文案按方案 §11 风险表硬性要求**：「收藏后云端永久保留，可跨设备打开」——7/14 天规则下「跨设备可用」≈「有没有收藏」，这句必须说清楚。未同步到云端的素材（无 assetId）明确提示不能收藏。
  ⑥**两控制台**：管理端新「收藏与配额」页（默认配额 + 扩容卡规格可调、按拥有者的收藏占用表带占比条、扩容卡签发/作废）+ 平台收藏端点 `POST /admin-api/quota/pin`；门户新「扩容卡」页（与团队码页同构：实时费用预览、余额不足禁用按钮、生成弹窗复制全部、作废）。
  实测：两端 tsc 干净 + **482 vitest 全过** + vite build 过 + 两控制台 script 语法过 + **服务端沙盒冒烟 41/41**（默认配额档位/收藏幂等/用量 SUM/取消不删对象且刷 last_ref_at/多人并集/平台优先/配额硬闸与差额计算/文案断言/规格冻结/事后调档不影响已发卡/重复核销拒/团队卡不能核销到个人/额度叠加 400MB/过期自动不计/删用户级联/缩略图前缀与标记）+ **dev 8787 端到端**（管理端「收藏与配额」页三卡渲染、默认值 200MB/2048MB/300/1000 全对、源站签发 2 张 sc- 卡并入表；用户侧 收藏→列表配额 200MB→核销扩容卡→400MB（基础 200+扩容 200）→重复核销拒→取消收藏后 `purged_at` 仍为 null 且 last_ref_at 已刷新）；测试写入的收藏/额度/扩容卡/settings 键全部清零。
  ⚠ **须重新部署服务端 + 客户端重新打包**（与第178轮 P1a 同一次发版）。**已知边界**：①门户「扩容卡」页只做了语法校验与结构同构核对，**未做浏览器端到端**（需渠道商登录态）——待真机 QA；②缩略图目前只在 `saveRemoteAsset` 路径生成，存量资产无缩略图（`has_thumb` 为空时前端仍用原图，行为不变）；③收藏迁移只能救回「本机三元映射里查得到 assetId」的旧收藏。**待真机 QA**：打包后资产助手收藏 ☆ 跨机可见、配额条与超额提示、扩容卡核销、缩略图是否真的生成（看桶里 `thumb/` 前缀）。

- **2026-08-01 第178轮（P1a：引用上报 `last_ref_at`·两端 · server/client tsc 干净 + 482 vitest 全过 + dev 8787 端到端 5/5 全过（测试数据已清）· 须两端一起发）**——P0 已在生产跑通（见本轮末「生产对账实测」）。P1 拆四段，**先做引用上报**，理由是时间线：P2 的观察期要 ≥30 天，而观察期依赖 `last_ref_at` 有数据 —— **引用上报晚上线一天，清理就晚上线一天**；收藏/配额/扩容卡/缩略图都不卡这条线。
  ①**服务端** [assets.ts](server/src/store/assets.ts) `touchAssetRefs(ids)`：一条 `UPDATE ... WHERE purged_at IS NULL AND id IN (...)` 走主键（EXPLAIN 实测 `SEARCH assets USING INDEX sqlite_autoindex_assets_1`），几千 id 毫秒级；⚠ **绝不复活墓碑**（purged_at 非空的行不刷——对象已经不在了，刷了会让统计撒谎），但把它们连同台账里没有的 id 一起回报给客户端作 `needHeal`（客户端据此走死链自愈 reputAsset）；上限 **2000 个/次**防滥用；⚠ **不做资产粒度鉴权**（报别人的 id 只会让那个资产多活一阵，无信息泄漏无利可图，为此引入几千次逐条归属查询不划算）。端点 `POST /v1/assets/ref`（authed）。
  ②**客户端** 新 [assetRefReport.ts](src/services/assetRefReport.ts)：id 源 = 三元映射 `assetBlobs` 的键（凡经服务端生成/上传并在本机注册过的资产都在里面）；**打开项目强制上报一次**（最完整的一次引用声明）+ **保存时节流 10 分钟**（自动保存很频繁，而保留策略粒度是「天」，每次保存都报纯属浪费）；全程 fire-and-forget（失败静默，绝不拖慢打开/保存）。[managedClient.ts](src/services/managedClient.ts) `reportAssetRefs` + [contract.ts](src/contract.ts) `Endpoints.assetRef`。**已知边界**：仅存在于共享素材库、本机从未下载过的素材不在 assetBlobs 里 → 不上报（由共享库素材自身的永久保留兜底）。
  实测：两端 tsc 干净 + **482 vitest 全过** + **dev 8787 端到端 5/5**（2 存活+1 不存在 → `updated:2, needHeal:["C99999999"]` / 幂等复报含重复 id 去重后仍 updated:2 / 缺 ids 400 / 空数组 updated:0 / 2500 个 id 只处理 2000 上限生效）+ 查询计划走主键；测试写入的 2000 行 last_ref_at 已清零、临时凭据文件已删。
  ⚠ **须重新部署服务端 + 客户端重新打包**：只部署服务端 = 端点在但没人报，`last_ref_at` 恒空 → **观察期不会开始**；只打包客户端 = 上报 404 被静默吞掉（无害但同样不产生数据）。**两端都上线那天才是观察期第 1 天。**
  ⚠ **排查记录（第七次踩同一个坑）**：本机 8787 上有 **4 个僵尸进程**，其中 PID 37712 是**不带 watch 的 `tsx src/index.ts`**（永不热重启），新起的实例 EADDRINUSE 当场死掉、请求全打在旧代码上 → 新端点报 404 的假象。已按 `Get-CimInstance Win32_Process | Where CommandLine -match tsx` 全树清杀后重启。**下次遇到「改了代码不生效」先查这个**（历史同款：第107/112/115/118/134/145/148/163轮）。
  **生产对账实测（P0 收尾，2026-08-01）**：台账 123,593 行 / 桶 112,992 个对象 521.30 GB；回填 size 112,774 行、死记录打墓碑 10,807 行（8.7%）、孤儿补登 168 行。⚠ **墓碑的正确性由数据自证**：完成后桶内 113,047 个对象 vs 存活台账 113,008 行、521.43 GB vs 521.35 GB —— 若那 10,807 条是误判（对象其实还在），桶里该有约 12.4 万个对象。剩 44 个「id 撞车孤儿」（台账 `.jpg`／桶里还有 `.png` 之类，80.58 MB）**全部确认无台账 url 指向、可删**，主要集中在 `TP00007607–7677` 一段历史区间，但 `TP00022303` 说明该路径仍可能偶发 —— 留待 P1 定位。**容量画像**：image 87,985 个 278.74 GB / video 20,936 个 241.17 GB / audio 4,000 个 1.44 GB；按年龄 ≤7 天 115.66 GB、7-14 天 170.40 GB、14-30 天 229.96 GB、>30 天仅 5.34 GB。**实际产出约 3,000 个/天、16.5 GB/天，只有原估（1.3 万/天）的 1/4**；按「被引用 +14 天」清理后稳态约 230–290 GB（现 521 GB），2T 配额非常宽裕。⚠ 但一次性开闸会删掉 235 GB（45%）——**P3 必须按 §9 存量特赦执行**。另：对账中途遇一次 `ECONNRESET`，崩在只读的列桶阶段、台账分毫未动（脚本「先全部列完 → 再算 → 最后一个事务写完」的结构使然），重跑即可；后续可把 S3 重试次数调高。

- **2026-07-31 第177轮（存储改造 P0：台账加 7 列 4 索引（启动自动迁移）+ 存储档（storage profile）落地 + 资产归属记账 + 管理端「存储」页 + 三个运维脚本 · server tsc 干净 + admin script 语法过 + 脚本沙盒 21 场景全过（伪 S3）+ 服务端沙盒冒烟 22/22 + dev 8787 真库热迁移实锤（27,833 行）+ 浏览器端到端 · 客户端零改动无需打包）**——接[存储与数据结构改造方案](docs/存储与数据结构改造方案.md) P0。
  ①**台账结构**（[assets.ts](server/src/store/assets.ts)）：`storage / size_bytes / user_id / agent_id / last_ref_at / purged_at / has_thumb` 七列 + `idx_assets_created|user|sweep|type` 四索引。⚠ **走「启动时幂等补列」而不是「先部署代码、再手工跑迁移」**（与第140轮 duration_ms 同模式）——消灭「新代码已上线、新列还不存在」的空窗；`scripts/migrate-p0.mjs` 做同一件事，两边幂等先跑哪个都行。⚠ **只加列只加索引，一列不动一行不删**（重建表风险真收益假）。存量一次性标 `storage='legacy'`。
  ②**存储档**（新 [storage.ts](server/src/store/storage.ts)）：`getProfiles/activeProfile/profileOf/keyFor/publicUrlOf/locate`。未配 `settings.storageProfiles` 时只有 legacy 档（= 当前桶 + `assets/<id>.<ext>` 平铺），**行为与改造前逐字一致**；将来换桶/换布局（`kind/yyyy/mm` 已实现）= 改配置不改代码。[oss.ts](server/src/store/oss.ts) 的 `ossPut/ossDelete/ossPresignPut/ossPublicUrl` 全部接受目标档，S3 客户端按「endpoint+key+region」多档缓存。⚠ **`reputAsset` 死链自愈必须按该行自己的 `storage` 档定位**（原来固定用当前配置的桶+平铺键）——老资产若自愈到新桶/新前缀，就落在所有 lifecycle 与清理规则的射程之外，而公网直链还指着老位置＝既没修好又白占容量。⚠ `ossDelete` 同理（删老资产用 active 档会打到新桶，「以为删了、其实老桶还占着」）。`assetUrl` 存的 url 永远优先（换布局后老对象照常可访问）。
  ③**资产归属记账**：`createAsset`/`commitDirectAsset` 入库即写 `size_bytes`（直传取 HEAD 的真实字节数，不信客户端声称的）+ `storage` + `user_id/agent_id`。⚠ **归属走 AsyncLocalStorage（`runWithAssetOwner`）而非逐层加参数**——资产产出点在 routes→dispatchGenerate→20+ 翻译器→十几个异步轮询驱动→rehostVideo→createAsset 的深链末端，逐层传参要改遍所有翻译器且漏一处就静默产出空归属；ALS 在 routes 三个入口（generate/batch/rehost）挂一次即全覆盖，**新增翻译器零改动**。实测穿透 `void` 起的后台轮询循环 + 多层 await/setTimeout，且无上下文时为空、不串上一个请求。已知边界：重启后的 `resumeVideoPolling` 不在任何请求上下文 → 归属为空（量极小，且那些资产本属重启前的任务）。
  ④**管理端「存储」页**（原「OSS 存储」页扩写，[admin/index.html](server/src/admin/index.html) + `GET /admin-api/storage/stats`）：容量 KPI（存活/占用/墓碑/存储档）+ 六张分布表（类型/年龄/月份/存储档/用户 Top20/渠道商 Top20，带占比条）。**纯 SQLite 聚合零 OSS 请求**，页面秒开可随时刷。`size_bytes` 未回填时出醒目告警条并给出回填命令（不假装数字是准的）。⚠ **对账/清理是写操作，不放管理端**——走脚本，避免同一套逻辑两处实现。
  ⑤**三个运维脚本**（`server/scripts/`，**默认 dry-run**，零 server 源码依赖）：`migrate-p0.mjs`（补列建索引）/ `reconcile.mjs`（回填 size、死记录打墓碑、孤儿补台账）/ `verify.mjs`（只读校验+容量分布，退出码 0/1/2）+ [README-P0.md](server/scripts/README-P0.md) 运维手册。⚠ **孤儿补台账必须同步抬 `asset_seqs`**（补登的 id 可能大于当前 seq，不抬会分配出重复 id——沙盒实测 100→901）。两道安全闸（写入前中止）：桶列到 0 个对象、死记录占比 >20%（防再犯「对静态快照对账」）。⚠ **脚本必须在服务器上对实时台账跑**。
  ⑥**顺带修方案文档的类型陷阱**：清理 SQL 里 `created_at < :now - N*86400` 是错的——`created_at` 是 TEXT ISO 串、`last_ref_at/purged_at` 才是 INTEGER epoch 秒，字符串比数字恒为 false ＝ **清理任务会静默不清任何未引用资产**。已改为与 ISO 串比较（字典序即时间序，且走 idx_assets_created）。
  实测：server tsc 干净 + admin script 语法过 + **脚本沙盒**（伪 S3 + 同构测试库：100 正常/12 死记录/7 孤儿（含 seq 900/901）/1 个 id 撞车孤儿/2 个命名不符/1 行无 oss_key —— 三类全识别、apply 后复跑收敛、幂等复跑「无事可做」、两道安全闸均在写入前中止退出码 2）+ **服务端沙盒冒烟 22/22**（档解析/键推导/ALS 穿透深层异步链/无上下文不串/统计口径）+ **dev 8787 真库热迁移**（启动自动补 7 列 4 索引、27,833 行标 legacy）+ **浏览器端到端**（「存储」页 KPI 四张、六张分布表、未回填告警条、配置表单并存、零控制台报错）+ **并发安全实测**（另一连接 ALTER TABLE + CREATE INDEX 后，服务端预编译语句照常读写 → **迁移不需要停服**）。
  ⚠ **须重新部署服务端**（首启自动迁移；客户端零改动无需打包）。部署后在服务器上跑 `reconcile.mjs` 回填存量 size/孤儿/死记录，管理端「存储」页的容量才准确。**下一步 P1**：引用上报 `last_ref_at` + 收藏表 + 配额 + 扩容卡（观察期 ≥30 天后才上清理）。

- **2026-07-30 第176轮（提示词模板开放范围改按渠道分组按键 + 模型管理同组内拖动排序·服务端 + 管理端 · server tsc 干净（54 源文件）+ admin script 语法过 + 沙盒冒烟 22/22 全过（scratchpad 独立 data，真实 dev 库零触碰实锤）+ 沙盒 8917 浏览器端到端全过 · 客户端零改动无需打包）**——用户两点：①「提示词模板选择开放范围时，应该和模型一样直接展示渠道分组按键（源站、vip……），根据亮灭启动禁止，不需要全部和不开放（因为通过选择可以达到统一效果）」；②「模型管理的模型可在同组内排序，会影响到客户端显示」。
  ①**模板开放范围按分组（与模型侧第167轮完全同构）**：[templates.ts](server/src/store/templates.ts) 新 `TemplateDef.shareGroupIds` + `templateVisibleToAgent` 改 `audienceChain(agentId).some(aud => 旧清单命中 || shareGroupIds.includes(audienceGroupId(aud)))`——**旧受众清单（shareAgentIds）存量继续生效**（OR 判定）；`updateTemplate` 补空数组归一（[]→undefined，与 updateModel 同尺）；admin PUT/POST 本就整体透传 body 零改动。
  ②**管理端模板编辑器**（[admin/index.html](server/src/admin/index.html)）：删「全部受众/指定受众/不开放」分段 + 受众复选框（`PLATFORM_AUD`/`tplShareAudiences`/`tplShareAgentChecks`/`toggleShareAgent` 全删），改**平铺分组按键**（`tplGroupChips`：●亮/○灭 + 成员数与「·源站」标记）；**保存时归一**（`tplSharePatch`）：全亮→`shareScope:'all'`（自动含以后新建的分组）/ 全灭→`'none'` / 部分亮→`select`+分组清单（旧清单一并清空）。⚠ **未动过按键且存量是旧受众清单 → 返回 null=保存时不带开放范围字段**（绝不把存量语义抹成 none，沙盒+浏览器双验）；与模型侧「点选即存」不同，这里随编辑器「保存」生效（其它字段同语义）。
  ③**⚠ 顺带修撞名（第175轮引入）**：模板页「按渠道商查看」下拉的设置器与开放范围旧设置器同名 `setTplScope`（后定义者胜=下拉失灵）→ 视图筛选改名 **`setTplViewScope`**，旧同名函数随分段控件一并删除（注释留告警勿再撞名）。
  ④**模型同组内排序**：[models.ts](server/src/store/models.ts) 新 `ModelDef.order`（不入 MODEL_REFRESH_FIELDS）+ `ensureModelOrders`（首次排序把未设的按当前显示序压实 1..n）+ **`reorderModels(ids)`＝只把这批模型自己的 order 槽位重新分配**（⚠ 语义勿改成 reorderModes 那种整表重排：管理端是「按渠道/模式/家族分组 + 状态筛选」的视图，整表重排会把被筛掉的模型甩到末尾、破坏其相对次序）；[catalog.ts](server/src/catalog.ts) 排序键 **模式 order → 模型 order → 原始加入序**（客户端家族内线路/同模式款式顺序跟随，persist bump 目录版本 ≤30s 热更）；新端点 `POST /admin-api/models/reorder`（⚠ 须注册在 `/admin-api/models/:id` 之前）；管理端模型卡加 ⠿ 拖动柄 + 每组一个 grid 各自 `wireMfSort`（复用第165轮拖动基建）+ 卡片按 order 排序渲染 + 组标题提示「按住 ⠿ 可在组内拖动排序」。
  实测：server tsc 干净 + admin script `new Function` 语法过 + **沙盒冒烟 22/22**（scratchpad 复制 src+skills + junction node_modules，DATA_DIR 相对模块路径指向沙盒独立 data：模板 select+VIP 分组 → 商V 用户 catalog 可见/默认分组商与平台直属不可见、select+默认分组 → 源站与默认组商可见（源站归默认分组）、旧清单 `[platform]` 兼容、all/none 三方一致、空数组归一；模型 反转 c<b<a 槽位置换、首次排序全表压实、未参与排序的 x/y 相对序不变、**catalog 下发顺序跟随**、二次排序生效、跨组互不影响、ids<2 与未命中 400、排序后 catalog version 变化）+ **8917 浏览器端到端**（模板页分组按键渲染（默认分组 2·源站 / VIP 1）+ 点灭点亮 hint 三态 + 保存落库 select+VIP + 重载回显、旧清单模板未动按键保存后**原样保留**、点亮分组后替换为分组制；模型页 16 组卡片全带 ⠿、合成 DragEvent 拖动 → 插入预览+落库 order 交换+卡片顺序跟随+toast、模型卡「开放范围」分组下拉回归正常、零控制台报错）+ 收尾核对**真实 dev 库 101 模型 order 全未写入**（沙盒已删）。
  ⚠ **须重新部署服务端**（管理端页面随部署生效；存量模板无 shareGroupIds、模型无 order = 行为完全不变）；**客户端零改动无需打包**（模板可见性/模型顺序全走 catalog 下发）。**运营注意**：①旧「指定受众」模板在编辑器里显示为全灭+告警条，**点亮任一分组即切换为分组制**（不点则保存不动它）；②模型首次拖动排序会把全表 order 压实成 1..n（相对顺序不变）；③排序影响**客户端下拉顺序**（含「自动取该能力第一个可用模型」的落点），把最想让新用户默认用的排最前。**待真机 QA**：生产强刷管理端后模板页分组按键观感、模型卡拖动手感（大分组如简梦Z 21 张）、拖动后客户端 ≤30s 下拉顺序变化。
- **2026-07-30 第175轮（服务端去掉「翻译官」管理页 + 源站各页统一「按渠道商查看」筛选·纯服务端 · server tsc 干净 + admin script 语法过 + 8787 浏览器端到端实测过（真实数据只读为主，临时改动已还原）· 客户端零改动无需打包）**——用户两点：「服务端去掉翻译官吧，没啥用」+「源站所有界面都可筛选渠道商查看（已有用户、兑换码、统计、请求记录等的筛选，补充其他的）」。
  ①**删「翻译官」页**（[admin/index.html](server/src/admin/index.html)）：侧栏入口/`#tab-protocols`/TAB_TITLES 条目/loadProtocols 分派 + 自定义协议管理 UI（PROTO_MODES/protoRow/toggleProto/delProto/editProto/saveProto/parseProtoPaste 等 ~374 行）整删。⚠ **保留**运行时自定义协议引擎（store/protocols.ts、translators/custom.ts）+ `GET /admin-api/protocols` + 前端 `fetchProtocols`/`protoOptions`——**模型「协议」下拉依赖它**（存量自定义协议的模型仍可正常显示/选中/生成）；核实 data/protocols.json 为空、101 模型 0 个用自定义协议，删管理页无数据风险。
  ②**「按渠道商查看」一把尺**（scopeSelect/scopeName/isAgentScope/ensureScopeAgents）：三态值恒定 `__all`=全部 / `platform`=源站 / `<agentId>`=某渠道商，八页共用**同一份下拉**，只有**缺省**不同——运营口径页（用户/兑换码/统计/请求记录）缺省 `platform`（源站视为一个渠道商，先分清自己的账，续第116/168/171轮）；资源清单页（团队/共享素材/模型/提示词模板）缺省 `__all`（原本就是全量视图，加筛选不改口径）。
  ③**服务端配套**（[admin.ts](server/src/routes/admin.ts)）：新 `audScope`/`inAudScope` 一把尺；logScope 收编三态（`__all`=不设范围）+ logCostViewFor 对具体商仍按源站口径；users/redeem/teams（⚠ 按 leaderId 过滤须在 `adminTeamView` 映射**之前**）/shared-libs/**models（按可用性 `modelAllowedForAgent` 而非创建者——模型全归平台）**/templates（`listPlatformTemplates` + 各商自营，带 agentName 投影）逐页按范围返回。
  ④**渠道商范围下的只读/隐藏**：模板页渠道商自营模板**只读**（saveTpl/cloneTpl/delTpl 三处守卫 + 顶部只读说明条——自营提示词属该商，改动走其门户）；用户页隐藏「新建用户/批量激活码」、共享素材页隐藏「＋新建平台共享库」。
  实测：server tsc 干净 + admin script 语法过 + 浏览器逐页端到端（团队页曾漏 `?agentId=` 参数经自测发现并修：全部 1 / 源站 1 / 该商 0；模型页三范围计数一致=dev 库 101 模型全 `shareScope:'all'`，属正确行为非 bug）。⚠ **须重新部署服务端**（管理端页面随部署生效）。**已知边界**：预设管理页未加筛选（`PresetDef` 无 agentId/shareScope，需先扩数据模型+catalog 交付）。
- **2026-07-28 第174轮（提示词与预设「数据层真拆分」+ 资产拆分前后缀预设化（画风前缀 + 按类别前后缀自动挂胶囊）·两端 + 管理端 · server/client tsc 干净 + 474 vitest(+8) 全过 + vite build 过 + admin script 语法过 + 沙盒冒烟 29+12+6 全过（scratchpad 双沙盒 复制 src+junction node_modules、DATA_DIR 相对模块路径指向沙盒、真实 data 零触碰）+ 沙盒 8917 浏览器端到端全过 + dev 8787 tsx watch 热迁移实锤）**——用户两点需求 + AskUserQuestion 三答定案：分开深度=**数据层真拆分**；前后缀粒度=**按资产类别**（画风前缀独立于此，恒按项目画风、全类别通用）；关联配置=**预设上勾选适用范围**。语义（⚠ 勿回退）：**拆分推理只产资产正文，固定前后缀由客户端按配置自动挂「预设胶囊」，不再消耗 token 不等推理；现有推理提示词一字不动**。①**服务端独立预设库**（新 [presets.ts](server/src/store/presets.ts) → presets.json）：`PresetDef{id,name,category(画风/预设方案/自由分组),body,images,position(前缀/后缀),group(互斥组),autoAttach(五类范围 characters/crowds/scenes/organisms/items),enabled,order}` + 自有版本号 + 内置墓碑；**迁移每启幂等**：[templates.ts](server/src/store/templates.ts) 新导出 `extractPlatformPresetTemplates()` 把模板库里 无 purpose 且 分类∈{画风,预设方案} 的**平台**模板连管理端改动整体搬入（presetGroup→group、presetPosition→position；搬空后恒返 [] 不落盘）；templates 种子剔除 画风3+宫格3（防补种复活）；画风补互斥组「画风」/宫格补「宫格」/宫格旧种子正文升级（第164轮补充2 逻辑随条目迁入 presets.ts 继续生效）。渠道商自营的预设类模板**不迁**（自营预设不属平台预设库，已知边界）。②**catalog**：新 `Catalog.presets`（CatalogPreset 全文下发含 autoAttach，[contract.ts](src/contract.ts) 单一来源）+ version 追加 **`.ps{presetsVersion}`** 段（预设独立后改预设不再 bump 模板版本，自带热更段）；**兼容投影（⚠ 客户端全量升级后可删）**：预设同时以「模板形状」附加进 catalog.templates 的 画风/预设方案 分类——旧打包客户端的 预设下拉/画风选择/胶囊展开 照旧可用（不补这层，只部署服务端会让旧客户端预设下拉变空、已插胶囊以标记文本漏发上游）；新客户端见 catalog.presets 非空即走新轨天然忽略。admin 新端点 `/admin-api/presets` CRUD（`normAutoAttach` 白名单清洗）。③**管理端「预设管理」页**（[admin/index.html](server/src/admin/index.html)，接入组新页签）：左列表按分组（画风→预设方案→自由分组）+ 右编辑器（正文全文/插入位置/互斥组/排序/启停/参考图上传 + **「自动附加（资产拆分）」五类勾选**）+ 新建（id 限 ASCII 供胶囊引用）/克隆/删除（墓碑）；「提示词模板」页剥离 预设互斥组/插入位置 字段与「预设（无用途）」分组语义、CATEGORIES 去画风——模板页只剩可执行推理模板。④**客户端**：[presetSchemes.ts](src/lib/presetSchemes.ts) 源切换（catalog.presets 优先——**画风也入预设清单**（画风胶囊可展开、同组互斥、可手插）；旧服务端回退 templatesByCategory 旧口径）+ PresetScheme 增 category/autoAttach；[catalogStore.ts](src/store/catalogStore.ts) 新 presets() 选择器 + artStyles() 双轨（新=预设库画风映射回 CatalogTemplate 形状，Frame164 零改动）；**项目新增 `visualStyleId`**（[projectFile.ts](src/services/projectFile.ts)/[projectStore.ts](src/store/projectStore.ts) 全链持久化：新建项目记录所选画风预设 id（[Frame164.tsx](src/views/Frame164.tsx)，仅 catalog 画风；兜底画风/旧项目=空不挂））；新 [splitPresetAttach.ts](src/lib/splitPresetAttach.ts)：纯组装 `composeAssetPrompt`（顺序=【画风前缀】【类别前缀…】正文【类别后缀…】、幂等去重、空正文不挂）+ store 版 `attachSplitPresets`（画风 id 不在当前预设清单=不挂——未知胶囊不展开会把标记文本漏发上游，宁可不挂）。⑤**挂载点两处同尺**：资产模式 [assetMerge.ts](src/lib/assetMerge.ts) `mergeExtraction` 新增 decoratePrompt 钩子——**只装饰新增基础资产**（已有资产字段/变体绝不动，纯合并语义不变；mergeApply 与 Frame1693 继续提取两处传入）；画布 [canvasSpawn.ts](src/lib/canvasSpawn.ts) 新 `setAssetPromptDecorator` 注入钩子（缺省恒等保纯函数可测）——buildSpawn/IncrementalSpawner/buildRespawn 三路装饰、二次解析防重把 裸文/挂胶囊 双形态都认防重复补建。⚠ **装饰器注册=splitPresetAttach 模块体自注册 + pluginRegistry/ContextMenu 裂变前动态 import**——绝不可在 pluginRegistry 静态 import（它在 debouncedSave→useCanvasDrag 加载链上，静态引 projectStore 会成 TDZ 循环，vitest 实测炸过，勿回退）。**出图展开收口**：[generationQueue.ts](src/services/generationQueue.ts) 提交前 `resolvePresets`（资产五页/变体/分镜出图唯一路径；画布节点第117轮已有）；AssetWorkbench 提示词放大弹窗接 getPresets（胶囊渲染成 pill；行内 textarea 显示标记原文属已知观感）。实测：两端 tsc 干净 + **474 vitest 全过（62 文件，+8：composeAssetPrompt 顺序/幂等/空正文、autoAttachIdsFor 位置与类别、attachSplitPresets 画风缺失回退、画风胶囊展开、assetMerge 只装饰新增+变体不动+无装饰器兼容）** + build 过 + admin script node --check 过 + **沙盒冒烟 29+12+6**（sand1 存量迁移 21 断言：管理端改过的画风正文原样带走/宫格旧种子正文升级/用户自建预设迁入含 suffix 映射/自营模板不迁/模板库清净/catalog .ps 段与全文下发/CRUD 清洗；二启 8 断言：零重复+墓碑不复活；sand2 全新种子 12 断言 + 兼容投影 6 断言）+ **沙盒 8917 浏览器端到端**（管理端登录→「预设管理」页 6 条按分组渲染→my.pre 编辑器勾 角色+场景 保存→API 复核 autoAttach 落库；模板页零预设字段零预设分组；用户侧登录绑机器后 catalog version=v2.t12.m1.f1.ps6、presets 全文含改动正文、templates 带兼容投影；零控制台报错）+ **dev 8787 热迁移实锤**（tsx watch 自动重启：data/presets.json 6 条、templates.json 13 条零预设残留）。⚠ **须重新部署服务端（首启自动迁移 templates→presets，生产部署前建议备份 data/templates.json）+ 客户端重新打包**（只部署服务端=旧客户端凭兼容投影照旧用预设/画风，仅无 自动挂胶囊/autoAttach 能力，拆分行为与现状一致）。**已知边界**：①推理模板正文未动（用户定「不修改现有提示词」）——LLM 仍可能把画风/前后缀写进资产正文，**要兑现省 token 需运营在管理端自行精简拆分模板正文**（机制已就位）；②本地自定义预设（客户端设置）无 autoAttach（自动附加仅服务端预设）；③新客户端手插预设下拉现含画风预设（画风也是预设，同组互斥，属预期）；④变体提示词不装饰（变体=图生图另一套前缀体系 §3）；⑤存量资产提示词不回改（胶囊只挂**之后新拆分**的资产）。**运营注意**：在「预设管理」建前后缀预设并勾选自动附加类别即生效（≤30s catalog 热更）；画风前缀正文=画风预设 body，要更丰富直接改画风预设。**待真机 QA**：新建项目（选 catalog 画风）→资产拆分→五类资产提示词头部出现画风+类别前缀胶囊、尾部后缀胶囊；出图后管理端请求记录②段可见展开后的完整正文；画布拆分节点裂变的图片节点同样带胶囊且提交展开；旧客户端（未升级）预设下拉与画风选择照常。
- **2026-07-28 第173轮（导入项目重做「新建项目并复制文件」+ 项目大厅/新建页导入入口·纯客户端 · client tsc 干净 + 465 vitest(+9) 全过 + build 过 + 浏览器端到端实测过（vite+dev 8787 活体+注入会话：两入口渲染与布局量测、零控制台报错、零临时数据；Tauri 复制链路待真机 QA））**——用户报「只有进入项目才能看见文件菜单才能导入，很奇怪」，定稿语义（⚠ 勿回退成「读进内存不落盘」）：**导入 = 新建项目并复制文件，被导入的源文件/源项目分毫不动**。①**纯逻辑层** [importCopy.ts](src/lib/importCopy.ts)（可单测）：`localRefOf` 只认三种本地引用形态——`http(s)://*.localhost/<编码路径>`（convertFileSrc Win 产物，decodeURIComponent 解出路径）/ `asset://` / 裸 Windows 绝对路径（盘符/UNC）；真公网 OSS url、data:、blob:、路由串（/frame1693）、>2048 长文本一律不认（杜绝把云端兜底链接误当本地）；`collectLocalRefs` 深度收集（按 raw 去重、只走值不走键）+ `applyRefRewrites` 按 raw→new 整串改写。②**projectStore.importProject 重写**（签名改 `Promise<boolean>`）：Tauri 走新 `importAsNewProject`——文件对话框选 .Qiji（主文件损坏按 .tmp/.bak 兜底，与 loadFromPath 同规则、全程只读）→ getNewProjectPath 新建带时间戳项目文件夹+assets/ → 引用到的本地素材**按文件名复制一次**（文件名=资产 id 全局唯一 §12.2；缺失/复制失败保持原引用，打开后凭公网 url 走既有自愈=与跨设备导入同路径）→ 本地引用改写为新路径（uri 形态经 convertFileSrc 回写）→ 落盘 project.Qiji → loadFromPath 打开（最近项目/快照路由恢复全复用）+ 汇总 alert（复制 N 个/缺失 M 个/原项目未动）；浏览器（dev）保留旧内存导入兜底（包 Promise，oncancel=false）。③**三入口**：大厅创建卡（[Frame21.tsx](src/views/Frame21.tsx)「点击立即开始」下新增虚线边「导入已有项目 (.Qiji)」，卡片高度放开 auto/minHeight 340）+ 新建项目页底栏左侧（[Frame164.tsx](src/views/Frame164.tsx) marginRight:auto 与右侧按钮组分开）+ 文件菜单保留（[TitleBar.tsx](src/canvas/TitleBar.tsx) 文案改「导入项目（复制为新项目）...」）；导入成功统一 `restoreRouteAfterLoad()` 跳快照页（该 helper 从 Frame21 抽到 [utils.ts](src/views/utils/utils.ts) 供大厅/新建页共用）。实测：client tsc 干净 + **465 vitest 全过（61 文件，+9 测：三形态识别/公网与路由串拒认/长文本排除/深度收集去重/整串改写逐处计数/空映射零开销）** + build 过 + 浏览器端到端（注入会话进大厅：创建卡 340 高内容底 318 无溢出、两按钮 32px 上下排布；新建页底栏 导入靠左(114)、取消/两进入靠右；零控制台报错——截图受「后台标签不合成帧」环境限制以 DOM 量测为准，历轮同款）。**须客户端重新打包**；服务端零改动。**已知边界**：①浏览器 dev 导入仍是内存导入（无文件系统，savePath=null 保存时另存）；②genMeta/assetRefImages 的**键**是 uri 不改写（只走值），旧键查不到仅影响「详情回看」观感；③同名不同源文件首见者胜（文件名=资产 id，实际不冲突）。**待真机 QA**：Tauri 下大厅导入真实 .Qiji → 新项目文件夹出现在用户数据目录、assets/ 有复制素材、图片视频离线可显、源项目文件夹分毫未动；导出的 _export.Qiji 再导入往返；文件菜单导入同语义。
- **2026-07-28 第172轮（团队体系：团队码开团 + 团长积分管理（共享/分发两模式）+ 团队共享素材库 + 管理端团队页·两端 + 管理端 · server/client tsc 干净 + 456 vitest 全过 + vite build 过 + admin script 语法过 + dev 8787 API 冒烟 40/41（唯一失败=脚本把 echo 异步回执误断言为同步，已修正——计费断言全对）+ 客户端 vite 浏览器 UI 端到端全过（临时用户/团队码/团队/共享库全清零残留、零控制台报错））**——用户三点需求全落地：①用户互相绑定=团队功能（开启者为团长，团长分发/收回团员积分、决定积分方式 共享/分发，两端更新）；②服务端可查看团队数量/积分情况；③服务端团队码：开团需团队码、每码默认附带一份共享素材库由团长管理。
  ①**存储**（新 [teams.ts](server/src/store/teams.ts) → teams.json）：`Team{id,name,leaderId,memberIds,creditMode(shared|dispatch 默认 dispatch),sharedLibId,code}` + `TeamCode{code(tc-hex),note,usedByTeamId,usedAt}`；一人同时只在一个团队（teamOfUser）；上限 50 人/团、200 码/批；**懒清理 sanitizeTeams**（被删用户剔出成员表、团长被删=团队视为解散，路由层级联删其共享库——GET /v1/team 与 admin teams 两处触发）；团队码一码一团核销、不复用；**删除语义：未用可删，已开团仅当团队已解散（留档结束）才可删**。
  ②**用户侧端点**（[routes.ts](server/src/routes.ts) `/v1/team*`，Bearer accessKey）：GET 详情（团长=全量含成员 积分/今日/累计消耗，团员=概要）；POST 开团（校验团队码+不在团+团名 1-20 字，自动建团队共享库）；PUT 改名/切积分方式（团长；改名共享库跟随「团队·<名>」）；POST members 按**登录账号**绑定团员（须已注册账号+**与团长同归属**（同渠道商或同为平台直属——共享模式计费链一致性）+不在其它团队）；DELETE members/:id 移除、POST leave 团员退出（团长须走 dissolve）、POST dissolve 解散（共享库级联删、码不复用）；POST credits 分发（delta>0 团长→团员）/收回（delta<0 团员→团长）=**零和转账**（[users.ts](server/src/store/users.ts) 新 `transferCredits`，双向不透支、不计消耗统计）；DELETE lib/folders|assets/:id 团长管理团队库（越权一律 404）。
  ③**共享积分计费（⚠ 核心语义勿回退）**：`teamPayerFor(user)`——shared 团队的非团长成员 → **扣款人=团长**（共享池=团长余额）；planBilling 对池校验（不足 402「团队积分不足：本次需 X，团队池剩余 Y」）；applyBilling 走新 `chargeCreditsAs(payerId, statsUserId, amount)`（[users.ts](server/src/store/users.ts)）——**钱扣付款人、当日/累计消耗统计记实际消耗人**；退款同构 `refundCreditsAs`（[tasks.ts](server/src/store/tasks.ts) `billing.payerId` 新字段随盘、RefundFn 加第三参、setTaskBilling 加 payerId——billing.userId 恒=实际用户，「本人任务」校验/请求记录归属不变）；渠道商链结算按用户归属照旧（同归属约束保证与团长一致）；**心跳/登录/me 下发 team 信息**（`sessionTeamView`）：shared 团员 `credits`=池余额（客户端积分显示/402 预检零特判天然对齐实扣）、me 另附 `ownCredits` 个人余额。dispatch 模式=完全既有行为（各扣各的）。
  ④**团队共享素材库**：开团自动 createLibrary（名「团队·<团名>」撞名补后缀、随机密码；ownerAudience=团长受众——与既有受众隔离体系兼容）+ 团长/团员 joinLibrary **自动成员免密**（资产助手「共享资产」直接可见，客户端零改动）；移除/退出 leaveLibrary；解散/管理端强拆/团长被删 deleteLibrary 级联（素材只是 OSS 记录不动字节）；团长删文件夹/素材记录走 `/v1/team/lib/*`（[sharedLibs.ts](server/src/store/sharedLibs.ts) 新导出 `getAssetRec`）。
  ⑤**管理端**（[admin.ts](server/src/routes/admin.ts)+[admin/index.html](server/src/admin/index.html)）：新「团队」页（运营组，角标=团队数）——KPI（团队数/成员总数/池积分合计=各团长余额/团员积分合计）+ 团队表（团名/团长@账号/成员数/积分方式徽标/池余额/团员积分合计/今日消耗/共享库/强制解散）+ 团队码卡（数量+备注生成→弹窗复制全部、列表带核销团名、未用/已解散可删）；`GET/DELETE /admin-api/teams(/:id)` + `GET/POST/DELETE /admin-api/team-codes(/:code)`。
  ⑥**客户端**：[contract.ts](src/contract.ts) `SessionTeamInfo/TeamMemberInfo/TeamDetail/TeamCreditMode` + SessionUser.team/UserStats.team+ownCredits + Endpoints.team*；[managedClient.ts](src/services/managedClient.ts) 8 个团队方法（无 body POST 不带 Content-Type 天然防 Fastify 空 JSON 拒）；[PersonalCenter.tsx](src/components/PersonalCenter.tsx) 新「团队」页签（宽 560）——未在团=开团表单（团队码+团名）；团员=概要卡（池余额/说明/退出）；团长=积分方式双按钮（切换走 confirmDialog 说明语义）+ 按账号绑定团员 + 成员列表（积分/今日消耗 + 统一数量输入 + 逐行 分发/收回/移除）+ 共享库名提示 + 解散（confirmDialog）；概览积分卡对 shared 团员显示「团队共享积分（池）」+ 个人积分小字。
  实测：两端 tsc 干净 + **456 vitest 全过** + vite build 过 + admin script 语法过 + **dev 8787 API 冒烟 40/41**（团队码生成/无效码拒/开团核销+自动建库/重复开团拒/加人（不存在 404/重复拒/非团员分发拒）/团员自动见库/团长删文件夹（团员 404）/分发收回 300·500 零和+超额拒/切共享模式 → 团员心跳 credits=池 1200+team 信息、me ownCredits=0、**团员消耗 echo 模型扣团长 1200→1195 团员余额不动 dailySpent=5**、池调 3 → 402「团队积分不足」/管理端列表（模式/成员数/池/团长）/团长不能退出/团员退出后库不可见+心跳回个人积分/解散级联删库/码删除语义/临时数据全清；唯一「失败」=脚本把 echo 异步 taskId 回执误断言为同步 success，属测试断言笔误已修正——顺带实锤异步任务 payerId 计费登记）+ **客户端 vite 浏览器 UI 端到端**（临时账号注册→登录→建项目进画布→个人中心「团队」页签：开团表单→凭真码开团→团长卡（1 人/分发模式/共享库名）→按账号绑定团员→分发 1000（100→1,100）→切共享模式（确认弹窗→池 4,000 显示）→解散（确认弹窗→回开团表单），零控制台报错；vite 临时项目/localStorage 已清、dev 侧临时用户/码/库全删）。
  ⚠ **须重新部署服务端 + 客户端重新打包**（只部署服务端=旧客户端无团队入口，但共享模式对团员的池扣费/池余额下发已生效（credits 口径随心跳自动变）；只打包客户端=旧服务端无 /v1/team 端点，团队页报错）。**已知边界**：①共享模式下团员消耗的「消耗统计」记团员、「钱」记团长——管理端用户表团长的今日/累计消耗不含团员池消耗（按消耗人口径，请求记录可按用户查）；②重启对账的「孤儿 running 日志补退」路径（任务落盘前的存量）按日志 userId 退款、不知 payer——正常路径（任务已落盘 billing.payerId）不受影响；③团员加入不需对方确认（团长凭登录账号直绑，v1 从简——对方可随时「退出团队」）；④跨归属（不同渠道商）用户不能互绑（计费链一致性，明确报错）；⑤团队码=纯开团凭证不含积分面额。**运营注意**：管理端「团队」页生成团队码发给用户即可开团；共享模式的池=团长个人余额（给团队充值=给团长充值/发兑换码）。**待真机 QA**：打包后个人中心团队页观感；两台真机 团长开团+团员被绑后 心跳池余额/共享库在资产助手出现；共享模式团员真实生成扣团长；分发模式下互不影响。
  - **补充（团队人数上限管理端可控·两端 + 管理端 · server/client tsc 干净 + admin script 语法过 + dev 8787 专项冒烟 14/14 全过（临时数据全清、默认上限恢复）+ 管理端浏览器 UI 实测过（默认上限 保存 30→复核→恢复 50，零控制台报错））**——用户令「团队人数上限可由管理端控制」。**口径=含团长**（原写死 MAX_MEMBERS=50 为团员数，本轮统一为总人数）。两层控制：①**全局默认**（[settings.ts](server/src/store/settings.ts) `teamMemberLimit` + `normTeamLimit` 收敛 2–500、空/非法=恢复默认 50；`PUT /admin-api/settings/team-member-limit`）；②**按团覆盖**（`Team.memberLimit`，[teams.ts](server/src/store/teams.ts) `effectiveTeamLimit(t)`=覆盖>默认；`PUT /admin-api/teams/:id {memberLimit}`，null=清除跟随默认）。加人闸在 addTeamMember 按生效上限拦（文案带「N 人，含团长」）；**压低上限不裁现有成员，只拦后续加人**。管理端「团队」页：表格上方「人数上限（默认，含团长）」输入+保存、成员数列改「N/生效上限」、新增「人数上限」列（行内输入按团覆盖，placeholder=默认值，留空=跟随默认）；`GET /admin-api/teams` 响应带 defaultMemberLimit + 每团 memberLimit/effectiveLimit。用户侧 `GET /v1/team` 下发生效 `memberLimit`（[contract.ts](src/contract.ts) TeamDetail.memberLimit），个人中心团队卡人数显示「N/上限 人」。冒烟：默认设 2 → 团长+1 即满、再加 400 拒（含团长口径）；越界 9999→收敛 500；按团覆盖 3 → 可再加、用户侧/管理端三值（覆盖/生效/默认）全对；清除覆盖回默认、超员存量不裁；恢复默认 50。随第172轮一并部署（**须重新部署服务端 + 客户端重新打包**——不打包仅少「N/上限」显示，服务端闸照拦）。
  - **补充2（⚠ 经济漏洞修复：绑定改「邀请-同意制」+ 收回上限=分发净额台账 + 退团自动结算·两端 · server/client tsc 干净 + 456 vitest 全过 + vite build 过 + dev 8787 专项冒烟 23/23 全过（临时数据全清））**——用户指出首版是**严重经济 bug**：团长可单方面按账号强拉任何人入团，再用「收回」收缴对方**自有积分**。三层修复（⚠ 三条均为经济安全语义，勿回退）：
    ①**入团必须对方同意**（[teams.ts](server/src/store/teams.ts)）：`Team.invites` 待接受邀请表——团长「绑定」改为**发出邀请**（`inviteToTeam`，满员判定含待接受防超发；重复邀请拒）；被邀请人在自己团队页**接受**（`acceptInvite`=唯一入团路径，接受时再验 不在团/未满员，入团即清其在所有团的其它邀请+加入团队共享库）或**拒绝**；团长可**撤销**（`removeInvite` 共用）；**7 天未处理自动过期**（sanitizeTeams 懒清）。端点：POST /v1/team/members=发邀请、DELETE /v1/team/invites/:userId=撤销、POST /v1/team/invites/accept|decline；GET /v1/team 未在团=返回收到的邀请（团名/团长/积分方式）、团长视角带 pendingInvites。
    ②**收回上限=分发净额**：`Team.granted` 台账（userId→团长对该团员的净分发，分发+/收回−恒≥0，`grantedOf/bumpGranted`）；POST /v1/team/credits 收回前置闸 `min(净额, 团员当前余额)`，超出 400「只能收回你分发的余量（当前可收回 N）——团员自有积分不可收缴」；团长视角成员条目下发 `granted/reclaimable`，客户端「可收回 N」外显+收回按钮按它禁用。
    ③**退团自动结算（防「入团领分发→退团带走」套利，也防团长丢分发）**：`settleMemberGrant`（teams.ts，用户端与管理端共用）——团员退出/被移除/团长解散/管理端强拆时，把 `min(净额, 当前余额)` 自动退回团长并清台账（**只结算分发部分，团员自有积分分文不动**）；响应带 settled、客户端确认弹窗文案明示。
    实测冒烟 23/23：邀请后成员数不变+对方可见邀请/接受入团/拒绝/撤销 四态；**未分发时收回自有积分被拒（漏洞修复实锤）**；分发 300→收回 400 拒/收回 200 净额跟随；退出/移除/解散三路自动结算后 A=1000、B=200 分文不差。**已知边界**：①团长被删的懒清理解散不结算（收款人已不存在）；②邀请无主动通知（对方打开个人中心「团队」页可见；心跳角标为后续优化）；③granted 是**净额**口径——团员把分发花掉后 reclaimable 相应降低（钱已消耗，符合直觉）。随第172轮一并部署（两端都须更新——旧客户端「绑定」按钮调的端点已改为发邀请语义，天然安全）。
  - **补充3（渠道商门户「团队」页：开码积分发团队码（99/枚可调）+ 团队码归属绑定·服务端 + 两控制台 · server/client tsc 干净 + 两控制台 script 语法过 + dev 8787 专项冒烟 20/20 全过 + 门户浏览器 UI 实测过（生成扣 99 弹窗/余额条/超余额禁用，零控制台报错；临时商/码/用户全清、单价恢复默认）· 客户端仅改一行提示文案）**——用户令「渠道商也增加团队界面，开团队码使用开码积分，暂定 99 积分开码」。
    ①~~团队码归属绑定~~（**已被补充4 按用户令推翻，勿恢复**——用户定「开团和拉人不限渠道商，自由点」）：`TeamCode.agentId` 现仅作**签发方留档/扣费归属**，不限制使用者；本条首版曾做「码与开团人同归属」校验，补充4 已删除。
    ②**单价管理端可调**（[settings.ts](server/src/store/settings.ts) `teamCodePrice`+get/set：未设=默认 99、**显式 0=免费**（第128轮「0 有效」教训）、空=恢复默认；`PUT /admin-api/settings/team-code-price`；管理端团队页团队码卡新增「商开码单价」输入+保存，GET /admin-api/teams 带 teamCodePrice）。
    ③**门户「团队」页**（[agent/index.html](server/src/agent/index.html) 侧栏运营组新页 + [agent.ts](server/src/routes/agent.ts) 端点组）：生成团队码=**从本商开码积分扣**（单级扣费 `changeAgentCodeCredits` 不透支、不足 402 带明细；`logCodeIssue` 计入消耗日志 tierLabel「团队码 ×N」——门户/渠道商详情请求记录可见）+ 实时费用预览（单价×数量 vs 开码积分余额，不足红字+按钮禁用）+ 生成弹窗复制全部；码列表（状态/核销团名/删除——未用可删、已开团团队解散后可删、**删除无退回**与激活码作废同规则）；**名下团队只读表**（团长/成员数/上限/积分方式/池余额/团员积分合计/共享库——团队由团长在客户端管理）。端点：GET/POST/DELETE `/agent-api/team-codes(/:code)`（响应带 price/codeCredits）+ GET `/agent-api/teams`（按团长∈名下用户过滤）。
    ④**管理端补归属可视**：团队列表新「归属」列（源站/商名）、团队码列表新「签发方」列（[admin.ts](server/src/routes/admin.ts) adminTeamView.agentName + team-codes.agentName）。⑤客户端开团输入框提示改「向管理员或你的服务商获取」（一行文案，顺带打包生效）。
    实测冒烟 20/20：门户读价 99/余额 150；发 2 枚需 198→402、发 1 枚扣 99 余 51；门户名下团队/码列表、管理端归属列/签发方列全对；单价 0=免费开码不扣、清除恢复 99；解散后商码可删（无退回）；临时数据全清。**运营注意**：①商发团队码前需先给商分发**开码积分**（与激活码同池；下游商开码还需**上游各级**开码积分充足，见补充4）；②改单价即时生效（门户预览实时读取）。**待真机 QA**：真实商门户「团队」页观感；商发码给用户真机开团全流程；门户请求记录出现「团队码 ×N」消耗条目。⚠ dev 库现存用户自建测试团队「测试」（团长=用户「简一」，源站码开团）——真实数据未触碰。
  - **补充4（团队全面放开归属 + 商开码改链式扣费堵 mint 绕过·服务端 + 两控制台 + 客户端一行文案 · server/client tsc 干净 + 474 vitest 全过 + 两控制台 script 语法过 + dev 8787 专项冒烟 15/15 全过（临时 商×2/用户×3/码/团队 全清））**——用户两点：①「团队码开团和拉人不限渠道商，只要是使用这个软件的都能成为团队，自由点」；②「下下级渠道商开团队码的开码积分没有被上游渠道商积分限制……绕过了积分限制，修复」。
    ①**团队不限归属（推翻补充3 的归属绑定与第172轮「团员同归属」限制，⚠ 定稿勿再收紧）**：开团（[teams.ts](server/src/store/teams.ts) createTeam 删归属校验——任何用户凭任意团队码开团）与邀请（[routes.ts](server/src/routes.ts) POST /v1/team/members 删同归属校验——跨渠道商/平台直属互邀）全放开。**计费口径天然互不越界**（无需特判）：共享模式扣团长池、用户售价按**消耗者自己**的归属定价、渠道商链按**消耗者归属链**逐笔结算。**配套：团队共享库豁免受众隔离**——`SharedLibrary.teamId`（[sharedLibs.ts](server/src/store/sharedLibs.ts)，开团建库时写入）：libFor 与 GET /v1/shared/libraries 对带 teamId 的库按**成员身份**放行（跨归属团员可见可用；非成员照旧 404，搜索/密码加入仍走受众）。
    ②~~商开团队码链式扣费~~（**已被补充5 按用户令改为「实扣制单级」，勿按本条恢复链式**）：本条曾以链式扣费堵「mint 分发→下游开码」绕过；补充5 把绕过根源（mint 分发）改为真实划转后，链式反而会对上游重复扣两道，故签发回归单级。
    实测冒烟 15/15（链式扣费部分已被补充5 取代）：**放开实锤**——平台用户凭商码开团、跨归属邀请/接受成团（2 人）、跨归属团员可见团队共享库并可建文件夹、非团员仍 404；临时数据全清。**已知边界**：门户「名下团队」列表按**团长归属**过滤——跨归属团队只在其团长归属方的门户可见（管理端全量可见带归属列）。随第172轮一并部署。
  - **补充5（⚠ 开码积分改「实扣制」+ 共享素材库全面开放·服务端 + 两控制台 + 客户端一行文案 · server/client tsc 干净 + 474 vitest 全过 + 两控制台 script 语法过 + dev 8787 专项冒烟 17/17 全过（临时数据全清））**——用户两点定稿：①「共享库有搜索和密码限制，不需要隔离，开放，只要是本软件就能搜索到和使用共享」；②「开码积分使用实扣制，上游给下游拨积分会扣自身开码积分，和消费积分不一样，有多少可以用多少」。
    ①**共享素材库全面开放（推翻第120轮受众隔离，⚠ 定稿勿恢复）**：[sharedLibs.ts](server/src/store/sharedLibs.ts) `searchLibraries(q)` 改**全局搜索**（去 audience 参数）；[routes.ts](server/src/routes.ts) `libFor` 删受众校验（补充4 的 teamId 豁免随之多余删除，字段留作团队库标记）、已加入列表删受众过滤——**任何用户可全局按名搜索、凭加入密码加入任意共享库**（密码即门槛；成员校验/未加入 404 原样保留）。`ownerAudience` 从此仅作**创建方归属**（门户/管理端各管自己建的库，管理面不变）。两控制台+客户端搜索空态文案同步（admin「全局开放（不限归属）」、AssetAssistant「全局搜索…凭加入密码加入」）。
    ②**开码积分实扣制（⚠ 定稿勿回退成 mint+链式）**：开码积分从此是**真实划转的配额**，与通用积分（mint 分发+消耗时逐笔链式结算）刻意不同——⑴**分发=划转**（[agent.ts](server/src/routes/agent.ts) `POST /agent-api/sub-agents/:id/code-credits` 改 [agents.ts](server/src/store/agents.ts) 新 `transferAgentCodeCredits`：分发扣上级自身（不足拒绝）、扣回反向划回；**管理端（源站）分发仍是发行 mint 不扣源站**）；⑵**签发=单级**「有多少用多少」：`planCodeIssue` 从链式改**只扣签发商本人**（按其进货价，第128轮价链解算不变；levels 形状兼容旧调用方）、门户团队码生成同改单级（补充4 的链式删除——分发已实扣，再链式=上游重复扣两道）。两控制台开码积分弹窗/签发弹窗/团队码卡文案全部改实扣制口径（门户分发弹窗明示「从你的开码积分真实划转给他」）。
    实测冒烟 17/17：**实扣实锤**——A 300 分发 120 给 B → A=180（扣自身）B=120、超余额分发拒、扣回 21 反向划回；**单级实锤**——B 开团队码 99 → B=0 而 A 不动、B 余额 0 再开 402、A 签发 2 枚激活码（单设价 50/枚）只扣 A 100；**开放实锤**——商建库→平台用户全局搜到→错密码 403→凭密码跨归属加入→可建文件夹、未加入者取内容仍 404；临时数据全清。**运营注意（存量行为变化）**：①此前给下游分发过开码积分的商：存量余额不追溯，但**今后分发会扣自身**——上游需先持有足额开码积分；②激活码签发从「链上各级各扣一份」改「只扣签发商」——上级不再在下游签发时被动扣费（上级的利益改在**分发划转**环节体现：给下游多少配额自己就少多少）；③共享库对全软件可搜——不想被陌生人加入的库请设强密码（搜索只暴露库名与数量，内容仍须成员身份）。随第172轮一并部署。⚠ 同轮用户追问确认：**消费积分（通用积分）保持链式不动**——每笔消耗仍沿归属链逐级按结算价扣各商积分池、分发给下游仍是 mint（约束在逐笔结算）；实扣制只改开码积分。
  - **补充6（客户端消耗统计：个人 今日/昨日/近7天 + 团长按团员筛选·两端 · server/client tsc 干净 + 474 vitest 全过 + vite build 过 + dev 8787 冒烟 9/10（唯一失败=脚本把团长 name 断言成账号名的笔误，积分值经「全团合计=团长+团员」双向印证正确）+ 客户端浏览器 UI 端到端全过（临时数据全清、零控制台报错））**——用户令「客户端新增统计，可查看个人今日、昨日、一周的消耗统计。团长查看团员统计情况，可按团员筛选」。
    ①**服务端** `GET /v1/stats`（[routes.ts](server/src/routes.ts)，authed）：按请求日志聚合（logSummary userIds+from/to；**共享积分模式下消耗记在消耗者名下**——正好支撑团长看各团员用量）三段=今日（当日 0 点起）/昨日/近 7 天，各含 credits/count/success/failed；`?userId=<团员>`=仅团长且对方在团内（越权一律 404 不暴露存在性）；`?scope=team`=全团合计（团长+全部团员，仅团长 403 拦）。开码日志无 userId 天然不入统计。
    ②**客户端**：[contract.ts](src/contract.ts) `ConsumeRangeStats/UserConsumeStats` + Endpoints.stats；[managedClient.ts](src/services/managedClient.ts) `getStats({userId?|scope?})`；[PersonalCenter.tsx](src/components/PersonalCenter.tsx) 新「统计」页签（BarChart3 图标，宽 560）——三卡（今日/昨日/近 7 天：消耗积分大字 + N 次（成功·失败）小字）+ 刷新；**团长额外出「统计对象」下拉**（我自己（团长）/ 全团合计 / 各团员 @账号——成员清单来自 getTeam），切换即重拉；底部注明共享模式统计口径。
    实测：冒烟——团员消耗 2×5 → 本人 今日 10/2 次·昨日 0·近7天 10；团长本人 5；团长查团员 10、全团 15=10+5；团员查团长 404、团员查全团 403、局外人查他人 404、局外人查自己 0；浏览器 UI——团长登录→个人中心「统计」页签：下拉三类选项渲染、本人三卡 0（消耗在团员）、切「团员」→ 今日 5/1 次（成功 1），零控制台报错；临时数据全清（echo 消耗日志留档无害）。**已知边界**：统计=请求日志口径（失败已退款的单计入 failed 次数、credits 按 logCostFor 用户视角）；日界按服务器时区 0 点；无逐日曲线（要看明细走「请求记录」页签）。随第172轮一并部署（**须重新部署服务端 + 客户端重新打包**）。
    **追加（按模型/按步骤明细·两端 · tsc/474 vitest/build 全过 + API 冒烟 4/4 + 浏览器 UI 实测过（临时数据全清、零控制台报错））**：用户令「统计页面增加按模型统计和按步骤统计，有请求成功次数和实际消耗积分」。[logs.ts](server/src/store/logs.ts) logSummary 的 byPurpose 聚合补 success/credits（附加字段，管理端/门户既有消费方不受影响）；/v1/stats 三段各带 `byModel[{model,count,success,credits}]`/`byPurpose[{purpose,label,count,success,credits}]`（credits=实际消耗，失败已退不计；筛选团员/全团时明细同口径跟随）；客户端统计页三卡下方新增「明细时段」chips（今日/昨日/近 7 天）+ 两张表（模型|成功次数|实际消耗积分；步骤列显中文代称，成功次数悬浮 title 带总次数）。实测：API——消耗 2×5 → byModel fee-thirdparty 2 次/10 积分、byPurpose「AI 对话」代称/10、昨日明细空；UI——两张表渲染（fee-thirdparty 1/5、AI 对话 1/5）、切「昨日」空态「该时段暂无消耗」。**收敛（用户截图报「太高了」）**：两张表并排 → **同一行 时段 chips + 按模型/按步骤 切换、同屏只显示一张表**（bkKind 状态），表体自身 `max-h-240px` 内部滚动（模型多时不撑面板）、表标题并入切换按钮省一行、成功次数列收窄。量测：5 步骤数据下面板高 **570px**（窗口 720，内容区 406 无需外层滚动），此前双表布局需整页滚动。
    **再收敛（用户令「按模型先按家族池统计，可点击打开查看具体模型」）**：/v1/stats 每段新增 `byFamily[{familyId,familyName,count,success,credits,models[]}]`——按 `getModelDef(model).familyId` → [families.ts](server/src/store/families.ts) `familyName` 聚合（已删模型/无家族/hidden 内部模型归**「其他」**，按次数降序）；[contract.ts](src/contract.ts) 同步类型；客户端「按模型」视图改 `FamilyBreakdownTable`——**家族行（名称 + 组内模型数 + 成功次数 + 实际消耗）点击展开**缩进列出组内具体模型（ChevronRight/Down，展开态 per-家族独立、切换维度/时段不丢），行数从「几十个模型」压到「几个家族」。实测：造 3 个跨家族临时 echo 模型消耗 → byFamily = Seedance 2.0（3 次/21）、GPT Image 2（2/14）、其他（1/3，无家族模型兜底）；UI 家族行渲染、点开出 `__t183-m1 3/21`、再点收起、切「按步骤」不受影响（AI 对话 6/38），面板高 537px、零控制台报错；临时用户与 3 个测试模型全清。
- **2026-07-28 第171轮（用户管理页「渠道商切换」下拉·纯管理端 UI · admin script 语法过（node --check）+ dev 8787 浏览器端到端实测过（真实数据只读，零数据变更、零控制台报错））**——用户令「服务端用户管理增加和请求记录一样的渠道商下拉菜单」。**服务端零改动**（第112轮起 `GET /admin-api/users?agentId=` 就支持按商查名下用户；PUT/DELETE/batch-op 等编辑端点本就不限归属——源站权限高）。[admin/index.html](server/src/admin/index.html) 用户页三改：①工具栏最左新增范围下拉（与请求记录页第168轮同款：「源站（平台直属）」+ 全部渠道商（含下游、带「· 上级 xx」后缀）；`uAgentId` 空=直属；渠道商清单每次 loadUsers 现拉保持新鲜、失败沿用上次）；②渠道商范围下**隐藏「新建用户/批量激活码」**（这两个入口创建的是平台直属用户/平台发码；商的激活码由商在其门户签发）+ 页脚文案随范围切换（商范围注明「源站权限高可直接编辑；免密直登走渠道商详情」）；③**侧栏「用户管理」角标恒=直属用户数**（不随范围切换漂移）；范围切换清空批量勾选。抽屉编辑/行内开关/排序筛选/批量条对商属用户照常可用（后端本就放行）。实测（dev 8787 真实库：1 直属用户 + 渠道商「简一」0 名下用户）：下拉两项、切「简一」→ 请求带 `?agentId=`、列表 0 条、按钮隐藏、页脚切换、角标仍 1；切回源站全部恢复；零控制台报错。⚠ **须重新部署服务端**（管理端页面 readFileSync 每请求现读，部署即生效；本机 dev 已生效）；**客户端零改动无需打包**。**待真机 QA**：生产部署后强刷管理端缓存看下拉观感；商属用户在此页改积分/开关模式后其客户端 ≤30s 生效；「渠道商 → 详情 → 名下用户」只读视图与本页可编辑视图并存的观感。
- **2026-07-28 第170轮（素材上传网络优化：直传 OSS（预签名两段式）+ 上传超时按体积自适应·两端 · server/client tsc 干净 + 456 vitest 全过 + dev 8787 端到端冒烟 15/15 全过（临时用户已删）+ 浏览器真跨域 PUT 200 实测）**——用户报「素材上传失败（未做 OSS 存储）：signal timed out」+ 问「国内用户、香港服务器怎么优化网络」。
  ①**根因**：[managedClient.ts](src/services/managedClient.ts) uploadAsset 固定 `AbortSignal.timeout(120000)`——素材上限 50MB，慢上行 120s 传不完就被客户端掐断（第169轮「提交超时」同款病在客户端）；实测 服务端→rains3 吞吐 ~10MB/s（20MB 2.1s）不是瓶颈，慢在 用户→香港服务器 跨境段；且上传路径原为 国内用户→香港→宁波桶 **两次跨境**。
  ②**上传超时自适应**：`uploadTimeoutMs(bytes)=120s + 1s/100KB`（容忍 ≈100KB/s 慢上行）上限 15 分钟；uploadAsset/reputAsset 共用；超时报错改中文（带体积与已等待秒数）。
  ③**直传 OSS（主优化，预签名两段式）**：**国内用户字节直传宁波桶、绕开香港中转**——服务端 [oss.ts](server/src/store/oss.ts) `ossPresignPut`（新依赖 @aws-sdk/s3-request-presigner）+ `ossDelete`，**S3Client 加 `requestChecksumCalculation:"WHEN_REQUIRED"`（⚠ 勿删：新版 SDK 默认把「空体 CRC32」校验参数钉进预签名 URL，rains3 宽松忽略但严格 S3 会拒真实字节）**；[assets.ts](server/src/store/assets.ts) `beginDirectAsset`（分配 id+对象键，**不落台账**；pending 仅内存 45 分钟 TTL——服务端重启丢失=complete 失败、客户端回退重传，孤儿对象无台账引用无害；id 消耗即弃用符合「永不复用」语义）+ `commitDirectAsset`（**HEAD 验对象真实存在**（403/405→GET Range 兜底同 isAssetAlive）+ 体积≤50MB（超限删对象拒收，防绕过 multipart 上限）→ 才补登台账，绝无「声称传了实际没传」的幽灵记录；视频时长不解析，留第143轮计费首用探测回填）；[routes.ts](server/src/routes.ts) 鉴权端点 `POST /v1/assets/direct`（contentType 必填、声明体积超限 400、prefix 白名单 ^[A-Za-z]{1,12}$、未配 OSS 400）+ `POST /v1/assets/direct/:id/complete`。客户端 uploadAsset **直传优先**：签发（20s）→ PUT 直传（体积自适应超时）→ complete（30s，⚠ 带空 body "{}" 防 Fastify 拒空 JSON——第101轮同款坑）；**任一步失败回退既有 multipart 通道**（语义完全一致）；旧服务端 404/未配 OSS → `_directUnavailable` 本会话记忆不再逐次白试——**画布拖入/粘贴/资产五页/垫图/音色/聊天图/共享素材全部上传入口经 uploadAsset 单点收口自动受益**。
  ④**rains3 实锤（维护参考）**：预签名 PUT 可用（2MB 249ms/20MB 2.1s）；**CORS 全开**（OPTIONS 回 Allow-Origin/Methods/Headers 全 `*`，浏览器真 preflight + 跨域 PUT 200 实测）——直传无需动桶配置；Content-Type 不参与其签名校验（宽松）；对象 PUT 完公网直链立即可读。
  ⑤**服务器侧运维（同轮，用户已在香港服务器执行）**：开启 BBR 拥塞控制（sysctl 落盘，重启保留）——改善 服务器→客户端 方向（API 响应/SSE 流式）；上传方向由直传根治（拥塞控制是发送方说了算，服务器开 BBR 帮不了上传）。
  实测：server/client tsc 干净 + **456 vitest 全过** + **dev 8787 端到端 15/15**（签发 TP 前缀 id/putUrl 指桶/直传 PUT 200/complete 登台账/台账可查/公网 HEAD 长度精确/未上传就 complete→400 且幽灵 id 不入台账/声明 51MB→400/重复 complete→400/multipart 旧通道回归/临时用户删净）+ **浏览器真 CORS**（localhost:8787 origin 跨域 PUT 128KB 441ms 200）。dev 台账残留 4 枚 KB 级 TP 测试资产（无害）。
  ⚠ **须重新部署服务端（部署机须 `cd server && npm i` 安装 @aws-sdk/s3-request-presigner）+ 客户端重新打包**（只部署服务端=旧客户端仍走 multipart 一切如旧；只打包客户端=探测到 404 自动回退 multipart 兼容无碍——但两者都上才有直传提速）。**已知边界**：①reputAsset（死链自愈重传）仍走服务器中转（低频量小，自适应超时已覆盖）；②直传对象在 complete 前无台账（重启丢 pending=客户端回退重传，桶里孤儿对象无引用无害）；③体积上限 50MB 两通道同尺（放宽须同改 [index.ts](server/src/index.ts) multipart fileSize + routes DIRECT_MAX_BYTES 两处）。**待真机 QA**：打包后国内用户传几十 MB 视频应十几秒级完成（管理端请求记录不再出现该上传的大 multipart 请求、只有 direct 签发/complete 两条小请求）；弱网/公司代理环境直传失败自动回退中转的观感；上传后垫图提交/显示链路照常（url 仍为同桶公网直链，形态与 multipart 完全一致）。
- **2026-07-27 第169轮（取消上游提交超时·亏损止血·纯服务端 · server tsc 干净 + 探针冒烟 3/3 + undici 接管双向实测 + 本机 dev（tsx watch）随改动自动重启 /health 活体验证 · 客户端零改动无需打包）**——用户令「取消提交超时」：大量提交在 60s 被我方 AbortSignal 主动掐断，而上游实际已受理并开始生成（已计费）——我方判失败自动退款、上游照扣费 = 纯亏损（第168轮截图的「渠道 提交失败：The operation was aborted due to timeout」即此类）。
  ①**共享安全闸**（新 [submitTimeout.ts](server/src/translators/submitTimeout.ts)，⚠ 勿回退成短超时）：`SUBMIT_TIMEOUT_MS`（env 可调；缺省 30 分钟；0=完全无超时）+ `submitSignal()`——提交/同步生成类上游请求的唯一超时来源。30 分钟只是防「死连接把任务永远挂住」的安全闸（真挂死由进程重启对账退款兜底），不再有提前掐断；提交响应慢 ≠ 失败，等就是了。
  ②**收口范围（14 翻译器 19 处 + custom 提交路径）**：13 家异步渠道 submit POST 的 60s 全换 `submitSignal()`（jianmeng/jianmengp/jmf/jmt/jmz×2（视频+图片）/musem/sudashui/huaying/aivide/dimensio/aistars×2（视频+图片）/videos/volc 提交）+ 同步生成类一并放开（volc 图像增强同步 180s、jmh 同步出图 300s×2、jmh SSE 视频整流 1200s）；[custom.ts](server/src/translators/custom.ts) `doRequest` 超时语义拆三档——提交类（runCustomText/runCustomImmediate/customSubmit）缺省走共享闸、轮询显式 `POLL_TIMEOUT_MS=120s`、结果下载 `DOWNLOAD_TIMEOUT_MS=120s`。**有意不动的（⚠ 勿顺手放大）**：各家轮询 30s（瞬时失败=继续轮询，不亏钱）、结果下载/垫图取字节（openai 图 2×30s+fallbackUrl 客户端接力=第158轮定稿、jmh/gemini 垫图 60s、rehost 下载 180s）、openai 图像上游 1h / gemini 图像 1h（本就够长）。
  ③**隐形第二道闸归零**（[index.ts](server/src/index.ts)，⚠ 配套勿删）：Node 内置全局 fetch 的 undici 缺省 headersTimeout/bodyTimeout=300s 会在各请求自己的 AbortSignal 之前掐断慢上游——只放宽 ① 也会在 5 分钟处被掐。新增依赖 **undici@8**，启动即 `setGlobalDispatcher(new Agent({ headersTimeout: 0, bodyTimeout: 0 }))`；实测 npm undici v8 经全局 symbol 注册表真接管 Node 24 内置 fetch（设 1.5s 闸 → 4s 慢头请求 2s 内被掐；归零 → 等满 4s 拿到 200，双向实锤）。
  实测：server tsc 干净 + 探针 3/3（缺省=1800000 且有 signal / env=0 → signal undefined 完全无超时 / env=1500 → 真实 fetch 1517ms 被掐=env 接线实锤）+ 本机 dev（tsx watch 正常实例）随改动自动重启、/health 200=带 undici 的 index.ts 活体启动验证；.env.example 补 `SUBMIT_TIMEOUT_MS` 说明。
  ⚠ **须重新部署服务端（部署机须 `cd server && npm i` 安装 undici，否则启动报模块缺失）**；客户端零改动无需打包。**行为变化（运营须知）**：①提交慢的单不再 60s 报错——任务持续显示生成中直到上游回执（最长 30 分钟）；客户端视频轮询 15 分钟超时先到的话按既有「断连找回/重连原任务」接回，服务端任务不丢、不重提交不重扣费；②真死连接从「60s 失败退款」变「最长 30 分钟后失败退款」（退款语义不变，只是更慢）；③要彻底无超时可设 `SUBMIT_TIMEOUT_MS=0`（不建议——死连接会把任务挂到进程重启对账才退款）。**待生产 QA**：部署后观察请求记录——「提交失败：…aborted due to timeout」类失败应基本消失；此前会 60s 超时的慢提交单最终 success 且不再退款。
- **2026-07-27 第168轮（错误文案渠道信息擦除 + 源站请求记录「渠道商切换」下拉·纯服务端 · server tsc 干净 + admin script 语法过 + 沙盒冒烟 21/21 全过（隔离目录 + junction 依赖，真实 data 零触碰）+ dev 真实库迁移实锤（468 条存量失败错误零品牌残留）+ 8787 浏览器端到端实测过（下拉渲染/切换隔离 0 vs 5187/页脚文案切换/切回源站；只读操作零数据变更）· 客户端零改动无需打包）**——用户截图报两点：①渠道商请求记录详情的错误徽标「Dimensio 提交失败：The operation was aborted due to timeout」**暴露渠道**（用户已把模式改名为 高速/线路N/备用N 等中性别名，翻译器硬编码的品牌前缀成了泄漏面）；②源站请求记录要能切换查看各渠道商。
  ①**擦除一把尺**（新 [errorScrub.ts](server/src/errorScrub.ts) `scrubChannelInfo`，⚠ 勿回退）：完整链接→「（链接已隐藏）」；上游域名（静态清单 + 渠道 baseUrl host **动态并入**，子域一并吞）→「（已隐藏）」；品牌词（静态清单：Dimensio/星辰/画影/苏打水/AIStartLab/MuseAI/ZhengAPI/Aivide/火山/**简梦X 系渠道别名**/协议 id 品牌段 dimensio·huaying·aistars·sudashui 等 + 渠道显示名**动态并入**）→「渠道」；**「当前模式名」经 NUL 占位符保护绝不擦**（模式名=客户端下拉可见的对外品牌——模式仍叫「星辰」时保留、改名后旧品牌词自动开始被擦；占位符而非逐词豁免，防「简梦」击中「简梦S」内部；沙盒 H2 实锤「模式「简梦S」已停用」原样保留）。幂等（二次擦除稳定）。**新接渠道翻译器照旧写品牌前缀，出口统一擦——只需把新品牌词/域名补进 errorScrub 两张清单**。
  ②**四收口点**：[tasks.ts](server/src/store/tasks.ts) `failTask`+`createCompletedTask`（任务 error 落盘前擦，经 /v1/tasks 下发）、[logs.ts](server/src/store/logs.ts) `applyFinishMeta`（finishLog/finishLogsBulk 共用，经 /v1/logs 与门户下发）、[routes.ts](server/src/routes.ts) /v1/generate 同步失败响应；**③④上游报文段不动**（仅源站管理端可见，留全量供排障——管理端详情看真因、看 ③ 段 URL 即知哪家渠道）。**存量迁移**：tasks 启动加载幂等补擦（终态仅留 48h 量小，每启过一遍）+ logs 一次性迁移（`data/logs-index/.error-scrub-v2` flag 防每启全量重扫；v1 首版漏简梦X 系故 bump v2 重扫）。无循环引用（errorScrub→channels/modes→models→agents→settings，均不回指 tasks/logs）。⚠ 教训：**源码里勿放裸 NUL 字节**（首版模板串内写真实 \0 被编辑工具吞写坏，改 `String.fromCharCode(0)`）；`grep -c $'\x00'` 的模式被 shell 截断成空串=匹配所有行，验 NUL 用 `grep -cP`。
  ③**源站请求记录渠道商切换**（[admin/index.html](server/src/admin/index.html)）：筛选栏最左新增范围下拉（「源站（平台直属）」+ 全部渠道商（含下游、带「· 上级 xx」后缀）；`logFilter.agentId` 空=直属）——复用第116轮 `?agentId=` logScope（该商名下用户消耗 + 该商开码记录），列表/顶部统计条/facets（切换即清缓存重拉）/导出 CSV 四处全带参；页脚文案随范围切换；「清空」只清筛选不清范围；「刷新」连渠道商清单一并重拉。渠道商详情里的原「请求记录」子页不动（两入口并存）。
  实测：server tsc 干净 + admin script 语法过 + **沙盒冒烟 21/21**（scratchpad 复制 src + junction node_modules，DATA_DIR 相对模块路径指向沙盒：全新种子态「模式名=品牌默认名→保留（对外名）」/改中性名后 品牌前缀→渠道·星辰擦除·协议段擦除/链接与裸域名（含子域、渠道 baseUrl 域）隐藏/渠道全名擦除/模式名与模型 id 与计费文案原样/幂等/failTask·createCompletedTask 集成/简梦X 双态并存（jmh 改名→擦、jms 保留→护））+ **dev 真实库实锤**（tsx watch 热重启自动跑迁移：截图同款「Dimensio 提交失败：…」→「渠道 提交失败：…」、「简梦Z 未配置上游密钥…」→「渠道 未配置…」、API 扫 468 条存量失败错误对 28 个品牌词/域名零命中）+ **浏览器端到端**（下拉=源站+「简一」；切简一→列表 0 条 vs 源站 5187 条=隔离实锤（该商名下确无用户日志）、页脚变「该渠道商体系」、切时间范围下拉选中保持、切回源站恢复；零控制台报错）。
  ⚠ **须重新部署服务端**（首启自动：logs 一次性迁移擦存量 + tasks 每启幂等擦；生产部署即把已落盘的旧品牌错误一并擦净）；**客户端零改动无需打包**（错误文案在服务端收口，客户端/门户显示的都是擦后文本）。**运营注意**：①擦除语义=「与当前模式名相同的词不擦」——想让某品牌词继续对用户可见就把模式名保持为它，改成中性别名后即自动隐藏；②管理端自己看真因：请求详情 ③④ 段保留上游 URL/报文不受影响；③新接渠道时把品牌词与域名补进 errorScrub 两张清单（接入轮顺手做）。**待真机 QA**：生产部署后渠道商门户/客户端请求记录里旧失败单的错误文案已中性化；新失败单（如上游超时）文案为「渠道 提交失败：…」；源站请求记录下拉切换各商观感。
- **2026-07-27 第167轮（渠道商分组体系 + 源站入列渠道商页 + 模型开放范围改按分组（卡面下拉多选）+ 分组定价 + 模型卡外显素材上限/启用开关·服务端 + 管理端 · server tsc 干净 + admin script 语法过 + 沙盒冒烟 33/33 全过（全新沙盒库零触碰真实数据）+ 8787 浏览器端到端实测过（临时模型/分组价已清；用户自建 VIP 分组与「简一」商完好）· 客户端零改动无需打包）**——用户定：①渠道商界面增加源站（有且最少会有源站）+ 渠道商分组（可按分组定价和开放模型）；②模型开放范围改成具体分组（不再具体到某个渠道商，后期商多了好管理）、启用开关外显；③（追加）开放范围不要二级弹窗，卡面下拉多选即可；④（追加）模型卡外显素材上限标签（如 9图/3视/3音）——顺带排查其「备注轮清空了素材上限」报障：**逐模型对比 r163 备份实锤数据零丢失**（101 模型 70  …（详见 git 历史）
- **2026-07-27 第166轮（模型备注（用户可见）+ 模型管理页卡片式重做 + 画布面板加宽·两端 + 管理端 · server/client tsc 干净 + 456 vitest(+5) 全过 + admin script 语法过 + 浏览器端到端实测过（8787 管理端卡片/设置弹窗全流程 + vite 客户端三态悬浮提示；临时账号已删、测试备注已还原、dm933 数据零残留））**——用户三点：①画布面板宽度不够，底栏胶囊与积分叠一起；②服务端模型管理增加单个模型备注，客户端鼠标悬浮积分消耗图标查看（默认备注为参考素材数量，可修改）；③模型管理改卡片式（仍可用当前分组和状态筛选；卡面仅显示 显示名/能力/模式/家族/计费方式 + 快捷键 开放范围/克隆/设置/删除，其余功能收敛到「设置」）。 ①**面板加宽**：[VideoOperationPanel.tsx](src/panel/VideoOperatio …（详见 git 历史）
- **2026-07-27 第165轮（服务端渠道页「模式管理/家族管理」卡片式重做：可拖动排序（影响客户端顺序）+ 全局启停（影响客户端显示）·纯服务端 · server tsc 干净 + admin script 语法过 + HTTP 冒烟 25/25 全过（临时数据全清、原序恢复）+ 浏览器端到端实测过（UI 建模式→拖拽置顶→落库→启停→删除，零控制台报错）· 客户端零改动无需打包）**——用户报「渠道管理、模式管理和家族管理 UI 太难看」，定「每个模式和家族做成卡片式，可排序（会影响客户端顺序）、可禁用启用（会影响客户端显示）」。 ①**数据层**（[modes.ts](server/src/store/modes.ts)/[families.ts](server/src/store/families.ts)）：Mode/Family 新增 `enabled?`（缺省 undefined=启用——存量数据零迁移行为 …（详见 git 历史）
- **2026-07-27 第164轮（分镜组功能栏改悬浮工具条·纯客户端 · client tsc 干净 + 442 vitest 全过 + 浏览器端到端实测过（vite+8787+临时账号，测毕已删））**——用户报「分镜组顶部功能栏内嵌在节点体里：影响节点边框、难以选择、缩小就看不清」，定「做成悬浮菜单，和 面板/信息/删除 处于同一个位置」。①**工具条迁入 BaseNode 悬停工具栏**：[ShotGroupView.tsx](src/nodes/ShotGroupView.tsx) 新导出 `ShotGroupToolbar`（比例/宫格数/序号/拼接 2k·4k/清空/解组，逻辑原样：applyLayout 重算高度、confirmDialog 清空、dissolveShotGroup 解组），[BaseNode.tsx](src/nodes/BaseNode.tsx) 悬停工具栏对 `display==="s …（详见 git 历史）
- **2026-07-26 第163轮（模型选择重构「家族 → 渠道/线路 → 模型 → 要求」·新增「家族」维度·两端 + 管理端 · server/client tsc 干净 + 442 vitest(+4) 全过 + build 过 + 沙盒冒烟 36/36 + 二启幂等 + dev 热迁移实锤 + 管理端/客户端浏览器端到端全过）**——用户定「以模型为首要筛选」换整合方式：从「渠道(模式)-模型-设置」改为「**家族(模型种类)-渠道/线路-模型-要求**」（例 seedance→简梦D→dm933→时长/比例）。背景统计（第163轮问答实锤）：Seedance 2.0 一种模型横跨 10+ 渠道占视频款式 42/66，旧「模式=一级」让用户先记渠道才能选模型。 ①**家族注册表**（新 [families.ts](server/src/store/families.ts)→families.json，与 modes …（详见 git 历史）
- **2026-07-26 第162轮（星辰按实时 config 更新：+53 漫剧优选线 2 模型 + 图片线 4 模型（新协议 aistars-image）+ 50 线 fast 改名 933 ·纯服务端 · server tsc 干净 + 沙盒冒烟 首启 47/47 + 二/三启收敛 5/5×2 + 存量迁移 9/9 全过 + admin script 语法过 + 真机 46 线 1K 图小额单 E2E SUCCESS · 客户端零改动无需打包）**——用户令「更新星辰模式的模型」→ 2026-07-26 真机 `GET /generation/config` 全量重测后用户定案：**接 53 漫剧优选线 + 星辰图片线；54 Seedance 2.5 内测线（临时·卡人脸·250 积分/秒·4-30s·图30视10音10）不接；50 线 fast 按素材量惯例改名**。 ①**config 变化全景**：+53「（限时 …（详见 git 历史）
- **2026-07-26 第161轮（简梦F（new.vosle.xyz）视频渠道接入 · 1 模型/新模式「简梦F」·纯服务端 · server tsc 干净 + 沙盒冒烟 A 69/69（种子/计费/catalog 零泄漏/翻译器 submit+poll stub fetch 全场景）+ 存量迁移 B 6/6 + 墓碑 C 3/3 全过 + admin script 语法过 · 客户端零改动无需打包）**——用户提供「视频生成 API 文档」（new.vosle.xyz），定「增加渠道简梦F」。 ①**上游（Base `https://new.vosle.xyz`，Bearer 鉴权；JSON 形态提交——素材是公网 URL 无需 multipart）**：提交 `POST /v1/videos` body `{model, prompt, generate_audio?, images?, videos?, audio …（详见 git 历史）
- **2026-07-25 第160轮（简梦T（llm.chre3.com）视频渠道接入 · 1 模型/新模式「简梦T」·纯服务端 · server tsc 干净 + 沙盒冒烟 A 71/71（种子/计费/catalog 零泄漏/翻译器 submit+poll stub fetch 全场景）+ 存量迁移 B 6/6 + 墓碑 C 3/3 全过 + admin script 语法过 · 客户端零改动无需打包）**——用户提供「sd2-c8 视频生成 API 文档」（最新版增加人脸参数），定「增加新渠道简梦T」。 ①**上游（Base `https://llm.chre3.com`，Bearer 鉴权；OpenAI 兼容形态）**：提交 `POST /v1/videos` body `{model:"sd2-c8", prompt, duration, aspect_ratio, image_refs?≤9, video_refs …（详见 git 历史）
- **2026-07-25 第159轮（简梦P 按 2026-07 版文档对齐：+`gemini-omni-flash` −veo31/veo31-ref · 7→6 模型·纯服务端 · server tsc 干净 + 沙盒冒烟 41+13 断言全过（含迁移幂等）· 客户端零改动无需打包）**——用户提供更新版「Video API」文档（pixelhub_video.md），定「简梦P新增模型，api如文档所示」+「（文档里）没有了你就删掉」。对照第147轮现状逐项比对：新增=`gemini-omni-flash`、除名=veo31/veo31-ref（其余存量模型能力零变化、翻译器提交/轮询零改动）： ①**能力（文档矩阵）**：分辨率 720p/1080p（**默认 720p，随请求下发**——CAPS resolutions 首档即默认，与 veo31 系「默认 1080p」方向相反勿混）· 比例 16:9/9:16 · …（详见 git 历史）
- **2026-07-25 第158轮（图片结果「下载失败」根因修复：服务端下载失败不再整单报废 → 原始直链回退 + 客户端接力转存 OSS·两端 + Rust · server/client tsc 干净 + 438 vitest 全过 + cargo check 过 + 沙盒冒烟 9/9 全过（stub 上游零真实调用、真实 data 零触碰））**——用户报生产 gpt-image-2 角色出图失败「下载图像失败：The operation was aborted due to timeout」（耗时 565.4s）。**根因（请求记录四段实锤）**：③④段显示上游 `dawosiquanjia.com/v1/images/edits` **200 已成功出图**（成品托管 `vip.edu888.top`），败在服务端 `downloadImageBytes` 3×120s **三次全烧满超时**（565.4s ≈  …（详见 git 历史）
- **2026-07-23 第157轮（云雾（yunwu.ai）文本渠道接入 · 新模式「云雾」·纯服务端零新协议 · server tsc 干净 + 沙盒 A 5/5 + B6 4/4 全过 + dev 热迁移实测 · 客户端零改动无需打包）**——用户提供 yunwu.ai 的 chat/completions 文档，定「增加文本渠道，模式云雾」。**判定：标准 OpenAI 协议（Bearer + SSE 流式 + 标准响应形状）→ 不写新翻译器**，现有 [openai.ts](server/src/translators/openai.ts) openai-chat 全套直接可用（流式 partialText/链式/文本异步任务管线零改动）。注册面三件套：①渠道 `ch-yunwu`（[channels.ts](server/src/store/channels.ts)，baseUrl **根域** https:// …（详见 git 历史）
- **2026-07-23 第156轮（苏打水收编 26→7 · 三模式（jmgf/jm431/jm933）合一为「简梦S」（jms）·纯服务端 · server tsc 干净 + 沙盒 A 7/7 + B5 8/8 全过 + dev 真实库热迁移实测 · 客户端零改动无需打包）**——用户报「苏打水模型太多」，按推荐定案（gf 不要 2 系）：**删 19 留 7、集合成简梦S、删旧三模式**。 ①**保留 7 款（全部改挂新模式 jms）**：`gf903-sd2.0`/`-fast`（**官方真人图独家** officialAssets + 720p/1080p/2k/4k 四档 routes）、`jm431-sd2.0`/`-fast`（hn 老线第131轮起、时长 5/10/15 三档）、`jm933-sd2.0-wf`/`-wf-fast`（**933 唯一含 1080p 档**的 wf 线）、`jm933-sd2 …（详见 git 历史）
- **2026-07-23 第155轮（简梦H 视频渠道接入 · 9 模型/并入既有「简梦H」模式·纯服务端 · server tsc 干净 + 沙盒冒烟 A 44/44（含 dispatch 全管线）+ 增量迁移 B4 6/6 全过 + dev 热迁移实测 · 客户端零改动无需打包）**——接第154轮用户「继续实现视频接入」：同站 ZhengAPI 视频系并入 jmh 模式（同渠道 ch-jmh、同密钥）。**⚠ 文档矛盾裁决（勿回退）**：旧版「grok-imagine-1.0-video 终端对接文档」（2026-05-31）说"模型名固定不拼后缀 + video_config + stream:false 推荐"，与新「视频生成接口文档」（统一 6 家族，"所有视频模型统一 chat/completions 流式 + 模型 ID 动态拼接"）正面冲突——**以新统一文档为准**（拼后缀+顶层 aspect_ratio …（详见 git 历史）
- **2026-07-23 第154轮（简梦H（ZhengAPI zhengapi.top）图片渠道接入 · 6 模型/新模式「简梦H」·纯服务端 · server tsc 干净 + 沙盒冒烟 A 32/32（含 dispatch 全管线）+ 增量迁移 B3 6/6 全过 + dev 热迁移实测 · 客户端零改动无需打包）**——用户提供三份附件文档（ZhengAPI 图片接口文档 + 同站两份视频文档），定「接入图片新渠道，命名为简梦H模式」。**⚠ 范围：只接图片**（用户指令明确；同站视频系（grok-imagine-1.0-video / firefly-sora2/veo31/kling3/runway45，chat 流式+模型 ID 拼时长比例分辨率）文档已在手、要接另起一轮）。 ①**上游（Base `https://zhengapi.top`，Bearer sk-）——同步单请求渠道，两种形态**（[jmh.t …（详见 git 历史）
- **2026-07-23 第153轮（简梦Z 图片渠道接入 · 7 模型/并入既有「简梦Z」模式·纯服务端 · server tsc 干净 + 沙盒冒烟 A 37/37（含 dispatch 全管线带鉴权下载）+ 增量迁移 B2 5/5 全过 + dev 热迁移实测 · 客户端零改动无需打包）**——用户提供同站「图片生成 API」文档（GPT Image 2 / Nano Banana / Grok Imagine），图片款并入第152轮的 jmz 模式（同渠道 ch-jmz、同一把密钥）。 ①**共享管线扩展（⚠ 本轮唯一动共享代码处，勿回退）**：该家**结果链接非公开**——`GET /v1/images/tasks/{id}/content?index=0` 下载须带 Bearer、成功结果仅保留约 2 小时。[jianmeng.ts](server/src/translators/jianmeng.ts) `Vi …（详见 git 历史）
- **2026-07-23 第152轮（简梦Z（zexitongxue.com）视频渠道接入 · 14 模型/新模式「简梦Z」·纯服务端 · server tsc 干净 + 沙盒冒烟 A 70/70 + 存量迁移 B 6/6 全过 + dev 热迁移实测 · 客户端零改动无需打包）**——用户提供对外「视频生成 API」文档，定「接入新渠道，命名为简梦Z模式」。**⚠ 本轮情报关键（后续维护先做这一步）：实时模型目录 `GET https://zexitongxue.com/ai-api/models?type=video` 免鉴权可拉、逐模型带 note/duration_profile/duration_rules/resolution_profile/max_reference_images/can_use——比文档正文细得多**（豆包线 note 明示「支持首尾帧+真人参考图已真实出片」——文档正文「真人照片暂不支持」 …（详见 git 历史）
- **2026-07-22 第151轮（简梦M（MuseAI museai.vip）视频渠道接入 · 4 模型/新模式「简梦M」·纯服务端 · server tsc 干净 + 沙盒冒烟 49/49 + 存量迁移 6/6 全过 + dev 热迁移实测 · 客户端零改动无需打包）**——用户提供 MuseAI OpenAPI 文档（提交+查询两份），定「接入新渠道，命名为简梦M模式」。 ①**上游（Base `https://museai.vip`，⚠ 鉴权=`apikey: MUSE-...` 请求头，非 Bearer——勿照抄其它翻译器）**：提交 `POST /api/v2/videos` body `{model, prompt, ratio(6 比例含 21:9), duration(4-15 整数), resolution?(480p/720p/1080p), images:[{tag,url}](schema 必填恒 …（详见 git 历史）
- **2026-07-22 第150轮（画布生成态断连保护：关软件重开可接回在途任务·纯客户端 · client tsc 干净 + 438 vitest(+7) 全过 + build 过 + 浏览器四场景端到端实测全过（vite+8787 真实服务端任务，临时数据已清零残留））**——用户报「客户端没有保存生成态：关软件重开=初始态、无法接收完成态」，核实**属实（仅画布模式；表格模式 pendingGens/inferTasks 本就有保护）**。**根因三层**：①画布节点运行态 `canvasStore.runtime` 明确「临时态不入库」，loadFromPath/switchCanvas 载入即 `runtime:{}`；②任务轮询器 taskCenter 纯内存 Map；③画布执行链 defaultNodeExecute→runPurpose 拿到的 taskId **从不持久化**——关软件即无凭据找回，服务端 …（详见 git 历史）
- **2026-07-22 第149轮（苏打水按上游清单全量更新 · 16→26 模型·纯服务端 · server tsc 干净 + 沙盒冒烟 31/31 全过（含 xc+sds 两轮迁移连跑回归）+ dev 热迁移实测 · 客户端零改动无需打包）**——接第148轮，用户定「苏打水模式也更新模型」。2026-07-22 真机 `GET /v1/models` 实测：**24→40 个上游 id**（该家 /v1/models 只给 id 无能力元数据——与第131轮一致，新线能力只能按命名推断）。变化：**my …（详见 git 历史）
- **2026-07-22 第148轮（星辰全面换线 48/50/51 · 14→6 模型·纯服务端 · server tsc 干净 + 沙盒冒烟 37+10 全过 + dev 热迁移实测 + 官方测试线零费 E2E · 客户端零改动无需打包）**——起因：用户让查星辰是否有新模型 → 2026-07-22 真机 `GET /generation/config` 全量实测：上游**模型名无新增**（仍 seedance-2.0/-fast、grok 1.0/1.5、gemini-omni-flash 五个），但* …（详见 git 历史）
- **2026-07-21 第147轮（简梦P（Sora/Veo 系）视频渠道接入 · 7 模型/新模式「简梦P」·纯服务端 · server tsc 干净 + 沙盒冒烟 50+9 全过 + admin script 语法过 + 本机 dev 热迁移实测 · 客户端零改动无需打包）**——用户提供对外「Video API」文档（`POST /v1/videos` + `GET /v1/videos/{task_id}`，Bearer sk-），定「新加模式简梦p，模型为 api 文档所示」。 ①**上游**：提交 …（详见 git 历史）
- **2026-07-21 第146轮（本地素材「丢失」自愈 + 音频节点重做·纯客户端 · client tsc 干净 + 426 vitest(+10) 全过 + build 过 + 浏览器端到端实测过（vite+8787+临时账号，测毕全删））**——用户真机截图两点：①本地素材上传画布后经常「丢失」不显示、要重新上传才恢复；②音频节点又高又空、拖出放置时点到进度条被误判「粘鼠标」放不下去——定稿改 波形图+单播放按钮、无进度条、双击放大才有进度条。 ①**素材丢失根因链（实锤）**：[nodeUpload …（详见 git 历史）
- **2026-07-21 第145轮（模型素材数量控制：管理端可调 图/视/音 上限·两端 + 管理端 · server/client tsc 干净 + 416 vitest(+5) 全过 + 纯函数冒烟 18/18 + 8787 端到端实测全过（临时数据已清））**——用户定「所有模型能力在管理端增加素材数量控制，比如 933 可改为 903，即不允许该模型垫视频」。 ①**数据模型**：`ModelDef.matLimits?: {img?,vid?,aud?}`（[models.ts](server/sr …（详见 git 历史）
- **2026-07-20 第144轮（客户端版本号显示 + vite.config.js 遮蔽事故根治·纯客户端 · client tsc 干净 + 411 vitest(+4) 全过 + `npm run build` 全链过 + 浏览器端到端实测过）**——用户定「个人中心和设置增加客户端版本号显示」（起因：上一轮排查分不清跑的是哪个构建）。 ①**版本标识**：[appVersion.ts](src/lib/appVersion.ts)——版本号=package.json version、**构建时间=打 …（详见 git 历史）
- **2026-07-20 第143轮（参考视频按秒计费·Dimensio 起用「与出片同价」·纯服务端 · server tsc 干净 + admin script 语法过 + 沙盒冒烟 25/25 全过 + dev 热迁移实测 · 客户端零改动无需打包）**——用户报「dimensio 模式的输入视频素材上游也按秒收费（不足1秒算1秒）」，定计价方式「与出片每秒价同价（系数1）」。 ①**计费模型（⚠ 核心设计，勿另起定价维度）**：新 `Model.refVideoSecondsWeight`（[model …（详见 git 历史）
- **2026-07-20 第142轮（素材↔图例「无法一一对应」错位修复·纯客户端 · tsc 干净 + 395 vitest(+12) 全过 + build 过 + 浏览器端到端实测过（截图场景复现，临时账号已清））**——用户截图报画布视频节点图例整段偏移一位（「(孟金珠) 是 贺长安，(贺长安) 是 昭阳长公主…」）。**根因**：图例文本是写入时的编号快照，而多条**改动素材集合/顺序的路径不重建图例**——逐对倒推实锤直接原因是「素材拖动排序」（用户把孟金珠/靖国公拖到主角位，[Frame161195 …（详见 git 历史）
- **2026-07-20 第141轮（门户「模型定价」页 UI 重做：单行 + 一键保存 + 筛选分组 + 渠道商启停模型·服务端 + 门户 · server tsc 干净 + 两控制台 script 语法过 + 8787 浏览器端到端实测过（临时商已清））**——用户报「UI 太差」定四点：单行去解释文字/保存收敛一键/按分类筛选（启用-禁用-按渠道-按模式）/渠道商可启停权限内模型。**①服务端**（[agent.ts](server/src/routes/agent.ts)）：新一把尺 `modelFro …（详见 git 历史）
- **2026-07-20 第140轮（渠道商可改模型显示名·服务端 + 门户 · server tsc 干净 + 两控制台 script 语法过 + 8787 浏览器端到端实测过（临时商已清））**——用户定「允许渠道商改模型显示名字」。**语义（锁定）**：改名只动**显示**——名下用户 catalog 的 `label` 沿归属链**就近取**（直属商改名 > 上级改名 > 平台名；直属平台用户恒平台名），计费/协议/upstreamModel/模型 id 一概不变；改名与售价同门槛（modelAllow …（详见 git 历史）
- **2026-07-19 第139轮（Aivide 2.0（aivideo.beauty）视频渠道接入 · 1 模型/新模式「Aivide」·纯服务端 · server tsc 干净 + 沙盒冒烟 28+47+1 全过 + 本机 dev 热迁移实测 · 客户端零改动无需打包）**——用户提供 Aivide 2.0 API 文档（v2.0，2026-07-17，Base `https://aivideo.beauty`，Bearer sk-），定「增加新模型」。 ①**上游**：提交 `POST /v2/gene …（详见 git 历史）
- **2026-07-19 第138轮（画布素材区按加入时间排序 + 上游素材前缀命名修复 · client tsc 干净 + 383 vitest(+5) 全过）**——用户指出两根因：①素材区旧序=上游连线素材恒在前、自加素材（匹配/上传/粘贴）恒在后 → **先匹配再连线**时上游素材插队到最前，同媒体 @ImageN 全体后移一位，而图例与正文胶囊只存序号 → 配对全部错位；②图例给上游素材命名取素材库文件名（生成产物名=`image.gen_output_<时间戳>` 乱码），节点标题（如「分镜」）不被 …（详见 git 历史）
- **2026-07-19 第137轮（机器码升级硬件指纹·Rust + 客户端 · cargo check 干净 + client tsc 干净 + 378 vitest(+13) 全过 + build 过 + 本机真机采集/恒定性实测过）**——用户问「机器码怎么获取的/同机是否恒定」→ 审计发现旧实现=**首启随机 UUID 存 localStorage**（非硬件派生：清数据/重装即变码、浏览器与桌面端不同码、localStorage 异常时全员共享 "mc-unknown"），用户确认升级为硬件指纹、* …（详见 git 历史）
- **2026-07-19 第136轮（渠道商门户品牌「灵创工场」+ 权限内界面与源站同步·服务端 + 门户 · server tsc 干净 + 两控制台 script 语法过 + 8787 浏览器端到端实测过（临时商已清））**——用户两点：①门户也用灵创工场名称/logo；②门户权限内界面与源站对齐（用户管理可用模式、模型定价分类等）。**①品牌（覆盖第111/124轮「门户品牌=商名」，⚠ 勿回退回商名）**：[agent/index.html](server/src/agent/index.html) ` …（详见 git 历史）
- **2026-07-19 第135轮（品牌 Logo「灵创工场」接入客户端 + 管理端·纯 UI · client tsc 干净 + build 过 + vite/8787 浏览器端到端实测过）**——用户提供两张品牌图（横版全 logo「灵创工场 + 万物皆为灵感 创作无限可能」与方形线条 mark），**均为透明底 PNG**（白色只是预览背景；全 logo 品牌蓝 **#6890F8**、mark 原黑）。处理管线=项目自带 ffmpeg（按内容 bbox 裁剪 / alphaextract+alpham …（详见 git 历史）
- **2026-07-18 第134轮（Dimensio（jimeng.dimensio.cn）视频渠道接入 · 3 模型/新模式「Dimensio」·纯服务端 · server tsc 干净 + 沙盒冒烟 42+6 全过 + 真机小额单 E2E SUCCESS + 管理端浏览器实测 · 客户端零改动无需打包）**——用户提供 Dimensio「视频接口规格」文档与 pk_ 密钥，定「新增 Dimensio 模式，将文档内的模型接入」。 **①上游（Base `https://jimeng.dimensio.cn …（详见 git 历史）
- **2026-07-18 第133轮（画影（AI-Studio aixyzz）视频渠道接入 · 6 模型/新模式「画影」·纯服务端 · server tsc 干净 + 沙盒冒烟 49+9 全过 + 真机小额单 E2E SUCCESS（requestId 幂等实锤）+ 管理端浏览器实测 · 客户端零改动无需打包）**——用户提供 ai-studio.aixyzz.com API 文档与 lv_ 密钥，定「新增画影模式，接入文档涉及的视频模型」。 **①上游（Base `https://ai-studio.aixy …（详见 git 历史）
- **2026-07-18 第132轮（星辰（AIStartLab）视频渠道接入 · 14 模型/新模式「星辰」+ 删设置「模型」页/未选模型自动取第一个·两端 · server/client tsc 干净 + 365 vitest 全过 + build 过 + 沙盒冒烟 43+7 全过 + 官方测试线零费 E2E + 真机小额单 SUCCESS + 浏览器端到端全过）**——用户定「全部接上（11 线 17 组合）、模式名=星辰、命名按素材量（如 xc933）、我们侧按秒占位价、设置模型页整删、未选模型自动取该 …（详见 git 历史）
- **2026-07-18 第131轮（苏打水（简梦）视频渠道接入 + 四级选择「模式(源头)+模型+方法+要求」·两端 + 管理端 · server/client tsc 干净 + 365 vitest(+18) 全过 + build 过 + admin script 语法过 + 沙盒冒烟 30+8 全过 + 真机双方法 SUCCESS）**——用户定稿术语：**模式（=源头：Qiji/简梦/libtv/即梦，即第130轮 modeId，内部名不改）+ 模型 + 方法（新维度：全能参考 omni / 首尾帧 f …（详见 git 历史）
- **2026-07-17 第130轮（视频「模式」动态注册表：现有视频→Qiji 模式 + 新增简梦模式 + 用户/渠道商可启用禁用 + 模型按模式分类 + 用户批量操作·服务端 + 管理端 · server/client tsc 干净 + 管理端 script 语法过 + 沙盒隔离冒烟 21/21+3 全过）**——用户定四点。**架构（锁定）**：**模式 = 一组模型的分类 + 一个可按用户/渠道商启用禁用的开关**（与 libtv/即梦在「可用模式」并列）。与 libtv/即梦（本地 CLI、纯客户端不 …（详见 git 历史）
- **2026-07-17 第129轮（请求记录索引改按天分文件 + 取消数量上限 + 修 agentId/agentCosts 重启丢失·纯服务端 + server tsc 干净 + 沙盒隔离冒烟全过）**——用户定「每天生成一个文件保存，取消 5000 条限制」。[logs.ts](server/src/store/logs.ts)：①**索引按天分文件** `data/logs-index/<YYYY-MM-DD>.jsonl`（替代单文件 `logs.jsonl`）——全部记录仍常驻内存，但 `flush …（详见 git 历史）
- **2026-07-11 第118轮（图生图 502「Upstream stream was interrupted」+「多垫图只读第一张」双根因修复：垫图死链探活/自愈/明确报错 + JSON `images[].image_url` 主模式定稿·纯服务端 + 本地起服端到端实测过）**：用户报图片生成一直 502「Upstream …（详见 git 历史）
- **2026-07-11 第117轮（画布图片节点「预设方案」预设胶囊——服务端模板管理 + 内置6宫格故事板·两端 + 274 vitest 全过 + 服务端 catalog 交付实测）**：用户定「图片节点增加预设方案，选择后在提示词框加预设胶囊，提交时替换成实际预设词，预设词在服务端提示词模板管理」。**架构=复用画风预设同构模 …（详见 git 历史）
- **2026-07-10 第116轮（请求记录归属重构：源站只看直属 + 渠道商详情「请求记录」（四段可见）+ 双扣费复核·纯服务端 + 本地起服/浏览器端到端实测过）**：用户两点。**①请求记录归属（「源站视为一个渠道商，不过是权限高一些」）**：管理端主「请求记录」与「统计」页从"全量"收敛为**只看平台直属用户**——[log …（详见 git 历史）
- **2026-07-10 第115轮（激活码签发价：渠道商发码按源站设定的档位价扣自身积分·纯服务端 + 本地起服/浏览器端到端实测过）**：用户定「渠道商签发激活码需要扣除自身积分，扣除的数值和模型一样，由源站设置（多价位，1天、3天、一个月、永久）」——对第112轮「发码不扣」的定向修改：**扣的是签发价（源站定），不是码的积分面 …（详见 git 历史）
- **2026-07-10 第114轮（画布三功能：图片/音频「绑定到资产」+ 系统剪贴板粘贴建节点·纯客户端 + 263 vitest 全过 + 浏览器端到端实测过）**：用户三点需求全落地。**①图片节点右键「绑定到资产…」**：[ContextMenu.tsx](src/canvas/ContextMenu.tsx) 有图片结果即 …（详见 git 历史）
- **2026-07-10 第113轮（图视同源类提示词：故事板/视频共用一段提示词 + 视频设置开关 + 单栏提示词 + 画布同源链路·两端 + 246 vitest 全过）**：用户定新增一种推理逻辑「图视同源」——故事板提示词与视频提示词**采用同一段**（链路 `文本-分集-原文-同源提示词-视频/图片`，区别于双结果的 `文本 …（详见 git 历史）
- **2026-07-09 第112轮（渠道商交易模式重做：双扣费结算 + 渠道商用户源站隐藏 + 激活码有效时长·主体服务端 + 本地起服端到端冒烟 32 断言全过）**：用户三点需求全落地。**①渠道商用户不在源站显示（用户定「由他自己管理，源站可去查看」）**：`GET /admin-api/users` 默认只列平台直属（`?a …（详见 git 历史）
- **2026-07-09 第111轮（源站对渠道商的管理力度五连：查看权/门户实名/模型开放范围（源站也是受众）/代调价/客户端日志页·两端 + 本地起服浏览器端到端实测过）**：用户五点需求全落地。**①渠道商详情+查看权**：管理端「渠道商」页每行新增「详情」→ 宽弹窗（基本信息 + <b>查看信息</b> + 代调价表）；「查看 …（详见 git 历史）
- **2026-07-09 第110轮（根除 `__fallback` 假模型请求：零默认 + 未配模型直接弹「设置 → 模型」·纯客户端 + 浏览器端到端实测过）**：用户请求记录出现假模型 `smart.infer__fallback`（服务端报「模型不存在或已禁用」，未扣费）。**根因链**：registerNodeSpec 给每 …（详见 git 历史）
- **2026-07-09 第109轮（渠道商模型定价：模型只读 + 逐档售价，仅名下用户生效·纯服务端 + 本地起服端到端实测过）**：用户定「渠道商允许查看模型和给模型定价但无法编辑模型」+ 追加「多档位时也可编辑（逐档定价）」。**语义（锁定）**：渠道商定价=绝对值覆盖，**只对其名下用户**的计费与客户端预估生效（平台直属/其 …（详见 git 历史）
- **2026-07-09 第108轮·补充（提示词放大编辑弹窗「一键替换」·纯客户端 + 浏览器端到端实测过）**：[PromptModal.tsx](src/components/PromptModal.tsx) 顶栏新增「替换」开关 → 展开替换栏（查找内容 → 替换为，可留空=删除）：实时命中计数（字面子串 split 计数，非 …（详见 git 历史）
- **2026-07-09 第107轮（即梦 Dreamina CLI 接入·Seedance 2.0 第二本地源·仿 LibTV + 真机 CLI 实测提交链路）**：用户定「即梦 cli 也做一个源，和 libtv 一样，暂只使用 seedance2.0」（安装源 `curl -s https://jimeng.jianying.c …（详见 git 历史）
- **2026-07-09 第108轮（单卡/多卡重做：单卡独立用途 + 画布按上游类型收敛可用模板·两端 + 单测 + 本地起服/浏览器端到端实测过）**：⚠ 同日曾先按「模板套系配对」（seriesKey/shotMode 标注+按节点语义选变体）实现并全链路实测通过，**用户判定逻辑有误整体回退**（代码零残留；data/temp …（详见 git 历史）
- **2026-07-09 第107轮·补充（本地渠道「渠道+模型」二级选择 + 参数按第三方 + 每次调用收 5 积分手续费·本地起服真扣费+浏览器端到端实测过）**：用户两点。**①渠道+模型二级选择**：画布视频面板的模型 pill 对本地 CLI 渠道显示**渠道名**（「即梦」/「LibTV」，下拉里两渠道各折叠为一项、管理端 …（详见 git 历史）
- **2026-07-09 第107轮·补充（LibTV/即梦 双渠道加 Seedance 2.0 Fast 模型·纯客户端）**：用户定「两渠道都加上 seedance 2.0 fast」。①**LibTV**：真机 `model search` 实锤线上有 **`star-video2-fast`「Seedance 2.0 Fast …（详见 git 历史）
- **2026-07-09 第106轮（画布智能推理节点提示词模板联动·纯客户端 + 单测 + 浏览器真 UI 端到端实测过）**：用户定「一个智能推理节点选了官方3，其余智能推理节点也切到官方3」——**反转第98轮「templateId 不联动」的锁定（仅对 smart.infer）**：[sharedNodeParams.ts] …（详见 git 历史）
- **2026-07-08 第105轮·补充（资产台账迁嵌入式 SQLite·本地起服真数据迁移+CRUD 冒烟全过）**：**根治「无上限增长 + 每次生成热写 + 全量进内存」的最后一个定时炸弹——资产台账**（`data/assets.json` 每次 `createAsset` 全量重写、永不删除永不复用、还全量常驻内存，与日志 …（详见 git 历史）
- **2026-07-08 第105轮**：**服务端「连接超时/心跳过不了/后台慢」根因修复——热路径同步全量写盘阻塞 event loop**（纯服务端；本地起服 + 生命周期冒烟实测全过；**生产已部署，须重新部署生效**）。**根因**：Fastify 单线程，`logs.jsonl`（5000 条含完整 ①②③④ 报文，每条约 …（详见 git 历史）
- **2026-07-08 第104轮**：**客户端用户注册登录系统（账号-密码-激活码-机器码四层防线）**（服务端协议扩展 + 客户端登录页/个人中心；本地起服真数据 + 浏览器 UI 端到端实测全过）。**核心设计（锁定）**：`accessKey` 仍是下游唯一凭证——账号+密码登录只是便捷层，服务端按账号解析出该用户的 ac …（详见 git 历史）
- **2026-07-08 第103轮**：**渠道商自营提示词模板（per-agent 隔离）+ 门户改名 + 管理端模板/日志详情优化**（服务端 catalog 协议扩展；本地起服真数据端到端实测全过）。①**模板 per-agent**（用户定：提示词为自营项目，每个渠道商管自己的）：`TemplateDef` 加 `agent …（详见 git 历史）
- **2026-07-07 第102轮**：**下游渠道商（Agent）体系 + 模板页高度铺满**（服务端协议扩展；本地起服真数据端到端实测全过）。**架构**：渠道商 = 平台下游分销——管理员给渠道商分发积分（记账关系），渠道商在**独立门户** `/agent`（账密登录）用**自己的积分池**签发激活码/兑换码，**发码即扣 …（详见 git 历史）
- **2026-07-07 第101轮**：**管理端 /admin UI 整体改版**（设计稿四轮迭代定稿后按稿实现；纯服务端 [index.html](server/src/admin/index.html) 重写，`readFileSync` 每请求现读=部署即生效；本地起服真数据逐页实测过）。①**布局壳**：顶部 8 页签 → …（详见 git 历史）
- **2026-07-07 第100轮**：**画布布局六优化**（纯客户端；浏览器 vite+本地管理端登录+注入状态+合成事件全链路实测过）。①**整理不重合**（[mindmapLayout.ts](src/lib/mindmapLayout.ts) 重写纵横算法，API 不变）：行列从「固定步长（240×200 一刀切）」改** …（详见 git 历史）
- **2026-07-06 第99轮**：**「锁妖塔」损坏项目修复**（微信收到的 project(2).Qiji，38MB 仅前 4MB 有效、后 34MB NUL 填充=写盘中断截断，与「三姐妹」同源；截断点在 `commits` 中途——scriptText/五类资产/episodes/assetBlobs/genMeta 等后 …（详见 git 历史）
- **2026-07-06 第98轮**：**画布同类型节点共享生成设置**（用户定：确保输出一致性——画布 5 个图片节点，任一节点改比例 9:16，其余 4 个跟随；视频/提示词节点同理。纯客户端；浏览器 vite 注入状态实测过）。①**单一事实来源** [sharedNodeParams.ts](src/lib/sharedNo …（详见 git 历史）
- **2026-07-05 第97轮**：**分镜组节点 + 图片宫格切分**（纯客户端；浏览器起 vite+本地管理端全链路实测过）。①**分镜组节点 `shot.group`**（[nodeSpecs.ts](src/nodes/nodeSpecs.ts) 新 spec，display=**shotgroup**、kind=seed …（详见 git 历史）
- **2026-07-04 第95轮**：**画布抽屉式堆叠**（纯客户端；保存历史记录的图片/视频节点升级为手机桌面图标抽屉式交互）。**语义**：堆叠=节点 `resultHistory`（≥2 条才算堆叠），堆叠节点除观感（纸边+层数角标）外与单节点无异，主图（resultAssetId）显示在抽屉外、参与画布连线。①**打开堆叠 …（详见 git 历史）
- **2026-07-04 第94轮**：**LibTV 授权 + Seedance 2.0 本地 CLI 生成**（用户定：登录=仅浏览器方式；管控=features.libtv 开关）。**锁定的架构例外**：LibTV 链路走**用户自己的 LibTV 账号**——凭据只在本机（`LIBTV_CONFIG_DIR=<appData …（详见 git 历史）
- **2026-07-04 第93轮**：**画布交互改版：单击选中/双击开面板 + 快捷键扩充 + 节点重叠开关 + 网格显隐修复**（纯客户端）。①**单击=只选中，双击才进节点面板**（选中即可对单节点用快捷键）：[BaseNode.tsx](src/nodes/BaseNode.tsx) 删节点本体 onClick 开面板；[C …（详见 git 历史）
- **2026-07-04 第92轮**：**服务端按用户控制 资产模式/画布模式 可用性**（用户定粒度=按用户；仅开单模式时客户端隐藏模式交互键）。**协议**：`User.features?: { assetMode?, canvasMode? }`（字段缺省=开），随 `/v1/login` 与 `/v1/heartbeat`  …（详见 git 历史）
- **2026-07-04 第91轮**：**生图分辨率档改为服务端控制**（用户定：不是客户端写死隐藏，而是管理端能实时控制客户端选项）。**约定**：图像模型 catalog params 里 `resolution` 枚举（key="resolution", type="enum"）的 options 放哪些档（1k/2k/4k） …（详见 git 历史）
- **2026-07-04 第90轮**：**视频超分/去字幕 UI（火山引擎，先 UI 后 API）+ 两个画布修复**（纯客户端）。①**资产模式**：视频区主视频右击菜单新增「超分（画质增强）…/去字幕…」→ `VideoProcessModal`（仿 ClipPicker 弹窗；超分带倍率 2x/4x，去字幕自动检测说明）→ 确 …（详见 git 历史）
- **2026-07-03 第89轮**：**服务端任务落盘 + 启动对账**（根因修复：用户报"10 点的任务 17 点还挂 running、④段已有 completed+video_url 却无②段结果"——日志状态只由进程内异步循环的 finishLog 回写，任务表纯内存，**进程重启即全部循环消失**，日志永久冻结 runni …（详见 git 历史）
- **2026-07-03 第87轮**：画布三项收整。①**删「生成故事板」节点**（[nodeSpecs.ts](src/nodes/nodeSpecs.ts)：智能推理一次出全分镜+故事板+视频提示词，单独 storyboard.toImagePrompt 节点无用；节点数 9→8，旧画布残留由 sanitizeCanvas 载入 …（详见 git 历史）
- **2026-07-03 第86轮**：**资产匹配优化 + 音色配对 bug 修复**。①**匹配范围扩大**：视频页「提取资产」从只扫分镜原文 → 扫 原文+故事板提示词+视频提示词（提示词先经 `stripLegendForMatch` 剥掉「【素材图例】」前缀行防自我循环）；画布 `applyAssetMatchToImage …（详见 git 历史）
- **2026-07-03 第85轮**：**资产模式 ⇄ 画布模式全面对齐**（审计定位 3 处对应错+若干缺失后一次修齐；理念锁定：画布=无严格界面的资产模式，同提示词/同逻辑/同数据）。①**画布拆分结果同链路入库**：合并落库逻辑从 Frame1693 抽出为 [assetMerge.ts](src/lib/assetMerge …（详见 git 历史）
- **2026-07-03 第84轮·补充（资产拆分调错提示词·根因修复）**：用户实测确认画布资产拆分调到了「智能拆分」（storyboard.split，拆原文）而非「资产提取」（script.analyze）。根因：面板模板列表用 `templatesByNode`（nodeTypes 白名单）过滤，但模板数据里 `asset.e …（详见 git 历史）
- **2026-07-03 第84轮**：画布**流式增量裂变** + 节点内文字流式实时可见。背景：用户反馈"画布运行资产拆分出不来图片节点"——排查确认裂变链路（buildSpawn/spawnNodes/命令总线）完好、第81轮并未屏蔽手动裂变（只删了同步投影），真正问题是三处观感/健壮性缺陷：①ResultView loadin …（详见 git 历史）
- **2026-07-03 第83轮**：节点面板/全界面去「默认」外壳（所有选择落到实际值）。①**文本类节点不再开放温度/最大长度**：[OperationPanel.tsx](src/panel/OperationPanel.tsx) 参数胶囊对 capability=text/null 的节点不回退 catalog 模型参数（s …（详见 git 历史）
- **2026-07-03 第82轮**：画布渲染性能四连修（根除"拖/缩/移一下=全画布节点逐帧重渲染"）。①**拖动**：[Canvas.tsx](src/canvas/Canvas.tsx) `rfNodes` 加 wrapper 引用缓存（按 store 节点对象引用+zIndex 复用旧 wrapper/data 对象）——拖 …（详见 git 历史）
- **2026-07-03 第81轮**：「同步本集到画布」**不再投影资产**（[canvasProjection.ts](src/services/canvasProjection.ts)：删资产拆分+每资产图片节点投影；一个项目几十个资产节点是画布卡顿主因）；同步时**清理历史残留**的资产投影节点（sourceRef=asset …（详见 git 历史）
- **2026-07-03 第80轮**：①CLAUDE.md 全面整理（删过时记录/已推翻方案的过程叙述，§11 按实测校准：Purpose 16 个、113 测试、store/lib/components 清单更新）。②**「三姐妹」损坏项目修复**：原文件 161.5MB 仅前 1MB 有效（NTFS NUL 填充=写盘中断截断） …（详见 git 历史）
- **2026-07-02 第79轮**：项目文件**原子保存**（tmp→.bak 轮换→rename）+ 加载失败自动从 .tmp/.bak 恢复 + 损坏弹窗明示（根治"异常中断进不去项目"）；capabilities 补 `fs:allow-rename`，**需重新打包**。
- **2026-07-02 第78轮**：资产匹配优先"资产助手选中造型"（`assetFormStore`，会话态）；取消场景模糊保底（五类一律精确命中名称/别名/造型名）；音频图例只留「@ImageN的声音参考@AudioM」配对。
- **2026-07-01 第77轮**：管理端批量生成激活码（`POST /admin-api/users/batch`，前缀续号）+ 「统计」tab（KPI+7 张 canvas 自绘图表，PNG/CSV 导出，`GET /admin-api/stats`）。
- **2026-07-01 第76轮**：视频界面故事板图/视频右击菜单（导出/首尾帧到相邻镜/视频片段到下一镜，走内置 **ffmpeg** 出真 mp4/PNG，浏览器回退 JS）；灯箱右击保存到本地（全界面通用）；**角色绑定音色**（上传音频→OSS，匹配自动加声音素材+图例配对）；剧集拆分与资产拆分解耦+「场n-n」容差；** …（详见 git 历史）
- **2026-06-30 第75轮**：智能推理/拆分**容错解析**（`extractCardsLoose` 不依赖整体 JSON 合法）+ 流式逐字段落盘 + **持久化锁定/断连找回**（`inferTasks`）；智能拆分与推理共用卡解析、互斥锁定；一键推理所有单镜；资产列表变量下发；文本流式 1.5s 自适应轮询；**多画布 …（详见 git 历史）
- **2026-06-29 第74轮**：画布本地上传素材统一走 OSS（`uploadMediaToCanvasAsset`：显示本地 uri、请求公网 url），三类素材（同步/自生成/本地上传）提交一律带公网 url。
- **2026-06-28 第73轮**：**积分闭环**——字段计费（`resolveModelCost`，seedance 按秒×分辨率档）、失败退款钩子、兑换码（管理端生成+个人中心兑换）、`/v1/me`；**模板库重置**为 5 功能+3 画风；`skills/` 移入 `server/skills/`；智能推理导演 prom …（详见 git 历史）
- **2026-06-28 第72轮**：**画布模式重做**——NodeSpec 单一事实来源（9 类节点）、裂变（spawnNodes 一次撤销）、资产模式→画布投影（canvasProjection）、思维导图整理/新增防重合/拖拽避让、AI对话改单轮+串联记忆+自动建下游、直连小圆点+单输入输出口、拖空白生成上下游节点、三助手可 …（详见 git 历史）
- **2026-06-23~28 第49-71轮（要点）**：视频界面重制为表格工作台+`storyboard.toImagePrompt`（43）；故事板/视频**本地直显**（43/48，服务端视频完成先转存永久资产）；在途生成**落盘持久化** pendingGens（47）+提示词推理也纳入（61）；界面快照 uiSnapsho …（详见 git 历史）
- **2026-06-20~21 第1-31轮（要点）**：确立网关架构与协议 v1、共享契约、阶段1 接入层切换（删 5 旧适配器/catbox/激活验证，声明式 JSON 插件，集中轮询）、阶段2 管理端骨架+OpenAI/Claude/Gemini 真翻译器+g-aisc 单网关、登录+心跳+用户库、模板数据化+链式、额度扣减、简 …（详见 git 历史）
