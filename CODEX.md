# Qiji 漫剧创作平台 · Codex 项目契约

> 本文保存跨会话稳定的项目事实、架构与不可回退规则。
> 当前目标、验证结果、发版与 QA 状态只写入 [docs/memory/ACTIVE.md](docs/memory/ACTIVE.md)。
> 精确行为以代码、测试和运行结果为准；精确历史以 Git 为准。

## 1. 协作与记忆契约

- 中文沟通。信息充分时直接执行；缺失信息会显著改变结果时才询问。
- 先确认目标、边界和交付标准，再实现用户端与管理端所需改动。
- 主动指出风险与更优方案；共同未知的问题转成最小可验证实验。
- `AGENTS.md` 只做入口导航，不承载项目事实。
- 本文件只收稳定结论；进行中的工作、临时假设、验证数字、部署/打包和 QA 队列写入 `docs/memory/ACTIVE.md`。
- 专项设计保留在对应 `docs/` 文档；不要在多处复制同一动态状态。
- 子 agent 只回传证据和建议，由主 agent 统一合并项目记忆。

## 2. 总体架构

```text
用户端（Tauri 2 + React）
  → Qiji 服务端（Fastify + TypeScript，网关/BFF）
    → 第三方文本、图片、视频和音频上游
```

- 用户端只与 Qiji 服务端通信，不保存或直连第三方密钥。
- 服务端负责协议翻译、转发、计费、对象存储、全局资产 ID 和任务状态归一。
- 模型、模板和节点能力经 catalog 下发并缓存，允许服务端热更新。
- 提示词正文只存服务端；客户端只发送 `templateId + variables`。
- 表格与画布是同一项目数据的两种视图，生成请求共用 `runPurpose → taskCenter`。

## 3. 通信与任务契约

- 用户鉴权：`Authorization: Bearer <accessKey>`；设备区分使用 `x-device-id`。旧 `x-machine-code` 只做兼容接收。
- relay 节点使用 `ank-` 节点密钥，只访问白名单端点。
- catalog：`GET /v1/catalog?since=<version>`。
- 生成：`POST /v1/generate`；批量：`POST /v1/batch`。
- 任务四态统一为 `queued | running | success | failed`。
- 个人信息与积分：`GET /v1/me`；兑换：`POST /v1/redeem`。
- 资产：ID 是身份，URL 是可更新缓存。引用保存 `assetId`，不要把临时 URL 当真理。
- 用户请求中的时长、比例、分辨率等参数不得静默夹钳、改档或回退；档位由 catalog 约束，非法值交给上游明确拒绝并走失败退款。

## 4. 领域模型与业务主链

- 树结构：小说/剧本 → 分集 → 分镜 → 垫素材（资产 ID 引用）→ 视频。
- 五类可见资产：角色、群像、场景、生物、物品；资产是项目级实体，分集和分镜只持 ID 引用。
- `code` 是项目内人读编号；`assetId` 是全局、不透明、单调分配的资产 ID。两者不可混用。
- 角色可绑定音色；匹配角色资产时声音引用随图例进入素材区。
- 变体生成是图生图：继承来源和 DNA，只修改指定项。
- 表格到画布的剧集/分镜投影为手动、单向、按 `sourceRef` 幂等；资产节点不批量投影，避免画布性能退化。
- 每分集一块独立画布，分别维护节点、连线、视口和撤销栈。

## 5. 模板与输出契约

- 运行时模板权威来源是服务端 `data/templates.json`，管理端可维护。
- `server/skills/*.md` 仅为历史种子原稿，已经过时；禁止通过提升 `SKILL_REFRESH_VERSION` 或 `SEED_VERSION` 覆盖线上已调整正文。
- 新增内置模板使用“只补缺失、不改存量”；删除内置项使用墓碑，禁止复活。
- 客户端按 purpose 选择模板，不依赖旧 `nodeTypes` 白名单。
- 主要输出契约：`asset.extract.v1`、`storyboard.v1`、`videoPrompt.v1`；解析器须容忍流式未闭合尾部。

## 6. 数据与文件安全红线

- 项目保存必须写临时文件、轮换备份，再原子 rename；加载失败依次尝试恢复候选并明确报错。
- 自动保存没有 `savePath` 时必须跳过，禁止制造幽灵项目。
- 对象键只允许 ASCII，不能包含用户名、项目名或任务名等可识别信息。
- `http://*.localhost` 是 Tauri 本地文件显示 URI；换源、自愈和下载逻辑必须用 `isWebviewLocalUri()` 排除它。`localhost:8787/raw` 不在此列。
- Fastify 热路径禁止同步重写大 JSON。高频可合并写使用异步、防抖、原子替换；大报文与小索引分离。
- URL 更新必须把旧 URL 记入 `AssetBlob.pastUrls`，并让 `blobByUri` 同时匹配历史 URL。
- 素材图例按 `@tag` 逐条解析与合并，新写入统一用中文分号 `；` 划分资产边界（兼容读取旧逗号格式），让资产说明中的普通逗号不再误切条目；保留用户改过的资产说明和提示词正文，只有编号对应的资产身份真实变化时才允许整体重建。
- 本地资产恢复先校验 `localPath` 是否真实存在；有台账 ID 时必须用同一 `assetId` 下载和登记。异步恢复绑定项目实例，项目切换后禁止写入新项目。
- 资产图片显示必须走统一显示 URI 解析（`useDisplayUri`/等价封装），禁止在资产页、缩略图等位置直接裸用项目快照中的 URL 或旧本地 URI；三元映射恢复成功后所有显示点必须能消费新地址，恢复期间用明确进度态且不得提前挂载必裂的远程 `<img>`。
- 单资产恢复只有 `src/services/assetRecover.ts` 的 `recoverAsset` 一份实现；探活换链不要求本地副本。
- 本机无副本与有副本但重传失败必须区分；重传失败带出原因并退避重试一次。
- 第三方本地 CLI 提交素材统一经 `resolveMaterialLocalPathOrThrow`；找不到素材必须整单明确拒绝，禁止静默跳过导致编号错位。
- 存储改造的表结构、阶段、迁移与生产安全以 `docs/存储与数据结构改造方案.md` 为专项权威。任何生产写入前还必须满足 `ACTIVE.md` 的安全门。

## 7. 画布与界面性能红线

- Canvas 本体和 BaseNode 不订阅 viewport；跟随视口的 UI 拆成小组件独立订阅。
- React Flow 节点 wrapper 按节点引用与 zIndex 复用；拖动只让目标节点重渲染。
- 低缩放使用 LOD；节点文本只预览必要片段。
- 随视口移动的元素禁用 `backdrop-filter`；毛玻璃只用于少量单例悬浮层。
- 连线不做常驻 dash 流动动画，只在激活、选中、悬停或续传状态显示。
- 小地图比例尺只按节点包围盒计算；不做会破坏连线和拖动流畅度的节点卸载式视口裁剪。
- 画布“当前结果”信息必须按 `resultAssetId` 读取该产物生成时保存的模型、比例、时长与提示词快照；禁止直接以之后可变的节点参数冒充历史结果参数。旧项目无快照时才回退当前参数。
- Alt 拖动复制用于继续抽卡：普通节点副本只继承输入、提示词和模型等配置，必须清空媒体/文本/分镜/对话结果、结果参数快照、备用文件地址与在途任务，并以待命态落下；`upload` 资产节点因自身只承载资产结果，复制时保留结果。
- 节点垫图区的右键取消是无确认的快捷操作：自加素材立即移除，上游素材立即断开对应连线；禁止重新插入确认弹窗或让事件冒泡打开画布菜单。

## 8. 渠道与计费通用红线

- 渠道上游模型名逐字采用上游清单，不能“顺手纠正”。
- 内置模型、渠道和模式经管理端删除后必须留下各自种子墓碑，后续重启或种子升级不得复活；部署包不携带 `server/data`，因此开发环境的数据删除不会自动传播到生产。
- 新渠道先用占位价；开放前必须以官方价格或小额真单对账定真价。
- 每个翻译器遵守用户参数原样透传；渠道特有的硬约束应明确拒单，不得静默丢素材或改参数。
- 失败退款、请求日志四段报文、错误擦除和成片转存必须与现有统一链路一致。
- relay 计费在转发前只做余额预检，真扣以源站回传实扣为准，保证两侧金额恒等。

### QiQi 双请求形态

- `seedace-2.0-720p` 拼写逐字保留，走 `content[]` 多模态形态；不发送 `resolution`。
- `sora-v3-933-pro` 走 flat 形态；发送 `resolution=720p`，时长仅 15 秒，不支持尾帧，图/视/音总数不超过 12。
- 两种形态都使用小写 `@imageN/@videoN/@audioN`。
- content 形态使用音频或视频参考时必须同时有参考图；flat 形态“建议带图”不是硬拦截。
- 请求形态由 `shapeOf(upstreamModel)` 分派，未知名在接入前先核对文档与分派规则。

## 9. 当前稳定实现概览

- 用户端主链包含登录与心跳、catalog、剧本/分集/资产拆分、五类资产、分镜与故事板、视频生成、画布和导出。
- 管理端包含用户、渠道、模型、模板、兑换码、注册安全、OSS、请求记录和统计等管理能力。
- 积分链支持前置余额校验、按字段/档位计费、异步失败退款和兑换。
- 服务端任务已持久化并在启动时对账；资产序列使用 SQLite `asset_seqs`。
- 项目文件当前仍有既有格式与兼容逻辑；向 SQLite 项目格式演进属于存储改造专项，不得只凭文档状态推断已完成。

## 10. 代码地图

- 客户端入口与界面：`src/`
- Tauri：`src-tauri/`
- 服务端：`server/`
- 跨端契约：`src/contract.ts` 及服务端对应副本
- 模型与能力：`server/src/store/models.ts`、catalog 相关 store
- 翻译器：`server/src/translators/`
- 生成统一入口：`src/services/purposeRunner.ts`、`src/lib/purposeRegistry.ts`
- 画布节点规格：`src/nodes/nodeSpecs.ts`
- 实时剪辑：`src/rtc/`
- 存储改造设计：`docs/存储与数据结构改造方案.md`
- 当前执行状态：`docs/memory/ACTIVE.md`

## 11. 收口规则

每个有实质改动的工作单元完成时：

1. 把真实执行过的验证与尚未完成的发版/QA 写进 `ACTIVE.md`。
2. 只有新的长期约束或稳定架构事实才进入本文件。
3. 详细过程、命令输出和精确 diff 留给 Git、测试报告或专项文档，不堆进项目入口。
4. 若状态与代码冲突，先核验代码/测试/运行时，再修正文档，不用旧文字覆盖现状。
